# TopCat Worktops — Session Handover

> ⛔ **11 Aug 2026 — THE FIFTH HELIX CARD SHIPPED INVISIBLE (D98).** Three causes stacked: the
> stage overlapped the copy so far that the top ghost sat entirely inside the mask's protective
> band, the mask's top stops were PERCENTAGES where they had to be PIXELS, and the ghost opacity
> was low enough that the mask finished it off. ⭐ **The keeper: a percentage stop scales with the
> STAGE, and the thing that stop has to line up with is the COPY.** The copy's bottom is a fixed
> px distance below the stage top and does not move when the text rewraps. Measured after, the
> two ghosts are balanced at 0.31 / 0.29 where the top one was invisible. ⚠️ Room for the fifth
> card came from the CARDS (width 0.76 → 0.70, and height is 0.66 of width), not the section.
> ⭐ The hero bevel's cuts are short now — 80% of the width is flat — and 22/38 = tan(30°) is what
> keeps the hairline lying on the cut.

> ⭐ **11 Aug 2026 — THE MOBILE HELIX SHOWS FIVE CARDS, TWO OF THEM INERT GHOSTS, AND THE HERO'S
> BEVEL IS 30° WITH ITS EDGE RAISED (D97).** The stage reaches up under the intro on a negative
> margin, so it gained two cards and got 20px SHORTER. ⭐ **The mask's top stop is 0.10 alpha, not
> 0** — that is what puts a card behind the copy at 0.022–0.08, visible as depth and unable to
> compete with the text. ⛔ **Interactivity is decided by POSITION, not opacity**, and
> pointer-events alone was not enough: a dispatched click bypasses hit-testing, so the listener
> refuses `.hx-ghost` outright. ⚠️ **sin is symmetric about 90°**, so the card one step out and
> the ghost two steps out share a sideways offset — widen the arc too far and both collapse into
> one sliver. ⛔ **The bevel hairline's gradient angle had to change with the bevel**: `to top
> right` traced the cut only because the box was square; the angle is `tan⁻¹(rise/run)`.

> ⭐ **11 Aug 2026 — THE SERVICES HELIX RUNS ON MOBILE NOW (D96), and the section went from
> 2,939px to 649px** because it replaces six stacked flip cards. ⛔ **The card CSS was LIFTED out
> of the desktop media query into a shared one, not copied** — a second copy would have been this
> project's signature bug for the fifth time. `--hxMode` is set by the media query and read by the
> script, so the breakpoint is declared once. ⚠️ **The desktop floors are the trap** (card 300, R
> 210, STEP 96 are all wider than a phone can give). ⭐ STEP is the edit as much as the rise: at
> 0.30 of the stage the ±2 cards climb clear and the mask takes them, so a phone shows three
> cards. ⚠️ The mask goes on the STAGE, never a card, or the 3D flattens. ⛔ Tablet keeps the grid.

> ⭐ **11 Aug 2026 — THE MOBILE REVIEW SPACING IS REBUILT ON THE CARD'S REAL HEIGHT (D95).**
> Subtitle→card 64→30, card→buttons 54→20. ⛔ **Most of that gap was invisible slack, not a
> spacing choice**: the stage was sized from the deck's UNSCALED height while the card is
> transform-scaled to ~0.84 inside it, leaving 27px of dead space a side that no rule named.
> `gridLayout()` now writes the measured scale out as `--revScale`, so the stage is sized from the
> card the customer can see. ⚠️ Solo must skip the stage-height cap on the scale, or it is
> circular. ⚠️ Half the air had to be added to the open-card pager drop too — missed first time,
> which put the buttons 4px under an open card where a closed one is 20.

> ⛔ **11 Aug 2026 — THE SWIPE ONLY ANSWERED TO A TOUCHSCREEN, SO IT DID NOTHING FOR THE CLIENT
> (D94).** It was bound to touch events only; his MacBook has no touch hardware, so neither a
> two-finger trackpad swipe nor a click-and-drag reached it. ⭐ **The lesson generalises to this
> whole round: a phone layout is looked at on a DESKTOP browser far more often than on a phone
> during a build, including by the client — so a phone gesture has to answer to a mouse and a
> trackpad as well.** ⚠️ It passed its own verification because the tests synthesised touch
> events, which proves the handler works and not that a person can reach it. Three paths now:
> touch, mouse drag, and a `wheel` with `deltaX`. ⭐ An open review also shows the WHOLE review in
> one go — no scrollbar inside a scrolling page — and tapping again closes it.

> ⭐ **11 Aug 2026 — THE MOBILE REVIEWS ARE A SWIPEABLE CAROUSEL (D93).** The neighbouring
> reviews now peek in from both edges, the centre card is slightly narrower to make room, and the
> two pager buttons have moved below it. ⭐ **The neighbours are the affordance, not decoration**:
> a lone centred card with two arrows tells a customer nothing about there being more to read.
> ⛔ **The phone dress is scoped by a CLASS set from `perPage()===1`, not by a media query** — the
> stylesheet and the layout maths must never hold separate opinions about what a phone is, which
> is D51/D59/D68/D78's failure mode and would have been its fifth outing. ⚠️ The pager rides DOWN
> with an open card rather than capping it; capping left a long review scrolling inside 290px.

> ⭐ **11 Aug 2026, the mobile round opens — THE MOBILE HERO IS CENTRED AND ITS BOTTOM EDGE IS A
> MATCHED BEVEL (D92).** Title, trust line, CTAs and chips all centre on the viewport centre; the
> three reasons go back to a **row of three**, icon over label, icons top-aligned so they read as
> one line. The curved bottom edge is gone: `--bev` is both the run and the rise of each corner
> cut, which is what keeps the angle at exactly 45°, and the hairline that traces it is three
> background gradients rather than an SVG, because a stretched SVG is only 45° at one width.
> ⚠️ **The trust line's gold LEFT rule stands down on mobile** — it cannot work against centred
> text — and the client should be told that, since the rule was his own request. ⛔ Desktop and
> tablet were **probed side by side against the pre-change file at 1440×900 and 768×1024 and came
> back identical**; that side-by-side probe is the pattern to reuse for every mobile change.

> ⛔ **11 Aug 2026, later — DESKTOP DESIGN IS CLOSED, AND WORK IS NOW MOBILE-ONLY (D90, D91).**
> The last desktop change was the final CTA card: it carried the page floor's own photographed
> slab at full strength behind the enquiry form, and the client asked for **plain grey** — it is
> now `--ink-2`, the panel grey every other card on the site already uses. ⛔ **Then desktop
> froze.** "When I say let's do mobile, then we only work on mobile. Everything else stays exactly
> as it is on the other devices." Tablet comes after mobile, and the client says when.
> ⚠️ **The trap is structural, not one of memory**: `index.html` is one file with inline CSS, so
> nearly every rule is unscoped and applies at every width. **A mobile change belongs inside a
> width-scoped media query; editing a base rule to fix mobile silently changes the frozen
> desktop.** See **§2 rule 15**. ⚠️ This is a DESIGN freeze — the enquiry backend, the
> photography and the licensing questions are unaffected and still open.

> ⭐ **11 Aug 2026 — ALPHABETICAL ORDER, V2 REMOVED, AND THE MIRROR RANGE. The range is 132.**
> Three client decisions in one session, and the last one exposed the worst mistake of the
> project so far. **(1)** Every stone on every surface is now **alphabetical** (D85) — "it would
> make it easier for people to navigate" — which ⛔ **reverses D74's dark/light spreading**,
> because a name says nothing about colour. Granite consequently opens on seven dark stones in a
> row; he has seen it. The wheel needed a real change, not just new data: its fan seated cards
> centre-out, which scrambles a sorted list into two interleaved runs. **(2)** The first five,
> then the whole visible nine, must be light (D86) — no landing card can do that alphabetically,
> so the darks inside the opening window are deferred just past it, split evenly either side.
> **(3)** **V2 is gone entirely** (D87), which also closed the standing risk that its
> un-rewritten in-house-fabrication claims could be published by a revival.
>
> ⛔ **THE ONE TO READ IF YOU READ NOTHING ELSE — D88 → D89.** Asked for "mirror grey, mirror
> black, mirror white", a search of one supplier listing concluded those names existed nowhere,
> and **shipped a different product under a search alias instead**. The supplier publishes all
> six Mirror stones as ordinary product pages; **their own search box returns them in one
> request**. The client found it in four seconds by typing "mirror" into the site. ⭐ **Search
> the supplier's own search before concluding a product does not exist, and never substitute a
> different product for the one you were asked for.** All six are live now under their real
> names. ⚠️ D88a is worth reading beside it: **super-resolution destroys speckle**, and D77's
> drift metric passed the ruined tile.

> ⭐ **10 Aug 2026, third session — THE RANGE IS NAMED "MARBLE & QUARTZITE".** The client walked
> the journey the last session had left half-fixed: he pressed **Marble**, opened a stone, and
> was told **Quartzite**. "We cannot have that confusion." ⛔ **The answer is not to call it
> marble** — 26 of that range's 45 stones are quartzite, one is travertine, and no merchant sells
> Taj Mahal as marble. **The stone always says what it is; the range is now named for what it
> contains.** Two constants carry the wording, `mat` never moved, so no price or filter changed.
> Read **§D D66–D68** and **§4p**, and **§2 rule 13** before renaming anything.
>
> ⚠️ Two things fell out of checking which surfaces name a material, and both were live: a
> hand-typed quartzite list had gone stale at 10 of 26, so **16 stones could not be found by
> searching the word that describes them**; and the HTML sitemap was built from the 52-stone
> snapshot, so **63 stone pages were missing from the page whose job is to prove nothing is
> orphaned**. ⭐ Both are the same root cause as D51 and D59 — **a second file that looks like
> the source of truth** — now hit for the third time.

> ⭐ **10 Aug 2026, second session — THE COPY ROUND.** Every stone description on the site was
> rewritten, all 115 of them, because the client read one and found it was **factually backwards**.
> Behind it sat a script that assembled 63 blurbs from a fixed phrase bank. Also fixed this round:
> **22 pages printing a wrong slab size**, **three unkeepable promises** in the shared facts block
> (one on all 45 marble pages, and it claimed fabrication TopCat outsource), **27 pages that
> contradicted themselves** by titling a quartzite "Marble", **four dead pages still live and
> indexable**, and a **correction from the day before that had silently reverted**.
>
> ⛔ **Read `§D` D56a–D65 before touching stone copy, `stones/descriptions.py` before writing any,
> and run `harvest/verify.py` before you call anything done.** It has seven checks now, not four.
>
> ⚠️ **The through-line of this session, and the thing to carry forward:** every defect was a
> sentence or a number that *looked* right on a page that *rendered* correctly. None of it would
> have been caught by looking at the site. The client caught three of them himself.


**THE single current handover. Start here in a new chat.**
Current as of **14 August 2026**, after the **tablet round (D197–D200)**.
⭐ **The range is 132 stones across 167 live pages, and the site is SEVEN pages built by
`build_pages.py` from the landing page's own parts.** Desktop design is CLOSED (D91). **The phone
(≤720px) is signed off, the TABLET BAND (721–1120px) has just been built, and he has not named what
comes next.** Open `HANDOVER-NEXT-CHAT-START-HERE.md` first in a new chat — it carries the
lift-don't-copy rule that makes one rule serve two bands, and why the tablet block must be the last
thing in the stylesheet. This file is the full record behind it.

**If you are picking this up cold, read in this order:** §D the Decision Register → §0 the current
state → §2 the standing rules. That is about ten minutes and it is enough to work safely. The two
narrative session write-ups (`HANDOVER-2026-08-07-*.md`) explain *why* things are the way they
are, and are worth reading before redesigning anything they cover.

---

## ⭐ HOW TO USE THIS DOCUMENT

1. **Read §D, the Decision Register, first.** It is the list of every client decision, including
   the ones that were later **reversed**. Several rules on this project have flipped more than
   once, so a rule read without its history will send you the wrong way.
2. Then §0 for the current state and the outstanding job.
3. §2 is the standing rules. They are the ones that get broken by accident.

### ⚠️ MAINTAINING THIS DOCUMENT — client instruction, 7 Aug 2026

> "Keep track of everything we do and every decision we make. If I change something that was
> previously made in the handover doc, then change and update the handover doc."

So, whenever the client makes or changes a decision:

- **Add a row to §D** with the date, what was decided, and what it supersedes.
- **If it reverses an earlier decision, mark the old row `⛔ REVERSED` and say what replaced it.
  Never silently delete a decision.** The reversal history is the most valuable thing in here,
  because it stops the next session re-proposing something already rejected.
- **Update the affected section** in the body, and the standing rule in §2 if there is one.
- **If it touches SEO in any way**, also update `Docs/topcat-worktops-SEO-LOG.md`, which is the
  record written for the client's SEO specialist.
- **If a design was rejected**, record the client's own words. They are what stops it coming back.

### The document set

| File | What it is |
|---|---|
| **`HANDOVER.md`** | ⭐ This file. The single current state. |
| **`Docs/topcat-worktops-SEO-LOG.md`** | ⭐ Every URL, title, target query, schema decision, linking structure, compliance scan and SEO change log. Written to hand to the client's SEO specialist. |
| `Docs/topcat-worktops-seo-build-plan.md` | The SEO research behind the build |
| `Docs/topcat-worktops-industry-brief.md` | Industry, materials, regulation, competitors |
| `Docs/topcat-worktops-customer-psychology.md` | Buyer psychology, homeowner and trade |
| `Docs/topcat-worktops-sales-call-notes.md` | What the owners actually said on the sales call |
| `HANDOVER-archive-to-2026-08-06.md` | ⚠️ Every earlier round and **every design the client rejected, with their words**. Read before redesigning anything. |
| `HANDOVER-2026-08-07-seo-build.md` | The narrative write-up of the 7 Aug SEO session (first session that day) |
| `HANDOVER-2026-08-07-design-round.md` | ⭐ The narrative write-up of the 7 Aug design round (second session): the sitemap, the two scroll animations, the FAQ redesign, the enquiry form's foot, the helix card backs. **Read §2 and §5 of it before touching either scroll build or the FAQ.** |
| `HANDOVER-2026-08-09-real-slab-photography.md` | The narrative write-up of the first slab-photography session (the pipeline itself: harvest, crop, gate, match). ⚠️ Its plan to harvest marble.com was **reversed the next day** — read the file below before acting on it. |
| **`HANDOVER-2026-08-10-slab-photography-complete.md`** | ⭐ The narrative write-up of the 9–10 Aug session that **finished** the photography: 52/52 stones, the six collection bugs that were hiding the images, the licensing decision on marble.com, the two automatic guards and the live wrong-image errors they caught, and the popularity ordering. **Read §2, §3 and §5 of it before touching anything in `stones/harvest/`** — most of what looks like a threshold worth tuning was arrived at by looking at several hundred photographs, and the obvious tuning has already failed. |
| **`HANDOVER-NEXT-CHAT-START-HERE.md`** | ⭐⭐ **Rewritten 14 Aug, after the TABLET ROUND (D197–D200). This is the file to open first in a new chat** — the three device bands and the lift-don't-copy rule that reaches two of them, why the tablet block must be the LAST thing in the stylesheet, the freeze probe and its current numbers, the four bugs this round produced, the environment traps, and the open list in priority order. |
| `HANDOVER-2026-08-13-internal-pages-start-here.md` | The START HERE from the internal pages round (D179–D196), superseded 14 Aug. ⚠️ Predates the tablet band entirely — it still says the tablet is "frozen, NEXT, on his word". |
| **`HANDOVER-2026-08-11-mobile-round.md`** | ⭐ The narrative write-up of the first mobile round (D90–D98): the desktop freeze and how it is proven, the hero's bevel geometry, the swipe that only answered to a touchscreen, the invisible slack behind a spacing complaint, and the three stacked causes of a helix card that shipped invisible. **Read before touching any mobile rule.** |
| `HANDOVER-2026-08-11-alphabetical-v2-mirror-start-here.md` | The START HERE from the alphabetical / V2-removal / Mirror round, superseded 11 Aug. ⚠️ Predates the desktop freeze and all mobile work |
| `HANDOVER-2026-08-10-logo-and-integrity-start-here.md` | The START HERE from the logo / name-integrity round, superseded 11 Aug. ⚠️ Describes a **126**-stone range ordered by POPULARITY on a site that still had a V2 — all three have moved |
| `HANDOVER-2026-08-10-naming-round-start-here.md` | The START HERE from the naming round, superseded 10 Aug. Still good on the range rename |
| **`stones/catalogue_mirror.py`** | ⭐ The Mirror range (D89), and the full write-up of the substitution mistake that preceded it |
| `HANDOVER-2026-08-10-photography-start-here.md` | ⚠️ The PREVIOUS start-here, written after the photography session and renamed when it was superseded. Still good on the pipeline and the traps, but **two rounds out of date**: it says 63 stones carry generated copy, it predates verify.py checks 5–7, and it predates the range being renamed Marble & Quartzite. |
| **`stones/descriptions.py`** | ⭐ The 115 stone descriptions and, at the top, the rules for writing them. Read before changing a single one. |
| `HANDOVER.pre-*.bak.md` | Previous copies of this file |

⚠️ **Section numbers here are referenced from code comments** (`§3`, `§4`, `§5a`, `§6.7`, `§7.5`
are live in `index.html`). **Do not renumber.** Grep `§[0-9]` across the Website Demo first.

---

## D. ⭐ DECISION REGISTER

Every client decision, newest first. **`⛔ REVERSED` rows are kept on purpose** — they are how the
next session avoids re-proposing something already killed.

### Active decisions

| # | Date | Decision | Notes |
|---|---|---|---|
| D290 | 17 Aug | ⭐⭐⭐ **ONE FOOTER, GENERATED FROM THE LANDING PAGE, ON ALL 183 PAGES.** Client: *"the inner pages footer on mobile doesn't look like the hero section foot on mobile. Just make sure that the footer are consistent on every device all across the site, the same as on the landing page."* ⛔⛔ **FOUR BUILDERS, FOUR FOOTERS.** `build_pages.py` has always LIFTED the footer out of index.html, which is why the seven internal pages were the only family that matched. The other three hand-wrote their own and all had drifted: **4497 characters on the landing page against 1928 (services, stones) and 1755 (the SEO layer, which used `class="foot"` and so missed every `#footer` rule in the stylesheet).** All three lift it now. ⛔⛔⛔ **AND THE MARKUP WAS ONLY HALF OF IT — THE CSS DOES NOT TRAVEL WITH IT.** Those families do not load `site.css`; they are dressed by `services/service.css`, which carried **18 footer rules against the landing page's 93**. Lifting the markup alone rendered the Instagram icon at ~500px, because service.css has no `.foot-social svg` sizing at all. ⭐ So `build_pages.py` now also writes **`assets/footer.css`** — the footer-scoped rules split out of the same `<style>` block, 7.8 KB, media queries included — and the three builders link it. **service.css's own footer rules are DELETED**: two sheets describing one component is how they drift, and they had. ⭐⭐ **THE FOOTER'S SCRIPT COMES WITH IT TOO.** On the tablet, Area and Hours move out of the contact column into `.foot-tail`, and no CSS property re-parents a node (D200). These pages do not load the 509 KB `site.js`, so the fifteen lines that matter are inlined. Without it the footer looks right and then rearranges wrongly the moment the window is a tablet. ⛔⛔⛔ **THREE TRAPS, ALL THE SAME SHAPE: A TRANSPLANTED COMPONENT INHERITS ITS HOST, NOT ITS ORIGIN.** (1) The CSS splitter took each rule's head by splitting on the first `{` — but one footer comment quotes the client and CONTAINS A BRACE, so the head came back as half a sentence, `#footer .foot-grid` was never matched, and the phone footer's whole `grid-template-areas` rule was dropped: it auto-placed into three columns. Strip comments BEFORE splitting. (2) `service.css` sets `body{line-height:1.6}` where this page sets 1.5 — every line in the footer came out 1.6 and it finished **28px taller**. (3) It also sets `body{font-weight:300}` where the landing page declares nothing, so the browser's 400 applies there — **"the landing page does not say" and "the footer should not care" are different statements**, and the legal row was 3px narrow for it. `#footer` now pins every inheritable text property, taken from this page's own `body` rule and falling back to the CSS INITIAL where it declares none. | Verified by capturing the landing footer's geometry and diffing every page family against it, element by element — height, grid columns, named areas, and the x/y/width/height of brand, explore, browse, phone, WhatsApp, email, area, hours, tail, social, bar and legal. **375: service leaf, stone and SEO all MATCH exactly, H=1102. 900: stone MATCHES, H=789, `--footTail` on and Area re-parented into `.foot-tail` on both. 1440: SEO MATCHES, H=504.** Footer markup byte-identical at 10,062 chars on all families. Freeze probe unchanged — `.gal-scroll` **4950**, `--revPer` **3**, `feTurbulence` **60**, `#svcNav` **8**, hero ink **164.683**, About **759**, collage **497x676**, no broken images, no horizontal overflow, no console error. `harvest/verify.py` **132/132/132 ✅** |
| D289 | 17 Aug | ⛔⛔⛔ **THE DEVELOPER UPLOADED THE NEW SITE AND IT CAME BACK AS THE OLD ONE. TWO CAUSES, BOTH FIXED, AND THE FIRST ONE HAS BEEN SITTING IN §13 AS ITEM 17 FOR DAYS.** ⭐⭐⭐ **CAUSE ONE — THREE STYLESHEETS SHIPPED WITH NO VERSION.** `assets/site.css` and `site.js` have carried a content hash for weeks. **`services/service.css`, `stones/stone.css` and `seo.css` never had one** — and `service.css` alone dresses **176 generated pages**. So a browser holding yesterday's copy took the brand-new HTML and re-dressed the entire site in the old design. **That is not a subtle staleness, it is the whole site looking untouched**, which is exactly what he reported. All three are stamped `?v=<sha1 of the file>` now, computed at build time in the three builders that link them — the same pattern `site.css` has always used, and the one D276 applied to the slabs. ⭐⭐ **CAUSE TWO — NOTHING TOLD THE HOST HOW LONG TO HOLD THE HTML**, and the HTML is the one file that must never be stale, because it is what carries the new `?v=` for everything else. A `.htaccess` now sets `no-cache, must-revalidate` on HTML, a year on the fingerprinted CSS and JS, and **deliberately only an hour on images** — slab tiles and the director portraits are named for their subject, not their contents, so a re-cut file is the SAME URL and relies on a hand-bumped stamp. ⛔ A year there would have made the Argento correction invisible for twelve months. ⚠️ **NEITHER FIX REACHES SITEGROUND'S OWN DYNAMIC CACHE**, which sits in front of Apache and must be flushed in Site Tools after every upload. That is in the upload guide, §7a. ⛔⛔ **AND "UPLOAD THE FOLDER" WAS ITSELF AMBIGUOUS.** `Website Demo/` is a WORKING directory: 60-odd `.bak` snapshots, every build script, the dev server and the original photography sit in the same place as the 155 pages. `make_upload.py` now writes a clean `../upload/` — 668 files, 183 HTML pages, 76.7 MB — with the scripts, backups, markdown and `.src-*` folders stripped and **`.htaccess` deliberately kept** (an FTP client that hides dotfiles will otherwise drop the one file that fixes the caching). It refuses to run if `index.html` is newer than `assets/site.css`, which is the signature of builders that were not re-run. | Verified: every stylesheet reference on every page family now carries a hash — `/assets/site.css?v=06fd25bf17`, `/assets/site.js?v=639b948b99`, `/services/service.css?v=6f8f58392f`, `/stones/stone.css?v=4c72f6abc6`, `/seo.css?v=2ee91ef254` — checked on a stone page, a service page and an SEO page. `upload/` contains **183 HTML pages against 183 in source**, no `.py`, no `.bak`, no `.md`, no `dev-server.js`, and `.htaccess` present. ⚠️ The version report inside `make_upload.py` reads the numbers back OUT of the built HTML rather than hashing the files: `build_pages.py` stamps `site.css` from the `<style>` block it extracted, which is not byte-identical to the file it writes, so hashing the file printed a number appearing nowhere in the site. `harvest/verify.py` **132/132/132 ✅** |
| D288 | 17 Aug | ⭐⭐ **THE WHEEL'S ARROWS STAND STILL WHATEVER THE STONE IS CALLED.** Client: *"some of the names are longer, and therefore, the buttons next to it are moving, and then the person has to move their fingers... the buttons have to be in one spot, and then all the words must fit between that... On all devices."* ⛔⛔ **`.wheel-ui` WAS A FLEX ROW CENTRED ON A READOUT SIZED BY ITS OWN TEXT.** Measured in the shipped font, the 132 names run **122px ("Argento") to 505px ("Travertine Romano Classico Honed")**, so the pair was swinging **up to 147px each side** and a customer paging the wheel had to chase them. ⭐ **IT IS A THREE-COLUMN GRID ON A FIXED 480px ROW NOW** — the two `auto` columns are the buttons, so they sit at the same x for every stone. ⚠️ 480 is chosen, not inherited: it puts them at **±240** from centre, the middle of the range they used to swing through (±159 shortest, ±306 longest), so nothing lurches against the wheel above. ⛔ **`min-width:0` ON THE READOUT IS LOAD-BEARING** — a grid item's automatic minimum is its content, so the grid ALONE does not fix this; without it the middle column is still sized by the longest name. ⭐⭐ **THE NAME BOX RESERVES TWO LINES AND CENTRES THE NAME IN THEM**, so a long stone wraps instead of pushing the buttons out and a short one does not pull the line below it up: the row's HEIGHT is as fixed as its width. `fitStoneName()` steps the type down for the handful that want a third line — on a 375 phone that is exactly **three** stones, to 18-22px. ⛔⛔⛔ **TWO MEASUREMENT TRAPS, BOTH CAUGHT BY SWEEPING RATHER THAN BY LOOKING.** (1) The fit first counted lines with a `Range` over `.r-name` — which is `display:flex`, so the text sits in an ANONYMOUS flex item and the Range returned **0 rects for all 132 stones**; the fit was silently inert. It measures an inner `<span>`'s height against the line-height now. (2) The two-line reservation was `min-height:2.3em`, and `em` resolves against the element's OWN font-size — which the fit overwrites — so a shrunk name shrank its own box and **the buttons moved DOWN**: on the phone the horizontal position held at one value while the vertical took FOUR. It is `calc(var(--rNameFS) * 2.3)` now, and the phone's override sets that VARIABLE rather than `font-size`. | Verified by sweeping **all three material tabs, 232 readouts, at each width**, recording the buttons' rect each time. **1440x1000: one x (480|908), one y, 26px throughout, 205 names on one line and 27 on two, none overflowing. 1440x900: one x, one y, 23px (the `max-height:960px` rule, pre-existing). 768: one x, one y. 375: one x, one y, three names stepped to 18/19/22px, none overflowing.** Freeze probe unchanged — `.gal-scroll` **4950**, `--revPer` **3**, `feTurbulence` **60**, `#svcNav` **8**, hero ink **164.683**, About **759**, collage **497x676**, no broken images, no horizontal overflow, no console error |
| D287 | 17 Aug | ⛔⛔⛔ **`top:50%` WAS THE STAGE'S MIDDLE, NOT THE CARD'S, AND THEY ARE NOT THE SAME BOX.** Client, with a screenshot: *"the arrows next to the review cards are not in the center of the card. Why is it below the center of the card? It has to be in the middle of the card."* ⭐⭐ **`.rev-stage` CARRIES `padding-bottom:var(--revPagerH)`** — 60px held for the pager row this section used to have BELOW the deck — and the deck is centred in what is left. So the stage's own middle sits half that padding lower than the card's, and the pager is absolutely positioned against the STAGE. Measured at 900x1000: stage **472** tall against a **362** card, deck inset **25** from the top, stage centre **432.8** against card centre **402.8**. **The arrow was exactly 30px low, which is half of 60.** ⭐ **THE FIX TAKES THE PADDING OUT OF THE SUM BEFORE HALVING IT** — `top:calc((100% - var(--revPagerH)) / 2)` — so it lands on the card at any height with no second measurement to keep in step. ⛔ **IT MUST STAY IN TERMS OF `--revPagerH`.** A hard `206px`, or a `-30px` nudge on the transform, would drift the moment that reservation or `--revCardH` changes, and BOTH are clamps that move with the viewport (`--revCardH` is `clamp(264px,38.5vh,362px)`, `--revAir` is `clamp(38px,5vh,56px)`). ⚠️ **THIS IS THE THIRD FAULT IN THIS ONE PAGER IN TWO DAYS AND ALL THREE WERE THE SAME MISTAKE** — a position expressed against the wrong box. D282: the horizontal offset assumed a peek gutter that does not exist on the tablet. D286: the stylesheet rebuilt the gap from the card scale instead of the geometry the cards are placed by. D287: the vertical offset assumed the stage was the card. ⭐ **BOTH AXES ARE NOW DERIVED FROM THE THING THEY ARE MEANT TO LINE UP WITH**, which is the only version that has measured right at every width. | Verified on fresh loads against the card's own rect. **Vertical: 0.00px off the card centre at 375, 721 and 900.** Horizontal unchanged and still exact — 0.07px at 375, 0.05 at 900, 0.05/0.06 at 721. Chrome identical everywhere, **26x44, `border-radius:0`, gold at 0.42**. ⛔ Desktop untouched: `#reviews` is `mode-grid` there, its pager is the 46px disc and its `top` computes to 188.5px off the base rule. Freeze probe at 1440 unchanged — `.gal-scroll` **4950**, `--revPer` **3**, `feTurbulence` **60**, `#svcNav` **8**, elements **2658**, hero ink **164.683**, About **759**, collage **497x676**, no broken images, no horizontal overflow, no console error |
| D286 | 17 Aug | ⛔⛔ **THE ROUND PAGER BUTTONS ON TABLET ARE REVERSED AFTER ONE DAY, AND THE POSITION IS NOW MEASURED RATHER THAN RECONSTRUCTED.** Client, with a screenshot: *"you added two arrows... in circles, which it's not supposed to be. It's supposed to look like the two golden arrows between the two cards and the exact center between the two cards... like it is on mobile."* ⭐⭐ **D282 DIAGNOSED THE BUG CORRECTLY AND THEN ANSWERED IT WRONGLY, WHICH IS THE PART WORTH KEEPING.** The arrows really were landing 13px ON the quote, and the cause really was `--revScale` measuring 1.0000 on this band. But the fix dressed them as the site's round `.wbtn` on the argument that every arrow control on the site is a circle — a rule about CONSISTENCY that he had never applied here, over a deliberate D93 decision that these are *a hint, not a control*. **A correct diagnosis does not license a redesign.** ⛔⛔⛔ **THE REAL FAULT WAS THAT THE STYLESHEET HELD A SECOND OPINION ABOUT WHERE THE CARDS ARE.** `.rev-solo` rebuilt the peek from the card scale — `deckW × (1 − scale) / 2` — and put the arrow in the middle of THAT. On the tablet the scale is 1, the term goes to zero, and the expression collapses to `cardLeft − 13px`. ⭐⭐⭐ **THE CARDS RIDE A 3D DRUM UNDER PERSPECTIVE, SO THE VISIBLE GAP IS A PROJECTED DISTANCE AND FLAT ARITHMETIC CANNOT REACH IT** — measured, the gap is **31.5px at 900 and 21.3 at 375 where `SOLO_GAP` states 12**. ⭐⭐ **`soloGapX()` PROJECTS THE NEIGHBOUR'S INNER EDGE THROUGH THE SAME TRANSFORM THE RENDERER USES** and publishes the midpoint as `--revPagerX`; the CSS is `50% ± var(--revPagerX)` and nothing else. It is the same projection `soloRadius()` inverts, so there is now ONE derivation of this distance where there were two. ⚠️ It also corrected the PHONE, which was **2.59px off centre** and had never been noticed — he believed mobile was already exact, and now it is. ⛔ Do not put a hand-typed offset back here: it cannot survive a change to `SOLO_GAP`, `SOLO_NS` or `SOLO_ANG`, and none of those is a constant a future round would think to check. ⚠️ **AND THE TABLET NEEDS NO PAGER RULE OF ITS OWN NOW.** `.rev-solo` is a CLASS, not a media query, so deleting D282's whole block hands this band the phone's own chevron with nothing restating it. **One dress, one position, two bands.** ⚠️ I removed the chrome along with the position on the first pass and the phone briefly wore the 46px disc — caught by measuring, not by looking. | Verified on fresh loads by comparing the arrow's centre against the midpoint of the two card rects. **375: off by 0.07px. 900: 0.05px. 721 and 1120: 0.06px.** Chrome identical at every width — **26x44, `border-radius:0`, no border, gold at 0.42**. ⛔ Desktop untouched: `#reviews` is `mode-grid` there, not `rev-solo`, and its pager is still the 46px disc. Freeze probe at 1440 unchanged — `.gal-scroll` **4950**, `--revPer` **3**, `feTurbulence` **60**, `#svcNav` **8**, elements **2658**, hero ink **164.683**, About **759**, collage **497x676**, no broken images, no horizontal overflow, no console error |
| D285 | 17 Aug | ⛔⛔⛔ **EVERY COMMIT GOES TO `main` FROM NOW ON, AND ONE `git push` UPDATES BOTH REFS.** Client: *"push to github!!! my developer cant see the new update. says updated 2 days ago"* — and then *"make sure it updates to the same place every time."* ⭐⭐⭐ **HIS DEVELOPER WAS RIGHT AND IT WAS NEVER A PUSH FAILURE.** Thirty-six commits were on `tablet-round-d197-d200`, pushed and current. **`main` was still at `f53b07c` from two days earlier**, the photography round — and `main` is what GitHub shows when you open the repo. Everything from D246 to D284 was invisible to anyone who did not know the branch name: the plates, the internal pages, the weld, the Argento correction, the directors, the new hero. **A branch that is reported as "pushed" is not the same as work anybody can see.** ⭐ **THE FIX IS STRUCTURAL, NOT A HABIT TO REMEMBER.** Local work is on `main` now, and `remote.origin.push` carries TWO refspecs, so a plain `git push` moves `main` AND the old round branch to the same commit every time:
`refs/heads/main:refs/heads/main` and `refs/heads/main:refs/heads/tablet-round-d197-d200`. ⛔ **THE STALE BRANCH IS DELIBERATELY NOT DELETED.** Its name is long out of date — it says D197-D200 and the register is at D285 — but **we still do not know how files reach `thadeusg3.sg-host.com` (§13 item 1, asked nine times)**, so if the developer's process pulls that branch by name, deleting it would break them silently. Keeping the two refs in lockstep costs nothing and cannot break anyone. ⚠️ Delete it only once the upload mechanism is known and confirmed not to reference it. ⚠️ **THIS IS ALSO WHY THE ARGENTO ROUND COST WHAT IT DID.** D276 version-stamped every slab URL so a corrected file would actually reach a browser — but the corrected file still has to reach the HOST, and the host is fed from the repo by a human. Cache-busting a URL nobody has re-uploaded fixes nothing. | Verified: `git ls-remote` shows `refs/heads/main` and `refs/heads/tablet-round-d197-d200` both at **1e4e2aa**, and the fast-forward was clean — `main` had **0** commits the branch lacked. The new asset files confirmed present on `main`: `hero-night-2752.webp`, `nick-320/640.webp`, `rimsha-320/640.webp`. `git push --dry-run` from `main` reports both refspecs up to date |
| D284 | 17 Aug | ⛔⛔⛔ **THE HERO'S LEFT-TO-RIGHT RAMP IS GONE. IT WAS AIMED AT A COMPOSITION THAT STOPPED EXISTING TWICE OVER.** Client, on the new photograph: *"because of the previous hero section that we had, the darker shadow is still coming in from the left side of the screen. So we now need to readjust that so it isn't just darkening out the left side of the screen. And just make sure the text is all very clear and very readable."* ⭐⭐⭐ **HE IS RIGHT TWICE AND BOTH HALVES MEASURE.** The third shade layer ran **0.94 at the left edge to 0.06 at 74%**, written in July for copy on the left and a photograph on the right. The copy has been CENTRED since D232 and the picture changed at D283, so that ramp was painting a wall over the one part of the frame with nothing in it: composited veil **0.944 left against 0.414 right, a spread of 0.53**. ⛔⛔ **AND THE TEXT WAS FAILING EXACTLY WHERE THE RAMP GAVE UP.** The new photograph is brighter on the right, which is where the old ramp had faded to 0.06 — worst pixel behind the gold word came out rgb(108,100,52), **2.56:1, BELOW the 3:1 large-text threshold**. The white headline was 4.95. ⭐⭐ **THE ALPHA MOVED FROM THE LEFT TO THE RIGHT, WHICH IS WHERE THE PICTURE'S OWN LIGHT IS**: a 0.26 → 0.46 lean, a 0.14 tilt toward the lit kitchen instead of a 0.53 wall over the dark living room. The centred scrim takes the rest, 0.58/0.30 → **0.70/0.36** on a slightly wider ellipse, because it sits under centred copy and that is the one place worth spending veil. ⭐⭐⭐ **AND D187's FLAT PHONE WASH IS RETIRED, WHICH IS THE PART WORTH KEEPING.** That wash existed for ONE stated reason — the big layer was a left-to-right ramp, so on a centred phone the title sat over its thin end. **That reason died with the ramp**, so the phone and tablet take the same centred scrim as everything else: one veil, one definition. ⭐ **It is better on both counts, measured at 375: the gold goes 2.68 → 4.67 (it was below threshold there too), the headline 4.58 → 6.03 — AND THE PHOTOGRAPH'S OWN EDGE GETS LIGHTER, 0.484 → 0.472.** ⛔ Raising the flat wash instead was scored and is the worse trade every time: at 0.42 the gold reaches only 3.40 while the edge goes to 0.573. That is D217's lesson intact. ⚠️ **This supersedes D238's "one veil per band"** — which was right while the flat wash WAS the band's veil. There is still only one. ⚠️ **A WORST-PIXEL FIGURE OVERSTATES THE PROBLEM AND IT IS WORTH KNOWING.** At 900 the gold's worst sample reads 3.05 — but it is **one sample in 14,175, at relX 0.00 of the element box**, and the 1st percentile is **7.04** with a median of 8.39. Quote the distribution, not the extreme, when deciding whether type is readable. | Measured by compositing, never by looking (§12): the served photograph drawn to a canvas at its own rendered `cover` scale and offset, all five shade layers evaluated analytically at each sample. **1440x900 after — title worst 5.64 / p1 10.9, gold worst 4.38 / p1 5.19, sub worst 14.34; edges 0.462 L, 0.753 mid, 0.605 R.** **375 — title 6.03, gold 4.67, sub 7.93.** **900 — title 6.20, gold p1 7.04, sub 8.75.** Ten alternatives were scored before this one was chosen. Freeze probe unchanged at 1440: `.gal-scroll` **4950**, `--revPer` **3**, `feTurbulence` **60**, `#svcNav` **8**, elements **2658**, hero ink **164.683**, About **759**, collage **497x676**, no broken images, no horizontal overflow, no console error |
| D283 | 17 Aug | ⭐⭐ **A NEW HERO PHOTOGRAPH.** Client, sending the file: *"this is the new hero section image. add it in."* It is the same penthouse kitchen at night, framed closer: the island is bigger and more central, the gold-veined marble reads at a glance, and the pendants are in the top third. ⭐ **IT IS ALSO A BETTER HERO THAN THE ONE IT REPLACES, MEASURED** — the left 42% of the frame, which is where the copy sits, goes from **32.6 to 24.2** mean luminance, so the headline has more to stand on and `.hero-shade`'s left-to-right ramp is doing less work for the same result. Whole-frame mean is near enough unchanged, 37.6 → 35.5. ⛔⛔ **IT WENT INTO FIVE PLACES, NOT ONE, AND THE FIFTH IS THE ONE THAT IS EASY TO MISS.** The `<img>`, the `<link rel=preload>` in the head, the `SS` ladder entry keyed on the top rung, and **BOTH `--pageHeadImg` DECLARATIONS** — the `.page-head` band on the seven internal pages. D196's own comment says that band IS *"the landing hero's own night kitchen… so there is one place to change the picture"*, so leaving it behind would have put a different kitchen on the internal pages from the one on the home page and quietly falsified the note explaining it. ⭐ **A NEW PREFIX, NOT NEW BYTES UNDER THE OLD ONE (D241)**: `hero-night-*`. ⚠️ **THE LADDER IS 1400/2000/2752, NOT THE OLD 1400/2000/2750** — the top rung is the file's own native width, because the pipeline never upscales. Aspect 1.7917 against the old 1.7904, so the `width`/`height` attributes went 2750 → 2752 and nothing about the layout moved. ⚠️ **`hero-kitchen-*` IS NOW UNREFERENCED ANYWHERE ON THE SITE** — §10's note listing it among the three shared photographs that must not be deleted is stale as of today. It is kept on disk regardless. | Cut with the pipeline's own rules — LANCZOS, WebP q85, method 6 — at **56.3 / 93.0 / 148.1 KB**, against the old ladder's 55.5 / 95.3 / 153.4, so there is no weight change to speak of. Verified at 1440x900 on a fresh load: `#heroImg` serves `hero-night-2752.webp`, the preload resolves to `hero-night-2000.webp`, hero ink unmoved at **164.683**, no broken images, no horizontal overflow, no console error. `/services/` re-checked and the head band carries the new picture. Freeze probe otherwise unchanged: `.gal-scroll` **4950**, `--revPer` **3**, `feTurbulence` **60**, `#svcNav` **8**, elements **2658**, About **759**, collage **497x676**. All four builders re-run; `harvest/verify.py` **132/132/132 ✅**. ⛔ Source PNG and the cut script kept in `assets/site/.src-2026-08-17/` |
| D282 | 16 Aug | ⛔⛔⛔ **THE REVIEW ARROWS SAT ON THE CARD ON TABLET, AND IT WAS A POSITION FAULT, NOT A STYLING ONE.** Client: *"the arrows between the reviews have to be better on tablet."* ⭐⭐ **`.rev-solo`'s ARROW POSITION IS BUILT OUT OF `--revScale` AND THIS BAND HAS NO SCALE.** On a phone the card is transform-scaled inside the deck, so it sits `deckW x (1 - scale) / 2` in from the edge and the arrow is placed in the middle of that inset — a real gutter. **On the tablet `--revScale` measures 1.0000**, the inset is zero, and the whole expression collapses to `cardLeft - 13px`. Measured at 900x1000: card left **248.1**, arrow spanning **235.1 to 261.1** — thirteen pixels of it over the quote. ⭐ **AND THE BARE CHEVRON WAS A PHONE DECISION (D93)**, taken because a 46px disc beside a card that fills a 375px screen is a control shouting over the content — *"he asked for a hint"*. This band leaves **210px of empty margin either side of the card**, where a hint reads as a stray mark instead. **§2 rule 11's own carve-out applies: a CONTROL IS NOT A MARK, and every arrow control on this site is a circle** — the lightbox pair, the stone wheel's pair, and the helix pair D273 put back into `.wbtn` on his instruction. So the base `.rev-page` disc returns at **46px**, which is also a real touch target. ⚠️ The offset is measured OUT from the card's own edge, `-60px` for a 46px button and a 14px gap, so it holds wherever `--revCardW` lands rather than assuming a stage width. ⚠️ The disabled floor is **0.3**, not the base 0.22 — at 46px on a dark ground that reads as a smudge rather than an end-of-run arrow. | Tablet band only (721-1120), inside `#reviews.rev-solo`. Verified at 900x1000: buttons **46x46**, prev **188.1-234.1** and next **665.9-711.9** against a card at **248.1-651.9** — **14.0px of clearance on both sides, symmetric**, no horizontal overflow. ⭐ **Phone re-checked and D93 is intact**: at 375 the arrow is still 26x44, `border-radius:0`, no border, opacity 0.42. Desktop untouched — `#reviews` is `mode-grid` there, not `rev-solo`, and its pager was already the 46px disc |
| D281 | 16 Aug | ⭐⭐ **THE AFTERCARE BANNER IS A LEDGER ON A NARROW SCREEN, NOT A WRAPPED ROW.** Client, with a screenshot: *"this does not look very good on mobile, so redesign that layout or whatever to make it look better on mobile."* ⚠️ **THE RULE HE IS LOOKING AT SAID SO ITSELF** — `flex-wrap:wrap` under a comment reading *"serviceable; proper mobile pass comes later"*. This is that pass. ⛔⛔ **WRAPPING A HORIZONTAL RUN DOES NOT PRODUCE A STACK, IT PRODUCES A RAGGED ROW.** Tile 05 is one line of facts with diamonds between them, so at 390px the wrap broke it after the diamond: `72h` and its caption filled line one, **the diamond was left stranded at the right-hand end of it**, and `10-year` began line two under nothing. Every piece was inside the box and none of it lined up. ⭐⭐⭐ **`display:contents` ON THE TWO CREDENTIAL GROUPS IS WHAT MAKES IT A LEDGER.** It dissolves `.pe-big` and `.pe-item` so their own `b` and `span` become items of the BANNER's grid — which means **both gold figures land in the same column and size it together**, and both captions start on one edge. Nesting two 2-column grids instead gives two columns that each fit their own number and agree with nothing. ⚠️ **THE DIAMOND STAYS A DIAMOND**, centred on a row of its own. A full-width rule between the two credentials would read as a gold line across a panel and §2 rule 10 is emphatic about those; the diamond is the site's own separator and needed no reinventing. | Applies wherever the Process flow is stacked, `max-width:980px`, so the phone and the lower tablet both take it. Verified at 375x812: banner **335x169**, the two figures sharing one column at 55 and 67px wide, captions on a single edge, no horizontal overflow. Re-checked at 900x1000: **600x166**, same ledger. ⛔ **Desktop untouched** — at 1440 tile 05 is still the horizontal run, `711x202`, `display:flex` |
| D280 | 16 Aug | ⛔ **THE DESCRIPTION LINE UNDER EACH DIRECTOR IS GONE, THE SAME EVENING IT WENT IN.** Client, seeing it: *"remove the details below their name and role, and move the text a little further down."* ⚠️ **IT WAS HIS OWN SUGGESTION AT D279** — *"some sort of description kind of part for them"* — so this is a reversal of his idea, not of mine, and the plate is back to a name and a role. ⭐ **THE `@container (min-width:190px)` RULE WENT WITH IT, AND `container-type` CAME OFF `.ac-p`**: it existed only to decide whether the card was wide enough to carry the sentence, and a containment boundary on every portrait that gates nothing is a cost with no return. ⛔ **Do not re-add a note without it** — the card's width does not follow the viewport's (241 on a wide desktop, 275 on an 800px tablet, 162 on a 900px one), so no breakpoint can stand in for a container query here. ⭐⭐ **AND "FURTHER DOWN" IS BOTH PADDINGS, NOT THE BOTTOM ONE.** `.ac-p` is `align-items:flex-end`, so the plate is pinned to the card's bottom edge and its own HEIGHT is what decides where its top lands — pulling the top padding in moves the whole block down exactly as pulling the bottom one in does. Together: `clamp(12px,1.8vh,20px)/clamp(9px,1.3vh,14px)` → `clamp(9px,1.4vh,15px)/clamp(3px,0.5vh,7px)`. | Verified on fresh loads at all three bands. **Desktop 1440x900:** plate **127px → 66**, 53% of the card → **27.4%**, the name now starting **61% down** the card; plates still equal. **Tablet 900:** plate 63, **38.9%**. **Phone 390:** plate 60, **36.8%**, name 15.1px. Nothing structural moved — section **759**, collage **497x676**, bought scroll **919.75** all unchanged, so the weld is untouched: all six tiles **0° / opacity 1** past contact on the real section and the mirror. No broken images, no horizontal overflow |
| D279b | 16 Aug | ⭐⭐ **THE TABLET GETS A FOURTH PHOTOGRAPH, A COLLAGE THAT FILLS ITS COLUMN, AND THE FEATURE IMAGE DROPPED TO THE SLAB.** Client: *"we also need to add more images to the collage for the tablet version as well, so it can just look better… the big first image in more reasons to choose our section, I want you to move that image down so you can see the slab in the image and not just the person standing there in the tablet version."* ⭐ **THE FOURTH IMAGE COST NOTHING.** `.ac-w4`, the Harrow island, has been in the DOM since D244 and `display:none` below 1121px — turning it on is a row in the tablet grid: no new file, no seventh tile, and no change to the build timing, which is bounded at six. **The tablet had only ever seen three.** ⛔⛔ **AND THE COLLAGE WAS STOPPING 150px SHORT OF THE STORY BESIDE IT** — 333x529 against a copy column of 679, the same fault D244 fixed on the desktop. `align-self:stretch` hands that height to the three work rows. ⚠️ **SCOPED TO 881px AND UP, WHICH IS WHERE `.about-wrap` STILL HAS TWO COLUMNS.** Below 880 the collage and the copy are STACKED, so there is no sibling to match and `stretch` would leave the fr rows nothing to share and collapse the mosaic onto its two definite rows. The derived ratio stays in charge on that side of the line. ⛔⛔ **THE FEATURE IMAGE IS A COVER CROP, AND HE DESCRIBED IT EXACTLY.** `why-team-900.webp` is SQUARE on purpose — the tile is a tall portrait on the desktop and a wide band on the tablet — and the marble is in the bottom third. At **810x260** the cover scale is 0.9, so the box shows 260 of 810 scaled pixels and a centred crop takes the middle: aprons, no stone. `object-position:50% 88%` slides that window to scaled y **484-744**, which is the lower aprons, the hands and the slab. ⚠️ **TABLET ONLY** — the phone puts the same tile at about 350x260, square enough that the centred crop already reaches the stone, and the desktop tile is a PORTRAIT where dropping the window would cut the heads off. | Verified at 900x1000 on a fresh load: collage **333x679, matching the copy column exactly**, four work tiles (219x219 / 104x117 / 104x243 / 333x128) and the pair at **162x162** with the note correctly off and the plate at 46%; the Why tile shows the slab and the hands. Phone re-checked at 390x844: collage **335x393**, cards **163x163**, note off, name 15.1px, `.ac-w4` still `display:none`, no horizontal overflow, no broken images. docH 14972 → **14918** on the phone and **14164** at 900, both by design — the directors are one square row now instead of three of six |
| D279 | 16 Aug | ⭐⭐⭐ **THE DIRECTORS ARE THE BOTTOM BAND OF THE COLLAGE NOW, AT EVERY WIDTH, AND D278's PAIR WAS TOO SMALL.** Client: *"on the mobile and tablet version, the images still have to be small. But, also, on the desktop version, the text on their names are too small… It needs to be the correct size for the site… there needs to be a little bit more emphasis on them, but also not too much. Right now, that's too small. So maybe we should put them on the bottom… bigger and on the bottom side part of the collage and taking up the full bottom space with maybe some sort of description kind of part for them."* ⭐⭐ **BOTH HALVES OF D278 WERE WRONG AND THEY WERE WRONG TOGETHER:** shrinking the tile to 114px forced the type down with it, and 10.4px is not a size this site uses anywhere. **The card is 241 x 241 on the desktop now — 3.4x the area — and the type went straight back to the base sizes, 17px name and 10px role, with nothing overriding them.** ⛔ The D278 re-key is DELETED rather than retuned, so there is one definition of this plate again. ⛔⛔⛔ **THE PAIR MOVED IN THE DOM, WHICH D278 WENT OUT OF ITS WAY TO AVOID, AND HERE IT IS CORRECT.** DOM order IS the build order, so a band at the foot of the grid must be at the foot of the markup; `picks` and `HINGE` are both indexed by DOM position and both were re-ordered with it. **The reward is that the three work photographs go back to shapes they do not have to be cropped into:** w2 lands at **1.19** against `about-fitting`'s native **1.199** and w3 at **0.567** against `fitting.jpg`'s native **0.562** — effectively uncropped, where the D278 top band had bent both. ⭐⭐ **THE CARD IS SQUARE, AND THAT IS THE ONLY SHAPE THAT SATISFIES BOTH ASKS.** He wants them bigger AND still stood back. Nick's frame carries **190px of paper above his crown**, so a wider card forces a shorter crop, which forces a close-up. At 1:1 his head is half the frame with his shoulders in it. ⭐⭐⭐ **AND THE NOTE LINE IS GATED BY A CONTAINER QUERY, NOT A BREAKPOINT, BECAUSE THE CARD'S WIDTH DOES NOT FOLLOW THE VIEWPORT'S.** Half the collage is **241px** on a wide desktop, **275** on an 800px tablet held single column, and **162** on a 900px one held in two — the tablet is bigger than the desktop at one end of its own band and smaller at the other. Below 190px the sentence runs to three lines and the plate takes 59% of the card, which put the scrim across both their chins at 900. `@container (min-width:190px)` turns the note and the roomier top padding on together. Plate lands at **46%** on a 162px card and **53%** on a 241px one. ⚠️ **THE TWO NOTE SENTENCES ARE MINE.** They describe the role and claim nothing (§2 rule 12) — Nick's restates the promise the copy beside it already makes. **His own wording is owed.** | Verified on fresh loads. **Desktop 1440x900:** cards **241x241**, name 17px, role 10px, note 11.9px on two lines, plate 127px; work tiles 327x276 / 156x131 / 156x276 / 327x131; collage 497x676 and section **759 — both unchanged**, so the weld is untouched. Weld swept: all six tiles **0° / opacity 1 at px 1400**, before contact at 1440, on the real section AND the mirror; hand-over exact at **0.00px in x, 0.43 in y**. ⭐ **AND THE PAIR NOW FOLDS LAST, IN FULL VIEW** — measured at px 1024, where the slab's leading edge has reached x=884, both directors are still at **78°** and have their whole fold ahead of them, which is D274's brief satisfied by the new order rather than in spite of it. Freeze probe: `.gal-scroll` **4950**, `--revPer` **3**, `feTurbulence` **60**, `#svcNav` **8**, hero ink **164.683**, elements **2660**, no broken images, no horizontal overflow, no console error. `harvest/verify.py` **132/132/132 ✅** |
| D278b | 16 Aug | ⛔⛔⛔ **THE STUDIO BACKGROUND STAYS — HE REVERSED THE CUT-OUT MID-ROUND.** Client: *"do not remove the backgrounds from their images and also put them maybe slightly further away so they're not so close to the screen and also maybe put a filter on so that they don't look so bright. So don't remove the background."* ⭐ **THE REASON THE FIRST PASS MASKED IT OUT IS WORTH KEEPING**, because the problem it solved is real: the paper measures **195** in the cut, which the collage's own `brightness(.85)` only brings to 166, against a mosaic whose photographs mean 124 — two lit studio rectangles shouting out of a block of dark stone. **He is right that removing it was the wrong instrument. The TONE was the fault, not the background**, so it is corrected in CSS on `.ac-p img` where it is one tunable line and nothing is baked into the file. ⚠️ **FILTERS DO NOT COMPOUND** — the declaration REPLACES `.ac-tile img`'s, so `brightness(.68)` is the whole of it, and the hover has to be restated or `.ac-tile:hover img` snaps the pair to full brightness, a jump nothing else in the collage makes. ⭐⭐ **STOOD BACK:** eye-to-chin goes from 23.5% of the crop height to **20%**, so the face is 15% smaller and the shoulders and chest carry the bottom half. ⛔ **IT CANNOT GO MUCH FURTHER** — Nick's frame has only **190px of paper above his crown**, and a smaller head in a 3:4 tile needs a TALLER crop, which can only grow downwards; at 0.20 his crop already starts 11px from the top edge. Both crops are also nearly as wide as their frames, so centring on the head runs off the left edge: clamped, each head sits at **47.5%** rather than 50, the same on both. ⛔ **AND THE FILES ARE VERSION-STAMPED (`?v=2`), WHICH IS D276's RULE APPLIED HERE** — the filename is the person, so a re-cut portrait is the same URL as the one before it, which is exactly how he was shown a stale Argento through two corrections. **Bump both tiles in step whenever either is re-cut.** | Measured **on the pixels the browser paints**, by drawing each served tile to a canvas with its own computed filter applied: the pair now reads **77.0 and 77.3** against the mosaic's **60.3 / 97.9 / 105.5** — inside the block's range and no longer the brightest thing in it, and matched to each other within 0.3. Eye lines matched to **1.5% of tile height**, face size matched (eye-to-chin 420px against 430px in the sources). Skin: unsharp at r2.0/46% to put the pore texture back under the AI smoothing, then a fine grain laid on AFTER the resize so the resample cannot eat it. Rungs 240 and 624, 3:4 exactly, LANCZOS / WebP q85 / method 6 — 5.3, 21.1, 6.7 and 28.0 KB. ⛔ Originals and the cut script kept in `assets/team/.src-2026-08-16/` |
| D278 | 16 Aug | ⭐⭐⭐ **THE DIRECTORS ARE REAL PHOTOGRAPHS NOW, AND THEY ARE SMALL.** The client sent both portraits — his own people, AI-retouched by him — and asked for them played DOWN: *"add this into their spots… make their images in the about us section smaller. Like, make them one of the smaller images next to each other. So add more images to the collage if you have to, but we just can't have their image be very big because I had to edit it with AI… crop them perfectly so that their heads are the correct height and that their skin maybe looks more realistic… just head and shoulders… we have to put less focus on them and just make them a part of it."* ⭐⭐ **THE PLATES HAVE STOOD EMPTY SINCE 9 AUG AND THE COMPONENT WAS BUILT FOR THIS** — its own comment said the only change when the shoot landed would be an `<img>` above the figcaption, and that is what it took. The `.acp-mark` medallion went with them: a ringed glyph over a photograph sits on the face. ⛔⛔⛔ **THE GRID GOES TO TWELVE COLUMNS ON DESKTOP AND NOTHING MOVES IN THE DOM, WHICH IS THE WHOLE POINT.** The pair drops from 241.4 x 321.9 to **113.7 x 151.7** — **22% of the area**, just under the smallest photograph in the block. Doubling the tracks is the same free trade D150 made going 3 to 6: every existing span doubles. **The arrangement was chosen so that each tile's first cell runs p1 p2 w1 w2 w3 w4 in reading order, which is already the markup order** — so `picks` keeps its mapping, `HINGE` keeps its indices, the six-tile timing is untouched and the frozen bands are not altered at all. Half a dozen tidier-looking layouts were measured and every one needed the figures shuffled, which would have re-ordered the build on the phone and tablet for nothing he asked for. ⚠️ **THE THREE WORK PHOTOGRAPHS KEEP CROPS THEY CAN TAKE:** w2 goes 1.386 → 1.42, no change at all; w3 stays the tall portrait its picture needs; **w4 goes from a 2.9 letterbox to 0.925 and is better for it** — the Harrow waterfall end, the gold tap and the navy units all read now where the strip cut them off. ⛔⛔ **AND THE PLATE TYPE HAD TO BE RE-KEYED, WHICH FAILED SILENTLY FIRST.** Keyed to `--sideW`, a 17px name landed in a 114px tile with the plate covering 57% of the picture. The override was written in the main `min-width:1121px` query, **which sits ABOVE the `.acp-*` declarations in the file, so it lost on SOURCE ORDER and did nothing** — the grid moved, the type did not, and every other number in the same edit landed. It lives directly under those declarations now. **Third time on this project (§5, D271).** ⚠️ **THE SECTION IS 26px SHORTER** (785 → 759) because the Topcat plate spans different tracks, so the copy governs the row rather than the collage. The weld absorbs it: the bought scroll goes 906.75 → **919.75**, and the landing gains air, 18.25 → **31.25px** above and below. | Desktop only for the layout; the photographs are in at every width. Verified at 1440x900 on a fresh load: pair **114x152**, plates equal at 56px, both roles on ONE line with no overflow, **no broken images, no horizontal overflow, no console error**. Weld swept end to end: rest window dead, portraits fold in on the right slab's leading edge, **all six tiles 0° / opacity 1 at contact** on the real section AND the mirror, hand-over exact — clone and real agree to **0.00px in x and 0.43px in y** one pixel short of the end. Freeze probe unchanged: `.gal-scroll` **4950**, `--revPer` **3**, `feTurbulence` **60**, `#svcNav` **8**, hero ink **164.683**, elements **2663 → 2658** (the two medallions out, two images in). **Frozen bands re-checked on fresh loads: tablet docH 13559, identical to the register; phone at 390x844 gives 14972 — and so does the UNTOUCHED file, measured by reverting index.html and reloading, so the register's 15125 was taken at another viewport height and is not a regression here.** `harvest/verify.py` **132/132/132 ✅** |
| D276 | 16 Aug | ⛔⛔⛔ **A CORRECTED SLAB IS THE SAME URL AS THE WRONG ONE, AND THAT IS WHY HE KEPT SEEING THE OLD PICTURE.** Argento was re-cut twice and reported wrong twice; the second report — *"most of the pink on the side is gone now, but there's still pink in it"* — **describes the INTERMEDIATE file exactly**: the side ramp reduced, the right third still pink. That is the global-gain pass, not the flat-field that replaced it. ⭐⭐ **SLAB FILES ARE NOT CONTENT-HASHED AND THE FILENAME IS THE SLUG**, so a browser holding `argento.webp` has no reason to ask for it again, and there is nothing in the URL to tell it otherwise. `site.css` and `site.js` have carried a hash for weeks; the 264 slab files never have. ⭐ **`SLAB_V` NOW STAMPS EVERY SLAB URL** — `tileURL`/`tileSrcset` in `index.html` and the two sites in `stones/build_stones.py`. **Bump it whenever any slab is re-cut**; one string busts all 132 at once, which is right, because slab corrections come in rounds rather than one at a time. ⚠️ **THE TWO CONSTANTS MUST STAY IN STEP** — the wheel reads one and the 132 pages read the other. ⛔ **IT DOES NOT HELP `thadeusg3.sg-host.com`**, which needs the build re-uploading whatever the URL says. ⭐ This also closes the same trap for `service.css`/`stone.css` in spirit, though those are still open (§13 item 15). | `SLAB_V="2"`. Verified: the wheel requests `/assets/slabs/argento-s.webp?v=2`, and the collection page carries **132 slab images, 132 of them versioned, 0 broken, no horizontal overflow**. `harvest/verify.py` **132/132/132 ✅** |
| D275 | 16 Aug | ⛔⛔⛔ **ARGENTO WAS PINK AND THE STONE IS NOT — THE PIPELINE DID IT, AND THE PAGE WAS THEN WRITTEN TO MATCH THE FAULT.** Client, with a screenshot: *"why does the stone have the pink colour on it? Because I just Googled the stone, and it doesn't look like it has any pink on it. So maybe you made an error with the image, but fix it."* ⭐⭐⭐ **HE IS RIGHT AND IT WAS MINE.** The supplier's own photograph (`harvest/raw/next/argento.jpg`) is a warm off-white, field r−g **+6.06**; the shipped tile came out at r−g **+7.59** with field chroma **10.48 against the source's 9.16** — the upscale lifted saturation about 14% on a stone whose whole character is a near-neutral white, and on a pale low-chroma field that small lift is the difference between "warm cream" and "pink". ⚠️ **THE MEAN BARELY MOVED, WHICH IS WHY NOTHING CAUGHT IT** — the fault is in the CHROMA of the flat field, not in the average colour. ⛔⛔⛔ **THE FIRST FIX WAS ONE GLOBAL WHITE BALANCE AND HE CAME STRAIGHT BACK: "the stone still has the pink". HE WAS RIGHT AGAIN, AND THE REASON IS THE ONE WORTH KEEPING — A SINGLE GAIN CANNOT CORRECT A GRADIENT.** Mapping r−g over a 6×6 grid instead of taking one mean showed the fault is a left-to-right RAMP: the shipped tile ran **+5 on the left to +14 on the right**, and the supplier's own photograph ramps **+4 to +8.5** across the frame — the slab was lit unevenly when it was shot and the upscale doubled the far side. **The global gain split the difference: it left the right third at +5 and pushed the left to −4, so one half was still pink and the other had gone faintly green.** ⚠️ **A MEAN HIDES A RAMP. MEASURE A GRID.** ⭐⭐ **THE FIX IS A COLOUR FLAT-FIELD, WHICH IS THE RIGHT INSTRUMENT FOR UNEVEN LIGHT:** take a heavy low-pass of the frame, and at every pixel divide by that blurred colour's own mean, so the slowly varying cast is removed and every local detail is untouched. **The veins keep their warmth because the correction varies only slowly and cannot see them** — measured, their lean against the field goes +1.22 before to +1.38 after. Grid after: every one of 36 cells between **−0.5 and +0.8**, against +4 to +14 before, luminance held at 214.5. ⛔⛔ **AND THE STONE PAGE'S OWN COPY SAID `"A white with a soft pink cast and fine grey veining across it"`** — written honestly against the bad tile, which is what `descriptions.py` tells you to do, so the page was selling a pink stone in words as well as in the picture. Rewritten against the corrected tile. ⚠️ **THAT FILE'S HEADER ALREADY WARNS ABOUT EXACTLY THIS** — *"IF A TILE IS RECROPPED OR REPLACED, ITS LINE HERE IS STALE"* — so any future slab correction must go looking for its sentence. ⚠️ **THE TILE IS OVERWRITTEN IN PLACE, NOT GIVEN A NEW PREFIX (D241's rule), AND THAT IS A CONSIDERED EXCEPTION:** slab filenames are slug-derived and read by the wheel, 132 stone pages, the collection and compare, so a new prefix means a per-stone override in four builders for one file. ⛔ **The originals are kept in `assets/slabs/.pre-wb-2026-08-16/`.** ⚠️ **It does mean his browser can serve the pink one from cache** — a hard reload, or ask the developer to re-upload. | `assets/slabs/argento.webp` + `-s`, LANCZOS / WebP q85 / method 6, 34.7 KB and 12.1 KB. Verified on the PIXELS THE BROWSER PAINTS, by drawing the served tile to a canvas and mapping r−g over 36 cells: **worst cell +0.8**, everything else between −0.5 and +0.5. `harvest/verify.py` **132/132/132 ✅**. ⭐ **A SWEEP OF ALL 132 FOR THE SAME FAULT** (pale stone, coloured flat field) leaves 15 others, and they are almost all TRUE to their stones — Taj Mahal, Colombo Juparana, Crema Evora, Travertine, Sahara Dunes, Calacatta Gold Oro, Concrete Earth are legitimately warm. ⚠️ **THE TWO WORTH A LOOK ARE BLUE, NOT PINK: `arabescato-grey` (r−b −13.78) and `calacatta-gold-shimmer` (−12.39)** — D257 already measured Gold Shimmer's blue cast when it was rejected for the About plate. **Told him; not touched without an instruction** |
| D274 | 16 Aug | ⛔⛔ **EVERY COLLAGE CARD FOLDS IN WHERE IT CAN BE SEEN — D272 OVERSHOT AND ONLY THE LAST ONE DID.** Client: *"in the about us section, make sure that every one of those cards are animating in, not just the last one. So the Nick and Rimsha card must fold, and the rest of each card must also have the fold and then still animate the right way."* ⭐⭐⭐ **HE IS DESCRIBING MY OWN FIX FROM AN HOUR EARLIER.** D272 pulled the build forward to beat the weld line and pulled it too far: everything was finished by **px 1087**, and the right-hand slab does not uncover the mosaic's left column until about **px 738** and does not clear it entirely until **1024**. So four of the six tiles landed behind a slab that had not arrived, and the last one was the only one still moving when you could see it. ⭐⭐ **THE BUILD IS CLOCKED TO THE WINDOW WHERE THE MOSAIC IS ON SCREEN NOW:** `start` 1.45 → **1.21** puts progress 0 at **px 760**, which is where the slab's leading edge first uncovers Nick, and `end` 0.85 → **0.576** lands it at **px 1330**, 110px before the slabs touch. The damping goes 0.16 → **0.26** so the settle still fits inside that. ⛔ **THE DEADLINE FROM D272 STILL HOLDS AND WAS RE-MEASURED, NOT ASSUMED** — this is the pair of asks pulling in opposite directions, and the window between them is only about 600px of scroll. | Verified by walking the phase at ~800px/s and reading each tile's own hinge angle per frame. The six finish at **948 / 1013 / 1078 / 1143 / 1208 / 1273**, evenly staggered, all before contact at **1440** and all after the slab starts uncovering them at **738** — so every card folds in view. Nothing still moving at contact, at the seam's fade or at the hand-over |
| D273 | 16 Aug | ⭐⭐ **THE HELIX ARROWS ARE THE SITE'S ROUND BUTTON AGAIN, AND THEY ACTUALLY SIT ON THE "CALL US" LINE NOW.** Client: *"the buttons underneath the middle card on the surfaces for every space section, make those buttons the round button with the arrow inside so that it's consistent with the rest of the site, and then also move it up to in line with the call us button."* ⛔⛔ **THIS REVERSES D243's SHAPE AND THE OLD REASON IS WORTH KEEPING:** on 15 Aug he asked for *"the buttons the exact same size as the call us block"*, so they were built as a 129.79 × 50.75 pair with 2px corners to read as the same component as the CTA. ⭐ **BUT `.wbtn` IS A SHARED CLASS AND EVERY OTHER ONE ON THE SITE IS A CIRCLE** — the base rule, the lightbox's `.pl-nav`, the stone wheel's pair — so the helix was the single exception. **The size override is DELETED rather than retyped, so `.wbtn`'s own 52px circle governs and there is one definition of this control.** ⛔⛔⛔ **AND THE ALIGNMENT WAS NOT A BROKEN FORMULA, IT WAS A LYING MEASUREMENT.** `footToCallUs` computes `row.bottom − call.bottom` and that sum is right — but `.svc-intro` carries `.rise`, which is a **34px translateY**, and `getBoundingClientRect()` reports it. Every read before the reveal landed put the button 34px low, so the stored offset was **14.82px where the answer is 48.20**, and the pair sat **exactly 34px** below the button it mirrors. **§12's rule, and this is the third time it has caught something on this project.** ⭐ The transform is now read off the column with `DOMMatrixReadOnly` and taken back out; heights are safe either way, because a Y translation does not resize a box. ⚠️ It also re-seats on `document.fonts.ready` and on a `ResizeObserver` watching the TITLE, the SUB and `#svcNav` — **not the column**, which is a stretch item whose height is the row's and does not move when the text inside it reflows. ⭐ Centres are matched now rather than bottom edges, because a 52px circle against a 50.75px button would otherwise sit 0.62px out. | Desktop only (≥1121px), where the helix exists at all. Verified at 1440×900 on a fresh load: stored foot **48.20px**, the arrow pair's centre and the "Call us" centre both at **892.8** — delta **0.00** — buttons 52 × 52 at `border-radius:50%`, pair 118 wide, still centred on the helix. Freeze probe unchanged: `.gal-scroll` **4950**, `--revPer` **3**, `feTurbulence` **60**, `#svcNav` **8**, elements **2663**, document **15469**, hero ink **164.683 / 97.2**, no broken images, no horizontal overflow |
| D272 | 16 Aug | ⭐⭐ **THE COLLAGE FINISHES BUILDING BEFORE THE WELD LINE GOES, NOT AFTER THE HAND-OVER.** Client: *"when the about us section comes in, especially with the images, let's make sure it's fully animated out, or the animation fully finishes before the centre line disappears or halfway between the centre line disappearing."* ⛔⛔ **HE IS RIGHT AND THE DEFAULT CLOCK COULD NEVER HAVE DONE IT.** `scrollSequence`'s `end:0.02` means progress only reaches 1 once the mosaic's top has climbed to **18px** off the top of the screen — and the weld parks the section at **138px** and holds it there, so the sequence topped out at **0.855** and the last tile was still at 97% while the seam faded. It finished afterwards, on the scroll out. ⛔⛔⛔ **AND MOVING `end` ALONE DID NOT FIX IT, WHICH IS THE PART WORTH KEEPING.** The playhead is DAMPED, so reaching progress 1 is where the settling STARTS: at 6.2% a frame the last tile needs about **44 frames**, three quarters of a second, and the seam's whole fade is 148px of scroll — ten frames at an ordinary wheel. A first pass at `end:0.62` still measured the build finishing at **1852px**, past the hand-over at 1710. ⭐ **BOTH ENDS MOVE: the target lands at `end:0.85`, which is 357px of scroll before the slabs touch, and the damping goes 0.062 → 0.16.** ⚠️ That is deliberately less lag than the client asked for on 6 Aug (*"delay that animation a little bit more"*) — **inside the weld the build has a deadline it never had as a section scrolling past**, and `span` and `step`, which are the choreography itself, are untouched. ⭐ `start` goes to **1.45**, past 1.0, which the engine's own header warns against — and that warning does not cover this case, because the mosaic is genuinely below the fold while it builds: it is riding INSIDE the right-hand slab. ⛔⛔ **CLOCKED THIS WAY ONLY WHERE THE WELD ACTUALLY RUNS.** `/about/` links the same file and has the same collage with no pin, and the frozen bands must not be re-clocked, so the switch is the weld's own three conditions — desktop width, motion allowed, `#process` on the page. ⚠️ **READ ONCE AT LOAD**, like every other option on this engine: a window dragged from desktop to tablet keeps the weld clocking until the next reload. | Verified by walking the phase at two speeds and reading the worst residual hinge angle and lowest opacity across all six tiles at each sample. **At an ordinary wheel (~800px/s) the build is quiet at px 1087** — 353px before the slabs touch, 420 before the line starts to fade — and reads **0° / opacity 1** at contact, at the fade start and at the hand-over. **At a hard flick (~2500px/s) it is quiet at px 1240**, and again 0° at all three marks. `/about/` re-checked and still on the original clock: at half a viewport its last two tiles are still hinging at 62° and 84°. Freeze probe 1440 fresh unchanged: `.gal-scroll` **4950**, `--revPer` **3**, `feTurbulence` **60**, `#svcNav` **8**, elements **2663**, document **15469**, hero ink **164.683 / 97.2**, no broken images, no horizontal overflow |
| D271 | 16 Aug | ⭐⭐ **THE TWO SECTIONS THE WELD HOLDS YOU ON ARE SMALLER, AND THE ROOM THEY GIVE BACK GOES TO THE EDGES.** Client, on the pinned frame: *"make the blocks in the process without surprises section a little bit smaller, or just optimise it somehow so that we can buy some more space for the title to come down slightly and the buttons to come up slightly so they're not close to the top and bottom edges of the screen. Then for the about us section as well, if there's any way for us to optimise both sides to be a little bit smaller so it doesn't fill up the screen so much."* ⭐⭐⭐ **THE WELD IS WHY THIS MATTERS NOW AND NOT BEFORE:** these two frames used to slide past, and D269 turned both into compositions you are HELD on, so their margins are being read rather than glanced at. ⛔⛔ **PROCESS — THE CAP IS THE WHOLE LEVER.** `--u` is `clamp(26px, (100vh−400)/6, 68px)` and the vh term is 83px at 900 and 71px at 824, so on every desktop window the tiles are taking the CAP. 68 → 58 takes the flow **478 → 418** and the tile **220 → 202**; the pill and the bottom text band need 118 between them, so 84px of headroom is left. ⛔⛔⛔ **AND THE AIR ONLY LOOKS EQUAL IF THE BAR IS IN THE SUM.** `justify-content:center` splits the free space evenly, so with 90px of top padding the title cleared the fixed bar by **16.9px** while the buttons had **43.2** under them — centred, and reading top-heavy, which is exactly what he was looking at. The top inset is the bottom inset PLUS the bar now, **the same relation the hero takes at D259**. Measured after: **60.1 / 60.1**. ⚠️ Scoped to `body.weld-live`, because only a pinned frame sits under the glass. ⛔⛔ **ABOUT — ONE COLUMN IS BOTH SIDES.** The collage has been `align-self:stretch` with no aspect ratio since D244, so the copy drives the row and the pictures follow it: shortening the prose shrinks both halves and nothing else needs touching. **Leading 1.72 → 1.62, the type cap 16 → 15.2, the paragraph gaps and the button's own padding in with them** — 20 lines at 27.52 became 20 lines at 24.62. ⛔ **NOT ONE WORD OF HIS COPY IS CUT** (it is his own document, D-14 Aug), and the cap only lowers the CEILING so the `min(1.4vw,2.2vh)` term still governs a narrow or short desktop. Section **843.8 → 785**, which finally fits inside the **821.5** the weld leaves under the bar: the landing offset goes 78.5 → **96.8** with 18.3px of air above and below the whole composition, where it used to overflow by 22. ⛔⛔ **THE `--u` OVERRIDE HAD TO MOVE, AND IT FAILED SILENTLY FIRST.** It was written in the `@media(min-width:1121px)` block that sits ABOVE `#procFlow`'s own declaration, and `#procFlow` against `#procFlow` is decided by SOURCE ORDER, not by the query — the tiles never moved and every other number in the same edit landed, which is what made it look like it had worked. It lives directly under the declaration now. ⚠️ **BOTH EDITS ARE DESKTOP-ONLY AND THE TABLET IS ON THE SAME PROCESS GRID** (the flex-column fallback only starts at ≤980), so neither could go near base scope. ⚠️ **THE FOUR GENERATED PAGES THAT REUSE THE PROCESS SECTION TAKE THE SMALLER TILES TOO, AND /about/ TAKES THE TIGHTER COPY** — same component, same size, deliberately not divergent; they have no pin, so they keep the 90px top padding. | Verified at 1440×900. **Process:** air above the title **16.9 → 60.1**, below the buttons **43.2 → 60.1**, flow 478 → 418, tile 202 laid out, section still exactly 900 so the pin stays at 0. **About:** 843.8 → 785, copy 761 → 702, collage followed it down. Weld re-swept end to end — rest window still dead at 0/200/360, slide, weld at 1440, hand-over at 1710, no errors, no horizontal overflow. **Frozen bands untouched:** at 900 the cap is still `68px`, the padding still 90, the copy still 13.5/1.72 and docH **13559**; at 390 docH **15125** — both identical to before this round. Freeze probe 1440 fresh: `.gal-scroll` **4950**, `--revPer` **3**, `feTurbulence` **60**, `#svcNav` **8**, elements **2663**, hero ink **164.683 / 97.2**, no broken images. Document 15510 → **15469** |
| D270 | 16 Aug | ⛔⛔⛔ **THE WELD NEEDED A RESTING WINDOW AND A LANDING THAT CLEARS THE BAR — BOTH FAULTS WERE MINE AND BOTH WERE INVISIBLE AT 1440×900.** Client, with a screenshot: *"the space where it comes in and attaches that section, it is basically cutting off the top part, and then it's also happening instantly. So if someone scrolls too far accidentally, it just covers the previous section, and they have to find the perfect spot with their mouse."* ⭐⭐⭐ **HE OFFERED TO REVERT IT AND ASKED FOR A DECISION FIRST; BOTH WERE ORDINARY BUGS AND HE TOOK THE FIX.** ⛔⛔ **FAULT ONE — THE LANDING WAS BOTTOM-FLUSH, FLOORED AT ZERO, WHICH IS RIGHT ONLY WHILE THE SECTION FITS.** At 1512×824 About is 791 tall, so bottom-flush put its top at 33px and **the title at 70.9 — 7.6px ABOVE the bar's lower edge, behind the glass**, with none of the section's own air. ⭐ **THE FLOOR IS THE BAR NOW**, and the section's own 38px padding supplies the air; leftover slack is split, capped at 6vh so a tall window does not float the block down the screen. **Whatever cannot fit falls off the BOTTOM, never off the head.** ⛔⛔ **AND THE PIN ITSELF WAS CHOOSING THE WRONG FRAME FOR THE SAME REASON.** `#process` needs 865px at that window against 824 of screen, so pinning it bottom-flush **held the reader on the one frame with "A PROCESS WITHOUT" cut off behind the bar.** The pin shows the section's CONTENT box from the bar's lower edge down and only falls back to bottom-flush when even that cannot reach — at 1512×824 that is −3.9px and the whole section reads, head to CTA. ⛔⛔⛔ **FAULT TWO — THERE WAS EXACTLY ONE SCROLL POSITION WHERE PROCESS SAT WHOLE.** The slabs began the instant it pinned, so a pixel past that they were already moving, and the close ran over 570px at his window — one trackpad flick, which is his "instantly". ⭐⭐ **THREE LENGTHS NOW, IN VIEWPORTS: REST 0.40, SHUT 1.20, WELD 0.30.** The rest window is pure held frame, nothing moving; with the ease-in on top of it the slabs have moved under 2% for the first ~450px, so the section reads as untouched for half a screen of scrolling. The close is over twice what it was. ⛔ **THE PAGE ONLY PROVIDES `procH + pin − target` PX BETWEEN THE TWO SECTIONS AND THE REST IS BOUGHT** with an inline `margin-top` on `#about`, which sits behind the pinned Process where nothing can see it — **889px at 1440×900, and it is cleared on teardown or the tablet inherits a hole.** ⛔⛔ **THE STAGE IS DOWN THROUGH THE REST WINDOW, THE SECTION IS HIDDEN THROUGHOUT — TWO GATES, NOT ONE.** About has to be hidden for the whole phase because it rises up the screen behind a Process with no background of its own; the stage only comes up once the slabs move, or both slab shadows would sit parked against the screen edges with nothing else happening. The edges also ramp in over the first 6% of the close for the same reason. ⛔ **AND `cloneNode` COPIES THE STYLE ATTRIBUTE, SO THE SLABS CARRIED THE BOUGHT MARGIN TOO** — the clone drew 783px below where the real section lands, measured as clone h2 at 899.8 against the real 117.8. Zeroed on the clone. ⚠️ **`node --check` PASSES A DEAD VARIABLE**, and a stale `TRAVEL` from the first build threw every frame; **the pane's console keeps messages across a reload, so the errors looked live after they were fixed** — proved current by fetching the running document and re-running with a fresh trap. | Verified at 1512×824 (his window shape) and 1440×900. **The landing:** title now **37.9px BELOW** the bar's lower edge, was 7.6 above it. **The pin:** whole Process section in frame at 824, headline to CTA. **The rest window:** at 0/120/360px past the pin the doors are parked at 0% and Process has not moved. **The close:** 1080px at 1440×900 (was 608). **Hand-over still exact** — clone and real agree to 0.00px in x and 1.4px in y one pixel before the end. Frozen bands re-checked both ways: fresh at 900 and 390 show no `weld-live`, no stage, margin `0px`, divider back; a 1440→900 resize with the stage built returns docH to **13559**, the same as a fresh tablet load. Freeze probe 1440 fresh: `.gal-scroll` **4950**, `--revPer` **3**, `feTurbulence` **60**, `#svcNav` **8**, elements **2663**, hero ink **164.683 / 97.2**, no broken images, no horizontal overflow, no console error on a full pass. Document **14621 → 15510** (the bought scroll) |
| D269 | 16 Aug | ⭐⭐⭐ **THE ABOUT SECTION CLOSES OVER PROCESS LIKE TWO SLABS BEING SET, AND THE SEAM WELDS. DESKTOP ONLY.** Client: *"in the process section… take the about us section, and instead of it being right below the process without surprises section, as the user scrolls it's essentially going to slide in from both sides, left and right, like a sliding door exactly in half, and then have this golden line that merges exactly, and it creates this quick flash line down, almost welding the two pieces together very quickly so that they sit perfectly together, and that simulates Topcat adding two stones perfectly together to match up… when they're at the process without surprises section, the scroll technically stops scrolling down, and then the about us section closes directly over that… and if the user scrolls back up, it will then slide the doors back out."* ⭐⭐ **THE PIN IS ONE DECLARATION, AND IT PINS AT THE MOMENT HE NAMED:** `#process` goes `position:sticky` at `100vh − its own height`, so it stops exactly as its bottom edge reaches the bottom of the screen — his "when the process section is perfectly in view" — and not a pixel earlier. The section is already `min-height:100vh`, so the pinned frame is the whole composition, head to CTA. ⛔⛔⛔ **THE SEAM AT 50% FALLS IN THE COLUMN GUTTER AT EVERY DESKTOP WIDTH, WHICH IS THE WHOLE REASON THE CUT IS INVISIBLE — AND IT WAS MEASURED BEFORE ANYTHING WAS BUILT.** `.about-wrap` is copy 1.02fr / collage 0.98fr in a 1140 cap, so the midline lands in the empty gutter between the two columns at 1121 (gutter 533.6–623.9, midline 560.5), at 1440 (694.7–779.9, midline 720) and at 1920 (934.7–1019.9, midline 960). **So the left slab carries the story and the right slab carries the collage, which is the section cut exactly in half as asked, with no word ever sliced.** ⭐⭐ **EACH SLAB CARRIES ITS OWN HALF OF THE PAGE FLOOR**, offset so the grain rejoins into one continuous stone when they meet — that is the "two stones matching up" made literal, and it is also what makes the hand-over invisible, because `body::before` is fixed and a leaf that reproduces it is pixel-identical to it. ⛔⛔ **THE LEAVES CARRY A CLONE OF `#about`, MIRRORED PER FRAME, NOT A SNAPSHOT.** The collage tiles are placed every frame by `scrollSequence` off the REAL section's travel, and that build now runs its whole course behind the closing slabs. **A snapshot froze the moment the wheel stopped** — the damped playhead keeps settling for the best part of a second after a scroll ends, so pausing mid-slide left the slab carrying a half-built collage while the real one finished behind it, and the hand-over popped. It has its own rAF loop while the stage is up, six property reads a frame. Same failure in the other column, same fix: the `.rise` copy is forced `in` with its transition killed on BOTH sides. ⛔⛔⛔ **EVERY FIGURE IS MEASURED OFF `#about`, NEVER OFF `#process`, BECAUSE A PINNED STICKY ELEMENT LIES ABOUT WHERE IT LIVES.** `offsetTop` carries the sticky shift in Chrome exactly as `getBoundingClientRect()` does, so re-measuring while Process was pinned returned its PINNED top, put the start line at the reader's own scroll position, and read progress 0 for ever — **the doors sat wide open and every number looked plausible.** Process's bottom edge IS About's top edge here, so `docTop(about) − vh` is the same instant in a coordinate that cannot move. ⛔ **THE GOLD DIVIDER BETWEEN THE TWO SECTIONS IS GONE ON DESKTOP: the weld is that seam now.** It could never be seen (Process is pinned over it) and it was 110px of scroll where nothing happened. ⭐ **THE WELD IS TIME-BASED, NOT SCRUBBED —** he asked for a QUICK flash, and a flash tied to the wheel is as slow as the slowest scroller. A bright head runs the full height in 420ms drawing a gold line behind it, then the line and both slab edges fade to nothing by 94% of the travel, so **nothing of the mechanism survives into the finished composition.** It re-arms if you scroll back up past the contact. ⛔ **AND THE STAGE IS `display:none` AT BASE SCOPE.** Every rule that gives it a shape is in the ≥1121px query; without that one line a desktop window dragged down to the tablet would unfold two full-width About sections into the bottom of the page, in a frozen band. | Desktop only (≥1121px), gated on `body.weld-live` which the IIFE adds only on the landing page, wide, with reduced-motion off. Travel **844px** at 1440×900 (slabs close over the first 72%, weld and hold over the rest). Verified at 1440×900: hand-over exact — clone and real agree to **0.00px** in x (150 / 779.94) and 0.93px in y at one pixel short of the end; tiles byte-identical; no horizontal overflow at any point. **Frozen bands untouched and re-checked BOTH ways** — fresh loads at 900 and 390 show no `weld-live`, no stage, Process `relative`, the divider back; and a 1440→900 resize with the stage already built returns docH to 13559, the same as a fresh tablet load. Freeze probe at 1440 fresh: `.gal-scroll` **4950**, `--revPer` **3**, `feTurbulence` **60**, `#svcNav` **8**, elements **2663 — unchanged from the same page with this round reverted** (the register's old 2664 was a ±1 transient; the stage is built lazily on approach and takes it to 2771 while the doors are live), document **14731 → 14621** on purpose, hero ink unmoved at 164.683 / 97.2, no broken images, no console error. `harvest/verify.py` **132/132/132 ✅** |
| D268 | 16 Aug | ⛔⛔⛔ **THE FOOTER'S WHATSAPP NUMBER WAS NEVER PLACED ON THE PHONE — IT WAS AUTO-PLACED, WHICH IS NOT THE SAME THING.** Client, with a screenshot: *"I also don't understand why the WhatsApp number is all the way down there and just randomly placed in that corner."* ⭐⭐⭐ **HE IS DESCRIBING CSS GRID AUTO-PLACEMENT EXACTLY, AND THE CAUSE IS D207 MEETING D153.** On the phone `.foot-contact` is `display:contents`, so its children become items of the footer's own grid — and that grid is driven by `grid-template-areas` whose every cell was already claimed. D207 added a fifth contact row a month after those areas were written, so the one item with no `grid-area` was pushed into an implicit eighth row, **in column one, at half width, below Hours**. ⭐ It now has a row of its own between the landline and the email, which is the order he set for the three contact routes at D246 (*"the landline, then the WhatsApp number, then the email"*), in the same badge the other two wear. ⛔ **IT KEEPS ITS MICRO-LABEL WHERE THE OTHER TWO LOSE THEIRS, AND THAT IS DELIBERATE:** a phone number and an email address announce themselves, and `07464 940287` in an identical badge under `0800 098 2812` reads as a second landline. The label is the same 10px caption AREA and HOURS carry two rows below. ⛔⛔ **PLACED BY `grid-area`, NOT BY MOVING THE MARKUP** — the D200 footer IIFE restores Area and Hours with `appendChild` and only works while those two are the last children of `.foot-contact` (D207's own note), and the tablet band is frozen. ⚠️ **ANY NEW CONTACT ROW NEEDS A LINE IN `grid-template-areas` AS WELL AS A `grid-area`, OR IT LANDS IN THAT SAME CORNER.** | Phone band only. Verified at 390: the five rows run 694 / 759 / 821 / 896 / 995, all full width at x=20. Tablet and desktop unmoved — phone, email, WhatsApp still stacked in the contact column at 900 and 1440, Area and Hours still lifted into `.foot-tail` by the D200 IIFE |
| D267 | 16 Aug | ⭐ **THE MESSAGE BOX ON EVERY FORM JUST SAYS "MESSAGE".** Client: *"in all of the forms at the bottom, where the contact forms are, change the space where it says 'your space, your stone, your timings, optional' — just say message, like it would normally look."* ⚠️ **THE "(optional)" GOES WITH IT AND THAT IS THE POINT OF THE INSTRUCTION** — no other field on either form marks itself optional or required, so this one was carrying a convention the form does not otherwise use. ⚠️ The three prompts it dropped are not lost: the stone rides along on its own chip above the box (`#ctaStone`), and the space and the timings are what step one of "What happens next" is for. ⭐ **TWO FORMS, TWO PLACES:** the enquiry card in `index.html` (which `build_pages.py` lifts onto five more pages) and the trade form's own `TRADE_CTA_SECTION` in the builder, which said *"The project, the programme and where it is (optional)"* and would otherwise have been the one place still asking the long way. | Verified on all seven generated pages plus the landing page: one `placeholder="Message"` each |
| D266 | 16 Aug | ⛔⛔⛔ **THE "BRANDS WE WORK WITH" MARQUEE IS DELETED AT EVERY WIDTH, AND NOTHING TAKES ITS PLACE — NOT EVEN A DIVIDER.** Client: *"I also asked you a long time ago to go and remove the brands we work with carousel and just make it a normal section divider, or actually better yet, don't have any divider in that section. Just have the costing below. Don't have any divider. Remove the carousel with all the suppliers and the brands we work with."* ⚠️⚠️ **HE ASKED ONCE BEFORE AND ONLY THE PHONE GOT IT — THAT IS THE PART WORTH REMEMBERING.** D143 swapped the marquee for a plain `.section-divider` inside `@media(max-width:720px)` and left the rolling band running on desktop and tablet, because those bands were frozen at the time. **"Remove that rolling bar divider completely" was the instruction and one breakpoint was the delivery** — the same shape as D229's eyebrow and D197's. ⭐ This time it is deleted outright, and past the divider as well. ⛔⛔ **THE BAND WAS THE STONES → ESTIMATOR SEAM BY DESIGN** (client, 30 July: *"that rotating bar becomes the divider. There is no gold divider underneath that"*), so deleting it left two full sections butted together with 41.4px between their content. ⭐⭐ **`#estimator` CARRIES THE SEAM AS PLAIN SPACE: EXACTLY WHAT A `.section-divider` SEAM OCCUPIES, WITH NO LINE IN IT** — two of the divider's own symmetric margins plus the 2px rule it would have drawn, written as that calculation rather than as a number so it tracks the divider at every viewport height and this one seam cannot drift out of the page's rhythm. **110px at 1440×900, 103px at 390×844.** ⭐ Everything that hung on the band went with it: the CSS block, the `SUPPLIERS` array and the marquee fill, `.strip-rule` in the divider-shine IIFE, the phone's `.strip-swap` divider, and D179's `.strip-band .strip-swap{margin-top:0}` cost-stays-behind rule. ⚠️ **THE FOUR COMMENTS THAT DESCRIBED IT NOW SAY IT IS DELETED** rather than describing a thing that is not there (D106/D113/D114). ⚠️ **`SUPPLIERS` WAS A MISLEADING NAME AND IS WORTH KNOWING BEFORE ANYBODY "RESTORES" IT:** it held twelve MANUFACTURER brands (Silestone, Caesarstone, Dekton, Neolith…), which were always safe to name. ⭐ **IT ALSO TAKES `Next Stone Slabs` OFF THE LANDING PAGE**, which is the §2 rule 9 exception D203 opened; ⚠️ **the quartz page's brand sentence still names it**, and that is the one place left. | Verified at 1440×900 and 390×844: no `.strip-band` in the DOM, seam 110/103, no horizontal overflow. Freeze probe moves on purpose — document **14884 → 14731**, elements **2696 → 2664**; `.gal-scroll` 4950, `--revPer` 3, `feTurbulence` 60, `#svcNav` 8, no broken images |
| D265 | 16 Aug | ⭐⭐ **THE TOPCAT MARK IS CROPPED OFF THE CONSULTATION PHOTOGRAPH, NOT RE-DRAWN AND NOT RE-GENERATED.** Client: *"this Topcat logo is terribly done, so make sure to go crop that image. Don't have to change it. Just crop it so that you can't see the Topcat logo."* ⭐ **HE NAMED THE CHEAPEST ANSWER HIMSELF AND IT COSTS NOTHING.** ⚠️ **THE MARK IS THE ONE D252 COMPOSITED FROM `topcat-icon.svg`'s OWN PATH DATA**, which was the careful way to do it (§2 rule 14 forbids an image model inventing one) — and at the size the tile actually draws, it still reads badly. **A correct mark is not the same as a mark that works at 392px.** ⭐ Measured before cutting: the mark occupies **x 1795–1915** of the 2048px master and the model's invented wordmark ghost, patched out in D252, sits at **1885–1950**, so the cut is at **1780** and takes both with 15px to spare. ⚠️ **THE RIGHT-HAND CUT WAS CHOSEN AGAINST A TOP CUT BY RENDERING BOTH AT THE TILE'S REAL SIZES** (392×230 and 758×198) and looking: cutting the top gives a 2.40:1 band but leaves a large empty black shirt on the right, while the right-hand cut keeps the whole scene — the slab, the handshake, the plans and the tape — and loses only the chest. ⛔ **A NEW PREFIX, NOT NEW BYTES UNDER THE OLD ONE:** `process-consult-shake`, so the old ladder stays on disk untouched (never delete the originals), the URL change is its own cache-bust on his phone, and one prefix still means one picture (D241's rule). Two rungs at **880/1600** because `#pmShot` in the step's detail card is 758 wide and takes `p.img` with no `srcset`. **1600 ≤ 1780, so nothing is upscaled.** | `SS` and the `SERVICES` entry both repointed. 1780×1152 master at q92, rungs cut LANCZOS / WebP q85 / method 6 (36 KB and 94 KB). Verified at 1440 on a fresh load: the tile serves `process-consult-shake-1600.webp`, no mark anywhere in the frame at 2.2× magnification, no broken images |
| D264 | 16 Aug | ⛔⛔⛔ **A CURVED EDGE IS A CUT, NOT A LINE DRAWN ACROSS A RECTANGLE — EVERY CURVE ON THE SITE, EVERY BAND.** Client, with a screenshot of the corner: *"everywhere where there's the curved divider, like currently on the nav bar, whether it's desktop, tablet or mobile, make sure there's no corner piece still there from the design… you should see the section behind, in that corner where it curves. So just make sure the curves are perfect, and it's not just a rectangle with a curve line on it."* ⭐⭐⭐ **HE IS DESCRIBING THE MECHANISM EXACTLY.** The arcs are `border-bottom-left/right-radius` on a 1px-bordered box (D122/D235) — a border only ever draws a LINE. The layers that actually paint the thing were plain rectangles at `inset:0`: `header.bar::before` (the glass) and `.page-head::before` (the photograph and its four shades). So both bottom corners stayed square and the gold arc ran across the middle of a solid block. ⭐ **CUT TO THE SAME BOX AND THE SAME TWO VARIABLES AS THEIR OWN ARC**, hanging out by `--navCurveOut`/`--curveOut` at each end, so the phone's 18/5 and the tablet's 26/7 follow without a second rule anywhere. ⛔⛔ **A `border-radius`, NOT A `clip-path`, ON THOSE TWO:** a radius clips the element's own background AND the region its `backdrop-filter` samples in every engine, while `clip-path` + `backdrop-filter` is the pairing Safari has historically dropped half of — an unclipped blur leaves two square corners of *softened photograph* even after the fill is cut. ⭐ The heroes keep `clip-path` because they have to cut a CHILD (`.hero-shade`, `.svc-hero-bg::after`), which a bare radius does not do. ⛔⛔ **AND THE LANDING HERO'S THREE iOS MECHANISMS ARE AT BASE SCOPE NOW, MOVED AND NOT COPIED:** radius + `overflow:hidden` + `translateZ(0)` on `.hero-bg`, and `will-change:auto` on the img. They were written inside `@media(max-width:720px)` for a fault only he could see, and **a promoted child compositing after its parent's clip-path is rasterised is not a phone behaviour** — he asked for all platforms and he is right. The 3s load zoom still runs. | Proved by the test he described: everything behind painted white and the glass painted red, magnified 5×, on the landing bar, the landing hero, `.page-head` and `.svc-hero`. `header.bar::before` −9px/34px on desktop and −5px/18px at 390. Freeze probe unmoved: `.gal-scroll` **4950**, `--revPer` **3**, `feTurbulence` **60**, elements **2696**, no broken images, no horizontal overflow |
| D263 | 16 Aug | ⭐⭐⭐ **THE INTERNAL PAGES TAKE THE LANDING PAGE'S NAV BAR, ITS HERO AND ITS STONE FLOOR — ALL 176 OF THEM.** Client: *"the inner pages, like for the services, the hero section should have the same design as on the landing page, and the nav bar should have the same design as on the landing page. And the background in those pages should still be the stone background. So just make sure to do things better than what you are now, and then redesign the frequently asked questions thing and also make the title say frequently asked questions. Right now these pages just have way too basic of a design. You can do better than that."* ⛔ **NOT BY LOADING `site.css`:** it is 538KB of design notes and it needs `site.js` to behave — the components are ported into `services/service.css`, which is the one sheet all 176 link. ⚠️ **THAT MEANS THE BUBBLES AND THE BAR NOW EXIST IN TWO STYLESHEETS AND MUST BE CHANGED IN BOTH.** ⭐⭐ **THE BAR:** fixed and transparent over the hero, glass and a curved gold edge poured in on scroll, 34px logo, the flat champagne pill (not the 100px outline pill, which agreed with nothing else on the site). ⛔⛔ **IT STOPPED OCCUPYING FLOW WHEN IT WENT FIXED, AND THESE PAGES START THREE DIFFERENT WAYS** — 22 with `.svc-hero`, 147 with a bare breadcrumb, and that trail is `display:none` on a phone. **One rule holds all of them: `body` pads by `--barH` and the hero takes it straight back with a negative margin**, because the hero is the one element that wants the photograph running up behind the glass. Padding the hero, the crumb and then `main` on the phone would have been three live descriptions of one clearance. ⭐⭐ **THE HERO:** centred like the landing page's, curved bottom edge with the gold arc, and the landing hero's four bubbles instead of three spans of 12.5px grey trust text. The location pages keep their dialling code and postcodes as a fifth and sixth bubble — `:last-child:nth-child(odd)` has been dormant since D205 and was written for exactly this. ⛔⛔⛔ **AND THE PAGE HEAD'S FLAT 0.62 VEIL WAS TRIED FIRST AND FAILS ON THESE PHOTOGRAPHS:** measured by the D232/D238 method, white h1 **5.01**, gold h1 **2.43**, lede **3.36**, nav links **3.38**. The landing hero can live on a flat wash because its photograph is a night kitchen; these nine are daylight interiors. ⭐⭐ **IT IS A CENTRED SCRIM NOW — 0.70 → 0.36 → 0 under the copy, over a flat that comes DOWN from 0.62 to 0.44** — so the shade is where the words are and the picture's edges are lighter than they have ever been here (this replaces a 0.95 → 0.5 left-to-right ramp). **Measured on all nine heroes: gold h1 3.81–4.39, white h1 7.1–8.6, lede 4.59–5.26, nav 4.87–5.76.** ⚠️⚠️ **AND THE FIRST MEASUREMENT OF THAT FIX SAID IT MADE THINGS WORSE, BECAUSE IT SCANNED THE FULL WIDTH OF EACH BAND** — a centred scrim is lightest at the edges, so the brightest pixel in a full-width strip is one the text never touches. **Measure inside the text's own `Range` rect.** ⭐ **THE FLOOR:** the landing page's photographed Portoro under the same 0.70 scrim and the same two corner glows, at the same numbers, with D123's 0.46 on the phone. ⛔ `z-index:-1`, not the landing's `0`: `section.block` is static and a fixed pseudo at 0 paints ABOVE static text. `.cta-band` and `footer.site` gave up their `--ink` so the floor runs the whole page. ⭐⭐ **THE FAQ:** cards in a two-column grid, chevrons not a `+` that rotates into an `×` (D249), and ⛔ **the `border-top` on every row is gone — that was §2 rule 10 broken twelve times down a page**, the same break D250 found on the CTA band. The SEO family's FAQ had no section padding and a browser-default `h2`, which is a fair part of what "way too basic" was looking at. | `services/service.css` + three builders (`services/build_services.py`, `build_seo_pages.py`, `stones/build_stones.py`), which is 9 service leaves, 26 SEO pages, 132 stone pages, the collection, compare and the sitemap. `harvest/verify.py` **132/132/132 ✅**. Verified at 1440, 1121, 900 and 390 on a service leaf, a material page, a town page, a stone page and compare: D251's slab-to-name delta still **0.00px**, the compare picker still opens over the bar, no horizontal overflow. ⚠️ **THE BAR KEEPS ITS QUOTE PILL AT EVERY WIDTH HERE, WHERE THE LANDING HIDES IT BELOW 1121 (D235)** — the landing has a burger holding the navigation and these pages have none, so below 960px the pill is the only control in the bar. **It does mean a phone shows it above the hero's own gold button, which is the pairing D235 disliked. Told him; one line either way** |
| D262 | 16 Aug | ⭐⭐ **THE FAQ IS CALLED "FREQUENTLY ASKED QUESTIONS", AND ON /contact/ THE REVIEWS COME BEFORE IT.** Client: *"on the dedicated contact us page, put the review section above the [FAQ] section. And then everywhere where it says 'answers set in stone' for it being the FAQ section, just make it say frequently asked questions."* ⭐ The gold last word is D229's shape, unchanged; only the words moved. ⚠️ The heading now sets to two lines at 1440 ("FREQUENTLY ASKED / QUESTIONS", the gold word alone on the second), which is the hero's own shape and reads as deliberate. ⭐ The order is also the better argument on that page: the form is the ask, other people vouching for us is what earns the answer, and the FAQ is the leftover objections. ⛔⛔⛔ **AND THE REORDER UNCOVERED A LIVE BUG ON TWO PAGES: `#faq`'s CLOSING TAG SAT AT COLUMN 0 WHERE `build_pages.py` LOOKS FOR IT AT TWO SPACES**, so the block it lifted ran straight past the end and **swallowed the whole `#cta` section behind it — /about/ and /contact/ have been shipping the enquiry card TWICE.** The balance check never caught it: two opens and two closes is balanced. ⭐ Fixed in the markup, with a second guard in the builder that raises if a lifted section contains another top-level `<section`. ⚠️ The SEO layer's own FAQ headings moved with it, and the location pages **keep the place in theirs** ("Frequently asked questions in St Albans") — that place name is the reason those pages rank. | `index.html` + `build_pages.py`. /about/ and /contact/ each dropped ~8KB. Verified: /contact/ is page-head → enquiry card → reviews → FAQ, one `.cta-card` on every generated page |
| D261 | 16 Aug | ⭐ **"WE REPLY WITHIN ONE WORKING DAY" RANGES WITH THE THREE ANSWERS ABOVE IT, IN GOLD, AND IS NOT A FOURTH ITEM.** Client: *"line that up with the text above that so that the W is in line with the A from number three's 'a fixed'. So it, being centred, looks a bit strange. It doesn't have to be a number four, it just needs to be its own thing, maybe in gold text or whatever."* ⭐ **HE IS DESCRIBING A RAGGED LEFT EDGE:** everything else in that column starts on one of two x positions and this line started at neither, because `text-align:center` puts it wherever its own length leaves it. Measured before: the numbered text at **766.48**, this at **909.65**. ⛔ And it must not become item four — no counter, a size of its own (11.5 against 13.68) and the brand gold rather than the list's muted bone. ⚠️ The 22px indent is a variable on the form now; two copies of an alignment constant is how the halves drift. | Verified at 1440: all four ink left edges **766.48**, colour `rgb(198,166,100)` |
| D260 | 16 Aug | ⭐⭐ **EVERY HERO BUBBLE CARRIES A GOLD MARK: THE 10 AND THE 72 ARE STRUCK NUMERALS AND THE HOME VISIT TAKES A HOUSE.** Client: *"I want all of the parts to have almost like icons. So for instance, ten year guarantee, the ten should look like a golden icon, and then the seventy two should look like a gold icon. And then the free home visit should have a house icon."* ⭐ **THE NUMERAL IS THE ICON, WHICH IS ALSO THE CHEAPEST TRUE ANSWER** — "10" and "72" were already gold text in those labels, so they are lifted out and set in the serif at 23px carrying the site's own champagne ramp through `background-clip:text`, the identical treatment every gold accent word on the page takes. The chips still read "10 year guarantee" and "72 hour aftercare" to a screen reader. ⛔⛔⛔ **THE FIRST BUILD PUT EACH MARK IN A 26px STRUCK RING, TO MATCH THE GOOGLE MARK'S DIAMETER, AND HE KILLED IT ON SIGHT:** *"the circle around the ten year and the seventy two hours and the home icon, I don't like the circles. Stop doing that. Just make the ten an actual icon, make the seventy two an actual icon, and then make the home an icon."* ⭐ **IT IS THE SAME INSTRUCTION HE GAVE ABOUT THE BACK ARROW** (10 Aug: *"a simple golden gradient back arrow, not a circle around it, not anything, just simple"*) — **this site does not put marks in containers. Do not re-offer one.** ⛔ The house is a STROKE so it takes `#tcGold`, not `#tcGoldSolid`; the solid ramp goes flat on a 1.3px line. ⚠️ The house needs its own class: `background-clip:text` on a box containing an SVG clips the box's paint to text that is not there. ⚠️ One appearance at every width (D225) — the bubbles have been one object since D237. | Reverses nothing older; the ring was rejected inside the same round and is recorded so nobody rebuilds it. Verified at 1440, 1121, 900 and 390: no wrapping, all chips 44px, slack 6.13–20.79 at the narrowest desktop column, no overflow |
| D259 | 16 Aug | ⭐⭐⭐ **THE HERO COMPOSITION IS CENTRED ON ITS OWN INK, IN THE FRAME UNDER THE NAV BAR.** Client: *"the hero section… if you take everything together, spaced out as it is, it doesn't look like it's exactly in the center on the vertical axis. It should be centred."* ⛔⛔ **AND THE FRAME WAS TESTED BOTH WAYS BEFORE CHOOSING, BECAUSE THE TWO ANSWERS PULL IN OPPOSITE DIRECTIONS.** Centred in the hero BOX (top of the screen to the gold arc) the block moves UP 27px and reads plainly top-heavy — built it, screenshotted it, rejected it. The bar is 78.5px of visible furniture at the top of the frame and the eye starts the composition below it. ⭐⭐ **AND IT IS THE INK THAT IS CENTRED, NOT THE BOX:** the headline's first line box carries 11px of leading above the cap at 89.28px, so a box centred by arithmetic still LOOKS low by that much. `--hTitle * 0.1234` is that leading measured off Cinzel's own metrics, in em, so it holds at every size the clamp produces; the chips' bottom edge IS their ink because they are bordered pills. ⭐⭐⭐ **SO THE RULE IS ONE RELATION, NOT TWO NUMBERS: the top padding is the bottom padding plus the header's height, less the leading the headline already provides.** ⛔ Do not re-tune them as a pair of independent clamps — that is what D235 did, and 15.6vh/10.8vh left the ink 179 above / 203 below at 1440x900. ⚠️ **`--barH` IS DECLARED NOW RATHER THAN INFERRED**, and `header.bar` takes it as its `height` so the variable is the authority. ⛔ **DECLARING THE DESKTOP'S 78.5 AT BASE SCOPE SILENTLY TOOK 1.5px OFF THE PHONE AND THE TABLET**, where the burger's own 44px control sets the height and both bands measure **80** — caught by measuring the frozen bands after a base-rule edit, which is the one thing §4 asks for. They set `--barH:80px` and keep it. | Verified on fresh loads: **191.26 / 191.27** at 1440×900, **158.54 / 159.05** at 1280×800, **257.91 / 258.19** at 1141×1000. Frozen bands unmoved: bar 80px and hero padding `96/54` at 900, `113.94/50.64` at 390 |
| D258 | 15 Aug | ⛔⛔⛔ **THE GALLERY PLATE PROVES IT FILLS A HOLE OR IT REMOVES ITSELF — NO COUNT FORMULA CAN DECIDE THIS.** Client: *"you can clearly see the Hornchurch project didn't need a logo because the collage was already full from the images. I'm saying only where there's a gap because of a lack of a single image, then you add the logo in. Don't just force it into every project."* ⭐⭐⭐ **HE IS RIGHT AND THE BUG WAS MINE FROM AN HOUR EARLIER** — D257 set `brand:true` on all eight projects on the assumption that the derived `(c − n mod c) mod c` would suppress the plate where it was not needed. **That formula assumes a uniform GRID and the gallery is `column-count:3`, which BALANCES.** Central London (13 photographs) and Hornchurch (7) both compute as "two slots short" and look nothing alike, because raggedness depends on the HEIGHTS of the pictures, not on how many there are. ⭐⭐ **SO IT IS MEASURED AFTER LAYOUT INSTEAD OF PREDICTED BEFORE IT:** group the tiles by `offsetLeft` to find the real columns, take each column's lowest edge, and the raggedness is the spread. Render both ways and keep the plate **only if it makes that spread smaller**. ⚠️ `offsetTop`/`offsetHeight`, never rects — these tiles carry `.glow-card`. ⭐ **THE RESULT ACROSS ALL EIGHT, MEASURED:** Wimbledon **KEPT** (1px), Central London **KEPT** (15px), Harlow **KEPT** (1px); Watford, Rickmansworth, Ruislip and Harrow already level so none; **Hornchurch removes its own plate** — his example, and the plate there would have left 73px of new raggedness. | `brand:true` stays on all eight on purpose: the flag now means "offer a plate", and the measurement decides. Verified by opening all eight in turn at 1160 |
| D257 | 15 Aug | ⭐⭐⭐ **THE PLATES ARE WHITE CALACATTA GOLD WITH AN EVEN DARK WASH, AND EVERY BLOCK IN THE ABOUT COLLAGE HAS A GOLD RIM.** Client, in three messages: *"put it on a white or gray stone. You can use any of the stones you suggest from the stone gallery and just add that with the golden rim around every block in the about us section"*, then *"don't use the fucking blue slab that makes it look like shit"*, then *"you should obviously be choosing the Calacatta Gold"*, then *"add a slight dark gradient onto the stones that the logo stands out without having to give it a shadow."* ⛔⛔ **THE FIRST STONE WAS CLOUD BURST AND IT WAS CHOSEN ON THE WRONG CRITERIA.** It measured best on luminance and calmness — and it is BLUE, which no brightness figure was ever going to reveal. ⭐ **THE FILTER GAINED A COLOUR TERM: `blue − red` PER SLAB.** Of 264 files, 8 are pale, neutral-or-warm and calm. ⚠️ **THERE ARE THREE CALACATTA GOLDS AND ONLY ONE IS THE FLAGSHIP:** Gold Shimmer measures a **+11 blue cast** — the same fault in another guise — and Gold Soft is sd **3**, so fine it reads as plain white rather than as stone. **Oro Honed** was picked first (mean 199, sd 23, cast −15) and **the client then sent a photograph of Calacatta Gold SOFT and said use that one.** ⛔ I had ruled Soft out for being sd **3.6**, "so fine it reads as plain white" — **and that reasoning was wrong the moment he also asked for a darker wash: a quiet stone is exactly what belongs under a heavy one, because its veining survives and does not fight the lockup.** Oro Honed would have gone to mud at this depth. Soft is lum 213, sd 3.6, cast −2.7, **10KB**. ⭐ Cut to a 700px square rung of its own (39KB against the 222KB the stone pages serve) because no plate is ever wider than ~340px. ⛔⛔⛔ **THE DARK GRADIENT IS AN EVEN WASH ACROSS THE WHOLE SLAB, NEVER A POOL BEHIND THE MARK.** A centred radial was built first and rejected on sight: on white marble a dark ellipse does not read as shade, **it reads as a stain**. ⛔⛔ **AND THE LEVEL WAS FOUND BY BUILDING THREE AND LOOKING, AFTER I CALLED A BAD ONE FINISHED.** `0.10→0.20` leaves a beautiful slab and the wordmark vanishes; `0.34→0.58` makes the gold crisp and is no longer a light block, which defeats going pale at all; `0.22→0.36` held both on Oro Honed — and he then asked for *"a full gradient over it so that the logo stands out even more. Make it darker"*, which is **`0.44→0.64`** and where it sits. ⛔ **THE STONE AND THE WASH ARE ONE DECISION:** lighten the wash later and the stone has to be reconsidered with it. ⭐⭐ **THE MARK NOW CARRIES `filter:none`** — the wash replaced a keyline and three recess shadows, which were four declarations doing a job the background should have done. ⭐ **THE GOLD RIM IS THE FULL 34% BORDER §2 RULE 10 ALLOWS**, on all six collage blocks; the banned thing is a bright band across the TOP, not a rim. | Verified at 1160: wash live at 0.22–0.36, mark `filter:none`, rendered ratio **1.0664** against intrinsic **1.0667**, all six rims `rgba(198,166,100,0.34)`, elements **2692**. ⚠️ **`Calacatta Gold` IS THE ONE STONE WITH AN OPEN LICENSING QUESTION (§7)** — already published as a stone page, so no new exposure, but it is now on the landing page and in three project galleries. **Told him** |
| D256 | 15 Aug | ⭐⭐⭐ **EVERY TOPCAT PLATE IS CUT STONE WITH THE MARK INLAID INTO IT, AND THE LOGO IS MUCH BIGGER.** Client: *"everywhere we were using a logo in the image like that, even in the gallery section or the project sections, especially now specifically talking about the about us section, make the logo inside that section bigger and add the stone slab thing design behind it instead of the just plain gray, or go ahead and cheaply generate a almost indent in the slab, but with the same gold colors. So the logo stays gold in that gradient, but it looks like it's actually in the slab."* ⭐⭐ **NOTHING WAS GENERATED AND NOTHING NEEDED TO BE** — he offered a cheap generation and the effect is two `drop-shadow`s over the site's existing slab. ⭐ **THE STONE IS `--marbleBG`, THE ONE SHARED SLAB** the nav glass, the CTA card and the page floor already use, so this adds no new material and no bytes. ⭐⭐ **THE GROUND OPENED FROM 0.92/0.96 TO 0.46/0.60 AND THAT WAS MEASURED, NOT EYEBALLED:** at the old alphas the veining varied by **10 levels** and was barely there; at 0.46/0.60 it varies by **17** — and the gold's contrast against the brightest part of the ground moves only **10.38 → 10.18:1**, so a slab that actually reads costs nothing in legibility. ⭐⭐ **THE INLAY IS LIGHTING, NOT ARTWORK (§2 rule 14 intact):** `drop-shadow` follows the file's own alpha, so a dark offset up-left is the stone edge falling into the recess and a warm one down-right is the lit lower lip. ⛔ **THE GROUND GRADIENT IS 158deg, SO THE LIGHT IS TOP-LEFT — FLIP ONE AND YOU MUST FLIP THE OTHER** or the recess pops back out as a bump. ⭐ Sizes: **68%** on the About plate (his emphasis), **58%** on the gallery plates, up from 46%. ⛔⛔ **`object-fit:contain` IS A RULE-14 GUARD, NOT DECORATION, AND IT ALREADY EARNED ITS PLACE:** the gallery plates take their aspect from whatever hole the masonry leaves, and on a 339x452 one the `max-width` clamp bit — **the BOX went to 0.9552 against the artwork's 1.0667**, and `contain` letterboxed instead of squashing. Measured the DRAWN artwork, not the box: **1.0667 exactly.** The clamp was then opened 74% → 84% so the mark reaches its intended 57.7% rather than 51.7%. ⭐⭐ **AND IT FIXED A MEASURED WEAKNESS FOR FREE:** the Why feature mark sat at **4.53:1** over the pale marble D247 put under it; the dark inlay offset takes it to **10.38:1**. | Verified at 1160: About plate mark **201.09 x 188.57**, rendered **1.0664** against intrinsic **1.0667**, height **67.6%**; Central London gallery plate drawn **278.1 x 260.7**, ratio **1.0667** exactly, height **57.7%**; stone present on both. `harvest/verify.py` **132/132/132 ✅** |
| D255 | 15 Aug | ⛔⛔⛔ **THE BIG COLLAGE TILE IS A TOPCAT PLATE, AND THE QUARRY CAME STRAIGHT BACK OUT — NO AI IMAGERY IN THE ABOUT BLOCK.** Client, on the quarry: *"You cannot just add an AI query image in there. Just use a different project image or something. Just use something different… or even add the vertical topcat logo in there."* ⚠️ **THE QUARRY WAS AN EXISTING ASSET, NOT A NEW GENERATION, BUT IT WAS STILL AI AND THAT WAS THE OBJECTION** — D254 lasted about ten minutes and the row is kept so nobody revives that file a third time. ⛔ **ALL 99 PROJECT PHOTOGRAPHS WERE LOOKED AT FIRST, AT THE TILE'S OWN SIZE, AND NONE EARNED IT:** the honest installation frames (`hornchurch-g2`, `g3`, `g4`) are building-site record shots — a slab on a trestle on a dusty floor, a step ladder in a bare room — and everything else is a fourth finished interior against w2, w3 and w4. **His own suggestion was the best answer on the table.** ⭐⭐ **IT IS THE SAME COMPONENT AS THE PROJECT PLATES (D240) AND THE WHY MARK (D245)** — same `topcat-vertical.svg`, same 158deg ground, same 46% height and 0.92 opacity — so the page's brand moments are one file and one treatment, not three lookalikes. ⛔⛔⛔ **THE SELECTOR CARRIES FOUR CLASSES AND THAT IS LOAD-BEARING:** `.about-collage>.ac-tile>img` is `position:absolute;inset:0` (0,2,1) and `.ac-tile img` is `width:100%;height:100%;object-fit:cover`, so a lone `.ac-mark` loses to both and **the lockup would have shipped stretched — the exact way D245 nearly shipped.** ⛔ Height only (§2 rule 14). ⭐⭐ **`picks[0]` IS `null`, NOT REMOVED:** the fill maps to `.ac-tile img` in DOM order, so shortening the array would slide w2, w3 and w4 up by one and silently re-shuffle the collage. ⚠️ The veil is off on the plate — it is a bottom-up scrim written for photographs. | Verified at 1160 and 390 with **layout properties, not rects** — the tiles hinge in 3D and a rect mid-build reported the lockup at ratio 11.0 when it is 1.066 (D221/D230). Desktop: tile 282x279, mark 136.03x127.56, **rendered 1.0664 against the artwork's own 1.0667**, height **45.9%**, centred to within a pixel of the odd-pixel split. Phone: tile 220x219, mark 106x100, height **45.7%**. No overflow, no broken images |
| D254 | 15 Aug | ⛔⛔ **THE QUARRY REPLACES THE GENERATED HANDSHAKE ON THE BIG COLLAGE TILE, AND IT REVIVES A FILE HE ONCE REJECTED.** Client: *"the shaking hands image that you're using in the about us section looks terrible. It just looks horrible. So add a different image that it may be something to do with a slab or some sort of installation or some sort of outside thing."* ⭐⭐⭐ **THE FIX WAS NOT A BETTER HANDSHAKE, IT WAS NOT A FOURTH INTERIOR** — w2 is a kitchen mid-fit, w3 a slab being carried, w4 a finished island, so every obvious candidate from the 107 project files was another room. **The quarry is cut blocks under open sky: two of his three suggestions in one frame, and the only thing in the library that changes the subject.** ⭐ It is also the brightest asset available, **198** against the tile's old **55**, which answers the *"two dark images"* complaint from the message before it — the four work tiles now read **198 / 115 / 71 / 124** instead of 55 / 115 / 71 / 115. ⛔⛔ **HE REJECTED THIS EXACT FILE IN D241 AND THE REASON DOES NOT CARRY OVER:** it was the Outdoor Spaces SERVICE tile, where a photograph of stone being cut out of the ground sat against copy promising barbecue runs and garden bars. Beside the About copy — sourcing the right stone, answering for the whole job — it is apt. ⚠️ **HE WAS TOLD IT HAS A HISTORY**, and told about the one adjacency worth watching: the copy two lines away says *"we keep everything under one roof"*, and a quarry beside it must not be read as a claim to quarrying. The tile carries no caption and the alt says only what the picture is. ⭐ **ZERO CREDITS** — both rungs and the `SS` entry have been on disk since D109 and unreferenced since D241, kept on purpose for exactly this. ⚠️ `about-handshake` is now superseded after one round; both rungs and the master stay on disk. | Verified at 1160 on a fresh load: w1 serving `quarry-955.webp`, elements **2692**, no broken images |
| D253 | 15 Aug | ⭐⭐ **THE COLLAGE'S WIDE TILE IS THE HARROW ISLAND NOW, NOT CENTRAL LONDON — AND IT COST NOTHING.** Client: *"we should use a different project image than the Central London project because we have two dark images there… and the London one, whatever image that we have there from the project gallery, that's just not good."* ⭐⭐ **TWO SEPARATE POINTS AND HE IS RIGHT ON BOTH.** Measured as they actually render, the four work tiles ran **55 / 115 / 71 / 115** — w1 and w3 carry the block's weight, so the one wide tile must not add to it. And the frame itself was the bigger fault: Central London's card is a corner with a **sink, a kettle and a bin** in it, so the stone is not the subject at 282x134. ⭐ **HARROW IS THE STRONGEST FRAME IN THE PORTFOLIO FOR THIS BOX** — a mitred waterfall end in warm light against navy units, mean **124** and stddev **58**, the highest contrast of all 39 landscape files (Central London's is 36). ⛔ **CHOSEN BY SURVEYING ALL 107 PROJECT FILES ON LUMINANCE, CONTRAST AND ASPECT AND THEN LOOKING AT THE SHORTLIST AT THE TILE'S OWN SIZE UNDER ITS OWN VEIL** — a bright thumbnail is not the same as a picture that reads at 282px behind a 0.42 scrim. ⭐⭐ **STILL A REAL TOPCAT JOB, WHICH IS THE WHOLE POINT**, and no credits: its ladder is already in `SS` under this exact key from D211. ⚠️ It is also the Harrow CARD image, so it appears twice on the site — the same trade D244 made with Central London, and acceptable for the same reason. ⚠️ The D244 comment calling w4 "the only real photograph in the list" was true when written and is not any more (w2 became his in D247); corrected in place. | Verified at 1160 on a fresh load: w4 serving `harrow-1400.webp`, tiles now **55 / 115 / 71 / 124**, elements **2692**, no broken images |
| D252 | 15 Aug | ⭐⭐⭐ **FOUR GENERATED PHOTOGRAPHS, NO FACES IN ANY OF THEM, AND THE LOGO ON THE SHIRT IS HIS OWN FILE AND NOT A DRAWING.** Client: *"a black shirt with a golden topcat logo on it, a collared shirt, and you don't see the person's face… block number four, we need to probably not show faces just the installation being done with hands on it… the big block where the two people are shaking hands, we need to regenerate that… for the more reasons to choose us, we should also generate another image that doesn't show faces."* ⛔⛔⛔ **THE LOGO WAS NEVER GENERATED AND MUST NEVER BE — §2 RULE 14.** An image model asked for "a gold Topcat logo" INVENTS a mark, which is exactly the re-drawing that rule forbids, and it did invent two of them plus a wordmark on the consultation shirt. Those were patched out with the shirt's own fabric and **`topcat-icon.svg` was composited on afterwards, rasterised from ITS OWN PATH DATA and ITS OWN gradient stops.** ⚠️ **EVEN-ODD FILL, OR IT IS THE WRONG MARK:** the icon is three contours and the inner two are HOLES — filling all three solid gives a shape that is nearly right and quite wrong. ⭐ Badge sized at 14.6% of the chest width, the proportion of a real embroidered logo, and multiplied by the fabric's own luminance so the folds read through it. ⭐⭐ **CONSULTATION TOOK TWO ATTEMPTS AND THE FIRST FAILURE IS THE USEFUL PART:** a correct faceless handshake in an empty kitchen said nothing about what the meeting WAS. Client: *"we definitely need a new image for consultation where it looks like a consultation… handshake with the slab thing in the background or some plans."* ⛔ **AND THE PROPS HAD TO BE IN THE TOP HALF OR THEY WOULD NOT EXIST** — `.pt-scrim` is 0.92+ opaque across the bottom 40%, which is why the better-composed variant (notebook, sample fan, tape measure on the counter) was REJECTED: at 392x230 its whole story sat under the shade. ⭐ **INSTALL WAS CROPPED, NOT REGENERATED**, on his instruction — *"crop out the part where you see the breast pockets… you can still use that same image"* — top 26% cut, so its top rung is **1516** and not 1600, which would be an upscale. ⚠️ **THE MODEL PUT RED-AND-WHITE PIPING ON EVERY POLO** and would not stop; it is recoloured to brand gold by a threshold measured PER IMAGE against that image's own reddest skin (trim r/g up to 32, skin never past 2.78), and the last pass needed a brightness floor of 28 because the collar's reds are darker than its cuffs. ⚠️ **THE WHY TILE IS TWO SHAPES** — 0.65 portrait on desktop, 1.60 landscape on a phone — so it is generated SQUARE with the subject centred, and the crop starts below the beard the model put in the top tenth. ⛔ **w4 OF THE COLLAGE WAS NOT TOUCHED**: he asked for "the block with the kitchen in it" to become an installation shot, it is the CENTRAL LONDON job from his own portfolio (D244), and on being told he kept the real photograph. | ⛔⛔⛔ **COST: 101.46 CREDITS FOR 8 GENERATIONS, ABOUT 12.7 EACH, AND THE CLIENT HAD SAID NOT TO SPEND MANY. THIS ROUND ALONE IS MORE THAN HIS ENTIRE STATED 100-CREDIT CEILING.** Balance **453.13 → 403.17 → 351.67**. ⛔⛔ **TWO SEPARATE FAILURES AND BOTH ARE ABOUT MEASURING:** (1) `get_cost` preflighted `soul_2` at **0.12 credits**, it was trusted, and it is wrong by a hundred times — ⛔ **NEVER TRUST THAT PREFLIGHT; IT IS NOT A PRICE.** (2) The balance was read once, AFTER all eight had run, so nothing could be stopped — ⛔⛔ **READ THE BALANCE AFTER THE FIRST GENERATION, ALWAYS, AND STOP IF IT IS NOT WHAT THE PREFLIGHT SAID.** One image would have cost 12.7 and told the whole story. ⚠️⚠️ **AND THE CHARGE SETTLES LATE:** the reading straight after the batch said 49.96 spent, which was reported to the client and was itself wrong — the rest landed minutes later. **Wait, then re-read, before quoting a figure to anybody.** ⚠️ Only **3** of the 8 are live (two consultation variants, a rejected second pair, and `about-handshake`, which D254 replaced an hour later). ⭐ **THE TWO SWAPS AFTER THIS ONE — D253 AND D254 — COST NOTHING**, because the answer was in his own portfolio and on disk already. **That is the first place to look, every time.** Verified at 1160: elements **2692**, no broken images |
| D251 | 15 Aug | ⭐⭐⭐ **THE COMPARE DOOR MOVES UNDER THE SLAB, AND THE SLAB'S TOP EDGE IS THE STONE'S NAME ON ALL 132 PAGES.** Client: *"on the individual stone pages, we should have, let's say, compare Azul Shimmer with other stones, and that should be higher up maybe underneath the actual slab. And then we can move the slabs to always be in line with the slab name in the individual slab pages."* ⛔⛔⛔ **"ALWAYS IN LINE" WAS THE REAL FAULT AND `align-items:center` WAS IT.** The slab was centred against a column whose height is the description, so a two-line description put it in one place and a four-line one somewhere else — **no two of the 132 pages agreed**, and even where it looked deliberate it was lining up with the KICKER, not the name. ⭐⭐⭐ **THE FIX IS STRUCTURAL, NOT AN OFFSET.** The kicker was inside `.stp-copy` above the `<h1>`, so the slab could never start lower than the copy column's top; it is a grid item of its own now and owns row 1 of the right column, which puts the slab and the name both on row 2. ⛔ **A `margin-top` of "about 30px" would have looked right and been wrong the moment the kicker's size, margin or line-height changed** — and it would have had to be re-derived at every breakpoint. **Measured on five stones including one- and two-line names: slab top minus h1 top = 0.00px on every one.** ⭐ **THE COMPARE LINK IS MOVED, NOT DUPLICATED**, and that REVERSES D141's reasoning, which put it at the foot beside the three near-identical alternatives it helps you weigh: *"it belongs here and not in the CTA row."* That was sound and it is superseded — somebody wants to compare while they are looking at the slab, not four screens later. **The comment at the old site now warns against putting a second one back.** ⭐ Wording is his: **"with other stones"**, not "with another stone", which is also the truer promise since compare takes a shortlist. ⚠️ **THE FROZEN BANDS KEEP THE ORDER THEY HAVE ALWAYS SHOWN** — slab, then kicker, then name — written as explicit single-column grid areas rather than left to DOM order, which now starts with the kicker. ⚠️ The link's label carries the stone's own name and wraps to two lines on a phone, so the glyph is aligned to the FIRST line rather than floating in the middle of the block. | `/stones/*.html`, 132 pages. Verified at 1160, 900 and 390: delta **0.00px** on azul-shimmer, arabescato-corchia-extra-honed, travertine-romano-classico, repen, argento and calacatta-borghini-light; link ink centred on the slab to **282 = 282** at 1160; phone order slab → compare → kicker → name; no duplicate link at the foot; no horizontal overflow. `harvest/verify.py` **132/132/132 ✅** |
| D250 | 15 Aug | ⭐⭐⭐ **THE CLOSING CTA BAND IS A CARD NOW, ON ALL 176 GENERATED PAGES, AND THE OLD ONE HAD BEEN BREAKING §2 RULE 10 SINCE IT WAS WRITTEN.** Client, with a screenshot of the two bands that close the compare page: *"redesign sections like this because it looks terrible and doesn't feel like it fits the branding or the design of the site at all. And then also any other of the pages that look like this or have anything like that redesign those as well so that it has our brand look. And what I mean is don't take that literally. Just design it better to fit better with the site."* ⛔⛔⛔ **THE RULE BREAK FIRST: `.cta-band` CARRIED `border-top:1px solid var(--hair)` — A GOLD LINE ACROSS THE TOP OF A SECTION**, which is the *"don't ever use that fingernail design on top of sections"* rule, on every service leaf, every stone page, the collection, compare and the whole SEO layer. Deleted. **The full 34% rim the card now carries is the sanctioned alternative — rule 10 forbids a top BAND, not a border.** ⭐⭐ **WHAT WAS ACTUALLY WRONG BEYOND THAT: IT WAS THE ONLY PLACE ON THE SITE WHERE AN ASK FLOATS ON A BARE SLAB.** Everything else that asks the visitor for something sits in a rimmed card with depth under it — the enquiry card, the review cards, the Why tiles, the About collage. This was edge-to-edge panel grey with type on it. ⭐⭐⭐ **ONE RULE SET REACHES ALL 176 AND NO BUILDER MARKUP CHANGED**, because the section took over the page gutter and `.wrap` became the card: `max-width:980px`, an 18px radius, the 34% rim, `--ink-2`, and the site's own `0 40px 90px -50px` shadow. ⛔ **THE GROUND IS PLAIN GREY BY CLIENT RULING, NOT BY DEFAULT** — 11 Aug, of the landing card: *"just make it a plain background, that grey that we have."* No stone, no bloom. ⭐ **THE HEADINGS TAKE A GOLD LAST WORD** (`Narrowed it <em>down?</em>`, `Can't choose from a <em>screen?</em>`, `Make it <em>yours</em>`, `Tell us about your <em>project</em>`), which is D229's white-then-gold shape; `em` was already gold-soft in the sheet so nothing was added for it. ⭐ **THE SEO LAYER GETS IT AUTOMATICALLY THROUGH `gold_last()`**, which wraps the last word of whatever heading a call site passes and **leaves a one-word heading white — D229's own exception**, not an oversight. ⚠️ **THE FOOTNOTE UNDER THE BUTTONS HAD NO STYLE AT ALL** and was the line colliding with them in his screenshot; it is a caption now, 12.5px faint with 26px of air, ⛔ separated by SPACE and not by a hairline, which would be the same fingernail one level down. ⭐ **AND THE BUTTON PAIR MATCHES ONCE IT WRAPS** (≤560px), because sized to their own words they stacked as two different widths down one centreline — exactly what D239 rejected on the landing page. | `services/service.css` is the single sheet every generated page links, which is why this is one edit. Rebuilt all four layers: 9 service leaves, 26 SEO pages, 132 stone pages + collection + compare. `harvest/verify.py` **132/132/132 ✅**. Verified on `/stones/compare.html`, `/services/kitchen-worktops.html` and `/materials/quartz-worktops.html` at 1160 and 390: card 980 wide centred to 90/90 at 1160 and 350 wide at 20/20 on a phone, no horizontal overflow, section `border-top` now **0px**. Buttons **304 = 304** stacked, 264/236 side by side |
| D249 | 15 Aug | ⛔⛔ **THE COMPARE PICKER CLOSES WITH A DOWNWARD CHEVRON, NEVER AN ×.** Client: *"on the compare stones thing, it's a pop up, but it should not be an x at the top. It should be a downward facing arrow because it's a pop up. If it was made to be an x, it looks like you're losing your saved progress."* ⭐⭐⭐ **HE IS TALKING ABOUT WHAT THE GLYPH PROMISES, AND AN × PROMISES DESTRUCTION.** The shortlist lives in the URL and the picker only ever ADDS to it, so closing it cannot lose anything — but × is the same mark that empties a basket and removes a chip, and one of those is `.cs-x` on the enquiry card. The user cannot know the sheet is safe; the glyph has to say so. ⭐ **IT IS ALSO LITERALLY TRUE:** `.cmp-pick` is `align-items:flex-end` with the panel square along its bottom edge and flush to the viewport floor (measured: panel bottom **900** of a 900 viewport) — a sheet that came UP, so down is the direction it goes. ⭐ **THE GLYPH IS THE NAV'S OWN CARET, NOT A NEW DRAWING** — same 2:1 chevron, same round caps and joins, in the 24 box this button's stroke weights were written for, so it draws at the same 1.06px the × did. ⚠️ **RENAMED `.cmp-pick-x` → `.cmp-pick-down`, `#cmpPickX` → `#cmpPickDown`, and the JS var with them.** A class called `-x` holding a chevron is an invitation to put the × back, and `stone.css` is hand-maintained. ⚠️ The box went 15x15 → **15x9**: a chevron forced into a square either shrinks or distorts. ⚠️ `aria-label` says what it does now — *"Close the picker and go back to your comparison"* — not "Close". | `/stones/compare.html` only; it is the one dialog on the page. Verified with four stones shortlisted: panel 940 wide, bottom flush at 900, button 38px at the head, path `M6 9.5l6 6 6-6`, svg 15x9. `harvest/verify.py` **132/132/132 ✅** |
| D248 | 15 Aug | ⛔⛔ **BOTH NUMBERS ARE GOLD, ALL THREE ROUTES SIT ON ONE LINE — AND THE MISALIGNMENT HE SPOTTED WAS 2px THAT D246 PUT THERE.** Client: *"with the WhatsApp number in the form also make that gold and line it up perfectly with the other number. Why is it not lined up on mobile? All three, the two numbers and the email, have to be perfectly in line."* ⭐⭐⭐ **HE WAS READING A TWO-PIXEL ERROR ON A PHONE AND HE WAS EXACTLY RIGHT.** Measured at 390 before touching anything: the phone and envelope rows started their text at **72.00px** and the WhatsApp row at **70.00px**. ⛔⛔ **THE CAUSE IS THE FIX FROM THE PREVIOUS MESSAGE.** D246 cut that icon to `width:14px` so the SOLID full-bleed glyph would not read a size larger than the two 1.5px outlines beside it — but the row is `icon + gap + text` in a flex line, so shrinking the icon's BOX drags the text left with it. **An optical correction was made with a layout property, and layout is shared with the neighbours.** ⭐⭐ **THE RIGHT INSTRUMENT IS THE `viewBox`, WHICH NOTHING ELSE MEASURES:** the box stays 16px like its neighbours and the artwork is padded to `-1.7 -1.7 27.4 27.4`, so 24 units of ink draw at **14.02px**, centred. Same optical size, same layout box. ⛔ **The lesson generalises past this row: when an icon has to look smaller, shrink the DRAWING, never the box.** ⭐ **AND THE GOLD IS A SHARED CLASS, NOT A SECOND RULE.** The WhatsApp link now carries `.cta-tel` itself rather than getting a matching declaration of its own, so the two numbers cannot drift apart in colour, size, tracking or underline weight — one place to change, and the email stays the quiet one at 13/14.5px bone. | Verified on fresh loads at 1440, 900 and 390. Text left edges identical to 0.00px on every band — **201.20 / 201.20 / 201.20** at 1440, **233.54** ×3 at 900, **72.00** ×3 at 390 — and the icon column with them. Both numbers `rgb(212,183,120)` at 16.5px desktop / 14px phone with the 0.6 underline; email 14.5/13px bone at 0.32. Freeze probe unmoved: elements **2692**, `.gal-scroll` **4950**, document **14833**, no horizontal overflow |
| D247 | 15 Aug | ⭐⭐ **TWO PHOTOGRAPHS HE SENT: THE DRAWINGS GO ON "DESIGN & QUOTE", A KITCHEN MID-FIT GOES IN THE ABOUT COLLAGE.** Client, in one message with two images: *"use this first screenshot as the image in the design and quote section, and then use the second screenshot as one of the images in the About section, one of the smaller parts."* ⭐⭐ **THE PROCESS IMAGE WAS A PORTRAIT IN A LANDSCAPE HOLE AND HAD BEEN SINCE IT SHIPPED.** `process-quote-675.webp` is **675x900**, and every box it is drawn into is wide: the tile is **348x148** on a phone, **598x148** on a tablet, **392x230** on the desktop, and `.pm-shot` in the step's detail card is **758x198**. Cover-cropping a 3:4 portrait into a 4:1 band throws away three-quarters of the file, so the new one is cut to **16:9** and the whole frame is used. ⛔ **NEW PREFIX, NOT A NEW RUNG — `process-quote-plans`.** `process-quote-675` is a different photograph and it stays on disk; two pictures sharing a rung prefix is the `SS` mis-read D241 wrote the rule about. ⭐ **THE LADDER STOPS AT 1600 AND THE REASON IS `#pmShot`**, which takes `p.img` straight with no `srcset` — 758 CSS px at 2x is 1516, so 1600 is the top rung and 2400 would be bytes nobody sees. The tile itself has `ss()` and takes **880** on a phone (32 KB), confirmed on a throwaway `?q=` probe because `currentSrc` reported the 1600 after the desktop pass had cached it — the D241 trap, twice in two rounds. ⭐⭐ **AND THE D242 CHECK WAS RUN BEFORE CLAIMING IT: the new picture is PALE, which is exactly what broke the tiles last round.** Measured by compositing the photograph under `.pt-scrim`'s own fourteen stops at the type's real band: **17.32:1 desktop, 11.02 tablet, 11.79 phone.** It holds because that scrim is near-opaque (0.92–0.985) across the text band, unlike the service tiles' — nothing needed changing, but it was measured, not assumed. ⭐ **THE ABOUT TILE IS `w2`, THE SMALLEST IN THE COLLAGE** — 156x132 desktop, 105x66 below it — so the crop is tight on the two people working over the worktop, not on the room, which is the only thing that reads at that size. ⚠️ **RE-CUT ONCE, FROM 4:3 TO 1.2:1**, because at 4:3 the near-square tile was throwing away 25% of the width and pushing the subject off centre. ⚠️ **IT REPLACES THE "CHOOSING STONE SAMPLES" AI STAND-IN, SO THE ROW NOW READS handshake → a job in progress → fitting → the finished island**, and `w3`'s fitting stand-in is the nearest neighbour in subject. Told him; whether `w3` moves is his call. ⚠️ Both files are stock he chose and sent, not his own photography, so the alt text describes the picture and claims nobody: *"Worktops going in on a kitchen fit"*. | Ladders cut by the pipeline's own rules (LANCZOS, WebP q85, method 6) with `.jpg` masters beside them, registered by hand in `SS` — `build_images.py` is one-shot (§7). `process-quote-plans` 880/1600 (32/72 KB), `about-fitting` 248/386 (5/10 KB). Freeze probe at 1440 on a fresh load is unmoved: `.gal-scroll` **4950**, `--revPer` **3**, `feTurbulence` **60**, document **14833**, elements **2692**, `#svcNav` **8**, no broken images, no horizontal overflow. Verified at 1160, 900 and 390 |
| D246 | 15 Aug | ⭐⭐ **THE ENQUIRY CARD SAYS "GET IN TOUCH WITH TOPCAT", AND ITS CONTACT ROUTES ARE LANDLINE → WHATSAPP → EMAIL.** Client, in one message: *"the contact form at the bottom of the page is where it says one visit, one price, one team. Just say something more general that speaks like the brand, like get in touch with Topcat or something like that or even just the word contact us"*, and *"on that form, remove the business hours and add the WhatsApp number. So it'll be the landline, then the WhatsApp number, then the email."* ⭐⭐ **HE IS RIGHT ABOUT WHAT THE TITLE WAS DOING: IT WAS A PROPOSITION STANDING WHERE A DOOR SHOULD BE.** "One visit, one price, one team" is the pitch, and the pitch is already made twice inside the same card — the line directly under it promises the free visit and the fixed price, and "What happens next" spells out all three steps beside it. The heading above a form should name the action. ⭐ The brand takes the gold, which is the white-then-gold shape every other title on the site uses (D229). ⭐ **THE ORDER IS HIS AND IT IS THE RIGHT ONE** — the two numbers sit together and the address follows. ⚠️ **HOURS ARE NOT LOST, THEY ARE IN THE FOOTER ON ALL 150 PAGES**, which is where somebody checks whether it is worth calling now; a contact card is for reaching us, not for opening times. ⚠️ **THE NUMBER IS WRITTEN `07464 940287` AND LINKED `447464940287`** — the national form a British customer would dial and save, the international one `wa.me` requires, same number and the only format that link accepts (D173, D207). ⚠️ **THE GLYPH IS CUT TO 14px WHERE THE PHONE AND ENVELOPE ARE 16px:** the WhatsApp mark is SOLID and drawn to the edges of its 24 box while the other two are 1.5px outlines drawn inset, so at a matched box it reads a size larger and much heavier. The icons look equal because they are optically equal, not because the numbers match. ⛔ **IT IS ALSO ON `/services/`, `/projects/`, `/estimate/`, `/about/` AND `/contact/`** — the card is the `s_cta` section `build_pages.py` lifts out of `index.html`, so one edit and a rebuild carries all six. ⚠️ **THE TRADE PAGE'S OWN CTA IS A DIFFERENT BLOCK** ("Open a trade account", `TRADE_CTA_SECTION` in the builder) and still carries the hours line with no WhatsApp. Left alone because he named this form; his call. | Verified on fresh loads at 1440, 1160, 900 and 390: three routes in his order, right hrefs, no hours, no horizontal overflow. Title is two lines at 390 (53px) and does not wrap awkwardly at any band |
| D245 | 15 Aug | ⭐ **THE VERTICAL LOCKUP GOES ON THE "MORE REASONS TO CHOOSE US" FEATURE PHOTOGRAPH.** Client: *"in the more reasons to choose our section where there's an image, just add the vertical topcat logo in there."* Same `topcat-vertical.svg` the project plates use (D240), so the page's two brand moments are one file. ⭐ **IT SITS AT THE FOOT OF THE TILE, NOT THE MIDDLE, AND BOTH REASONS ARE ABOUT THE PICTURE UNDER IT:** `.wy-p-veil` is a bottom-up scrim that reaches 0.46 at the floor and only 0.04 by 58%, so the foot is the one band where white artwork is legible without deepening anything; and the subject of that shot is PEOPLE, so a centred lockup lands on their faces. ⛔ **HEIGHT ONLY** (§2 rule 14). ⛔⛔⛔ **THE SELECTOR IS `.wy-p img.wy-p-mark` AND THE EXTRA CLASS IS LOAD-BEARING.** `.wy-p img` is written for the PHOTOGRAPH — `position:absolute;inset:0;width:100%;height:100%;object-fit:cover` — it matches ANY img in that figure and it out-specifies a lone class (0,1,1 against 0,1,0). **The first attempt shipped the client's lockup stretched to 181x251 against its own 528x495 artwork, and it looked entirely plausible in a screenshot.** ⭐⭐ **IT WAS CAUGHT BY COMPARING THE RENDERED RATIO TO THE INTRINSIC ONE, WHICH IS THE CHECK ANY "HEIGHT ONLY" RULE NEEDS** — rule 14 is unenforceable by eye at small sizes, which is exactly what it warns about. ⚠️ The hover scale is stood down for this img only; the photograph still grows. | Verified at 1440 and 390 on fresh loads: rendered ratio **1.0658** against the artwork's own **1.0667**, centred on the tile to **0.0px**, foot gap 19px / 17.9px, height 11.7% / 20.8% of the tile. No overflow at either width. Landing page otherwise unmoved: `.gal-scroll` 4950, `--revPer` 3, `feTurbulence` 60, doc 14833, `#svcNav` 8, zero broken images. **Elements 2691 → 2692**, which is the one `<img>`. CSS gate 0/0, JS parses, `verify.py` ✅ PASS 132/132/132 |
| D244 | 15 Aug | ⭐⭐⭐ **THE ABOUT COLLAGE RUNS THE FULL HEIGHT OF THE COPY NOW, AND TAKES A FOURTH WORK TILE.** Client: *"add more blocks to the collage so that the about us section fills up nicely in line with the text and the images lines up with the text and goes all the way and matches up with the bottom of the chat to the top cat button… Resize the images, add more take some away if you want to. Just do whatever you think is best."* ⛔⛔ **THE GAP WAS `aspect-ratio:3/4` PLUS `align-self:start`, NOT A MISSING TILE, AND THAT DISTINCTION IS THE WHOLE FIX.** The collage took its height from its own WIDTH — 496.8 wide gave 662.4 tall — while the copy column ran 761. **Measured: it ended 99px above the "Chat with Topcat" button.** ⭐ Adding tiles would have changed nothing; four tiles would have shared the same 662px. ⭐⭐ **THE HEIGHT COMES FROM THE GRID ROW NOW (`align-self:stretch`, no aspect-ratio), SO THE BOTTOM EDGE *IS* THE COPY COLUMN'S BOTTOM EDGE** — which is the button. It needs no number and cannot drift with the copy at any width. ⚠️ `align-self:start` was load-bearing for the TOP edge and `stretch` keeps it. ⭐ The directors keep their 3/4 pair because row one is `auto` and `.ac-p` carries its own aspect-ratio, so all 99px lands in the work band: **324 → 423**. ⛔ Six equal rows would have stretched the portraits, and "those images can be the same size" (D150) is about each other, not about being tall. ⛔⛔⛔ **TWO GRID TRAPS, BOTH MEASURED, AND THE SECOND ONE IS THE SUBTLE ONE.** (1) A bare `1fr` track carries an automatic MIN-CONTENT floor. (2) ⚠️ **EVEN `minmax(0,1fr)` RESOLVES TO MAX-CONTENT WHEN THE PARENT ROW IS AUTO-SIZED** — so the collage reported a 1019px contribution, WON the row against the copy's 761, and the section grew from 844 to 1101. **Putting the tile images out of flow (`position:absolute;inset:0`) drops that contribution to zero.** ⭐ That is the same lesson already written on `.wy-p img` one section later, for the same reason — a photograph in flow sizes its own tile. ⭐⭐ **THE FOURTH TILE IS THE ONLY REAL PHOTOGRAPH IN THAT COLLAGE**: the Central London waterfall island out of the client's own portfolio, keyed into `SS` already (D211), against three AI stand-ins that are still owed a shoot (§7.5). ⚠️ FOUR WORK TILES IS THE CEILING: `scrollSequence`'s bound is `(n−1)*step + span < 1` and at six tiles that is `5*0.112 + 0.42 = 0.98`. A seventh needs `step` cut to 0.096. ⛔ DESKTOP ONLY — `.ac-w4` is `display:none` at base so an unplaced grid child cannot drop into an implicit row on the frozen bands. | Fresh loads at 1440x900. Collage top and bottom both **0px** from the copy's top and the button's bottom, measured with `offsetTop` not rects (the button sits in `.about-cta.rise`). Section back to **844** from the 1101 the first attempt produced. Rows **321.87 / 132.39 / 132.40 / 132.39**. Frozen bands verified unchanged: **900** keeps `aspect-ratio 3/4`, `align-self:start`, six rows, `.ac-w4` none, collage 333x444, section 750; **390** the same, collage 335x447, section 1275, no overflow. Landing page: `.gal-scroll` 4950, `--revPer` 3, `feTurbulence` 60, doc **14833**, `#svcNav` 8, zero broken images, no overflow. **Elements 2687 → 2691**, the new tile's four nodes exactly. CSS gate 0/0, JS parses |
| D243 | 15 Aug | ⭐⭐⭐ **THE HELIX ARROWS ARE THE "CALL US" BLOCK, MIRRORED UNDER THE SPIRAL AND SITTING ON ITS LINE.** Client: *"the one left and the one right button underneath the helix, I want you to bring that slightly higher up and make the buttons the exact same size as the call us block and then also the same height and move it exactly into the center of the helix. and line it up exactly center with the word call us… Then also move the helix slightly down so that… the top of the title with the top of the helix."* ⭐ The pair is now **129.79 x 50.75**, `.rev-cta-ghost`'s own box, as two equal columns (D239's answer to making two buttons one size without typing a second number). ⭐⭐ **A px BOX IS SAFE HERE AND THAT WAS CHECKED BEFORE IT WAS TYPED**: that button is `font-size:12.5px` with `padding:15px 28px` and a 1px border, none of them viewport-scaled, so it measures identically at 1132 and 1440. ⛔ **D239's warning is about vw-LED things** — the rule is not "never a px", it is "never a px for something that moves". ⚠️ Re-measure if the label or the CTA's padding changes; the width is `58px + 5.7431em` of "Call us". ⚠️ Square corners, not circles: a 50% radius on a 60x50 box is an ellipse. ⭐⭐ **THE HELIX TOP MEETS THE TITLE TOP BY REPEATING `.svc-intro`'s OWN `clamp(6px,2.5vh,30px)`, NOT BY A NUMBER MEASURED OFF IT** — that padding is the only reason the title starts below the row, so the two tops now move together at every window height (21.6px at 863, 22.5px at 900). ⛔⛔⛔ **THE FOOT IS SET FROM SCRIPT AND NO CSS VALUE CAN DO IT — MEASURED BEFORE REACHING FOR JS.** The two columns are a stretch row inside a `min-height:100vh` frame, so the row's height follows the WINDOW while the left column's content height is intrinsic: **the gap below the CTA pair is 6.5px at 863 tall and 48.8px at 900.** A fixed padding is right at one height and wrong everywhere else. ⛔ The obvious alternative — parking the CTA pair at the column's bottom — is the one the client already rejected. ⭐⭐ **RECOMPUTED FROM A FRESH READ ON EVERY RESIZE, NEVER ACCUMULATED (D236's RULE)**, and it runs BEFORE `metrics()` because it changes the stage rect that `metrics()` reads. | Fresh loads. **1440x900**: arrows centred on the helix **0.00px**, on the Call us centre line **0.00px**, bottom line **0.00px**, size delta **0.00 x 0.00**, tops align **23 = 23** (`offsetTop`, not rects — the h2 carries a reveal and its rect lies by 34px). **1280x1000**: all four checks still 0.00 and tops **25 = 25**, with the computed foot moving **14.82px → 90.66px** — the exact spread a fixed value would have got wrong. Landing page unmoved: `.gal-scroll` 4950, `--revPer` 3, `feTurbulence` 60, doc 14833, elements 2687, `#svcNav` 8, zero broken images, no overflow, no console errors. CSS gate 0/0, JS parses |
| D242 | 15 Aug | ⭐⭐⭐ **THE SERVICE NAMES ARE LEGIBLE ON THE TILES AGAIN, AND THE REASON THEY STOPPED BEING IS D241 ITSELF.** Client, straight after the photographs went in: *"make double sure that on desktop mobile and tablet that the words are easy enough to read on those blocks, especially in mobile. In mobile, it just looks a bit hard to read right now… make it so that it's really easy to read the name of whatever that section is, and feel free to move the text slightly closer to the bottom on mobile and on tablet."* ⛔⛔ **THE SCRIMS WERE TUNED AGAINST PHOTOGRAPHS THAT NO LONGER EXIST.** The 13 Aug scrim was measured against a set where four of the images were dark; the eight the client sent on 15 Aug are pale stone — a white marble vanity, a Carrara fireplace, a quartz peninsula — and pale stone lands exactly in the name's band. **Measured by compositing each real photograph under its real gradient: phone worst 1.76:1 (Bathrooms), then 1.97, 2.32, 2.42, 2.55, 2.58, 3.17 — seven of eight under the 4.5:1 bar.** ⭐⭐ **THIS IS THE SHAPE TO REMEMBER: A CONTRAST DECISION IS ONLY VALID FOR THE PICTURE IT WAS MEASURED AGAINST.** Change the photograph and the scrim silently stops working; nothing errors, nothing moves, the type just goes soft. ⭐⭐⭐ **HIS OWN SUGGESTION WAS RIGHT BUT NOT SUFFICIENT, AND THAT WAS MEASURED BEFORE ANYTHING WAS WRITTEN:** moving the name down 12px with the gradient untouched reaches **3.85:1**, still under the bar. Deepening the foot alone and leaving the name where it was reaches 3.30. **The two together reach 6.16 in simulation and 6.32 on the built page.** ⛔⛔ **THE TRANSPARENT WINDOW IS UNTOUCHED, 47%→76%, TO THE CHARACTER** — that is D-13-Aug's "I want the images to be full brightness" ruling and only the stops BELOW the name moved. Deepening the middle is the move that would quietly undo it. ⚠️ **THE NAME MOVES BY THE `h3`'s OWN BOTTOM MARGIN (12px → 6px), NOT BY THE CARD'S PADDING** — the padding also sets the name's left inset and the number's inset at the top, and neither was asked to change. `.flip-hint` is `display:none` on that grid, so the 12px was holding nothing up. ⭐⭐ **THE DESKTOP NEEDED THE OPPOSITE FIX AND THE MEASUREMENT IS WHY.** Its name floats at **23–57%** of the card, not on the floor, because `.hx-meta` also holds the "View this service" button, which keeps its space on every card while it is transparent on all but the front one. **So a nudge downwards moves nothing, and deepening the FOOT moves almost nothing: 3.85 → 3.95.** The whole gain is in the 20–60% band where the glyphs are: **3.87 → 5.72 worst, and the front card, the one actually being read, 4.70 → 8.07.** ⚠️ Its top stop went 0.12 → 0.10, so the clear corner is fractionally clearer than before. ⭐⭐ **THE TABLET WAS MEASURED AND DELIBERATELY LEFT ALONE AT 8.39:1.** ⚠️ **AND MEASURING IT TURNED UP SOMETHING NOBODY HAD NOTICED: THE 13 Aug "FULL BRIGHTNESS" FIX ONLY EVER APPLIED BELOW 600px.** The tablet's tiles still carry `.face.front .stone{opacity:0.5}` — the photograph at HALF strength over near-black — which is exactly the "dark radiant" the client rejected on the phone. It is why the tablet reads so well, and it is a look he has not seen. **Told him; his call, and it is one line.** ⚠️ The `@media(max-width:600px)` boundary is NOT the site's 720px phone band (§2 rule 15) — 601–720 falls through to the base rules with the tablet. | Fresh loads, every figure re-measured on the built page by compositing the real photograph under the real computed gradient at the real name band. **Phone 390: worst 6.32:1** (Splashbacks), then 6.87 / 7.22 / 7.33 / 7.41 / 7.61 / 7.78 / 13.95 — from 1.76 worst. Name band moved 19–32% → **15–27%** up from the tile floor. **Desktop 1440: worst 5.72:1** (Vanity Tops), front card **8.07** — from 3.87 / 4.70. **Tablet 900 unchanged and verified unchanged: 8.39:1 worst, veil string and `h3` margin-bottom 12px both untouched.** Landing page unmoved: `.gal-scroll` **4950**, `--revPer` **3**, `feTurbulence` **60**, doc **14833**, elements **2687**, `#svcNav` 8, zero broken images, no overflow. CSS gate 0/0, JS parses |
| D241 | 15 Aug | ⭐⭐⭐ **ALL EIGHT SERVICE TILES BECOME THE CLIENT'S OWN PHOTOGRAPHS IN ONE SITTING, AND EVERY ONE CARRIES THROUGH TO ITS LEAF PAGE'S HERO.** Client, in seven messages, one service at a time: *"use this image for the dining tables service card image, crop it if you need to. this will also automatically become the hero section in the dedicated page"*, *"then this image you use fr bathrooms"*, *"use this image for outdoor"*, *"ues this one for fireplace but crop it so the focus is more on the fireplace"*, *"use this one for vanity tops"*, *"use this for commercial"*, *"use this for splashbacks"*, *"use this for kitchen worktops"*. ⭐⭐ **THAT CLOSED EVERY OUTSTANDING PHOTOGRAPHY ITEM ON THE TILES AT ONCE:** the three "PHOTO TO COME" plates (Dining Tables, Fireplaces, Vanity Tops), all three wrong subjects (a QUARRY on Outdoor Spaces, a SLAB on Bathrooms, a DOMESTIC KITCHEN on Commercial), and the last two July stock images (Splashbacks and Kitchen Worktops). ⛔ **`phImg()` STAYS ANYWAY** — both renderers still fall back to it, and D206 shipped `src="undefined"` the day a service arrived without a photograph. ⚠️ **TWO PREFIXES ARE DELIBERATELY NOT THE OBVIOUS ONES: `service-splash-marble` and `service-worktops-quartz`.** `service-splashbacks-706` and `service-worktops-554` are DIFFERENT photographs — the July ones these replaced — and two pictures sharing a rung prefix is a `SS` table waiting to be mis-read. Both old pairs stay on disk, unreferenced, with `service-islands-468`, `quarry` and the six unused project files. ⭐⭐⭐ **THE CROP IS THE WHOLE JOB AND IT IS DECIDED BY THREE BOXES, NOT BY TASTE.** Each file is centre-cropped by the card grid at **1.52:1**, the leaf hero at **2.69:1** and that same hero on a phone at **0.73:1** — measured, not assumed — so a desktop hero shows the middle **55.8%** of the file's HEIGHT and a phone hero the middle **48.4%** of its WIDTH. ⛔ **A SUBJECT SITTING OFF-CENTRE SURVIVES NONE OF THEM**, and in all three frames he sent it was off-centre: the marble table is in the right-hand third, the vanity is on the left, the garden kitchen is in the right-hand third behind rocks and a palm. All eight are cut to 1.5:1 with the subject on the centre — **dining (2639,1472)–(7275,4563), bathrooms (331,1398)–(5601,4912), outdoor (3496,1288)–(7360,3864), commercial (3974,1398)–(7360,3654), kitchen worktops (1914,1969)–(6330,4912)** of 7360x4912 originals, **fireplaces (1450,880)–(3000,1913)** of a 3000x2000, **vanity (737,4301)–(4731,6963)** of a 5464x8192 PORTRAIT, and **splashbacks (0,0)–(1620,1080)** of a 1920x1080. ⭐⭐ **THE CROP DID FOUR JOBS BEYOND FRAMING AND EACH ONE IS A REASON, NOT A PREFERENCE:** it took a **wall-mounted television** out of the fireplace shot, a **red wall sign and a shelf of ornaments** out of the kitchen, and **Cyrillic shopfront lettering** out of the commercial one — a UK company's page should not be showing any of the three — and it kept the **sockets** in the splashback, because that tile's own copy says "cut around sockets and hobs". ⚠️ Those sockets are not UK pattern; at card size it is not legible and the alternative crop lost the run of marble that makes the point. ⛔⛔ **THREE OF THE EIGHT SOURCES ARE SMALL AND THEIR LADDERS ARE CLAMPED TO THE NATIVE CROP — 1550 for fireplaces, 1620 for splashbacks.** There is no 2400 rung for either and there must not be one: upscaling adds bytes and no detail, which is the rule `build_images.py` already applies. The card grid asks for 2320 at 2x and takes what exists, exactly as `service-worktops-554` always did. ⭐⭐ **THE VEIL WAS SIMULATED BEFORE ANY FILE WAS WRITTEN.** `.svc-hero-bg::after` is two gradients — 0.95→0.82→0.50 across and 0.92→0.22 up — which composite to between **0.61 and 0.96** over the photograph. Compositing candidate crops under that arithmetic offline is what caught the first dining crop **cutting the lily heads off the top of the hero band**; raising the crop's top edge by 65 source px fixed it, and that is a change the card would never have shown. ⛔⛔ **AND IT IS ALSO WHY THE HEROES TAKE THE 1600 RUNG AND NOT THE 2400 ONE.** A CSS `background-image` has no `srcset`, so whatever the builder names is what a phone downloads; under 0.61–0.96 of veil the extra pixels are not visible, which is the trade `build_images.py` already wrote down for hero-kitchen — *"a photograph that is heavily darkened and sits behind text"*. It keeps the eight heroes at **98–348 KB** against the **149 KB** hero-kitchen already ships. ⚠️ **`hero-kitchen.jpg`, `kitchen-day.jpg` and `cta-slab.jpg` ARE NOT RETIRED.** hero-kitchen is the LANDING PAGE's hero, kitchen-day is still `/services/kitchen-islands.html`, and cta-slab is still the landing page's CTA band. What ended is three service pages sharing two pictures between them. ⚠️⚠️ **THE OUTDOOR FILE IS THE EXPENSIVE ONE AND NO CROP FIXES IT:** foliage, gravel and cut stone are all high-frequency, so the same 2400px rung that costs 291 KB as a dining room costs **770 KB** here. ⛔ **QUALITY WAS NOT LOWERED FOR THAT ONE FILE** — 85 is the level build_images.py verified by eye, and a per-image quality is a second rule nobody would find later. The ladder is what makes it affordable: a phone takes **102 KB**. ⭐⭐ **THE LADDERS ARE HAND-WRITTEN INTO `SS` AND THAT IS NOW THE ONLY WAY:** `build_images.py` extracts seven base64 photographs out of `index.html` and exits if it does not find exactly seven, and `patch_images.py` took them out in D109 — **both scripts are one-shot and can no longer run.** The rungs were cut by their rules anyway (LANCZOS, WebP q85, method 6) against the two `sizes` these images are served at, 440/1160 in the grid and 210/385 in the helix. ⚠️⚠️ **A MEASUREMENT TRAP THAT COST TWENTY MINUTES: `img.currentSrc` LIES ONCE YOU HAVE BROWSED THE SITE.** The dining card reported the 2400 rung on the landing page, which would have meant the ladder was decorative — but Chrome prefers a candidate it has ALREADY downloaded, and the leaf hero had put a bigger file in the cache. **A throwaway probe page with `?q=` on every candidate is the only honest test**, and it showed 880 at both phone and desktop card sizes. ⛔ Do not read `currentSrc` on a page you have been clicking around. ⚠️ `cta-slab`, `quarry` and their rungs all stay on disk and stay in `SS` — nothing points at quarry now, and a deleted photograph cannot be put back from the browser. | Fresh loads. **Landing page at 1440 unmoved:** `.gal-scroll` **4950**, `--revPer` **3**, `feTurbulence` **60**, doc **14833**, elements **2687**, `#svcNav` **8**, zero broken images, no horizontal overflow. Phone 390: no overflow, zero broken images, **zero plates**, and all eight cards render their own photograph. Rung selection on clean URLs: **880 at 390 for both the grid and the helix**, 880 for the helix at 1132, 2400 for the grid's 1160px `sizes`. Heroes verified on the page at 1180 for dining, bathrooms, outdoor, splashbacks and commercial; dining also at 390. **Nine service pages still resolve nine heroes** — eight of them the client's files, kitchen-islands still `kitchen-day.jpg`. CSS gate 0/0, JS parses, `verify.py` ✅ PASS 132/132/132 |
| D240 | 15 Aug | ⭐⭐ **THREE DUPLICATE PHOTOGRAPHS OUT OF THREE PROJECT GALLERIES, AND A TOPCAT PLATE STANDS IN THE HOLES THE MASONRY LEAVES.** Client, with five screenshots: *"this image is in the Hornchurch project, two of these images are the same, so remove the one on the left side of the screenshot and then re-stack the images so that they're neatly in a block"*; *"from the Rickmansworth project, remove this image… it's at the bottom of that page"*; *"in the Central London project you'll see there's an open gap at the bottom of the page next to the images, I want you to add the vertical Topcat logo into that open gap going across the two spaces… at a big enough size that looks good"*; *"in the Harlow project those two images are the same, so remove the one on the right"*; *"the fifth screenshot is from the Harrow project, add the Topcat logo next to it there as well."* ⭐ **WHICH FILES, AND HOW THEY WERE IDENTIFIED:** none of the pairs is byte-identical — they are the same shot exported twice at different sizes — so `md5` found nothing and a contact sheet of each gallery did. **Hornchurch `-g6` (the left of the pair, `-g8` kept), Rickmansworth `-g16`, Harlow `-g7` (the right of the pair, `-g2` kept).** ⚠️ The overlay gallery is a **3-column `column-count` masonry**, so "left" and "right" are adjacent COLUMNS at the same height, not array neighbours — `g6` and `g8` are four apart in the array and side by side on the screen. Read the rendered x, not the index. ⭐⭐⭐ **THE PLATE LANDS IN THE GAP BY ARITHMETIC, NOT BY POSITIONING.** `column-count` balances to one shared height and fills columns in source order, so the LAST item, given exactly the height of the missing slots, can only go where the hole is. `(cols − n mod cols) mod cols` IS the hole: **13 photographs leave two slots at three columns, 14 leave one, 2 leave one** — and the plate takes its aspect from the project's own first photograph, so it is exactly as tall as the pictures beside it at any column width. ⛔ No `order`, no absolute positioning, nothing that could disagree with the balancer at a width nobody checked. ⛔ **THE SLOT COUNT WAS A TYPED NUMBER IN THE DATA FOR AN HOUR AND THAT WAS WRONG** — add a photograph and it is stale, the same "what was derived from it stays behind" fault as D129's pull and D142's `animPx`. **It is computed from the gallery length now and the field is just `brand:true`.** ⭐ It also answers each band separately: at TWO columns Harrow's two photographs already make a square block, so the plate does not render there at all, and one column never has a hole, so the phone hides it outright. ⚠️ **THE PLATE IS NOT A PHOTOGRAPH AND MUST NOT BEHAVE LIKE ONE:** it is out of `MEDIA`, so the lightbox never opens a blank slide between two pictures, and it carries `aria-hidden` with no `tabindex` and no `role` — the logo is in the header of every page already. ⛔ **HEIGHT ONLY ON THE ARTWORK** (§2 rule 14); the rim is the GOLD `--hair` rather than the photographs' soft one, because a plate with no picture in it reads as a hole at 10%. ⚠️⚠️ **HARLOW'S PLATE IS NOT SOMETHING HE ASKED FOR AND HE MUST BE TOLD:** removing the duplicate took it from 15 photographs to 14, which balances 5/5/4 with the hole at the foot of the FIRST column — a gap in the middle of the block rather than at its end. One plate closes it. ⛔ One field to delete if he would rather have the hole. ⚠️⚠️ **AND HORNCHURCH STILL HAS A SECOND DUPLICATE HE HAS NOT NAMED: `-g1` AND `-g9` ARE THE SAME NOOK**, `-g1` being the tighter crop. **It was removed, measured and put back**: it is the only portrait balancing that gallery's two landscape shots, and without it the two of them sit alone in column one under a **399px** hole. With it the block is 3/2/2 and the bottom edge steps by **74px**, which is the neatest seven mixed-height photographs can make. **His call, and the reason is in the numbers.** | Verified on fresh loads at 1440. Hornchurch **7 photographs, 3/2/2, step 74px** (was a 471px step with the duplicate in). Rickmansworth **15, 5/5/5, step 1px**. Central London **13 + plate, 5/5/4, step 15px**, the plate two slots tall at the foot of column three. Harlow **14 + plate, 5/5/5, step 1px**. Harrow **2 + plate, one a column, step 1px**. Tablet 900: Central London 7/6 + a ONE-slot plate; Harlow and Harrow show none, correctly. Phone 390: no plate at all, no overflow. Lightbox on Harrow opens `harrow-g2` and reads **2 / 2** — the plate is not in the list. Landing page unmoved: `.gal-scroll` **4950**, `--revPer` **3**, `feTurbulence` **60**, doc **14833**, elements **2687**, `#svcNav` 8, zero broken images, no overflow, and a fresh tab logs no console errors. CSS gate 0/0, JS parses, `verify.py` ✅ PASS 132/132/132 |
| D239 | 15 Aug | ⭐⭐⭐ **THE BUBBLE ROW ENDS EXACTLY WHERE THE HEADLINE DOES, THE TWO BUTTONS ARE ONE SIZE AND CENTRED, AND THE TRUST LINE WENT TO ONE LINE AND STRAIGHT BACK TO TWO.** Client, in three messages: *"make those four blocks on the bottom a little bit smaller each so that it doesn't go past the side of the b from the word building and the side of the d from the word round. So it all fits perfectly in line"*, then *"the buttons for get your free quote and request a call have to be the same size and center"*, then *"put the subtitle back in two lines like it was before."* ⭐⭐⭐ **THE CAP IS MEASURED IN THE HEADLINE'S OWN TYPE SIZE, WHICH IS THE WHOLE REASON IT HOLDS.** "building around" sets in **9.4374 em** — checked at 72, 80, 89.28 and 92px and identical at all four, because the tracking is in em too — so the row is `calc(var(--hTitle) * 9.4374)` and the two edges cannot drift at a width nobody looked at. ⛔ **A FIXED px CAP WOULD HAVE BEEN RIGHT AT 1440 AND WRONG EVERYWHERE ELSE**: the headline is vw-led between 1161 and 1484, so its second line runs **679.5px at 1121** and **868px at 1600**. ⭐ `--hTitle` exists for this; the phone and tablet still set `font-size` outright, and the ≤1120 block has to say `max-width:none` or it would size that row off a number nothing there renders. ⚠️⚠️ **MEASURE THE TEXT, NOT ITS BOX.** `.hl` is a full-width `overflow:hidden` mask, so `getBoundingClientRect()` on it says 1040 — the copy width — not 842.6. **A `Range` over the text contents is what gives the glyph extent**, and it is the only way to answer "where does the B start and the D end". ⚠️ **AND `margin-inline:auto` WAS NEEDED ON THE ROW ITSELF**: a block box with a `max-width` inside a wider parent aligns LEFT, so the first attempt was the right WIDTH and **98.7px to the left** of the headline it was matching — D199's omission, one level down. ⭐⭐ **THE TWO BUTTONS ARE AN EQUAL-COLUMN GRID NOW, WHICH IS HOW TWO BUTTONS BECOME THE SAME SIZE WITHOUT TYPING A NUMBER.** They were content-sized flex items — 259.4px against 219.9 — and no gap value makes those match. `repeat(2,1fr)` at `width:max-content` resolves both tracks to the wider label and `margin-inline:auto` centres the pair. ⭐ **IT ABSORBS D188 RATHER THAN FIGHTING IT**: the phone and tablet are the same grid with **one** column and a 76% container, so the shared measure moved off the two buttons and onto the row — which is what D188's own warning ("width is written once, do NOT reintroduce a second rule") was asking for. Measured identical at 390 and 900: 266px and 501.6px. ⛔ **THE ONE-LINE TRUST LINE LASTED ONE MESSAGE.** `max-width:56ch` computes to **718.5px** against a sentence that sets in **795.9px**, so that cap is the only thing deciding this; taking it off gave one line, and he looked at it and wanted the two back. **The cap is restored and written down as load-bearing** — and `.nowrap` is what keeps "across England and the British Isles." whole once it wraps. ⚠️⚠️ **A MEASUREMENT TRAP WORTH THE ROW ON ITS OWN: `scrollWidth > clientWidth` IS NOT AN OVERFLOW TEST AT SUB-PIXEL WIDTHS.** Both round to whole pixels, so on a 163.82px chip **all four bubbles reported one pixel of overflow — including one with 122px of slack** — and I shipped a 2px padding change to "fix" it before measuring properly. **Compare the children against the content box in sub-pixels.** The truth at 1121, the tightest desktop: the Google chip has **6px** of real slack, and the padding change was reverted. | Fresh loads. **1440×900**: bubble row **842.6px**, left and right edges **0.0px** from the glyph edges of "building around", four columns of 202.9px, slack 45 / 164 / 75 / 86. **1121** (headline on its 72px floor): row 679.5px, edges 0.0/0.0, columns 163.8px, Google chip slack 6px, nothing clipped. Buttons **259.4 = 259.4** and centred on the viewport to 0.0px at both widths. Trust line back to **two** lines at every width. Tablet 900 and 1120 untouched: CTA row 501.6px (76%), both buttons 502, title 66.6/72px, sub 18.5px two lines, chips 2×323, pill 8/10, scrim 0. Phone 390 untouched: buttons 266/266, title 38px, chips 2×170.5, padding **113.94/50.64**. Frozen desktop otherwise unmoved: `.gal-scroll` **4950**, `--revPer` **3**, `feTurbulence` **60**, doc **14833**, elements **2687**, zero broken images, no overflow. CSS gate 0/0, JS parses, `verify.py` ✅ PASS 132/132/132 |
| D238 | 14 Aug | ⭐⭐⭐ **THE DESKTOP HERO IS CENTRED NOW — ONE COMPOSITION AT ALL THREE BANDS — THE GOLD RULE IS DELETED, THE HEADLINE IS 89px, AND THE FOUR BUBBLES SPREAD ACROSS IN ONE ROW.** Client, an hour after D237: *"I have a better idea. For the desktop version, remove the line next to the subtitle, and then center the hero section design just like on the tablet and mobile version. So the title is going to be bigger, then the subtitle, then the buttons, and then the four buttons will be spread out better across just so that we take up the hero section."* ⛔ **THIS REVERSES TWO THINGS FROM D237, ONE HOUR OLD, AND BOTH REASONS MUST SURVIVE THE REVERSAL.** (1) D237 kept the trust line's gold rule because he had said *"as it is there with a golden line"* — that was a description of the desktop as it stood, and this is an instruction to change it. (2) D237 capped the bubble row at **480px because that was the two buttons' measured width**, which gave a LEFT-aligned block its right edge; centred, the block has both edges by construction and the cap only kept it small. ⭐⭐ **THE CENTRING WAS FIVE DECLARATIONS IN `@media(max-width:1120px)` AND THEY ARE LIFTED, NOT COPIED** — `text-align`, the `.hl` mask's symmetric padding, the trust line's alignment, the button row's justification, and the copy box's auto margins. ⭐ **AND THE COPY BOX WAS NEVER CENTRED, IT WAS LEFT BY OMISSION**, which is D199's finding on the tablet arriving at the desktop: `max-width` with no auto margins parks the box at the left while every child centres neatly inside it. ⛔⛔ **THE GOLD RULE IS DELETED, NOT HIDDEN.** The phone and tablet already stood it down for the reason that has now caught up with the desktop — a LEFT-EDGE mark against CENTRED text strands itself and reads as a stray mark — so a third `display:none` would have described an element rendering at no width (D234). The pseudo-element, its `position:relative` host and the `padding-left` that made room for it all went. ⛔⛔⛔ **THE TRAP IN THIS EDIT WAS THE PHONE'S INHERITED TYPE SIZES.** The phone had NO title or subtitle size of its own — it sat on the base clamp's FLOOR, 38px and 14.5px. Making the base rule the big centred desktop one would have grown the frozen phone headline from 38px to 52 on the spot, and nothing in the phone block would have shown why. **Both are written into the ≤1120 block now, unchanged to the character.** ⚠️ The copy box's new `min(92vw,1040px)` needed no such treatment and that was CHECKED, not assumed: at 390 the inner box is 350px, so neither the old 560px floor nor the new one ever binds. ⭐⭐⭐ **AND CENTRING THE COPY PUT THE HEADLINE STRAIGHT ACROSS THE THREE LIT PENDANTS, WHICH IS A REAL CONTRAST FAILURE AND WAS MEASURED, NOT EYEBALLED.** The big shade layer is a LEFT-TO-RIGHT ramp — 0.94 at the left edge, 0.06 at 74% — aimed at a composition that no longer exists, and behind the title it is only **0.19**. Sampling the photograph itself (D232's method, drawn to a canvas at its own rendered `cover` scale and offset, every 4px through each line's band): worst pixel **rgb(149,133,128)**, giving **2.45:1 for the white line and 1.52:1 for the gold one**. Both fail. ⛔ **A FLAT WASH IS THE OBVIOUS FIX AND IS THE ONE D217 TRIED AND REVERTED**: at 0.42 the gold still only reaches 3.36 (2.93 at its gradient's dark stop) and the whole photograph goes down with it. ⭐ **A SCRIM UNDER THE COPY DOES THE JOB WHERE THE WORDS ARE AND NOWHERE ELSE** — `radial-gradient(66% 62% at 50% 42%)`, zero at the edges, so the island's far end and the right-hand cabinetry keep their light. ⚠️ **IT IS TWO VARIABLES (`--heroScrimA/B`) SO THE FROZEN BANDS SWITCH IT OFF**: the phone and tablet keep D187's flat 0.30 wash, which the client asked for and signed off, and two veils on one photograph would be a third of a stop darker than anything he has seen. ⚠️ **THE 72px FLOOR ON THE DESKTOP HEADLINE IS THE TABLET'S OWN CEILING**: 6.2vw only reaches 72 at ~1161px, so without it the headline would have got SMALLER crossing 1120 → 1121. ⚠️ The subtitle takes a **56ch** measure with it — there was deliberately none while the box was 720px, but a 19.5px line across 1040px runs to ~118 characters. | Fresh loads. **1440×900**: headline **89.3px** on two lines, copy **1040px** centred, four bubbles at **251px each** in one row, composition **166.1 above / 201.4 below** (45.2:54.8, D235's balance). Contrast against the brightest pixel of the photograph itself, shadow-free: white line **6.29:1**, gold line **3.90:1**, the gold gradient's darkest stop **3.40:1**, trust line **7.79:1** — from 2.45 / 1.52 before the scrim. **1121**: headline 72px, no step at the seam, 4×243px. **1440×700**: headline 74.2px (the vh term taking over), whole composition still above the fold. **Tablet 1120 untouched**: 72px / 18.5px / 660px copy / 2×323px / gap 14 / scrim **0** / D187 wash intact. **Phone 390 untouched**: 38px / 14.5px / 350px / 2×170.5px / gap 9 / padding **113.94/50.64**. Internal heads unchanged — they carry the chips, never the hero's copy block. Frozen desktop otherwise unmoved: `.gal-scroll` **4950**, `--revPer` **3**, `feTurbulence` **60**, doc **14833**, elements **2687**, `#svcNav` 8, zero broken images, no overflow, no console errors. CSS gate 0/0, JS parses, `verify.py` ✅ PASS 132/132/132 |
| D237 | 14 Aug | ⭐⭐⭐ **THE DESKTOP HERO IS THE TABLET'S COMPOSITION NOW: TITLE, TRUST LINE, TWO BUTTONS, FOUR BUBBLES — AND THE THREE ICON FACTS ARE DELETED.** Client: *"look at the tablet version and look at the desktop version's hero sections. Look at the way it is set up. It is title, then subtitle, then two buttons, and then four reasons to choose them. I want you to now add this to the hero section of the desktop version. So what you are going to do is you are going to remove the icons, and… you are gonna have the title, then the subtitle, as it is there with a golden line, then you will have the two buttons, and then you will have the four buttons below or the four reasons to choose."* ⭐⭐ **THE SUBTITLE IS UNTOUCHED AND THAT IS WHAT HE ASKED FOR** — "as it is there with a golden line" describes the desktop line he is looking at, which already carries `.hero-sub::before`. ⛔ The phone and tablet stand that rule down on purpose (a left-edge mark against centred text reads as a stray mark), and that is unchanged. ⛔⛔ **THE ICON ROW IS DELETED, NOT HIDDEN, AND D234 IS THE PRECEDENT.** It was already `display:none` below 1120 (D169); hiding it on the desktop as well would have left an element that renders at NO width, described by eleven live rules — the exact shape D234 refused for the scroll cue. **So the `.hero-facts` list, its three inline SVGs, `.hf`, `.hf svg`, `.hf-t`, `.hf-t b`, `.hf-t i`, the 760px one-column block and the tablet block's five rules all went together**, and with them `--hfGap`/`--hfIcon` and the `.hero-ctas .btn-*` width that was still `calc(66.667% + var(--hfGap)*0.667 + var(--hfIcon))` — a measurement of a row that has not rendered since 13 Aug. ⚠️ **`#tcGoldSolid` STAYS**: the WhatsApp button still strokes against it and `build_pages.py` lifts the tc-defs sprite onto every page. ⚠️ The L-plan icon's five-attempt history is in D169 and in git; a plain rectangle holding smaller shapes reads as a camera, every time. ⭐⭐ **THE FOUR BUBBLES ARE ONE 2×2 GRID AT EVERY WIDTH, AND THE MECHANISM WENT TO BASE SCOPE — MOVED, NOT COPIED (D235's rule).** `.chip-reason` came out of the base `display:none` list and the grid, the centred chip and the odd-child span came out of the ≤1120 block; that block now holds only what is genuinely different there — a 9px gutter, `width:100%` and the tighter pill. ⛔ Pasting the grid into a `min-width:1121px` block would have made two live descriptions of one row, which is D106/D113/D114 again. ⭐⭐ **WHY A GRID AND NOT THE WRAPPED FLEX ROW THE DESKTOP HAD**: four content-sized pills wrap **3-then-1 in the 576px column at 1200 and 4-across in the 691px one at 1440**, so the row re-arranges itself inside the desktop band and no two edges line up in either arrangement. That is D186's finding on the phone — "it is what content-sized boxes DO, and no gap value fixes it". ⭐⭐⭐ **AND THE 480px CAP IS A MEASUREMENT, NOT A TASTE CALL: `Get your free quote` + 16px + `Request a call` is 479.3px at 1200 AND at 1440**, because both labels are `nowrap` at a fixed desktop type size. Left-aligned copy gives the block its left edge for free; the cap is what gives it the right one, so the buttons and the bubbles read as one column. Sized to its content the row landed **121px short** and read as unfinished. ⛔ Change either button label and this cap must be re-measured. ⭐ **THE STAGGER CLOSED UP WITH THE ROW THAT LEFT IT** — the facts held the 700ms slot, so the buttons and chips move to 720/880ms and the sequence is one 160ms cadence again. ⛔⛔ **AND THAT NEARLY BROKE EVERY INTERNAL PAGE SILENTLY: `build_pages.py` STRIPPED `hero-el` WITH AN EXACT STRING MATCH ON `--hd:1040ms`.** `str.replace` with no match does nothing and raises nothing, so the bubbles would have shipped still wearing the hero's staged entrance — opacity 0, holding their space, invisible. **It is a regex now** and cannot drift again. ⛔⛔⛔ **THE ONE THAT ACTUALLY COST A BUILD: A CSS COMMENT QUOTED `<svg class="tc-defs"` AND `build_pages.py` FOUND IT THERE FIRST.** Its markers are plain string searches from byte 0, and the stylesheet is 240KB of prose — so `defs` came back as a slab of CSS and **every internal page rendered its own stylesheet as visible text on the screen**. ⭐ Fixed at the class, not the instance: **every markup marker now searches from `</style>` onwards.** ⚠️ ⛔ **AND THE SAME COMMENT LEFT A STRAY `*/` THAT SILENTLY DELETED `.hero-ctas`** — the §6 gate caught it, which is exactly what that gate is for. | Fresh load at 1440: bubbles and buttons share **both** edges — left 124/124, right 604 vs 603.3 — grid `234px 234px`, composition **180.4 above / 215.7 below** (45.5:54.5, D235's balance holds because the paddings are vh-led). 1121: 480px grid inside a 560px column, edges 56/536 against 56/535.3. Tablet 900 unchanged: 660px grid, 323px columns, 9px gap, 8px/10px pill. Phone 390 unchanged: 350px grid, 170.5px columns, hero padding still **113.94/50.64**. Internal heads now show the same four at every width (trade at 390: 350px, 170.5px columns, no overflow). Frozen desktop otherwise unmoved: `.gal-scroll` **4950**, `--revPer` **3**, `feTurbulence` **60**, doc height **14833**, `#svcNav` 8, zero broken images, no overflow, no console errors. Elements **2709 → 2687**, which is the icon row's 22 nodes exactly. CSS gate 0/0, JS parses, `verify.py` ✅ PASS 132/132/132 |
| D236 | 14 Aug | ⛔⛔⛔ **THE PROJECT GALLERY BROKE ON A RESIZE AND WAS PERFECT AFTER A REFRESH, AND THE CAUSE WAS ONE `classList.add` WITH NO MATCHING REMOVE.** Client: *"the animation in the view our project gallery, as you can see in the screenshot, is broken, but it is only broken when I change the responsiveness. So if I refresh it, then it still works perfectly. So there is some sort of bug somewhere that you just need to fix."* ⭐⭐ **HIS DIAGNOSIS WAS EXACT AND IT IS A ONE-WAY DOOR.** `measure()` added `.gal-static` when `--galMode` read `phone` or `grid`, and nothing ever took it off. **Every rule of the static layout is gated on that class at BASE scope** — D195 lifted them out of the phone query on purpose — so the class ALONE, at any width, takes `position:sticky` off `.gal-pin`, makes `.gal-stage` static and turns the eight absolutely-positioned cards into flex items. Narrowing past 1120 added it; widening back left it on while `--galMode` went unset and the accordion started drawing again. ⭐ **MEASURED AT 1200 AFTER A 900 → 1200 RESIZE: `gal-static` still on, pin `static`, stage `static`, cards `relative` and carrying `translate3d(-1311.6px, 355.8px)` at `opacity:0`** — the engine writing absolute geometry onto elements in normal flow, which is his screenshot exactly. A refresh hid it because a fresh load never adds the class at desktop. ⭐⭐ **THE FIX IS A `toggle` FROM THE SAME READ THAT DRIVES THE ENGINE, NOT AN `add` HERE AND A `remove` THERE** — the class and the `phone` flag are now one decision, so they cannot describe different states. The stylesheet's safety argument is unchanged: it is still written in the same breath as the early return, so a script that never reaches it leaves a working page. ⚠️ `sizeRunway()` already asked `galStatic()` fresh every time and was always correct in both directions; only the class was one-way. ⚠️ `/projects/` sets `--galMode:phone` at every width (D195), so the toggle keeps the class ON there — verified. | Verified with no reload between widths: **390 → 1440** leaves class `(none)`, pin `sticky`, stage and cards `absolute`, runway **4950**, doc **14833** — identical to a fresh load. **1440 → 700 → 1000 → 1440** the same, and the accordion draws its walls and captions correctly when scrolled into mid-run. **1440 → 900** mid-run drops cleanly to the two-up static grid in his order, Harlow before Harrow. `/projects/` at 1440 still `gal-static`, still two columns at x 185/766. CSS gate 0/0, JS parses |
| D235 | 14 Aug | ⭐⭐⭐ **THE CURVED BOTTOM EDGE IS ONE MECHANISM AT EVERY WIDTH NOW, THE DESKTOP HERO IS REBALANCED, AND THE NAV'S QUOTE BUTTON STOPPED SURVIVING IN A 290px GAP.** Four asks. ⭐⭐ **THE CURVES: LIFTED, NOT COPIED.** Client: *"the same curve style at the bottom of the tablet and mobile hero sections and at the bottom of the nav bar, I want you to add that to desktop as well. So when the nav bar eventually forms, it forms in the same way… same for the bottom gold line of the hero section on desktop."* Both curves were written inside band blocks and the desktop had **flat** edges: a straight gradient hairline on the bar and a plain `border-bottom` on the hero. ⛔ **THE OBVIOUS MOVE WAS TO PASTE THE SIX DECLARATIONS INTO A `min-width:1121px` BLOCK, AND THAT WOULD HAVE LEFT THREE LIVE DESCRIPTIONS OF ONE EDGE** — the D106/D113/D114 failure, in triplicate. **The mechanism moved to base scope and each band now declares only its two numbers**: nav 18/5 phone, 26/7 tablet, **34/9 desktop**; the hero is **48/14 everywhere**, which the phone and tablet already shared, so it needed no per-band value at all. ⛔ The straight gradient is deleted: a border takes one colour, so the 0.08→champagne ramp cannot survive, and the colour chosen is that ramp's own midpoint (D122's reasoning, now applying everywhere). ⭐ The formation is untouched and therefore identical across bands — `scaleX(0)` → `scaleX(1)` from the centre — which is what he means by "forms in the same way". ⭐⭐ **THE HERO COMPOSITION WAS MEASURED, NOT EYEBALLED.** Client: *"you can resize the hero section like you said because you left [space] for it."* At 1440×900 the slack between the fixed nav and the hero floor is 365px, and it was split **140 above / 225 below** — 38:62 — because D234's deleted arrow used to stand in that lower band. Now **164 / 201**, about 45:55. ⛔ Not a dead 50:50: a block centred by arithmetic reads as sitting low. ⚠️ The phone and tablet are untouched because `#hero .hero-inner` out-specifies the base `.hero-inner` — verified at 390, still 113.94/50.64. ⛔⛔ **AND THE QUOTE BUTTON LIVED ON IN A 290px GAP NOBODY HAD LOOKED AT.** Client, previewing widths: *"why is the get a free quote still in the hero section?"* — two gold buttons of the same name, the bar's sitting directly above the hero's. **D198 removed it for the TABLET on his instruction and a separate `max-width:430px` rule removed it on small phones, so it survived only between 431 and 720px** — exactly the width he was previewing. ⭐ **ONE RULE AT 1120px NOW, MATCHING THE BURGER'S OWN BREAKPOINT**: wherever the burger is, the bar is logo + burger. The desktop keeps the button — no burger there, and the bar has room. ⚠️⚠️ **THAT IS THE FOURTH TIME THIS ROUND AN INSTRUCTION REACHED SOME BANDS AND NOT OTHERS** (the eyebrow D197→D229, the scroll arrow D174→D234, and now this). **Check every band before calling one of these done.** ⭐ Also: **Harlow now comes before Harrow** — client: *"in our projects, the Harlow project must come before the Harrow project."* One letter apart, and they were the other way round. | Desktop 1440: nav `::after` R **34px**, 35px tall, out-hang −9px, 1px border, no gradient; hero `::after` R **48px**, 49px tall, `.hero-bg` inset −14px and clipped `inset(0 round 0 0 48px 48px)`, hero `border-bottom` **0**. Spacing **140/225 → 164/201**. Screenshotted at 1141: both curves visibly turning at the ends, photograph cut to match. Tablet 900: nav 26, hero 48, button hidden, grid's last row now **Harlow \| Harrow**. 668: button hidden, burger shown. Phone 390: nav 18, hero 48, padding unchanged at 113.94/50.64. No horizontal overflow at any of the four widths. Frozen desktop otherwise unmoved: `.gal-scroll` **4950**, `--revPer` **3**, `feTurbulence` **60**, doc height **14833**, elements **2709**, zero broken images. CSS gate 0/0, JS parses, `verify.py` ✅ PASS 132/132/132 |
| D234 | 14 Aug | ⛔⛔ **THE HERO SCROLL CUE IS DELETED OUTRIGHT, AND THE GRID OVERLAY IS "PROJECT GALLERY".** Client: *"on the hero section of the desktop version, remove the down arrow that says scroll above it. So remove that completely."* and *"when you say view as grid, the title is every project. It shouldn't say every project. Just say project gallery, and then one of the words being gold."* ⭐⭐ **HE ASKED FOR THE ARROW ONCE BEFORE AND ONLY TWO BANDS GOT IT.** D174 quotes him — *"remove the scroll arrow at the bottom of the hero section"* — and it shipped as `#hero .hero-cue{display:none}` inside the `max-width:1120px` block, so the phone and the tablet lost it and **the desktop kept it**. ⚠️⚠️ **THAT IS THE THIRD INSTRUCTION DELIVERED TO SOME BANDS AND NOT OTHERS** — the eyebrow was D197 → D229, and this is D174 → D234. **When he names an element, check which bands the fix actually reached before calling it done.** ⛔⛔ **HIDING IT ON THE DESKTOP WAS THE FIRST ATTEMPT AND IT WAS THE WRONG ONE.** With the other two bands already hiding it, a third `display:none` would have left an element rendering nowhere, described by six live rules — the exact shape that cost D106, D113 and D114 a rule each, and the same disease as D231's cards. **So it is gone: the `<div class="hero-cue">`, its positioning, its two entrance transforms, its reduced-motion override, the mobile block's three rules, and the tablet block's hide.** ⭐⭐ **AND DELETING IT ORPHANED FOUR MORE RULES, WHICH WENT TOO:** `.scrollcue` and its two children plus the `cue` keyframe, and `.ph .scrollcue`. **The hero cue was the only thing wearing that class** — a grep of every built page finds only `proj-scrollcue`, which is a different class with its own rules and its own bob on the project hero, and which still renders. ⚠️ **`.hero-inner`'s `padding-bottom` IS DELIBERATELY UNTOUCHED.** It was cut to fit around this arrow, so it now reserves space for nothing — but changing it would move a frozen desktop hero he has not asked to move. **That is a spacing question for him, not a tidy-up.** ⭐ "Every project" was also a promise the overlay could not keep: it shows the eight in `PROJECTS`, not every job Topcat have done. The gold word is the LAST one, the rule the hero, the section titles and the leaf headings all follow (D229). | Verified at 1440 before and after: hero **900px**, `.hero-inner` **694px**, chips bottom **675px**, document **14833px** — every one identical, because the cue was `position:absolute` and out of flow. `.gal-scroll` **4950**, `--revPer` **3**, `feTurbulence` **60**, no overflow, no broken images. Elements **2713 → 2709**, which is the five nodes of the cue out and the one `<em>` in the overlay title in, exactly. Zero live `hero-cue`/`Scroll` markup and zero `.scrollcue` rules left in the tree; the remaining matches are all comments. Grid overlay reads **"Project gallery"** with `gallery` in gold and opens on Wimbledon, Watford, Hornchurch, Rickmansworth. ⚠️ Two console SyntaxErrors seen mid-round were **the probe harness, not the page** — a clean load in a fresh tab logs nothing at all. CSS gate 0/0, JS parses, `verify.py` ✅ PASS 132/132/132 |
| D233 | 14 Aug | ⛔⛔ **"ONE TRADE OFF YOUR CRITICAL PATH" IS DEAD. THE SECTION IS "TRADE WITH TOPCAT".** Client: *"I also hate the title that says one trade off your critical path. What does that even mean? The trade page is for essentially other people to use Topcat to do the work for them, and also for bigger companies to buy their services. So there should be, like, something trade with Topcat, or something that just speaks to what this is for."* ⭐⭐ **HE IS RIGHT TWICE OVER AND BOTH FAULTS ARE WORTH NAMING.** *Critical path* is construction-programme jargon, and half the audience he just listed — kitchen designers, developers — do not talk that way. And *"One trade off"* parses as *"one trade-off"* on first sight, which says the opposite of what it meant. **A heading that has to be worked out is a heading that has failed.** ⭐ **HIS OWN PHRASE, AND IT ANSWERS THE H1 RATHER THAN REPEATING IT:** the page is titled "Trade", so the first section says trade WITH US, with the brand taking the gold `<em>`. ⛔⛔ **THE PHRASE WAS IN FOUR PLACES AND ALL FOUR ARE CHANGED** — the section title, the closing line (*"One less thing on your critical path"* → *"One less thing to chase"*), the Builders card (*"One trade off your critical path"* → *"One less trade to manage"*), and a fourth copy in `trade/build_trade.py`. ⛔⛔⛔ **AND THAT FOURTH COPY IS THE REAL FIND: `trade/build_trade.py` IS A LIVE BUILDER THAT STILL WRITES `/trade/index.html`, AND IT HAS BEEN SUPERSEDED BY `build_pages.py` SINCE D190.** Running it would silently replace the page with the 1 August version — **the rejected title back, D232's two new sections gone, D231's `<strong>`/`<span>` markup back with its CSS that never matched, and D230's enquiry card back on a section with no padding, sitting on the footer.** Its docstring even still declared *"fabrication is OUTSOURCED, never claim in-house"*, which §2 rule 1 reversed on 14 Aug. **It is now marked ⛔ DO NOT RUN at the top of the file, its copy brought into step as a seatbelt, and the stale fabrication line left in place as evidence rather than quietly corrected.** ⚠️ **LOOK FOR THIS PATTERN AGAIN:** a superseded builder that nothing imports and nothing points at is invisible until somebody runs it, and this project has four other builders. | Verified on the rebuilt page: title **"Trade with Topcat"** with `Topcat` in gold, closing line **"One team. One point of contact. One less thing to chase."**, the Builders card reading **"One less trade to manage."**, and **zero occurrences of "critical path" in any page's visible text** — the two remaining matches in the tree are both inside code comments quoting the client. CSS gate 0/0, JS parses, `verify.py` ✅ PASS 132/132/132 |
| D232 | 14 Aug | ⭐⭐ **THE TRADE PAGE, FINISHED: ONE CENTRED PARAGRAPH, THE GOLD RIM BACK, A READABLE HERO, AND THE TWO SECTIONS IT WAS MISSING.** Client, in two messages: *"why would you go and split it up into two paragraphs instead of just making one long paragraph. Merge them together again. Keep the width similar or slightly smaller than the actual blocks below it. And then add the golden rim around… make sure the page actually has everything that we need, and it looks better and functions better, has more sections to it maybe, and that the part that's on the actual hero section is easier to read because the text is hard to read with that background."* then *"the paragraph under one trade off your critical path, it has to be centered."* ⛔ **THIS REVERSES D230's TWO-COLUMN ARRANGEMENT WITHIN THE HOUR, AND THE REASON D230 EXISTED MUST SURVIVE THE REVERSAL:** the copy began as three paragraphs inside `.section-head`, which is `text-align:center` with a **52ch** measure, and that is what made the 547px ribbon he called terrible. **The fault was the ribbon, not the paragraph count and not the centring.** One centred paragraph at **1000px against the card grid's 1027px** is not that. ⭐⭐ **THE GOLD RIM WAS TAKEN AWAY BY D231, BY ACCIDENT, AND THE LESSON IS ABOUT COPYING COMPONENTS:** borrowing `.wy-r`'s skin wholesale brought its `--hair-soft` border with it — bone at 10% — and the trade cards had been on `--hair`, **gold at 34%**, since they were written. The review cards, the enquiry card and the FAQ plate all carry the gold rim; **the Why tiles are the exception, and copying a component copies its exceptions too.** ⭐⭐⭐ **THE HERO LEDE WAS GENUINELY UNREADABLE AND THE NUMBER PROVES IT, BECAUSE THE PHOTOGRAPH ITSELF WAS SAMPLED:** the head's background was drawn to a canvas at its own rendered `cover` scale and offset, and the brightest pixel in the band the lede occupies is **rgb(211,189,168)**, a lit worktop edge. Under the existing 0.62 veil that is an effective background of rgb(85,76,69), and `--muted` is bone at 0.55 alpha — **3.51:1**, under the 4.5 threshold. ⛔⛔ **DEEPENING THE VEIL IS THE OBVIOUS FIX AND D217 ALREADY TRIED AND REVERTED IT:** contrast bought that way "fixed the label by flattening the photograph, and on this site the photograph IS the product". **The words needed shade, not the picture.** The veil is untouched; the lede goes to 0.78 alpha for **5.28:1**, plus a `0 0`-led shadow so the rim sits on all four sides of the letterform (D220 — small text needs the rim more than the drop). ⚠️ The measured figure is shadow-free, because a text-shadow is local separation a contrast ratio cannot see. ⚠️ It is the shared internal head, so every page gains it. ⭐⭐ **AND TWO SECTIONS THE PAGE NEEDED: `#tradeScope` AND `#tradeFaq`.** A trade buyer could not find **what Topcat supplies** — the page linked to neither `/services/` nor `/stones/` anywhere — or **answers to the operational questions** a specifier asks before risking a client on a new supplier. ⛔⛔ **EVERY FAQ ANSWER IS ALREADY TRUE AND ALREADY CLAIMED ELSEWHERE ON THIS SITE. TRADE TERMS, PAYMENT AND MINIMUM ORDER ARE NOT AMONG THEM** — §2 rule 12, and the client has never supplied them — so "what are your trade terms" is answered by pointing at the form, which is what the CTA already promises. ⛔ **NO `FAQPage` SCHEMA** (dead since 7 May 2026, §10). ⚠️ The FAQ is `<details>`/`<summary>`, the idiom the 166 leaf pages already use, **NOT** the landing page's `#faq` — that is a grouped contents-page component with a JS answer panel built for twelve questions in four groups, and borrowing it would have been D231's mistake in reverse. ⚠️ The nine scope links are a **sixth copy of the service list**; see the D228 note in `build_seo_pages.py`. ⚠️ `build_pages.py` had no escaping helper and now has one — the older templates are deliberately not retro-fitted because they contain intentional entities. | Trade page at 1141: eight sections (was six), one centred paragraph 1000px against a 1027px card grid, card rims back to **rgba(198,166,100,0.34)**, hero lede **3.51 → 5.28:1** shadow-free against the brightest pixel of its own photograph, six FAQ rows opening and closing, nine scope links. Phone 390: no overflow, scope links wrap inside 365px, FAQ 350px, paragraph still centred. Frozen desktop landing page unmoved at a fresh 1440: `.gal-scroll` **4950**, `--revPer` **3**, `feTurbulence` **60**, doc height **14833**, elements **2713**, `.page-head` and `.trade-stone` counts both **0** so neither change can reach it, no broken images, no overflow. CSS gate 0/0, JS parses, `verify.py` ✅ PASS 132/132/132 |
| D231 | 14 Aug | ⛔⛔⛔ **THE TRADE CARDS WERE STYLED BY RULES THAT MATCHED NOTHING, FOR AS LONG AS THEY HAVE EXISTED.** Client: *"These blocks look absolutely terrible. We don't have this kind of design across the entire site. Why is the title, like, right up against the text and the text sizing… everything is wrong with this. Redesign it based on our design language in the home page. The trade page is so incredibly important. How could you even think about risking it to make it look like this?"* ⭐⭐⭐ **THE CSS STYLED `.trade-card strong` AND `.trade-card span`. `trade_cards()` EMITS `<h3>` AND `<p>`.** Six declarations — the serif face, the gold, the `margin-bottom:5px`, both font sizes, the muted body colour — **none of them ever applied.** What shipped was the browser's defaults with the site reset on top. ⚠️⚠️ **THIS IS INVISIBLE IN A CODE REVIEW AND OBVIOUS IN A MEASUREMENT**, and D207's rule is the one that catches it: **print the computed value before you change the declaration.** Measured before: heading and body **both `rgb(244,241,234)`** — the same colour, so no hierarchy at all — heading at **700 weight**, which nothing else on this site uses, **0px** between heading and body (his "right up against the text", exactly), no background, no shadow, a 6px radius, and `auto-fit minmax(178px,…)` producing **five 206px columns and an orphan**, at **21 characters a line**. ⭐⭐ **THE HOME PAGE'S OWN ANSWER TO A GRID OF REASON TILES IS `.wy-r`, SO THAT IS WHAT THIS IS NOW:** 14px radius, `--hair-soft` border, the same 158deg panel gradient, the same `0 24px 54px -30px` shadow, **the same black marble veiled back at 0.74→0.86** — the stone the client himself asked for on the Why tiles (*"the same black stone marble you have in the review cards, but faded so you can read the text properly"*) — and the same type roles, sans 400 bone over 300 muted. ⛔ Nothing gold across the top of a card (§2 rule 10); the stone is what stops it reading flat. ⭐ **COLUMN COUNT FOLLOWS CONTENT COUNT:** six cards read 3×2 and four read 4×1, both full rows, because `auto-fit` is what produced the orphan. ⚠️ The stone needs JS — `marbleSVG()` is procedural and every card needs its own seed — so a guarded IIFE fills `.trade-stone`. **It matches nothing on the landing page, whose `feTurbulence` count is part of the freeze probe; measured after, still 60.** ⭐⭐ **AND THE FOURTH HERO BUBBLE IS "FREE HOME VISIT" ON PHONE AND TABLET.** Client: *"where it says every detail included, change it to something else that would help convert more."* **The other three bubbles are all trust — a rating, a guarantee, an aftercare promise — and a fourth trust line gives a visitor nothing to ACT on.** This one names the next step and prices it at zero risk. ⛔ Not discount language (§2 rule 5): a free visit is a service already claimed on every leaf page and in step one of the process, not money off the stone. ⚠️ It is also six characters shorter than the label it replaces, which matters because `.chip-reason` is `white-space:nowrap` and D224 recorded an overlong label overflowing that grid rather than wrapping in it. ⚠️ **THE DESKTOP FACT ROW STILL SAYS "Every detail included" and should** — he named the tablet and the mobile, and the desktop's is a different element (D225). | Measured after at 1180: **three columns of 347px** (was five of 206), heading 15.6px/400 bone over body 13px/300 muted at 55% — real hierarchy — **12px between heading and body** (was 0), 14px radius, shadow on, stone filled on all six, **47 characters a line** (was 21). "Who we work with" reads 4×255px. Phone 390: one column, 350px, 11px heading gap, 48 characters, no overflow. Hero bubbles at 390: four chips in an even 2×2, all 171px, nothing clipped, no overflow. Frozen desktop landing page unmoved at a fresh 1440: `.gal-scroll` **4950**, `--revPer` **3**, `feTurbulence` **60**, doc height **14833**, elements **2713**, `.trade-stone` count **0**, desktop fact row unchanged, no broken images, no overflow. CSS gate 0/0, JS parses, `verify.py` ✅ PASS 132/132/132 |
| D230 | 14 Aug | ⛔⛔ **THE TRADE PAGE'S CENTRED RIBBON, THE LAST SECTION EYEBROWS, THE FORM SITTING ON THE FOOTER, AND THE PROJECT GRID IN HIS OWN ORDER.** Client: *"Why is this so terrible? Why is it all the text like that? Why don't you just spread it across so it matches up with the rest of the thing and just the title maybe stays like that? And then why do the titles all have eyebrows? Don't have eyebrows. I don't understand what your obsession is with that. And then also the form on the bottom is touching the footer, which it shouldn't."* ⭐⭐ **THE RIBBON WAS A COMPONENT USED FOR SOMETHING IT WAS NEVER SHAPED FOR.** `.section-head` is `text-align:center` and `.section-sub` is `max-width:52ch` — that pair is built for a title and ONE short line, which is all the landing page ever asks of it. The trade intro poured **three full paragraphs** into it and got a 547px centred ribbon down the middle of a 1200px page. **The prose is now a two-column grid at `.section-head`'s own 1320px width, left-aligned, with the closing line spanning both columns; the title still centres, which is what he asked for.** ⛔ `max-width:none` on the paragraphs is required, not tidying — `.section-sub`'s 52ch would otherwise hold each COLUMN to a ribbon one level down. ⚠️ One column below 860px: two columns of body copy on a phone is 22 characters a line. ⭐⭐ **AND THE HONEST ANSWER TO "CHECK THE OTHER PAGES" IS THAT THE TRADE PAGE WAS THE ONLY ONE.** Every `.section-head` on all nine built pages was scanned for more than one paragraph or a single one over 340 characters: the only other hit is the services intro at 553 characters, which is **left-aligned in a 432px column by `#services .svc-intro`** and is not the fault. ⛔⛔ **THE FORM WAS NOT "TOUCHING" THE FOOTER, IT WAS FLUSH WITH IT AT EXACTLY 0px, AND THE CAUSE IS A COPIED COMPONENT UNDER A DIFFERENT ID:** `#cta{padding:clamp(18px,4.2vh,64px) …}` holds the landing page's card off the floor, and the trade page wraps the same card in `<section id="tradeCta">`, which matched none of it and computed `padding:0px`. ⭐ `#tradeCta` was ADDED TO THE EXISTING SELECTOR rather than given a second rule (the D106/D113/D114 lesson). Now 38px. ⚠️⚠️ **THE FIRST MEASUREMENT SAID −34px AND WAS A LIE:** `.cta-card` carries `.rise`, so a `getBoundingClientRect()` mid-reveal includes the animation's `translateY`. **`offsetTop` ignores transforms** — D221's trap, hit again. ⭐ **THE LAST TWO SECTION EYEBROWS ARE GONE** (trade ×2, stones hub, compare page). ⚠️ **TWO `.eyebrow` ELEMENTS SURVIVE ON PURPOSE AND HE MAY STILL WANT THEM GONE:** the project overlay's is `#projPlace`, which holds the PLACE ("Wimbledon, London") and is the element D221 tuned the arrow's spacing against; the marquee's *"Brands we work with"* is that strip's ONLY label, and removing it leaves a row of logos explaining nothing. **Flagged, not removed.** | ⭐⭐ **AND THE GRID HE DESCRIBED DID NOT EXIST YET.** *"The project I want in the first top corner should be the Wimbledon project. Then below that, the Hornchurch project. Then after that, on the opposite side across the Wimbledon project, needs to be the Watford project. Then below that goes the Rickmansworth project."* **"The opposite side" needs two columns and `/projects/` had ONE at every width** — measured at 1440: a single 460px column at x=490 with 490px of empty page either side, which is the same fault he had just called terrible. D195 set `--galMode:phone` there to stop the accordion, and that also handed the page the PHONE's single-column arrangement. The two-up rules existed but were **tablet-only**; they are lifted to `min-width:721px` beside the other `.gal-static` rules, with a card-size step at 1121px so two cards fill the page instead of leaving the same margins with a gap down the middle. ⛔⛔ **GATED ON `.gal-static`, WHICH IS THE SAFETY ARGUMENT: the desktop landing page never carries that class** — the accordion is running, proven by the unchanged 4950px `.gal-scroll` — **so this cannot reach the frozen desktop gallery.** ⛔⛔ **A ROW-MAJOR GRID FILLS ACROSS BEFORE IT FILLS DOWN, SO THE ARRAY IS NOT THE ORDER HE SPOKE:** taking his sentence literally would have put Hornchurch top-right, the one place he said it must not go. The array interleaves the columns — **wimbledon, watford, hornchurch, rickmansworth**, then the four he did not name in their existing order. ⚠️ This array is also what the landing accordion deals, so Wimbledon leads there too; nothing reads it by index (D214). | Measured at 1440 and 1200: the grid reads **Wimbledon \| Watford / Hornchurch \| Rickmansworth / Ruislip \| Central London / Harrow \| Harlow**, 490px cards on an 1180px stage; tablet 900 keeps two-up at 378px with the same order. Trade prose: two columns at 1200, one at 390, no overflow at either; card-to-footer **0px → 38px** at 1200 and 36px at 390, and `/services/` measures the same 38px. Zero `.eyebrow` left on the trade and stones pages. Frozen desktop landing page unmoved at a fresh 1440: `.gal-scroll` **4950**, `--revPer` **3**, `feTurbulence` **60**, doc height **14833**, elements **2713**, `.gal-static` **false**, no broken images, no overflow. CSS gate 0/0, JS parses, `verify.py` ✅ PASS 132/132/132 |
| D229 | 14 Aug | ⭐⭐ **THE LEAF PAGE HEROES: NO EYEBROW, THE CARD'S OWN NAME AS THE TITLE, THE LAST WORD IN GOLD, AND THE TRAIL ON THE PHOTOGRAPH.** Client, four asks in one message: *"remove the eyebrow from each of the services sections in the pages, and then just keep the title, the name of the section. So the first word is gonna be white, and then second word is gonna be gold. And if it's just one word, it's gonna be a white word. But also, there's no need to have a black bar, like in the screenshot for the directory you have on the top. It can just be on screen as long as it's easy to read. And then if you look at vanity tops or outdoor spaces, it just needs to say outdoor space. It doesn't need to say outdoor kitchen worktops or anything like that. Just the actual name that was on the card before they clicked on it."* ⛔⛔ **THE EYEBROW WAS ASKED FOR ONCE BEFORE AND ONLY THE PHONE GOT IT.** D197 quotes him saying *"you can also remove eyebrows from the title, so it's just title and subtitle for all the internal pages"*, and it shipped as `@media(max-width:720px){.svc-hero .eyebrow{display:none}}`. **"All the internal pages" was the instruction and one breakpoint was the delivery** — worth remembering, because the register row read as done. ⭐⭐ **THE H1 IS THE HELIX CARD'S NAME NOW, AND THE `eyebrow` FIELD ALREADY HELD EXACTLY THAT STRING** for all nine services, so the change was to promote it and delete the old SEO-shaped `h1`: *Outdoor Kitchen Worktops* → **Outdoor Spaces**, *Stone Vanity Tops* → **Vanity Tops**, *Bathroom Worktops & Shower Surrounds* → **Bathrooms**, *Commercial Stone Surfaces* → **Commercial**, and so on. ⛔ **THE `<title>` AND `metadesc` ARE UNCHANGED AND THAT IS DELIBERATE** — "outdoor kitchen worktops" is the phrase people search, D206 kept the slug and meta for exactly that reason, and only the on-page heading moves. ⭐ It also settles §2 rule 13 harder than the eyebrow did: the tile and the page it opens now call the thing ONE name. ⭐⭐ **THE "BLACK BAR" WAS NOT A BAR.** `.crumb` was a SIBLING of `.svc-hero`, so it occupied a band of page background and the photograph began underneath it — a black strip by arithmetic, not by decoration. **The trail is a child of the hero now**, moved in the markup in both builders rather than dragged up with a negative margin, so there is one description of where it lives. ⚠️ `position:relative`, not `absolute`: it keeps its height in the flow so the title cannot slide under it at a width nobody measured. ⚠️⚠️ **AND THE HERO'S TOP PADDING HAD TO COME DOWN ONLY ABOVE 720px.** That padding used to sit above the TITLE; with the trail inside, it sat above the TRAIL and pushed the title 40px down the photograph — but the trail is `display:none` on the phone (D197), where that padding is the only thing holding the title off the header, **which is the exact trap D197's own comment warned about**. ⭐ **READABILITY WAS MEASURED, BECAUSE HE MADE IT THE CONDITION** — and the first measurement was WRONG and said 2.36:1: `.crumb` is a full-width flex box, so sampling the veil at the BOX's right edge samples where no text is. **Measured at each item's own rect against a hypothetical PURE WHITE photograph — the strongest case, not the average one: Home 6.2:1, Services 6.0:1, the current page in gold 6.3:1.** ⚠️ The hub, guide, sitemap and index pages were left alone and are not an inconsistency: **they have no hero photograph at all**, so their trail has nothing to sit on. | All nine service pages plus the five material and eight area pages: H1 is the card name, last word gold (`Spaces`, `Tops`, `Worktops`, `Islands`, `Tables`) and one-word titles white (`Splashbacks`, `Bathrooms`, `Fireplaces`, `Commercial`); zero eyebrows in any `.svc-hero`; exactly one `<nav class="crumb">` per page across **169 pages checked**. ⭐ **All 134 stone pages keep their own `.stp-copy` eyebrow** — that selector was never reached. Phone 390: no trail, title clear of the header. Tablet 900 and desktop 1183: trail on the photograph, 18px to the title, measured gap was 0px before the fix. ⛔ Landing page not touched at all this round. CSS gate 0/0, `verify.py` ✅ PASS 132/132/132 |
| D228 | 14 Aug | ⭐⭐⭐ **EVERY SERVICE IN THE RANGE HAS ITS OWN PAGE NOW, AND VANITY TOPS NO LONGER OPENS THE BATHROOM PAGE.** Client: *"each one of these options needs to have its own dedicated page. So when you go to view the service, you should be able to view the service. So for those that don't have it, like fireplaces and dining tables, make sure that you write a dedicated page. That sounds like it was written by a human. No AI slop. And also says what it needs to say. Because when I go into vanity tops, it takes me to bathroom worktops and vanity tops. That's not right. Bathroom should take me to bathroom, and vanity tops should take me to vanity tops."* **Three new pages: `/services/fireplaces.html`, `/services/dining-tables.html`, `/services/vanity-tops.html`.** Nine leaf pages from six. ⭐⭐ **THE VANITY FIX IS A SPLIT, NOT A REDIRECT.** The bathroom page WAS "Bathroom Worktops & Vanity Tops", and the old reasoning — recorded in the `SERVICES` comment — was that a second leaf page would compete with it for the same words. **That is a real concern and it was answered by dividing the content instead of by sharing a page:** `bathroom-worktops` KEEPS ITS SLUG, ITS URL AND ITS RANKING and now covers shower surrounds, thresholds, sills and bath panels; the basins, tap holes and undermount detail moved to the new page. The two no longer say the same sentence. ⛔ **Do not re-point the Vanity Tops tile at the bathroom page.** ⭐⭐ **THE FIREPLACE PAGE'S SCOPE PARAGRAPH IS THE POINT OF IT, NOT A HEDGE.** §2 rule 12 forbids stating what we cannot guarantee, and a fireplace carries building-regulation duties around hearth sizes and clearances that nobody has said Topcat take on. The page says plainly that they cut and fit the stone, do not install stoves, flues or gas appliances, and cut to the figures the customer's own installer gives. ⚠️ **AND ONE FAQ IS LOAD-BEARING AND TRUE: engineered quartz is resin-bound, so it is kept away from direct heat and suits a surround or a mantel rather than the hearth under an appliance.** Recommending quartz for a hearth would have been wrong. **Do not "simplify" that answer.** ⭐ The dining-table page carries the same honesty about the base: Topcat make the top, not the frame, and a joint in a table is always visible because there is nothing for it to hide behind — §2 rule 12 in the client's own house position. ⚠️⚠️ **THE PHOTOGRAPHY WAS CHECKED, NOT ASSUMED:** all 91 gallery files were tiled onto one contact sheet and **his portfolio holds no fireplace and no dining table**, so all three new pages take the slab shot. ⛔ A near-enough room photograph is what put a QUARRY on Outdoor Spaces and a KITCHEN on Commercial and he has asked for both to be replaced — **the wrong subject reads as wrong, the material itself cannot.** ⛔⛔ **A SERVICE PAGE LIVES IN FOUR PLACES AND `build_seo_pages.py` HELD A SILENT FIFTH:** its `SERVICE_PAGES` list was a hardcoded copy justified by a comment reading *"six entries that have not changed in weeks is the cheaper trade"* — **that trade came due here**, and nothing would have flagged it. The list, `APPLICATIONS` (on all ~166 leaf pages, where "Fireplaces, hearths and tables" pointed at `/contact/` for want of a page), the `SERVICES` array and BOTH nav menus all had to move together. ⭐ **AND THE TWO LIVE AI-TELLS D213 FLAGGED AND LEFT ARE GONE, BECAUSE "NO AI SLOP" IS THE RULING THEY WERE WAITING ON:** the reviews CTA *"Let's bring your vision to life"* is now *"Tell us about your kitchen"*, and the Why subtitle's *"and ours is second to none"* is *"and that is the part we are judged on"*. ⚠️ The services hub lead said *"Six ways we work in stone"* and had been wrong since D206 made the range eight. | All nine pages 200; the eight helix cards each carry a "View this service" button to their own page, verified from the DOM; both nav menus and the hub list all nine; the bathroom page now mentions a basin once, to say basin tops have their own page, and links to it. Frozen desktop unmoved at a fresh 1440: `.gal-scroll` **4950**, `--revPer` **3**, `feTurbulence` **60**, doc height **14833**, zero broken images, no overflow. Elements 2705 → **2713**, which is the six new nav links and the two new buttons exactly. Copy scans clean on the new pages: no "laser", no centimetres, no exclamation marks, no em dashes, no "TopCat", no showroom. CSS gate 0/0, JS parses, `verify.py` ✅ PASS 132/132/132 |
| D227 | 14 Aug | ⛔⛔⛔ **D226 FIXED HALF THE HELIX AND THE CLIENT REJECTED IT ON SIGHT. THE SPIRAL'S OWN ARC STEP WAS `360/N` TOO, AND IT IS A FIXED 60° NOW.** Client, with two screenshots — the current build and the six-service build: *"I asked you to fix the fucking helix, and you got it wrong. Look at screenshot one. That is what you did recently, and it's fucking wrong. Then look at screenshot two. That is what it's supposed to look like."* ⭐⭐ **HE WAS RIGHT, AND D226'S OWN WRITTEN REASSURANCE IS WHAT CAUSED IT.** That row says *"the SPIRAL still uses `360/N` for x/z/y and should — that part genuinely does absorb any count"*, so the yaw was corrected and `th=d*2π/N` was left alone. **The poles came back turned and still landed in the wrong place.** ⭐⭐ **ONLY FIVE SLOTS ARE EVER VISIBLE — the front card and two steps either side — so `360/N` does not spread the same picture over more cards, it RESHAPES those five.** Measured at 1200, R 218.2 / STEP 165: the ±2 poles sat at **x 218.2, z −218.2, scale 0.840, brightness 0.750** against the six-service **x 189.0, z −327.3, scale 0.760, brightness 0.625** — **15% further out sideways, a third as deep, 11% larger and 20% brighter.** Big flat slabs hung off the corners instead of small, dim cards turned away behind the stack, which is exactly what the two screenshots differ by. ⭐⭐ **AND THE SILHOUETTE IS THE PART THAT READS AT A GLANCE: at 60° the ±1 and ±2 cards share ONE sideways offset (`sin60 == sin120`)**, so the five cards stack into a single overlapping column; at 45°/90° they splay outward and the column comes apart. ⚠️ The phone branch's own `R` was tuned on that symmetry and **says so in its comment** — which had been quietly false since D206. ⛔ Fixed as `ANG_STEP=60` with `YAW_STEP=ANG_STEP*0.9` (=54), so the wind is 90% of the arc, which is where the original `(d*360/N)*0.9` got its 54° at six services. **Both angles are fixed gestures now and a ninth service cannot move either.** ⚠️⚠️ **THE LESSON IS D226'S OWN, APPLIED ONE LEVEL DEEPER: A REASSURING COMMENT IS NOT A PROOF, AND THAT INCLUDES THE COMMENT WRITTEN BY THE LAST FIX.** D206 wrote the first false reassurance, D226 repeated it about the half it had not tested, and neither was visible on a read-through. ⭐ **The helix is DESKTOP-ONLY** — the stage measures 0×0 at 390 and 900, so this touched neither frozen band. ⚠️ `phoneHx` is dead code: `--hxMode` is no longer set to `phone` anywhere (see the note at the `#services .svc-helix{display:none}` rule). | Measured at 1200 and 1440 from `el.style.transform`, the authored string: five cards visible, **two at ±108° showing their gold-framed backs**, ±1 and ±2 sharing one x (188.9 at 1200, 229.7 at 1440), scales **1 / 0.92 / 0.76** and brightness **1 / 0.875 / 0.625** — the six-service values exactly. A spin advances one service and settles back to five with the poles at ±108°. Frozen desktop unmoved at a fresh 1440: `.gal-scroll` **4950**, `--revPer` **3**, `feTurbulence` **60**, doc height **14833**, elements **2705**, `#svcNav` 8, zero broken images, no overflow, no console errors. CSS gate 0/0, JS parses, `verify.py` ✅ PASS 132/132/132 |
| D226 | 14 Aug | ⛔⛔⛔ **THE SERVICES HELIX LOST ITS BACK-TURNED POLES WHEN THE RANGE GREW TO EIGHT, AND NOBODY NOTICED FOR TWO ROUNDS.** Client: *"the very top and the very bottom cards that are supposed to be turned kind of backwards for the helix is now broken."* ⭐⭐ **THE WIND ANGLE WAS A SHARE OF THE CIRCLE WHEN IT SHOULD HAVE BEEN A FIXED GESTURE.** The line read `yaw=(d*360/N)*0.9`. At **six** services that is **54° per step**, so two steps out reached **108°** — past 90°, which is the whole reason the top and bottom cards show their gold-framed BACKS (the cards are genuinely two-sided, `.hx-face`). At **eight** services the same expression gives **40.5° per step**, so two steps out reaches **81° and never crosses 90°**: every card stayed face-forward and the poles simply looked like flat, half-cropped photographs. **Measured before: max |yaw| 81° across all eight. After: 0, ±54, ±108, ±162, 216, with two cards at 108° — the poles — exactly as at six services.** ⛔ Fixed as a CONSTANT `YAW_STEP=54`, so it is N-independent and a ninth service cannot repeat it. ⚠️ The SPIRAL still uses `360/N` for x/z/y and should — that part genuinely does absorb any count. ⚠️⚠️ **THE TRAP IS THE PART TO REMEMBER, AND D206 WROTE THE FALSE REASSURANCE ITSELF:** its comment says the geometry *"is written from `items.length`, so it absorbed six → seven → eight with no tuning"*. That is true of the positions and false of the wind, and the difference is invisible in code review — **an expression that merely CONTAINS `N` is not automatically correct for every `N`.** ⭐ Nothing in this round caused it; it shipped with D206 and survived D214–D225 because the helix is below the fold and no proof covered the yaw. ⭐ **A YAW READ BACK FROM `getComputedStyle` CANNOT SEE IT EITHER**, which is worth knowing before the next attempt: recovering the angle from the matrix folds everything into ±90°, so 108° reads as 72°. **Read `el.style.transform` — the authored string — not the computed matrix.** | Authored yaws at 1141 and 1440: `0, 54, 108, 162, 216, -162, -108, -54`; two cards at ±108° show their reverse, five past 90°, the |d|>2 pair still fades to nothing. Screenshotted at 1141: both poles show the gold-framed Topcat back. Frozen desktop unmoved — `.gal-scroll` **4950**, `feTurbulence` **60**, doc height **14833**, `#svcNav` 8, zero broken images, no overflow |
| D225 | 14 Aug | ⛔⛔⛔ **THE RATING READS `5.0 ★★★★★` — SCORE ON THE LEFT, STARS ON THE RIGHT — AND THIS REVERSES D176.** Client: *"I have told you so many times that the five point o goes underneath the g of the Google, and then the stars next to the five point o. So the five point o is on the left and the stars on the right."* ⚠️⚠️ **HE HAD TO SAY IT MORE THAN ONCE, AND THE REASON IS A MISTAKE WORTH NOT REPEATING:** the previous round read his description of the chip as a description rather than an instruction, and deferred to D176 — which recorded the OPPOSITE order from his own earlier ask — on the grounds that "a loose description of an existing thing is not an instruction to reverse it". ⭐⭐ **WHEN THE CLIENT DESCRIBES A THING TWICE AND THE PAGE DISAGREES WITH HIS DESCRIPTION BOTH TIMES, THE PAGE IS WRONG, NOT THE DESCRIPTION.** A register row is a record of what he asked for once; it does not outrank what he is asking for now. ⛔ **D176 IS SUPERSEDED. Do not restore `★★★★★ 5.0` from it.** ⭐ The score's left edge measures **0px from the left edge of "Google reviews"**, so it sits directly under the G as he described. ⭐⭐ **AND THE DESKTOP TAKES THE WHOLE CHIP NOW.** Client: *"for the desktop version, I want you to have the Google reviews part look the same as it does on mobile and tablet."* The desktop had been showing `.chip-legacy` — the plain text "★★★★★ 5.0 on Google" — while the phone and tablet showed the mark-and-stack: **one component with two appearances, which is the rule 13 confusion in another costume.** The eight Google-chip rules were **MOVED out of `@media(max-width:1120px)` into base scope, not copied**, and `.g-mark`/`.g-stack` came out of the base `display:none` list. ⚠️ `.chip-legacy` is still CLIPPED rather than `display:none` at every width — it is what a screen reader reads, because a logo and five star glyphs do not say "rated 5.0 on Google" on their own. ⚠️ **`.chip-reason` was deliberately NOT part of that move**: the desktop shows the three reasons as its icon fact row, and he excluded the desktop explicitly. ⛔ **AND "YOUR PROJECT EXPERTLY HANDLED" CAME BACK OUT OF THE BUBBLES MINUTES AFTER GOING IN** — *"remove the your project expertly handled and replace that with every detail included on the mobile version and the tablet version. Desktop, don't worry about that."* **That returns the row to four bubbles, the even 2×2 D186 tuned it for**, so D224's odd-child span goes quiet on its own without being removed. The desktop fact row still carries all three reasons. | Verified at 390, 900 and 1200: rating order `5.0` then `★★★★★` on all three, score 0px under the word; the G mark and stack now render on the desktop too, with the Google and guarantee chips at an equal 48px; four bubbles in two rows on phone and tablet; nothing clipped, no overflow; CSS gate 0/0, `verify.py` ✅ PASS, all 8 page families 200 |
| D224 | 14 Aug | ⭐⭐ **THE HERO'S THREE REASONS, RE-WORDED, RE-ALIGNED, AND CARRIED INTO THE BUBBLES.** Four asks in one message. ⭐ **"72 hours of aftercare", not "Aftercare in 72 hours"** — his wording, the number leads. ⭐ **"Google reviews", not "Google"**, on the phone/tablet chip. ⚠️ **THE LINE BELOW IT WAS NOT TOUCHED**: he described it as "five point o and then the five stars", but the live order is ★★★★★ then 5.0 and that IS D176, his own earlier instruction — a loose description of an existing thing is not an instruction to reverse it. ⭐⭐ **THE THREE REASONS ARE BUBBLES ON PHONE AND TABLET NOW**, in his order: Google reviews · 10 year guarantee · 72-hour aftercare · Your project expertly handled · Every detail included. ⛔ **"QUICK TURNAROUND TIME" IS GONE** — his word was "instead of". It was D172's deliberately vague stand-in. ⭐⭐ **NOTHING HAD TO BE MOVED TO REACH THE TABLET, AND THAT IS WORTH KNOWING BEFORE THE NEXT SIMILAR ASK:** the hero's whole chip block was ALREADY widened to `@media(max-width:1120px)` in the tablet round, so the two new bubbles landed on the tablet by themselves. ⚠️ Its inner comment still said "Phone only; the hero above 720px is frozen" — **a stale comment inside a widened block, corrected in the same pass**, because that sentence is what the next session would have believed. ⚠️⚠️ **TWO THINGS BROKE UNDER FIVE BUBBLES AND BOTH WERE DESIGNED FOR FOUR:** the 2×2 grid left the fifth alone with a hole beside it (D205's fault exactly, and it takes D205's fix — `:last-child:nth-child(odd)` spans it), and `.chip-reason`'s `white-space:nowrap` **cannot hold "Your project expertly handled"**, which is eight characters longer than the longest label the grid was tuned for — `nowrap` does not wrap an overlong label, it overflows it. ⭐⭐ **AND THE ALIGNMENT COMPLAINT WAS REAL AND WAS NOT SPACING.** Client: *"make sure that the word aftercare is perfectly lined up with the other two, the top white part."* The three facts are one 48px row each, but their CONTENT is different heights — one has a two-line heading, one a two-line sub, and "72 hours of aftercare" is one line of each and therefore the shortest. **`align-items:center` centres the middles of three things that are not the same height, so its heading measured 8px below the other two.** The headings are what the eye reads across, so the tops are what must agree: `flex-start`. ⛔ Not a margin on one item — the wrap points move with the viewport and the offset would be wrong at every other width. | Measured after: heading tops **465/466/473 → 491/491/491, spread 0** at 1440. Phone and tablet both show 5 bubbles in 3 rows, the fifth spanning both columns, nothing clipped, no overflow. Desktop unchanged — reason bubbles still `display:none`, the Google chip still reads "★★★★★ 5.0 on Google" |
| D223 | 14 Aug | ⭐⭐ **THE PROJECT NAMES ARE ON THE CARDS THROUGHOUT THE DEAL, NOT ONLY ONCE THEY LAND.** Client: *"make sure when the stack comes in, all the names are already on the cards during the animation, not only once they settle into their respective spaces."* `.gal-meta` was `opacity:0` until `.gal-set.settled`. ⛔ **THE GATE IS DELETED, NOT OVERRIDDEN.** Its comment read *"labels and hover only on the set that's parked at the front — mid-move they're just noise"*, which was a taste call and is now his call. ⭐ **STACKED CAPTIONS DO NOT PILE UP, WHICH IS WHY THIS IS SAFE:** in the pile the cards are opaque and overlap, so a rear card's caption is BEHIND the card in front of it — only the front card's name is ever legible, which is the same arrangement the phone branch already makes by hand. ⚠️ The hover rules stay gated on `.settled`: a hover effect on a card in mid-flight IS noise, and he did not ask for it. | 8 of 8 captions at full opacity at all 13 sampled scroll positions through the pinned run |
| D222 | 14 Aug | ⛔⛔⛔ **THE DESKTOP ACCORDION COULD LOAD WITH THE WRONG GEOMETRY AND STAY WRONG UNTIL A REFRESH.** Client, with a screenshot of cards scattered and stuck in mid-air: *"the desktop version animation is sometimes now broken. I have to refresh the page for it to work correctly."* ⭐⭐ **`measure()` READS `stage.clientWidth/clientHeight` AND ONLY EVER RE-RAN ON A WINDOW `resize`.** Every number the scene draws from — the card size, the slot positions, the stack origin — is derived there, once, at boot. **So anything that changed the stage's box WITHOUT a window resize left the whole accordion dealing to slots that no longer matched the screen, and nothing re-measured, because nothing resized.** A late web font, a late image, a scrollbar, a restored scroll position on a back-navigation, or simply a boot that won a race against layout. Refreshing worked because boot then won the race. ⭐⭐ **MEASURED, NOT INFERRED, AND THE NUMBERS ARE THE PROOF:** at one fixed scroll position the cards were **1253px wide before a synthetic resize event and 1771px after it**, with zero settled sets before and one after — a 40% error in the card size alone. ⭐ **A `ResizeObserver` ON THE STAGE CLOSES THE WHOLE CLASS** rather than enumerating the causes: it fires on the element's real box, whatever moved it. Plus `window.load` and `document.fonts.ready` for the two one-shot cases that can move the runway under a still viewport. ⚠️ Guarded on a changed integer box so it cannot feed back. ⚠️⚠️ **AND ONE FALSE NEGATIVE ON THE WAY IS WORTH RECORDING:** the first attempt to verify the fix reported the observer never firing. It was firing — the probe was testing against a page loaded BEFORE the rebuild. **A negative result from an unverified build is not a result**; it was re-proven with a counter in the callback and then re-proven again with the counter removed. | `--cw` now tracks the stage exactly — **413.2px → 285.6px → 413.2px** as the box shrinks and returns, identical before and after a forced resize (it was not before). 19 of 41 scroll positions settle, against the 21 of 41 recorded in §6. Fresh load at 1440: `.gal-scroll` **4950**, doc height **14833**, `feTurbulence` 60, zero broken images. A 1440 → 390 → 1440 round trip recovers to 4950 exactly |
| D221 | 14 Aug | ⭐ **THE ARROW SITS UNDER THE TITLE, AT THE EYEBROW'S OWN SPACING, WITH THE SAME SHADOW — REVERSING THE "PIN IT TO THE FOOT" CALL IN D220, ONE MESSAGE LATER.** Client: *"that arrow at the bottom should be right below the title with the same spacing as the eyebrow part, and it should also have the shadow."* D220 had pinned it to the bottom of the hero on the reasoning that a scroll cue belongs at the foot; he wants the three lines as one centred group. ⭐ **THE GAP IS ONE TOKEN, `--heroGap`, NOT TWO MATCHING NUMBERS** — the eyebrow's `margin-bottom` and the arrow's `margin-top` are the same variable, so the group cannot drift when it changes. ⭐ A `drop-shadow` FILTER, not `text-shadow`: the arrow is an SVG stroke and `text-shadow` does not apply to it. ⚠️ **A MEASUREMENT TRAP WORTH KEEPING:** the rect gap read 23px against the eyebrow's 18px and looked wrong — that is the bob animation's `translateY` caught mid-cycle, not a layout error. `offsetTop` ignores transforms and reads 18/18. **Measure layout with layout properties, never with a rect on an animated element.** | Layout gap above the title 18px, below 18px, equal; arrow centred to 0px; copy still 0px from both centre lines |
| D220 | 14 Aug | ⛔⛔ **"IN THE MIDDLE" MEANT THE MIDDLE OF THE SCREEN, AND THE SHADE BEHIND THE TEXT IS REVERSED OUT — SAME DAY, ONE MESSAGE LATER (REVERSES HALF OF D217).** Client, with a screenshot: *"when I said text in the middle, I meant, like, in the middle of the screen, and then just add a shadow behind the text so that it stands out, and don't add this kind of black bar or whatever to the bottom. Just straight up in the center of the screen. That's what I meant. For all projects."* ⭐⭐ **THE FIRST PASS CENTRED ONE AXIS AND ASSUMED THE OTHER.** "Center" was read as `text-align:center` on a block that was still `align-items:flex-end`, so the words were centred horizontally and still sitting on the floor of the hero. **The hero is `align-items:center` now and the copy measures 0px from both centre lines on all eight projects.** ⭐⭐ **AND THE "BLACK BAR" HE POINTED AT WAS A REAL RENDERING FAULT IN MY OWN FIX, NOT A TASTE NOTE — THIS IS THE PART TO KEEP.** D217's shade was `radial-gradient(66% 78% …)` on a pseudo-element: **a radial gradient is clipped by its own box, and that ellipse was TALLER than the box it sat in**, so it was cut off dead flat at the top and bottom edges. A soft pool everywhere except two perfectly straight horizontal lines — which is exactly what a black bar looks like. ⭐ **A GRADIENT THAT REACHES ITS BOX EDGE STILL CARRYING COLOUR DRAWS AN EDGE THERE. Feather to zero INSIDE the box or do not use one.** ⛔ **DELETED, NOT OVERRIDDEN** (the D106/D113/D114 rule). ⭐ **THE VEIL WENT FLAT TOO.** It was bottom-weighted (0.96 at 3%) because the copy used to sit on the floor and needed a footing; with the copy in the middle, a dark band at the bottom holds nothing up and is just a bar. It is an even `rgba(8,8,10,0.34)` wash now — no edge, no direction, and the photograph keeps its shape top to bottom. ⭐ **THE WORDS ARE CARRIED BY A SHADOW, WHICH IS WHAT HE ASKED FOR**, and the first attempt at that was still not right: an offset-only shadow sits BELOW a letter and leaves its top edge naked against a pale photograph. Both now lead with a `0 0` CENTRED blur that rims the letterform on all four sides — **small text needs the rim more than the drop, display type needs the drop more than the rim.** ⚠️ **THE ARROW DID NOT FOLLOW THE COPY TO THE MIDDLE.** It is a child of `.proj-hero` now, pinned ~44px off the foot: a "scroll down" cue floating directly under the title reads as part of the title. ⚠️ Centred with `left:0;right:0`, never `translateX(-50%)`, because the bob animates `transform` and the two would fight. ⚠️⚠️ **THE MEASURED CONTRAST FROM D217 NO LONGER APPLIES AND NOTHING PRETENDS OTHERWISE:** a text-shadow is local separation that a contrast-ratio formula cannot see, so this was judged BY EYE against the brightest photograph of the 97 (`hornchurch-g1`, a near-white wall filling the frame) rather than by a number. **He asked for a shadow and no panel; that is what is built.** | All eight projects at 375, 768 and 1440: copy **0px from both centre lines**, arrow 44–51px off the foot with no overlap, veil flat with no gradient, `::before` gone. Frozen desktop unmoved — `.gal-scroll` **4950**, `--revPer` **3**, `feTurbulence` **60**, doc height **14833**, elements **2705**, zero broken images, no console errors |
| D219 | 14 Aug | ⭐⭐ **EACH PROJECT CAN CARRY ITS CUSTOMER'S OWN GOOGLE REVIEW, BESIDE WHAT WAS DONE.** Client: *"you will see in the drive that there are reviews in some of the drives, a screenshot of the Google review for that project. You just add that text into a review format from the client in the project page by the descriptions."* Central London carries **Joel Brizman** and Wimbledon carries **Kinga Skubiszewska**. ⛔⛔ **THE TEXT IS NOT STORED IN `PROJECTS`. `reviewBy` HOLDS A NAME AND THE WORDS ARE LOOKED UP IN `REVIEWS` AT RENDER TIME**, so the document contains exactly one copy of every review. **That is D168 applied before it could bite again**: a review deleted from `REVIEWS` but left behind in a second array still ships to every visitor and still sits in view-source on a PUBLIC repo. One copy means one delete. ⭐⭐ **AND THE SCREENSHOT WAS THE WORSE SOURCE, WHICH IS THE PART WORTH KEEPING.** His Brizman screenshot is Google's own collapsed card — it ends *"our vision and our requirements, which … More"*. Transcribing it would have published a sentence that stops mid-clause; the **full review was already in `REVIEWS`** from the reviews deck, so the customer's whole text runs on the project page. ⚠️ **THE RIGHT-HAND COLUMN NOW EARNS ITS PLACE WITH EITHER A STORY OR A REVIEW.** Central London has a review and no story, and the old test keyed the whole column off `story` alone, so the review would have been built and then hidden. | Verified by opening all eight at 768: Central London shows Brizman at 625 characters with the story block hidden and the column still present; Wimbledon shows Kinga; the other six show neither and drop to one column |
| D218 | 14 Aug | ⭐⭐ **THE NAV BAR STAYS ON A PROJECT PAGE.** Client: *"there must still be a nav bar when they go into one of the project pages."* The detail view is a fixed overlay at `z-index:120` and `header.bar` sits at 50, so the bar was simply underneath it. ⛔⛔ **THE WHOLE NAV STACK HAD TO MOVE, NOT JUST THE BAR, AND ONLY RAISING `header.bar` WOULD HAVE LOOKED RIGHT ON A DESKTOP AND BEEN BROKEN ON A PHONE:** `.mobile-nav` is 40 and would have stayed stranded under the overlay, so the burger would have opened a menu nobody could see. Sheet 121, bar 122, burger 123 — the same order they already had, lifted over 120. ⚠️ **THE LIGHTBOX STAYS AT 130 AND STILL COVERS THE NAV**, which is right: it is a fullscreen photograph, not a page. ⭐ **A VISIBLE NAV THAT DOES NOTHING WOULD BE WORSE THAN NO NAV**, so a click on any link in the bar closes the overlay first — in the CAPTURE phase, because the smooth-scroll IIFE also listens for `a[href^="#"]` and would otherwise scroll the page hidden behind the overlay. ⚠️ **AND THE BACK BUTTON HAD TO COME DOWN OFF THE LOGO** — `.proj-close` is `position:fixed;top:22px;left:22px`, which is exactly where the lockup now sits; it drops to 92px while a project is open. | Measured: nav `z-index:122` and hit-testable (`elementFromPoint` over the bar returns `.brand-logo`) with a project open at 375, 768 and 1440; lightbox 130 still above it; `/#stones` in the bar closes the overlay and clears `html.proj-open` |
| D217 | 14 Aug | ⛔⛔ **THE SHADE-BEHIND-THE-COPY PART OF THIS ROW WAS REVERSED BY D220 THE SAME DAY — READ D220 FIRST.** The centring stands and went further (both axes); the ellipse behind the text is deleted. **The row is kept because it records the measurements that made the problem real, and because the next session will otherwise re-propose exactly this.** ⭐ **THE PROJECT HERO IS CENTRED, THE SCROLL CUE IS AN ARROW AND NOTHING ELSE, AND THE COPY NOW CARRIES ITS OWN SHADE.** Client: *"also add the text center. And also don't say scroll to view the full story. Just add the arrow down jumping. You know? Don't… nothing else."* The words are gone, the bob animation is unchanged, and the arrow is `aria-hidden` — a cue that says nothing to the eye should not announce a sentence to a screen reader either. ⚠️⚠️ **CENTRING IT EXPOSED A LEGIBILITY FAULT THAT WAS ALREADY THERE, AND MEASURING IT IS THE POINT OF THIS ROW.** Across the 97 photographs that can appear behind this block, the gold eyebrow measured **1.42:1 against its own background on the worst of them** and under 2:1 on fifteen — gold on a pale grey worktop, present and unreadable. ⛔⛔ **DEEPENING THE PAGE VEIL WAS TRIED FIRST AND REVERTED, AND THAT IS THE LESSON:** reaching 3:1 that way needed about 0.65 alpha across the middle of the frame, which fixed the label by flattening the photograph — and on this site the photograph IS the product. **The words needed shade, not the picture.** A soft feathered ellipse behind the copy only, sized from the copy's own box so it holds at every width. ⛔ Not a box, not a panel, no line anywhere on it (§2 rule 10). ⚠️ The veil also needed an explicit `z-index` for the first time: the slides now carry `z-index` up to 2 and a positioned element with `z-index:auto` paints UNDER them, so the veil would have dropped behind the photograph mid-change and taken the title's contrast with it. | Measured before and after over all 97: worst case **1.42:1 → 3.38:1 at 375** and **→ 4.6:1 at 1280**, none below 3:1 at either. Judged by eye on `hornchurch-g1`, the worst of them |
| D216 | 14 Aug | ⛔⛔⛔ **THE PROJECT HERO SAT ON BLACK FOR EIGHTEEN SECONDS OUT OF EVERY TWENTY-TWO, AND IT WAS A DATA SHAPE, NOT A TIMING.** Client: *"there is a long pause of just black between the images on the projects that are, like, in the hero section… no pause on black ever in any of the gallery sections."* ⭐⭐ **`gallery` STOPPED BEING A LIST OF URLS IN D212 AND `startHero()` WAS NEVER TOLD.** The collage needs intrinsic dimensions, so every entry became `[url, width, height]` — and `url(${entry})` stringifies the whole array to `url(/assets/…webp,1067,1600)`, which is not a valid CSS `url()`, so the declaration is dropped and the slide paints nothing. Slide 1 was the clicked card's `src`, still a plain string, still fine; **slides 2 to 5 were empty.** One photograph, then four black screens at 4.5s each. ⭐⭐ **IT LOOKED LIKE A TRANSITION BUG AND WAS A TYPE BUG**, which is why staring at the fade timing would never have found it — and why the fix is `srcOf()`, not a new duration. ⭐ **THE CROSSFADE COULD ALSO REACH BLACK ON ITS OWN AND THAT WAS FIXED TOO:** two layers fading in opposite directions over a dark page composite to about 75% coverage at the midpoint, so every change dipped even when both images loaded. The outgoing slide now HOLDS at full opacity underneath while the incoming one fades in above it, and is cleared only once the fade has finished. ⛔ **AND NOTHING IS SHOWN BEFORE IT HAS DECODED** — a slide swapped to an image the browser has not fetched is a third way to paint black, so `advance()` awaits the decode and holds the current picture until then. ⛔ **THE PAD-TO-FIVE LOOP IS GONE:** it repeated image one to fill the array, which is why a photograph appeared to "pause" twice in a row on short galleries. | Sampled every 250ms for 22s across four changeovers on two projects: **minimum composite coverage 1.000, zero frames below 99%, zero frames showing an empty slide** — against four of every five slides empty before |
| D215 | 14 Aug | ⭐⭐ **THE EIGHTH PROJECT IS REAL: THE WIMBLEDON PROJECT.** Client: *"the Wimbledon project is project number eight, but this one doesn't have any description or anything. And it's quite complicated because it was done in multiple apartments. So it's not all the same apartment. So just describe that it was done in multiple apartments."* The placeholder card that had been holding position since D211 is gone. **11 photographs, a card at 1400 and 560, project type "Worktops and splashbacks, across multiple apartments".** ⭐ **THE STORY SAYS WHAT HE SAID AND NOTHING MORE** — several apartments, each templated and fitted on its own, one stone through every flat. ⚠️ **THE "MULTIPLE APARTMENTS" LINE IS DOING REAL WORK, NOT DECORATION:** the gallery visibly contains more than one kitchen, and a reader who spots that without being told reads it as an error on the page. ⛔ **NO STONE IS NAMED.** His Drive folder records none, and naming one on a real customer's flat is the integrity rule in a different costume — the same reason the other seven carry no material field (D211). ⛔ No completion date either (D212). | 8 cards at 375, 768 and 1440, all eight **hit-tested with `elementFromPoint`** on `/projects/` and clickable; the Wimbledon overlay opens with 11 tiles, its own story and its own review |
| D214 | 14 Aug | ⭐⭐⭐ **53 NEW PHOTOGRAPHS FROM THE CLIENT'S OWN GOOGLE DRIVE, ACROSS FIVE PROJECTS.** Client sent a shared folder: *"find the best images or doing whatever you think is best. Probably the more images we have, the better… but if something doesn't look good, don't use it."* Central London +7, Hornchurch +4, Harlow +9, Rickmansworth +10, Watford +12, Wimbledon 11 new (D215). **Gallery totals now 6 / 13 / 8 / 2 / 15 / 16 / 18 / 11.** ⛔ **RUISLIP IS UNTOUCHED ON HIS INSTRUCTION** — *"ignore the Ruislip project and just keep it as is"*. ⭐ **HOW TO PULL A SHARED DRIVE FOLDER, BECAUSE IT WILL BE NEEDED AGAIN:** `https://drive.google.com/embeddedfolderview?id=<ID>#list` returns a plain HTML list of every child with its file id, recursively; `uc?export=download&id=<ID>` returns the ORIGINAL bytes. ⛔⛔ **NEVER BUILD FROM `thumbnail?id=…&sz=…`** — it serves a downscaled copy, and building the gallery out of previews is D212's blur in a new costume. Thumbnails were used ONLY to triage. ⭐⭐ **THE TRIAGE METHOD IS THE REUSABLE PART:** 131 candidates is too many to judge one by one, so they were tiled into labelled contact sheets AND scored for sharpness by the variance of a Laplacian, then chosen on both — the eye for composition, the number for focus. Twelve of Watford's 63 were kept; the folder is full of near-duplicates and soft close-ups. ⚠️ **`Copy of DSC0…` AND `DSC0…` ARE THE SAME FILE TWICE** in his Watford folder. ⭐ Originals are 7008×4672 and 4032×3024, so every file is a DOWNSCALE to a 1600px long edge and **nothing was upscaled**. ⚠️ **EXIF ORIENTATION MATTERS HERE:** the Sony files are landscape on the sensor and portrait on the flag, so `ImageOps.exif_transpose` before resizing is what keeps them upright — checked by eye afterwards, not assumed. ⛔⛔ **TWO OF HIS SITE'S OWN HORNCHURCH PHOTOGRAPHS WERE DROPPED, WHICH IS A JUDGEMENT CALL HE CAN REVERSE IN ONE LINE:** the old `-g2` is a slab on a trolley in a dusty workshop with an air hose across the floor, and `-g3` is a half-built room with a stepladder, a mitre saw and loose cables. **Both files are still on disk**; their paths are simply not in the array. ⚠️⚠️ **TWO DRIVE FOLDERS MATCH NO PROJECT AND WERE NOT GUESSED AT:** *Ali Jaffer Project* holds a review screenshot and no photographs, and *Kav project – Uxbridge* is EMPTY. **Both men's reviews are already live in `REVIEWS`**, so nothing is lost, but neither is a project on this site and inventing a location for one would be inventing a job. Flagged to him. | Verified at 375, 768 and 1440 with `loading='eager'`: **89 tiles across the eight, zero broken, every aspect ratio exact, max scale factor 0.76 at 2× DPR** so every file is still larger than the box it is drawn into. Frozen desktop unmoved — `.gal-scroll` **4950**, `--revPer` **3**, `feTurbulence` **60**, document height **14833 identical to the pre-change file measured the same way**, elements 2695 → 2705, which is the review block (7) and the arrow (3) exactly. `verify.py` ✅ PASS 132/132/132, CSS gate 0/0 |
| D213 | 14 Aug | ⛔⛔ **THE PROJECT DESCRIPTIONS ARE REWRITTEN IN THE HOUSE VOICE, WHICH REVERSES "SAME DETAILS" FROM D211 — ONE ROUND LATER.** Client, reading his OWN site's copy back off our page: *"it just sounds so much like AI… I want you to speak like the brand speaks. We don't have to talk about our expertise or our experienced team. Just simply what was done."* ⭐⭐ **THE REVERSAL IS NARROW AND WORTH GETTING RIGHT: THE FACTS STAY HIS, THE SENTENCES BECOME OURS.** Stone, thickness, which pieces, the edge profile — all his. "Showcases our expertise", "bring your vision to life", "no matter how bespoke", "trust our experienced team" — all gone. Ruislip now reads *"Calacatta Sydney Black quartz at 20mm, across the worktop, the island and the splashbacks. The breakfast bar was cut to the room, and the doorway into the kitchen lined in the same stone so the run carries through."* ⛔⛔ **DO NOT RE-SYNC THESE FROM HIS SITE.** D211 scraped them verbatim and that is exactly what he has now rejected; a future scrape pulls the marketing copy straight back in. **Take the facts and write them again.** ⚠️⚠️ **AND ONE FACT WAS CORRECTED RATHER THAN COPIED, WHICH IS THE PART TO REMEMBER: HIS SITE CALLS TAJ MAHAL "QUARTZ".** This site's own stone page is titled **Taj Mahal Quartzite** and files it under Marble & Quartzite. Carrying his word across would have had a project page contradict the stone page it sits beside — the exact confusion §2 rule 13 exists for (*"on the collection page it shows marble, but on the actual page it says quartzite, we cannot have that confusion"*). ⭐ **SCRAPED COPY IS NOT AUTOMATICALLY TRUE COPY**, and the catalogue is the authority on what a stone is called. ⚠️ **TWO LIVE AI-TELLS ELSEWHERE WERE FOUND AND LEFT ALONE, AND FLAGGED TO HIM** — the reviews CTA reads *"Let's bring your vision to life"* (the phrase he just objected to, near-verbatim) and the Why subtitle ends *"and ours is second to none"*. Both are outside what he asked for and one of them he approved on 9 Aug, so they are his call, not an inference. | Read back live from both overlays after rebuild. AI-tell scan across the project copy returns nothing; the remaining hits in `index.html` are this rule's own documentation and the two flagged CTAs. CSS gate 0/0, `verify.py` ✅ PASS |
| D212 | 14 Aug | ⛔⛔⛔ **THE PROJECT CARDS HAVE NEVER BEEN CLICKABLE ON A PHONE, AND THAT IS THE BIGGEST THING IN THIS ROUND.** Client: *"you have made it so that the projects aren't clickable… the projects aren't currently clickable on mobile either."* ⭐⭐ **HE IS RIGHT, AND IT PREDATES THIS ROUND ENTIRELY.** `.gal-set` is `pointer-events:none` on purpose — the set plane is stage-sized and would otherwise swallow every click over the title block — and the cards get their events back from **`.gal-set.settled`, a class only the pinned accordion adds.** `.gal-static` (D163's phone column, and D195's `/projects/`) turns that engine OFF, so on every handset and on the whole Projects page the cards computed `pointer-events:none`. `elementFromPoint()` over a card's centre returned `.gal-stage`. One rule fixes both: `#gallery.gal-static .gal-card{pointer-events:auto}`, gated so the desktop accordion's mid-flight cards still cannot be clicked. ⛔⛔ **AND THE REASON MY OWN TESTING MISSED IT IS THE LESSON TO KEEP: a scripted `card.click()` DISPATCHES STRAIGHT AT THE NODE AND NEVER HIT-TESTS.** It "worked" every time. Only a real pointer goes through the compositor. ⭐ **TEST A CLICK TARGET WITH `elementFromPoint`, NOT `.click()`.** ⭐⭐ **THE BLUR WAS THE CROP, NOT THE FILES.** Client: *"you resize some of them and now they're blurry… keep them in a collage style with the original sizing."* Every tile was forced to `aspect-ratio:3/2`, the wide ones to **21/9**, and filled with `background-size:cover`. His photographs are **4672×7008 portrait** phone shots and 1084×1446 uprights — pouring a 21:9 letterbox out of a 3:4 portrait takes a thin strip and blows it up to 1180px on a 2× screen. **The source was never the problem; the shape it was poured into was.** ⭐ Rebuilt as a `column-count` masonry collage, 3 → 2 → 1 columns, every photograph at its own proportions, nothing cropped or stretched. ⭐ **`<img>` WITH REAL `width`/`height`, NOT A BACKGROUND** — a background cannot declare an aspect ratio, so the columns packed at the wrong heights and every tile jumped as the photos arrived; the dimensions are baked into `PROJECTS` from the files themselves. ⭐ **THE GALLERY FILES WERE REGENERATED AT A 1600px LONG EDGE, NEVER UPSCALED** — several originals are only 604–882px wide, and enlarging them to fill a tile is the blur in another form. **Measured after: max scale factor 0.64 at 2× DPR on all three bands, i.e. every file is still larger than the box it is drawn into.** ⭐ Tiles take the site's own `.glow-card` cursor-tracked gold rim, `.sheen` gloss and a 14px radius, plus keyboard focus, rather than a new hover style. ⛔ **THE COMPLETION DATE IS GONE** — client: *"I don't want you to say what date the project was completed."* ⚠️ It also ages badly: a date reads as stale the moment the year turns. ⛔ **THE DESCRIPTIONS NO LONGER OPEN "Our recent project, The X project…"** — client's wording, *"just say this x project"*; it named the project twice in six words and "recent" is a claim that quietly expires. Do not restore it when re-syncing from his site. | **Hit-tested with `elementFromPoint` at 390 and 768: 8 of 8 cards clickable, was 0 of 8.** Desktop accordion swept at 41 scroll positions — settles at 21 of them and hit-tests true, unchanged. Collage verified at 390/768/1280: 1/2/3 columns, **every aspect ratio exact**, nothing upscaled, no overflow. Frozen desktop holds — `.gal-scroll` **4950**, `--revPer` 3, `feTurbulence` 60, zero broken images. `verify.py` ✅ PASS, CSS gate 0/0 |
| D211 | 14 Aug | ⭐⭐⭐ **THE PROJECT GALLERY IS THE CLIENT'S REAL PORTFOLIO NOW.** Client: *"go scrape all of that… same names, same details, same images."* **Seven real installations taken from his own live site, `topcatworktops.co.uk/portfolio` — his project names, locations, project types, dates, descriptions and photographs.** ⭐ **HOW, BECAUSE IT WILL BE NEEDED AGAIN:** the site is a Next.js app and the project text is NOT in the served HTML (`curl` returns nothing for it) — it is client-hydrated. The image URLs ARE in the HTML, and the text comes back by fetching each route with an **`RSC: 1` header**, which returns the flight payload. Reading the rendered `innerText` per page was the reliable route for the four labelled fields. ⛔ The blob storage serves **AVIF bytes under `.png` names**, and `sips` reads them where PIL will not — the working pipeline is `sips → PNG → PIL → WebP`. **45 originals became 52 WebP files, 2.4MB total**, at 1400/560 for the cards and 1100 for the galleries, with the 560 pair registered in `SS` so a phone is not sent the 1400. ⭐⭐ **THE OVERLAY IS PER PROJECT NOW, AND IT WAS NOT BEFORE.** Location, project type and date were hard-coded — every card claimed **"Calacatta Gold Quartz, Spring 2025"** and told one invented story about a bookmatched island, and the media grid and hero slideshow were fed by `SAMPLE`, every card's photo shared by all eight. Cards carry `dataset.key`, `openFocus` looks the project up, and the grid, the hero and the meta are all its own. ⛔ **MATERIAL WAS REMOVED RATHER THAN GUESSED:** his portfolio does not record a stone per project, and naming one on a real customer's installation is the integrity rule in a different costume. ⛔ **THE LABELS AND THE VIDEO TILE WENT WITH IT** — "Island detail", "Edge profile" and a play button on a walkthrough that does not exist were harmless over placeholders and are captions we invented once the photographs are real. ⛔⛔ **FIVE OF THE SEVEN HAVE NO DESCRIPTION ON HIS SITE AND NONE WAS WRITTEN FOR THEM.** The story column hides itself and the intro drops to one column, so those show the facts and the photographs. Writing a project narrative would be publishing invented copy as his account of a real job. ⚠️ **ONE SENTENCE WAS DROPPED FROM HORNCHURCH AND HE WAS TOLD:** his own text reads *"we outdid competitors by being £1000 cheaper"* — discount language arguing on price, §2 rule 5. The rest is verbatim. ⚠️ **THE EIGHTH CARD IS A DELIBERATE PLACEHOLDER** — he says there are eight and his site lists seven, and he is sending the missing one. No photograph, so it shows the "photo to come" plate rather than borrowing another job's kitchen. ⚠️ `/projects/`'s own lead promised "the stone named on every one and the story of how it was cut and fitted" and **that had become false**, so it was rewritten to what the gallery actually contains. | 8 cards at 375 and 1440, every image loads, **zero broken images**, no overflow. Per-project verified by opening three: Ruislip 6 tiles + story, Harrow 2 tiles + no story, the eighth no media. Desktop frozen proofs hold — `.gal-scroll` **4950**, `--revPer` 3, `feTurbulence` 60. `verify.py` ✅ PASS, CSS gate 0/0, all pages 200 |
| D210 | 14 Aug | ⛔⛔ **THE PROJECT STORY STILL CLAIMED THE SEAMS "ALL BUT DISAPPEAR", HOURS AFTER HE KILLED THAT CLAIM.** Client, pointing at the project detail overlay: *"you can see under the words 'the slab was cut and polished to that template in our own workshop', matching, something isn't right there."* ⭐⭐ **HE WAS READING THE SENTENCE, AND THE SENTENCE WAS WRONG.** D203 took his correction — *"seams will always be visible and never invisible, we do our best to make it invisible"* — and rewrote the FAQ answer. **This paragraph said the same retired thing in different words and survived**, because the scan that morning looked for the FAQ's own phrasing ("cannot find them once the worktop is installed") rather than for the CLAIM. ⭐⭐ **THAT IS THE LESSON: WHEN A CLAIM IS RETIRED, SEARCH FOR THE CLAIM IN EVERY PHRASING IT COULD TAKE, NOT FOR THE STRING YOU JUST REPLACED.** ⚠️ "across each joint" was an absolute as well (§2 rule 12). It now says what is actually done — the joints are placed where they interrupt the pattern least and the veining is matched through them — and promises nothing about what the customer will see. ⛔ **THE TWO REVIEWS CONTAINING "seamless" WERE LEFT ALONE: they are customers' own words and are not ours to edit.** ⚠️⚠️ **THE OTHER HALF OF HIS REPORT — "the text is kinda cut off, so something is broken" — COULD NOT BE REPRODUCED, AND THAT IS RECORDED HONESTLY RATHER THAN CLAIMED FIXED.** His screenshot's composition was reproduced to the pixel at **983×704 with the overlay scrolled 317px** (hero bottom lands at 55% of the viewport, exactly as in his image), and at that state: **no two elements overlap, the overlay scrolls on a real wheel (317 → 817), the page behind stays at 0, and `openFocus` resets `scrollTop` to 0 on every open** — verified by opening, scrolling to 900, closing and reopening. Swept **768×1024, 900×600, 983×704, 1024×640, 1180×820, 1280×800 and 1440×900**: zero overlaps, no horizontal overflow, `.proj-desc` never escapes `.proj-intro`, and the overlay is scrollable at every one. ⭐ **THE CUT LINE IN HIS SCREENSHOT IS THE FOLD** — `.proj-hero` is `100vh`, so the body's first screen always begins mid-paragraph. ⚠️ If it is still cut for him AFTER scrolling, it is a paint artifact on his machine and needs his browser and zoom level, not another layout change. | `all but disappear` returns nothing on either the landing page or the generated `/projects/`; the one remaining hit is an unrelated comment about the gold ramp. CSS gate 0/0, `verify.py` ✅ PASS |
| D209 | 14 Aug | ⭐⭐ **AREA AND HOURS COME OUT OF THE CONTACT COLUMN ON THE DESKTOP TOO, AND THEY SIT ON THE FOOTER'S OWN COLUMN LINES.** Client: *"in the footer, which shows area and hours, I think we should lay those down horizontally across the bottom line so that it doesn't make the contact row have everything in it, so we can even out or space things out better."* ⭐ **HE IS DESCRIBING A BALANCE PROBLEM AND HE IS RIGHT** — Contact had grown to five rows (Phone, Email, WhatsApp, an eight-county Area string and Hours) against three or four short rows in every other column, so one column carried most of the footer's text. ⭐ **D200 ALREADY BUILT THE MECHANISM; THIS ONLY TURNED IT ON.** `--footTail:on` is now declared for ≥1121 as well as the tablet band, and the tail's LAYOUT moved to base scope — safe unscoped because `.foot-tail:empty{display:none}` already makes an unfilled tail contribute no box, no gap and no margin. **One description of the block, and only the switch has a width.** ⛔ The phone keeps them in the column: at ≤720 the footer is already one stack, so both arrangements render the same thing and the signed-off band stays untouched. ⚠️⚠️ **THE FIRST PASS SHIPPED CENTRED AND HE REJECTED IT IMMEDIATELY:** *"I don't want it to be centered. Keep the word area in line with the ones inside explore… so it starts in line with the e from the word explore."* ⭐⭐ **THE SECOND PASS BORROWS `.foot-grid`'S OWN TEMPLATE INSTEAD OF GUESSING AT AN INDENT.** Measured at 1280 first: Explore's label starts at **x=441**, Browse at 688, Contact at 935, and the email's ink ends at **1123**. Repeating the four-column template on the tail puts Area on the Explore line and Hours on the Contact line **by construction**, so they cannot drift when the columns re-proportion at another desktop width — a hand-tuned `padding-left` would have been right at exactly 1280 and wrong everywhere else. ⚠️ Area takes columns 2–3 and Hours column 4, directly under PHONE, EMAIL and WHATSAPP. ⚠️ **ONE DEVIATION, STATED:** the row's right end lands about 30px past the email's "k" rather than exactly on it. Pinning it there means hard-coding the pixel width of that email address into the layout, and one changed character breaks it. ⚠️ **THE TEMPLATE AND GAP ARE COPIED FROM `.foot-grid` AND MUST STAY IN STEP** — that is the maintenance cost of aligning two separate elements to one set of lines. ⚠️ A first attempt at the horizontal row also failed silently: a flex item sizes to its content, so the eight-county Area string took the whole 872px container and pushed Hours onto a second row — **two stacked blocks wearing a flex row's clothes.** Capped with a percentage, not a pixel width, so it holds at other widths. | Measured after: Area label and text both start at **441**, exactly Explore's line; Hours label and text both at **935**, exactly Contact's; both on one line, no overflow. Tablet unchanged (centred, one line); phone tail still `display:none` with both blocks in the column |
| D208 | 14 Aug | ⭐ **STONES GETS A NAV PANEL, THE SAME SHAPE AS SERVICES.** Client: *"I want the stones part word in the nav bar to also have a drop down to where you can select either stone selector or all stones or compare stones. So those three pages."* ⛔ **THE WORD STILL GOES TO `/stones/` AND THE CARET STILL OPENS THE PANEL** — the split D189 established and D194 repeated on the phone; nothing in the panel is a control that only opens something. ⚠️⚠️ **"STONE SELECTOR" IS A SECTION, NOT A PAGE, AND THE HREF HAS TO SAY SO.** It is `#stones` on the LANDING page, so the link is **`/#stones` and not `#stones`**: a bare hash resolves against whatever page you are already on, and this nav is on all eleven page families, so from a stone page it would have hunted for a section that is not there. The smooth-scroll IIFE only intercepts `a[href^="#"]`, so `/#stones` navigates home and lands on the section. ⚠️ **`/stones/` IS THE COLLECTION PAGE AND `#stones` IS THE LANDING SECTION** — similar names, different things, do not tidy one into the other. ⭐ **THE PHONE SUBMENU TOOK THE SAME THREE AT THE TOP RATHER THAN BEING REPLACED BY THEM.** Quartz, Marble & Quartzite and Granite were his own D194 ask and they are not competing destinations — they are filters ON "All stones" (`/stones/#quartz` is that page with a range preselected). Removing them to make the two menus identical would undo an earlier ask to satisfy a later one that never conflicted with it. ⚠️ Six rows now: the sheet scrolls and every row is `flex:none`, which is the D194 fix, so it cannot clip the way "Outdoor kitchens" once did. | Both panels verified: Services 7 rows, Stones 3 on the desktop and 6 on the phone. All three targets 200. Phone menu still measures **≤0.01px from the centreline on all 20 single-word rows** (D204) with the two new ones added |
| D207 | 14 Aug | ⭐ **THE SERVICES DROPDOWN, THE DESKTOP WHATSAPP BUTTON, AND THE WHATSAPP NUMBER IN WRITING.** Three asks in two messages, all desktop, all named by him so all unfrozen. ⛔⛔ **THE DROPDOWN COMPLAINT WAS A LIVE BUG, NOT A TASTE NOTE, AND THAT IS THE PART WORTH KEEPING.** Client: *"we need to have the words underlined or something like that so it looks better and space them further away from the side."* The panel's own rule said `.nav-menu a{padding:9px 20px}` — and **it had never once applied.** `nav.top a` (0,1,2) sets `padding:8px 1px` and out-specifies `.nav-menu a` (0,1,1), so every item in that menu had been sitting **one pixel** from the border since D189 built it. **Measured `paddingLeft:1px` on all seven rows before touching anything.** ⭐⭐ **THE DECLARATION WAS RIGHT THERE AND CORRECT, WHICH IS EXACTLY WHY RE-READING IT PROVED NOTHING** — parsed, present, losing. That is D106/D113/D114/D197 for the fifth time, and the fourth time it has been a padding or display value quietly overruled by a shorter selector further up. ⭐ **PRINT THE COMPUTED VALUE BEFORE YOU CHANGE THE DECLARATION.** Fixed by raising the selector to `nav.top .nav-menu a`, then 26px of side padding against a 244px panel, a `--hair` rule between rows written `a + a` **so it can only fall BETWEEN two links and never across the top of the panel** (§2 rule 10, the fingernail), and the underline the client asked for done as a gold `text-decoration` on hover — the nav's own gesture, at each row's own width, which is why `nav.top a::after` had to be switched off in here in the first place (a descendant selector drew it at full nav width under every row at once). ⭐ **THE FLOATING WHATSAPP BUTTON IS PHONE AND TABLET ONLY NOW.** Client: *"remove the WhatsApp corner floating button for desktop, but keep it on the mobile and tablet."* ⚠️ **HE IS RIGHT ABOUT THE DEVICE AND NOT ONLY THE LOOK:** on a handset the link opens an app the customer already has, on a desktop it opens WhatsApp Web or a download page, and the button was positioned to clear `.mbar` (D173) which is itself a phone control — so on desktop it floated over a composition that never asked for it. `display:none` rather than `visibility:hidden`, so it leaves the tab order too. ⭐ **AND THE NUMBER IS WRITTEN OUT.** Client: *"we should have the WhatsApp number displayed as well."* The footer glyph and the floating button were both silent about WHICH number they opened, so nobody could save it or type it into their own phone. `07464 940287` in the footer contact column, national form to read and `wa.me/447464940287` in the href. ⛔ **IT SITS ABOVE AREA AND HOURS ON PURPOSE** — D200's footer IIFE restores those two with `appendChild`, which is only correct while they remain the last two children of `.foot-contact`. ⚠️ The dropdown and the phone submenu also took D206's names and order, by hand, in both families. | Measured after: side padding 1px → 26px, ink 27px in from the panel edge on all seven rows, separators between rows only, hover underline gold at 1px/5px offset. Fab `none` at 1280, `flex` at 390 and 768. WhatsApp row present in `.foot-contact` at all three widths and Area/Hours still move to `.foot-tail` at 768. `verify.py` ✅ PASS, CSS gate 0/0, all pages 200 |
| D206 | 14 Aug | ⭐⭐⭐ **THE RANGE IS EIGHT SERVICES, IN THE CLIENT'S OWN ORDER.** He sent it as a sentence: *"kitchen worktops, splashbacks, bathrooms, outdoor spaces, fireplace, dining tables, vanity tops, and commercial."* **`SERVICES[]` IS NOW THAT SENTENCE.** ⭐⭐ **ONE ORDER GOVERNS ALL THREE RENDERINGS, AND THAT IS THE ARCHITECTURAL PART.** The desktop helix had `ORDER` and the phone grid had `PHONE_ORDER`, each a re-shuffle of an older popularity order; both are the identity now, so the array is the only place order lives. **Two lists that can disagree with the array are two more places to lose a card** — which is exactly what D205 did. ⭐ **THREE THINGS CHANGED NAME:** "Bespoke Worktops" → **Kitchen Worktops**, "Outdoor Kitchens" → **Outdoor Spaces**, and both leaf pages' EYEBROWS were changed to match, because a tile and the page it opens must not call one thing two names (rule 13, the marble/quartzite lesson). ⛔ **THE SLUGS, TITLES AND META DID NOT CHANGE** — "outdoor kitchen worktops" is the phrase people search, and a URL change costs its ranking and every link into it. ⚠️⚠️ **KITCHEN ISLANDS IS NOT ON HIS LIST AND IS NO LONGER A TILE.** Its copy was folded into Kitchen Worktops, which is where a customer looks for it. ⛔ **THE PAGE IS DELIBERATELY STILL LIVE**: it ranks for its own term, it is linked from the nav submenu, the footer and other leaf pages, and deleting a page is the one move here that cannot be undone from a browser. **Flagged to him rather than inferred from a list.** ⭐ **TWO ARE NEW AND HAVE NO PAGE YET** — Fireplaces and Dining Tables. Their cards carry no `href`, which is the designed behaviour: the card flips to its description and shows no "Read more". ⚠️ **THE FIREPLACE COPY IS DELIBERATELY CLAIM-LIGHT** — hearths and clearances carry building-regulation duties nobody has said they take on, and rule 3 forbids stating what we cannot guarantee. ⛔⛔ **AND THIS SHOOK OUT A REAL BUG THAT LOOKED LIKE NOTHING:** the helix template interpolated `s.img` and `s.href` raw, which was safe only while every service had both. With two page-less services it rendered **`href="undefined"` on a confident-looking "View this service" button** — a 404 that nothing about the page's appearance gave away — and `src="undefined"` on three cards. Both now fall back the way `buildCards` already did: `phImg()` for the photograph, and **no button at all** rather than a dead one. `label()` also stopped announcing page-less cards to a screen reader as "link". ⚠️ **FIVE REAL SUBJECTS FOR EIGHT TILES**, so Fireplaces, Dining Tables and Vanity Tops show the "photo to come" plate. That was chosen over reusing a near-enough photograph, which is what put a quarry on Outdoor and a kitchen on Commercial — **a plate reads as pending, a wrong photograph reads as wrong.** | Verified at 375, 390, 768, 1280 and 1440: eight tiles in his order numbered 01–08, four clean rows of two, eight helix nodes and eight nav rows, no `href="undefined"` or `src="undefined"` anywhere, no broken image, no overflow, no console errors. **Desktop frozen proofs unmoved: `.gal-scroll` 4950, `--revPer` 3, `feTurbulence` 60, doc height 14762 — a helix is fixed-height, so an eighth service does not lengthen the page.** Elements 2669 → 2711, which is one service (29 + 12 + 1) exactly |
| D205 | 14 Aug | ⭐ **A SEVENTH SERVICE: VANITY TOPS.** Client: *"add in another section for Vanity tops (bathroom) in the same section as the above."* ⭐ **INSERTED AT INDEX 4, NOT APPENDED** — `PROJECTS` reads `SERVICES[0..3]` for its placeholder photography and the helix's `ORDER` is written by index, so anything added after position 3 must leave the first four alone. `ORDER` went `[1,0,2,3,5,4]` → `[1,0,2,3,4,6,5]`, keeping his own Outdoor-before-Commercial tail. The spiral is computed from `items.length`, so the geometry absorbed a seventh node with no tuning. ⛔ **IT POINTS AT THE BATHROOM PAGE ON PURPOSE:** that page IS "Bathroom Worktops & Vanity Tops", and a second near-identical leaf page would compete with it for the same words. **The Bathrooms tile was re-cut in the same edit** so the two no longer say the same sentence — Bathrooms is now the room (shower surrounds, thresholds, sills) and Vanity Tops is the basin top. ⚠️ **SEVEN DIVIDES BY NEITHER TWO NOR THREE**, so the last tile sat alone in the left column with a hole beside it, which reads as a card that failed to load. `> *:last-child:nth-child(odd){grid-column:1/-1}` spans it instead. ⛔ That rule is deliberately NOT in a media query and it is rule 15's stated exemption, not a breach: the grid is `display:none` from 1121px, so it cannot reach the frozen desktop, and at ≤600px one column makes `1/-1` a no-op. ⚠️ The photograph is the Bathrooms placeholder repeated — six real subjects for seven tiles until the shoot. | Verified at 768: seven cards, three rows of two and one at 691px, no overflow. Helix nodes 7, `#svcNav` 7, grid 7 at 1440 |
| D204 | 14 Aug | ⭐⭐ **THE PHONE MENU NOW HAS ONE CENTRELINE, AND THE CAUSE WAS THE CARET.** Client, by voice: *"align everything perfectly in the mobile drop down bar."* ⭐⭐ **MEASURED BEFORE TOUCHING IT, WHICH IS THE WHOLE POINT:** on a 390px sheet every plain row's word sat exactly on the centreline and **Services and Stones sat 16px LEFT of it**. Neither row was wrong. The ROW was centred correctly — and the row is the word PLUS a 44px caret PLUS a 2px gap, so centring the pair necessarily pushes the word left by about half the caret. ⭐ **THE FIX IS TO TAKE THE CARET OUT OF FLOW** (`position:absolute;left:100%`), which leaves `.mn-group` exactly as wide as its anchor so it centres identically to Projects, Estimate, About us, Trade and Contact. The anchor's extra `padding-right:8px` went with it — it existed to hold the word off the caret and was itself part of the offset. ⛔ The caret keeps its 44px square and its own tap target; it is still a sibling `<button>` outside the anchor (D194), so the word still opens the hub and the caret still opens the list. ⭐⭐ **AND A SECOND, SUBTLER ONE: `letter-spacing` PUTS ITS SPACE AFTER THE LAST LETTER TOO**, so every centred uppercase row carried an invisible 3.3px tail on its right and the INK sat half of that left of centre. It affects every row equally, so it never reads as one row being wrong — it reads as the whole menu sitting slightly left, which is what he saw. ⚠️ **THE CORRECTION IS THE FULL TRACKING, NOT HALF, AND THAT IS COUNTER-INTUITIVE:** `text-indent` also WIDENS the anchor, and a centred box gives half of any extra width straight back, so one tracking unit of indent moves the ink by half of one. 0.11em landed it 0.8px left; 0.22em landed it on zero. | All **20 rows** (9 links, 10 submenu items, the quote button) measured at **≤0.01px from the centreline**, against 16px before. Screenshotted at 390 with both submenus open |
| D203 | 14 Aug | ⭐⭐ **THE CLIENT'S NOTES DOCUMENT — THE COPY ROUND.** He sent a Word document of his own notes with four screenshots, all taken on `thadeusg3.sg-host.com`, and said *"take this as the truth and go ahead and implement everything besides the image generation."* ⭐ **HERO:** "across London and the Home Counties" → **"across England and the British Isles"**; the two fact chips become "Your project, expertly handled / from start to finish" and "Every detail included / pencil edges, grooves and all cut-outs" — his single sentences split across the chip's existing bold-and-sub structure rather than crammed onto one line. ⭐ **GALLERY:** the subtitle drops the county list for "Recent projects across England", and the eight cards take his eight category names in his order — Bookmatched Worktops, Waterfall Edges, Full Height Splashbacks, Bespoke Layout, Bespoke Features, Bespoke Edge Profiles, Dining Tables, Exclusive Fireplaces. ⚠️ **THE PLACE LINE IS KEPT:** he changed the names only, the design carries a two-line meta, and "Bookmatched Worktops / Hampstead" still reads as a real job in a real town. ⭐ **ABOUT US** is his own six paragraphs verbatim, contractions and one em dash normalised to the house voice. ⚠️ **THAT REVERSES HIS 9 Aug "less about us text"**, which had cut it from three paragraphs to two; he supplied the longer copy himself. ⭐ **FAQ, FOUR CHANGES:** the slab answer gains the distributor's-warehouse visit he recommends; **the seams answer was corrected by him and he was right** — the live copy claimed most clients cannot find a seam, and his note is *"seams will always be visible and never invisible, we do our best to make it invisible"*, so the answer now says a seam is always visible and what we do about it, which is a rule 12 improvement rather than a cost; the silica answer moves in-house (D202); the areas answer takes the new county list. ⭐ **TRADE:** "building contractors" joins the audience list, "we can deal with your customer directly" leads the offer, "one job or forty plots" becomes **"one worktop or a 150 unit build, a dedicated project manager who can be on site from templating to fit"**, and the critical-path section takes his five new paragraphs. ⭐ **AREAS:** four counties become **eight** — Buckinghamshire, Surrey, Oxfordshire and Bedfordshire join — in the footer of all 150 pages, the FAQ, the schema `areaServed`, the `AREA`/`AREAS_SERVED` constants in all four builders, and `/worktops/`, plus his "if you are just outside these areas, ask" line. ⚠️ **THE ENQUIRY LEAD DID NOT TAKE "England":** a free home VISIT is the one promise on that card and "across England" would promise a visit we cannot always make, so it reads "across our service area" and the counties are named in the footer, the FAQ and on `/worktops/`. ⭐ **NEVER "LASER" AGAIN** — *"we template everything by hand"* — 21 occurrences across 18 files, including the Step 3 heading on thirteen pages, and including his own document's "laser-measure", which he wrote before the instruction that supersedes it. ⭐ **BRANDS:** "Next Stone Slabs" in, "CQ Stone" out, in the marquee and in the quartz page's brand sentence (D202 covers the rule that crosses). ⭐ **UPLOADS 10 MB → 50 MB.** ⚠️ **NO IMAGES WERE GENERATED, ON HIS INSTRUCTION** — the Outdoor Kitchens quarry photograph and the Commercial kitchen photograph are still wrong and still there, deliberately. | Rule 11's centimetres scan still returns `index.html` alone; `harvest/verify.py` ✅ PASS 132/132/132; CSS gate 0/0; all 13 page families 200 with no console errors |
| D202 | 14 Aug | ⛔⛔⛔ **FABRICATION IS IN-HOUSE AGAIN, REVERSING THE 7 AUGUST DECISION THAT THE WHOLE SITE WAS REWRITTEN AROUND.** His notes document says *"by workshops we trust, this should be 'by our experienced fabricators'"*, and then claims in-house fabrication in **four more places**: the silica FAQ (*"we carry out our fabrication in our own workshop, where we control the cutting process, use appropriate dust extraction and wet-cutting methods, and follow current HSE guidance"*), the About rewrite (*"fabricating and completing the installation, we keep everything under one roof"*), the trade lede (*"template, fabricate, fit"*) and the trade critical-path section (*"through to fabrication and installation"*). ⭐⭐ **FIVE CONSISTENT PLACES IS A POSITION, NOT A SLIP**, which is why it was implemented rather than queried. ⚠️ **THE HISTORY MATTERS AND IS WHY THIS ROW IS LONG:** July presented it as in-house; **7 Aug 2026 the owner banned every in-house claim outright** (*"we don't do in house fabrication, do not talk about in house fabrication"*) and the positioning that replaced it — *one contract, one contact, and we answer for all of it* — was built on the outsourcing being true; **14 Aug reverses that again.** ⛔ **THE SCAN IN §2 RULE 1 IS NOW OBSOLETE** and returns four hits by design. ⭐ **WHAT CHANGED, BEYOND THE FIVE HE NAMED:** the "who comes to my home" FAQ (its outsourcing sentence was asterisked in his document, and it contradicted the new line), the Process step "Template & Craft", the Why tile on all seven page families, the projects story, and the whole `/guides/is-quartz-safe-silica.html` section that existed to argue *"we do not cut stone ourselves"* — its heading was "What we require of the workshops that cut our stone" and is now "How we control the dust". ⛔⛔ **THE RULE HEADERS IN `build_seo_pages.py` AND `services/build_services.py` WERE REWRITTEN IN THE SAME PASS**, with the full reversal history, because those comments are what the next session reads before it writes copy — leaving them would have had the builders re-assert the old rule on the next run. ⚠️⚠️ **ONE THING HE SHOULD CONFIRM IN HIS OWN WORDS:** the silica answer is a **health-and-safety claim about a workshop and its dust control**, and it is the one sentence here where being wrong is not a copy problem. It was implemented as written, because it is his business and his document, and flagged to him. | Rule 1's scan now returns 4 files by design. `verify.py` check 7 (absolutes) still passes |
| D201 | 14 Aug | ⭐ **THE BRAND IS "Topcat", ONE WORD.** Client: *"Topcat not TopCat as it's one word"*, said twice in the same document. **1,431 occurrences across 196 files** — 173 HTML pages, all four builders, the generated `assets/site.js`, and the CSS comments. ⭐ **SURVEYED BEFORE REPLACING, WHICH IS WHY IT WAS SAFE:** the only forms in the tree were the bare word and the possessive, with no id, class, filename, URL or email carrying it, so a plain replacement could not break a selector or a link. The brand artwork is unaffected — the files are `TOPCAT_*` and `topcat-*`, and the lockup is set in caps by the designer. ⛔ **THE BUILDERS WERE DONE WITH THE PAGES**, or the next `build_pages.py` run would have put 1,294 of them straight back. ⚠️ **The `<title>` still reads "London & the Home Counties"** — he changed the hero's wording, not the title, and the title is a search asset. Flagged, not guessed at. | `grep -rl "TopCat"` returns nothing outside `.bak` and `.removed` |
| D200 | 13 Aug | ⭐⭐ **THE ENQUIRY BLOCK WAS FOUR DIFFERENT WIDTHS DOWN ONE CENTRELINE, AND AREA/HOURS MOVED TO THE FOOT OF THE FOOTER.** ⛔⛔ **FIRST, THE MISTAKE: HE ASKED FOR THE FOOTER MOVE TWICE AND I EXPLAINED INSTEAD OF BUILDING IT.** D199 put the choice back to him — markup change vs scripted move — because a markup move would touch the frozen desktop. His reply: *"I also fucking asked you to move the area and the hours in the footer to the bottom of the screen flat. The fuck is wrong with you?"* ⭐⭐ **THIS IS D189 EXACTLY, ONE ROUND LATER: THE REASONING WAS SOUND AND THE ANSWER WAS STILL WRONG.** The constraint was real, but the job was to find the option that satisfies both and ship it, not to hand him the dilemma. **If he names a thing, build the thing.** ⭐ **THE OPTION THAT COSTS NOTHING IS A SCRIPTED REPARENT.** `.foot-c-area`/`.foot-c-hours` are nested inside `.foot-contact`, a grid ITEM — CSS cannot lift a node out of its grid parent, and `display:contents` on that column dissolves Phone and Email with them. A second copy of the markup would ship duplicate text into view-source on a PUBLIC repo (D168's lesson); moving the markup would change the frozen desktop footer. **Moving the one copy of the node costs neither.** `--footTail:on` is declared in the tablet band and read back by a footer IIFE — the `--galMode`/`--revPer`/`--hxMode` idiom — and it MOVES BOTH NODES BACK on a resize out of the band, verified at the 720/721 and 1120/1121 edges in both directions, because a one-way move would leave the desktop footer permanently altered after a rotate. ⛔ **NO RULE ACROSS ITS TOP** — that is the fingernail line §2 rule 10 forbids outright; the blocks are separated by space alone. ⭐⭐ **THE FORM: "IT LOOKS HALF EMPTY" WAS NEVER ABOUT VERTICAL SPACING, WHICH IS WHY TWO PREVIOUS ATTEMPTS MISSED IT.** Measured in a 691px card: title and lead at **381px**, contact block at **246px**, form at **520px** — four centred blocks of four different widths, leaving ragged gutters down both sides that belong to nothing. **Everything now takes the form's 520px measure**, one column edge down the whole card. ⭐ **AND THAT IS WHAT FINALLY MADE THE CONTACT ROW WORK.** D199 recorded the flex row as a dead end after it measured worse twice — the real cause was that `.cta-reach` was only 246px wide, so its two blocks could never fit side by side and wrapped anyway, paying the gap for nothing. At 520px they fit: **227px → 165px**, and `#cta` **1063 → 980px**. **The earlier failure was a width problem wearing a layout problem's clothes.** ⭐ **THE HERO TITLE RISES AND GROWS AGAIN** — `clamp(52px,7.4vw,72px)`, so **56.8px at 768 and 72px at 1024** against D199's 47.6/60 — with the freed padding spent on the gaps between elements rather than pooled at the bottom (the D157/D169 slack lesson). ⚠️ **AND A FLOOR HAD TO GO BACK UNDER IT:** at a 64px padding floor the title measured a top of **77px on a 1024×768 landscape tablet, 8px below a 69px fixed nav.** The floor is 96px and it is the nav, not taste. | Phone and desktop re-proven: **375 → 14285**, **1440 → 14641**, `.gal-scroll` 4950, no overflow at any width. Elements 2622 → **2623**, which is the one empty `.foot-tail` div and nothing else. At 1440 `.foot-c-area` is back inside `.foot-contact`, verified |
| D199 | 13 Aug | ⛔⛔⛔ **THE TABLET HERO WAS NEVER CENTRED, AND `text-align:center` IS WHAT HID IT FOR A WHOLE ROUND.** Client: *"the hero section isn't even centred, so do that much better. Make the title bigger. Make the subtitle better. Move the button centre. Everything. Why is it not centre?"* ⭐⭐ **HE IS RIGHT AND IT WAS ONE CAUSE.** `.hero-copy` carries `max-width:clamp(560px,48vw,720px)` from the DESKTOP composition, where the copy sits left against a photograph on the right — so it has no auto margins. At 768 that clamp floors at 560px and the box parked at **x=38 with 170px of empty screen to its right**, while every child centred neatly INSIDE it. The buttons were 131px off-centre on the screen and **67px from centre within their own parent, i.e. perfectly centred**. ⛔⛔ **THIS IS WHY D198's VERIFICATION PASSED: I MEASURED THE CHILDREN, AND EVERY CHILD WAS CENTRED.** A centred child of an off-centre box is not a centred layout. **Measure the BOX against the VIEWPORT, not the content against the box** — one `margin-inline:auto` was the entire fix. ⭐ The box also widens 560 → `min(92vw,660px)`, because 560 was a desktop clamp's floor rather than a tablet decision. ⭐ **THE TITLE WAS SITTING ON ITS OWN FLOOR TOO:** `clamp(38px,min(4.7vw,8.6vh),64px)` — at 768, 4.7vw is 36px, so the min() always lost to the 38px floor and the tablet rendered the smallest size the expression can produce, the same size a 500px window gets. **A vw term that only ever loses is not a size.** Now `clamp(44px,6.2vw,60px)` → **47.6px at 768, 60px at 1024**. Subtitle 14.5 → 16.1px on a 46ch measure (it had been running ~90 characters). ⭐ Reviews given air: head margin 14 → 24–38px, `--revAir` up, section 635 → **735px**. ⚠️⚠️ **AND TWO THINGS I GOT WRONG AND REVERSED, BOTH RECORDED SO THEY ARE NOT RE-PROPOSED.** (1) **`.cta-reach` as a flex row is a DEAD END:** `flex-direction` alone is inert on a block and changed nothing; adding `display:flex` made it **WORSE — 227→232px at 768, 198→251px at 1024** — because each contact route is a wide two-line block that wraps to one per row anyway and then pays the gap on top. **The height is in the routes' own WIDTH, not in their stacking.** Deleted rather than left at a net loss. (2) **A CLAMP IS NOT A REDUCTION UNTIL YOU EVALUATE IT:** `#cta{padding:clamp(32px,4.6vh,48px)}` computes to 47px at a 1024-tall window against the base rule's 43, so the "trim" made the section 8px TALLER. Now 3.2vh → 33px, and `#cta` is **1063 → 1042px**. ⚠️ **THE FORM IS NOT FINISHED AND HE WAS TOLD** — the section is barely smaller and the real saving needs the contact routes re-cut, not re-flowed. ⛔ **THE FOOTER ASK IS NOT DONE AND WAS NOT BODGED:** Area and Hours are nested inside `.foot-contact`, and **no CSS can move a nested element out of its grid parent** — `display:contents` on the column dissolves Phone and Email with them. It needs a markup move or a scripted reparent, and a markup move would change the FROZEN desktop footer. Put to him as a choice rather than guessed at (§2 rule 15). | Phone and desktop re-proven after every step: **375 → 14285**, **1440 → 14641**, elements 2622, `.gal-scroll` 4950, `feTurbulence` 60. Hero box measured centred at 768 (54/54) and 1024 (182/182); desktop still L124/R625, i.e. left, as frozen |
| D198 | 13 Aug | ⭐⭐⭐ **THE TABLET BAND IS OPEN, AND THE TABLET IS THE MOBILE BUILD MADE BIGGER.** Client: *"now we're gonna move on to working on the tablet version and the tablet version only right now. So just like mobile, we're gonna form the tablet version the same way we formed the mobile version. So it's gonna be centre. Everything is just gonna be bigger. Same text as on the mobile version, same styling as on the mobile version."* **Fourteen asks in one message; ten needed work and four were "this is already fine" (services, the stone selector, the Why tiles, the FAQ) and were left untouched.** ⭐⭐ **THE ARCHITECTURE IS THE DECISION: "SAME AS MOBILE" IS DONE BY WIDENING THE PHONE RULE'S OWN QUERY TO 1120, NEVER BY WRITING IT AGAIN.** Widening a `max-width` is **additive for the phone and invisible to the desktop** — every rule still applies at ≤720 in the same source order, and ≥1121 never matched it and still does not. Four blocks were widened or re-parented this way (the nav bar, the hero, the hero's call button, the `.gs-swap` order swap) and **nothing was copied**; a second copy for the tablet would have been this project's signature bug for the seventh time (D51, D59, D68, D78, D93, D105). ⭐ **ONE TABLET-ONLY BLOCK, AT THE FOOT OF THE STYLESHEET, CONDITIONED `(min-width:721px) and (max-width:1120px)` — BOTH ENDS ON PURPOSE**: a bare `max-width:1120px` reaches the signed-off phone, a bare `min-width:721px` reaches the frozen desktop. ⛔⛔ **IT HAD TO MOVE TO THE END, AND THAT IS THE ROUND'S SHARPEST LESSON.** Written near the top it lost to base rules further down at IDENTICAL specificity — media queries add no specificity, so only source order separates `#gallery.gal-static .gal-stage` from `#gallery.gal-static .gal-stage`. **This is D106/D113/D114 and D195 in one.** ⭐ **WHAT CHANGED:** the nav loses its quote button and takes the phone's curved bottom line at 26px against the phone's 18 (the same 3.6:1 out-hang proportion — 18px on a 768px bar reads as dead straight); the hero centres, drops the scroll cue and the three icon facts, takes the phone's subtitle, labels and 2×2 chip grid, both buttons at 426px; the reviews become the solo swipeable carousel, centre card **404px against the phone's 270**; the stone selector leads and **the project gallery is a two-up grid — the pinned accordion stops and the section falls from 5632px to 1300px**; the estimator's `Thickness mm` header goes back to **one line (29px → 14px)**; the process tiles go 12px → 26px apart; About centres and its collage goes 420px → 560px; the enquiry form centres at exactly (768−520)/2; the footer centres with a row gap raised above its column gap. ⭐⭐ **TWO JS WIDTH TESTS WERE THE REAL BLOCKERS AND BOTH ARE NOW STYLESHEET-DRIVEN.** `perPage()` hard-coded `innerWidth<720` — the second opinion its own neighbouring comment warns about — and now reads `--revPer`, declared 3 in the base rule and 1 at `max-width:1120px`; every phone carousel rule then arrives at tablet for free, because they all hang off `.rev-solo` and not off a width. `galPhone()` was **renamed `galStatic()`** and takes a second value: it never asked "is this a phone", it asked "has the stylesheet switched the engine off", and that is now true of the tablet too — `phone` lays the cards in one column, `grid` in two, and the difference lives entirely in CSS. ⚠️⚠️ **AND ONE DIAGNOSIS I GOT WRONG AND CORRECTED BY MEASURING AGAIN.** The About collage was reported broken — every tile at `opacity:0`, portraits 61px wide, a 262×14 strip. **It was never broken.** Probed off-screen, its scroll-sequenced entrance had never run, and a transition parked at `currentTime:0` is a stopped clock, not a layout fault (§8.6). Scrolled to and measured live it was correct and even. **The real fault was that it was UNDERSIZED**, capped at 420px by a number chosen for a phone. ⛔ Its centring rules were moved to the **`max-width:880px`** block — the query that actually stacks the copy under the collage — rather than to a device band, because that arrangement is the only one centring is right for; from 881px it is two columns and centred body text beside an image reads worse than left. ⚠️ **881–1120px keeps the two-column About and the estimator's five-column row — flagged to him, not guessed at.** ⚠️ The internal pages inherit all of it through `site.css`: `/projects/` picks up the same two-up grid, and the ten paired `#hero .x, .page-head .x` selectors give the page heads the same 2×2 chip grid, which is the consistency that pairing exists to guarantee. | ⭐ **Verified at SEVEN widths — 375, 721, 768, 1024, 1120, 1121, 1440** — no console errors and no horizontal overflow at any of them, and the build flips cleanly between 1120 and 1121. **FROZEN BANDS PROVEN UNMOVED: 375 → 14285 and 1440 → 14641, elements 2622, `.gal-scroll` 4950, `feTurbulence` 60 — the §1 baseline to the number.** Tablet doc height 18418 → **14225** at 768 |
| D197 | 13 Aug | ⭐ **THE BREADCRUMB AND THE HEAD EYEBROW COME OFF EVERY INTERNAL PAGE ON THE PHONE — TITLE AND SUBTITLE ONLY.** Client: *"when you go to the internal pages, currently, at the top, there's a directory that says home services slash kitchen worktops, we don't need the directory. So you can just start with the title… and you can also remove eyebrows from the title. So it's just title and subtitle for all the internal pages. So on mobile, we don't need a directory of which page you're on or going back to pages, only on desktop."* ⭐⭐ **HE NAMED A LEAF PAGE, AND THE SITE HAS TWO KINDS OF INTERNAL PAGE WITH TWO DIFFERENT CRUMBS.** "Home / Services / Kitchen worktops" is `/services/kitchen-worktops.html`, which is the SEO/leaf layer (`.crumb` + `.crumb-back`, styled in `services/service.css`) — not the six D190 pages, which carry `.page-crumb` from `index.html`'s stylesheet and have had no eyebrow since D193. **Both families are done, because he said "all the internal pages".** ⭐ **THE BACK ARROW GOES WITH IT, DELIBERATELY** — he named both halves ("which page you're on" is the trail, "going back to pages" is the arrow) and `.crumb-back` lives INSIDE the nav, so one rule takes both. ⭐⭐ **ONE RULE IN `service.css` REACHES SIX FAMILIES** — service, material, guide, area and stone pages and the sitemap all load it, so the trail cannot come off one family and stay on another; **166 leaf pages changed with no rebuild, because it is pure CSS.** ⛔ **720px, NOT this stylesheet's own 600/640 scheme:** ≤720 is the site's mobile band (§2 rule 15). **The tablet band keeps both and he was told** — he said "only on desktop", tablet is frozen and unnamed, and this is the reversible reading; it is one number when he calls the tablet round. ⛔⛔ **AND IT SHIPPED WRONG THE FIRST TIME, IN THIS PROJECT'S OWN SIGNATURE WAY:** the eyebrow rule was written up beside the breadcrumb block, which put it **ABOVE** the base `.svc-hero .eyebrow` at **IDENTICAL specificity** — so source order decided it and the eyebrow stayed on. Measured, not assumed. **It sits directly under the rule it overrides now, with a comment saying why it must stay there.** That is D106/D113/D114 for the fourth time. ⚠️⚠️ **A NEW TRAP, AND IT IS NOT CLOSED: `service.css` AND `stone.css` ARE NOT CONTENT-HASHED.** D190 closed the five-minute asset cache for `assets/site.css` and `site.js` only; the leaf stylesheets are linked bare, so a reload served the **previous edit's copy** and the fix looked broken on disk-correct CSS. ⭐ **The tell was `document.styleSheets`: both rules were parsed and present, in the WRONG ORDER — the order of a file I had already replaced.** Forcing `fetch(url,{cache:'reload'})` on the bare URL overwrites the entry the `<link>` uses. ⛔ **This will bite again, and it will bite the client on his phone.** | Phone only. Verified at 375 across all eleven page families — crumb, back arrow and head eyebrow all unpainted, no horizontal overflow, no console errors. Verified UNCHANGED at 1440 on both families. Landing page re-probed: **14285 / 18418 / 14641**, elements **2622**, `.gal-scroll` **5632 / 4950**, `feTurbulence` **60** — the §1 baseline to the number |
| D196 | 13 Aug | ⭐ **THE INTERNAL PAGE HEADS GET THE NIGHT KITCHEN BEHIND THEM.** Client: *"the hero section of the new internal pages have to have an image behind it. So the image of the kitchen may be the nighttime or the daytime one, but possibly the nighttime one."* ⭐ **THE LANDING HERO'S OWN PHOTOGRAPH**, same file and same srcset sizes, so it is already cached from the home page and costs a repeat visitor nothing — and an internal page can never show a different kitchen from the one that sold the customer thirty seconds earlier. ⛔ **A BACKGROUND, NOT AN `<img>`:** the head is decorative (the page's meaning is its H1), so it carries no alt text and stays out of the accessibility tree, and it cannot be dragged or long-pressed on a phone. ⚠️ **THE SHADE IS HEAVIER THAN THE HERO'S ON PURPOSE** — the landing hero puts its copy on the LEFT against a ramp that thins to 0.06 on the right, while the page head centres everything over the middle of the frame, which is the brightest part of this photograph. ⚠️ `--pageHeadImg` drops to the 1400px file ≤720px (the head is ~340px tall there; the 11 Aug rule is to size by the cover-scaled render width). ⛔ `background-attachment` stays `scroll` — `fixed` costs a full-viewport repaint per scroll frame, the exact class of thing D162–D166 removed. |
| D195 | 13 Aug | ⛔⛔ **THE PINNED GALLERY CANNOT LEAD A PAGE — THE "GIGANTIC GAP" ON /projects/.** Client, with a screenshot: *"why the fuck is there a gigantic gap between the title of our project gallery and the actual project gallery?"* **Measured: a 5632px runway with the stage pinned and the cards choreographed by scroll progress.** ⭐⭐ **NOTHING WAS BROKEN — THE SECTION WAS DOING ITS JOB WHERE THAT JOB MAKES NO SENSE.** On the landing page you arrive at the gallery having already scrolled, so the accordion has run; on /projects/ it is the FIRST section, so you arrive at the START of its runway, which is exactly the frame where the cards are gathered off-stage. The heading sat alone above five thousand pixels of nothing. ⭐ **THE FIX WAS ALREADY WRITTEN** — the plain column the phone has used since D163 — but its rules were locked inside `@media(max-width:720px)`, so no other width could have them. They are lifted to the global scope; **every one is gated on `.gal-static`**, a class the script only adds when `--galMode` reads `phone`, so the frozen landing page at desktop and tablet cannot see them. Moving them changes WHERE they can apply, not WHEN. ⭐ `build_pages.py` puts `.pg-col` on the Projects page's `<main>` and `.pg-col #gallery{--galMode:phone}` is the whole switch. ⛔ **A SECOND `matchMedia` IN JS WAS THE OBVIOUS REACH AND IS THIS PROJECT'S MOST REPEATED BUG** (D51, D59, D68, D78, D93, D105) — the stylesheet stays the only thing that decides. ⚠️ **AND A MISTAKE WORTH RECORDING:** the first attempt deleted the block and re-inserted it at the SAME index, so it never left the media query — the symptom was `.gal-mid` still computing to `display:block` instead of `contents`, with cards rendering ABOVE the heading. Check the destination, not just the diff. ⚠️ Landing page after: **14285** at 375, unchanged. |
| D194 | 13 Aug | ⛔⛔ **THE PHONE MENU GETS THE SUBMENUS, REVERSING D189's OWN REASONING — AND THE LOGO GOES HOME.** Client: *"did I not fucking ask you to create a drop down in the menu? So, like, for instance, on mobile, when someone clicks the menu, there is a little arrow next to the word services so they can go to the individual pages… And for every other option that that needs to be there… And then the logo also needs to work to take you back to the home page."* ⭐⭐ **D189 ARGUED ITSELF OUT OF THIS AND WAS WRONG, WHICH IS WORTH KEEPING.** Its reasoning: a phone cannot hover, and six more links would push the sheet past one screen to save one tap — so tapping Services opens the hub, which lists them. **That answers a question he did not ask.** He wants a service page reachable FROM THE MENU without loading a hub first; a caret that expands in place costs nothing until it is tapped. ⭐ **THE WORD AND THE ARROW ARE SEPARATE CONTROLS** — the word goes to the hub, the caret opens the list — which is exactly the split he described for the desktop nav, and it means neither can steal the other's tap target. ⛔ The toggle is a `<button>` and must NOT be nested inside the anchor: the sheet closes on any click that hits an `<a>`, so a nested caret would navigate AND expand on one tap. ⭐ **Stones got one too** (`#quartz`/`#marble`/`#granite` + compare — the collection page has read those off the URL for weeks); Projects, Estimate, About, Trade and Contact have no child pages and stay plain links. ⛔ **THE LOGO WAS `href="#hero"` IN BOTH THE HEADER AND THE FOOTER** — an anchor that only exists on the landing page, so on all six generated pages it pointed at nothing. Both are `/` now. ⚠️ **THREE BUGS THIS SHOOK OUT.** (1) The entrance stagger was `a:nth-child(n)`; two rows are `.mn-group` wrappers now, so Services and Stones would have opened at `opacity:0` — **missing entirely**. It counts rows (`> *:nth-child`) instead. (2) **`flex:none` was the real fix for the clipped sixth link, and the height cap was a red herring**: the panel asked for 216px and RENDERED at 194, because `.mobile-nav` is a flex column and the default `flex-shrink:1` squashed it once the sheet outgrew the viewport, cutting "Outdoor kitchens" in half. The overflow belongs to the sheet's own `overflow-y`. (3) The open height is written from `scrollHeight` rather than guessed, so a seventh link cannot outgrow it. ⚠️ **AND ONE FALSE ALARM WORTH RECORDING:** every row read `opacity:0` with the menu open, for twenty minutes — because the pane was `visibilityState:'hidden'`, so its transitions sat at `currentTime:0` forever. The cascade was correct all along (§8's trap, and `getAnimations()` is what proved it). |
| D193 | 13 Aug | ⛔⛔ **THE INTERNAL PAGE HEAD WAS REJECTED OUTRIGHT AND REBUILT.** Client: *"your internal pages look beyond shit… if they click on services, the main title is services with a description of the services that they do. Then it's not two dividers. It's just the one divider, and it has to have the same thing from the hero section like the Google, the ten year guarantee, those buttons also go below the subtitle text."* Then, with a screenshot: *"Why the fuck are there two dividers? You see?"* **THREE FAULTS, ALL FAIR.** ⛔ **(1) TWO RULES ACROSS THE PAGE ~35px APART:** `.page-head` draws its own curved hairline at its bottom edge AND the builder was emitting a `.section-divider` before the first section. **The head's edge IS the first seam** — the builder now skips the divider before section one and keeps every later one. ⛔ **(2) THE TITLE WAS A SLOGAN WHERE A LABEL WAS WANTED.** "Every kitchen began as one slab" over /projects/ is hero copy on a page whose job is to say where you are. **The H1 is the page's NAME now** — Services, Projects, Estimate, About us, Contact, Trade — and the selling line moved into the description. ⭐ **The eyebrow went with it:** once the title is the label, an eyebrow above it says the same word twice in two sizes. ⛔ **(3) IT WAS TEXT ALONE ON BLACK** — crumb, eyebrow, huge title, paragraph, then dead space. **The hero's four bubbles now sit under the description**, the hero's own `.hero-chips` markup lifted verbatim so the Google mark and the rating can never drift from the landing page's. Padding came down 9vh→6vh and 8vh→5vh with them. ⚠️ **TWO BUGS THIS TURNED UP.** `.hero-el` had to be STRIPPED from the copied bubbles: it is the hero's staged entrance, released by a class the HERO gets on load, so on a page with no hero the bubbles render at **opacity 0** — in the DOM, 89px tall, holding their space, invisible. It reads as "the chips were not inserted", which is the wrong thing to go looking for. And the **estimator's stone-picker modal is a SIBLING of `#estimator`, not inside it** (a full-screen dialog has to be, or the section's stacking context traps it), so /estimate/ shipped an estimator wired against nulls that threw at the IIFE's closing brace — a line number that points at nothing. It is carried with the section now. ⚠️ Two page descriptions were also rewritten because they repeated, almost word for word, the subtitle of the section directly beneath them. |
| D192 | 13 Aug | ⭐ **THE OTHER 166 PAGES' NAV WAS REPOINTED WITH THEM.** The stone, service, material, guide, area and sitemap pages all carried `<nav class="top">` with `/index.html#services`, `#gallery`, `#estimator`, `#about`, `#cta` — the landing page's anchors. Left alone, a customer on a stone page clicking "Projects" would land back on the landing page mid-scroll while the same word on the landing page opened a dedicated page. ⛔⛔ **THE BUILDERS WERE UPDATED IN THE SAME PASS, AND THAT IS THE PART THAT MATTERS:** that nav string is written by **four** scripts — `stones/build_stones.py`, `build_seo_pages.py`, `services/build_services.py`, `trade/build_trade.py` — so fixing only the generated HTML would have been undone by the next build of any of them. **166 HTML files and 4 builders, in one pass.** ⚠️ The 7 remaining hits are inside `stones/.removed-2026-08-10/` and `.removed-2026-08-11/` and are not live. ⭐ Verified after: `harvest/verify.py` still ✅ PASS (132/132/132), rule 1's in-house-fabrication scan returns nothing, and rule 11's centimetres scan returns only `index.html` — Judy Z.'s documented "10cm". |
| D191 | 13 Aug | ⭐ **THE `<select>` HAD TO BE UNDRESSED BY HAND, AND IT IS NOT PEDANTRY.** The trade form is the first form on this site with a dropdown, and a `<select>` is one of the few controls the browser paints ITSELF: left alone it arrived as a **white box with a system chevron**, sitting on a near-black card between two dark inputs — screenshotted, and it did not read as "a slightly different field", it read as a broken page. `appearance:none` surrenders the native paint and takes the arrow with it, so the chevron is drawn back as an inline SVG in the site's champagne. ⚠️ **`color-scheme:dark` IS THE HALF THAT IS EASY TO MISS:** it is what makes the OPTION LIST — drawn outside the page, where no CSS of ours reaches — come up dark. Without it the field looks right until it is opened. ⚠️ `<option>` gets an explicit background too, because Windows draws options from the control's own colours rather than the scheme. Added to `.cta-form`, so every future form on the site inherits it. |
| D190 | 13 Aug | ⭐⭐⭐ **THE INTERNAL PAGES ARE BUILT, AND `build_pages.py` IS THE ARCHITECTURE.** Client: *"create all of them, and do not stop until they are finished… this happens for desktop, tablet, and mobile."* **Six pages: `/services/`, `/projects/`, `/estimate/`, `/about/`, `/contact/` and a rebuilt `/trade/`.** ⭐⭐ **NOTHING WAS COPIED.** `index.html` is one file with an inline `<style>` and `<script>`, and that is the only place the signed-off design lives — so the builder LIFTS the real thing out at build time to `assets/site.css` and `assets/site.js` and composes each page from the landing page's own header, mobile nav, sticky bar, WhatsApp button, footer and `<section>` blocks. **One source for the chrome; a page cannot drift, and a fix reaches all seven on the next run.** ⛔ **To change an internal page's look, edit `index.html`'s `<style>` and re-run — never hand-edit a generated file.** ⭐ Each page leads with the thing it is named for and takes two or three supporting sections; **a page that repeated the whole landing page would teach a customer that the nav does nothing.** ⛔⛔ **THREE REAL BUGS CAME OUT OF THIS AND ARE WORTH KNOWING.** (1) **The shared script is FLAT top-level code, not IIFEs**, so one `wheel.addEventListener` on a null threw at the top level and every `const` below it stayed in the temporal dead zone — the symptom was `Cannot access 'glowItems' before initialization` from three unrelated subsystems, which names nothing. `orNull()` hands back a detached element; `buildCards` gained a null guard. (2) **Every relative asset path broke**: `assets/site/…` from `/services/` asks for `/services/assets/…`, and `url('assets/stone-floor.webp')` inside a stylesheet at `/assets/site.css` asks for `/assets/assets/…` — **a `url()` resolves against the STYLESHEET's address, not the page's.** 39 + 12 references made root-relative. (3) **The five-minute asset cache served a JS error that had already been fixed on disk** — assets are `public, max-age=300` while index.html is `no-cache`. Fixed permanently with **content-hashed URLs** (`site.js?v=<sha1>`), so a stale copy can never be served again, on the dev server or the client's phone. ⭐ Verified at **375, 768 and 1440**: every page 200, no failed requests, no console errors, no horizontal overflow, and the landing page unchanged at **14641** on desktop. ⚠️ **`/stones/` was NOT rebuilt** — the collection page with its 132 stones, search and compare already is the stones page; the nav simply points at it now. |
| D189 | 13 Aug | ⭐⭐ **THE NAV POINTS AT PAGES, NOT SECTIONS, AND SERVICES GETS A MENU.** Client: *"you can have a drop down in the nav bar menu showing that they can choose a specific one, not just go to the services page alone, and let's say click on the word services instead of clicking on the drop down… just understand how websites are laid out and do it perfectly."* ⭐ **THE WORD IS A LINK AND THE MENU IS A MENU** — the parent goes to the hub, the six children to the leaves, and nothing in it is a control that only opens something. ⛔ **THE HASHES ARE GONE FROM THE LANDING PAGE TOO**, which changes its behaviour: the smooth-scroll IIFE only intercepts `a[href^="#"]`, so these now navigate. That is the point, and he unfroze desktop and tablet for exactly this. ⛔ **THE `::before` BRIDGE ON `.nav-menu` IS LOAD-BEARING** — the header's 18px bottom padding leaves dead space between the link and the panel, and a pointer that leaves the trigger before it enters the panel closes the menu mid-travel. **That is the single most common way a CSS dropdown ships broken.** ⭐ Opens on hover AND `:focus-within`, so a keyboard reaches it with no JS at all. ⚠️ **The phone menu deliberately has NO submenu** — a phone cannot hover, and stacking six more links would push the sheet past one screen to save a single tap; tapping Services opens the hub, which lists all six. ⚠️ Footer "Explore" repointed with it, closing a comment that had been waiting for `/services/` to exist. |
| D188 | 13 Aug | ⛔ **REVERSES D186 ONE MESSAGE LATER: BOTH HERO BUTTONS ARE THE SAME SIZE, AND IT IS THE NARROW ONE THAT SURVIVED.** Client: *"you can make the get a free quote and call us button the same size again and make it the same size as what the call us button is now… and I want you to say, give us a call."* ⭐ **HE KEPT THE MEASURE AND THREW AWAY THE HIERARCHY, WHICH IS THE OPPOSITE OF WHAT D186 ASSUMED.** D186 read "the call us button can be smaller" as a request to state the primary action in the shape; what he actually wanted was the pair pulled in off the screen edges. So 76% stays and the difference goes — one width on both selectors, centred. Measured: both **254.6px**, neither overflowing. ⭐ The call button's phone label goes "Call us" → **"Give us a call"**, which is `.cta-long`'s wording arriving in `.cta-short`. ⚠️ Written as ONE rule covering both buttons rather than a second rule out-specifying the first — the D106/D113/D114 failure mode. |
| D187 | 13 Aug | ⭐ **THE HERO PHOTOGRAPH GOES DOWN A STOP ON THE PHONE.** Client: *"we might need to make the background image slightly darker so that the words stand out."* ⭐ **ONE FLAT WASH, NOT A RE-TUNED GRADIENT STACK:** `#hero .hero-shade{background-color:rgba(7,7,9,0.30)}` in the phone block. `.hero-shade` carries four layers and the big one is a LEFT-TO-RIGHT ramp (0.94 at the left edge → 0.06 at 74%) because the desktop composition puts copy left and photograph right. **The phone centres everything, so the title sits over the ramp's thin end** while the shading is spent on an empty edge. ⛔ Re-weighting those stops for the phone would mean maintaining two versions of a hand-tuned four-layer stack for one device. `background-color` is a separate longhand, so it lands UNDER all four gradients without editing any of them. ⚠️ 0.30 is deliberately modest — he asked for *slightly* darker and the photograph is the one thing on that screen that says what the business sells. |
| D186 | 13 Aug | ⭐⭐ **THE HERO CTAs GET THEIR LONG LABEL BACK, THE CALL BUTTON SHRINKS, AND THE FOUR BUBBLES BECOME A GRID.** Client: *"it needs to say get a free quote and then call us. The call us button can be smaller than the get your free quote… and I want the buttons below the call us to be visually balanced out somehow that it looks neat."* **This closes the open question D177 left** (short vs long labels). ⭐ Only `.cta-short` changed — "Free quote" → **"Get a free quote"** — because that span is the phone's and the frozen desktop reads `.cta-long` ("Get your free quote"), the D120 two-spans-one-shown idiom. ⭐ The call button takes `width:76%` against the quote's 100%: two stacked buttons of equal width read as two equal offers and the page has one primary, which is the same thing the sticky bar says with `flex:1.35`. ⭐⭐ **THE BUBBLES WERE WRAPPED FLEX AND COULD NOT BE TUNED INTO TIDINESS** — each was `flex:0 1 auto`, so all four sized to their own text and the centred rows left no two edges aligned with each other or with the buttons. **That is what content-sized boxes DO; no gap value fixes it.** A 2×2 grid makes the column the authority: measured 163 / 163 / 163 / 163 with the block flush to the quote button's own edges. ⚠️ Chip padding 13px → 10px, because the text now has to fit the box rather than the box the text. |
| D185 | 13 Aug | ⭐ **THE BURGER MENU GETS THREE ACTIONS.** Client: *"on that drop down page, there needs to be a get a free quote, call us, or the WhatsApp icon. So free quote, WhatsApp, or phone, just three CTAs. Doesn't have to be the way that I say they need to look. Just needs to look great."* Built as `.mn-row`: the filled **Get a free quote** slab full width, then **WhatsApp** and **Call us** as a ringed pair on one row. ⭐ **ONE FILLED, TWO OUTLINED IS THE PAGE'S OWN GRAMMAR** — the header, the sticky bar and the hero all say exactly one gold slab with quieter actions beside it; three filled buttons would give the sheet three equal shouts and no answer to "what does this page want me to do". The two outlined ones take `.mbar-a`'s dress on purpose: the sheet and the sticky bar are the same three actions and a customer who has seen one should recognise the other. ⛔ **THE NUMBERS ARE D178's TWO, NOT ONE** — WhatsApp to the mobile `447464940287`, Call to the 0800 landline. ⚠️ Row width capped at `min(320px,78vw)`: the links above are centred type with no box, so an action block running to the screen edges would read as a different page. |
| D184 | 13 Aug | ⛔⛔ **THE BURGER LOSES ITS BOX IN THE BASE RULE AND THE STRIPES ARE SIZED TO THE LOGO — HE ASKED TWICE.** Client: *"in the mobile nav bar there are those three lines that it currently has a box around those three lines… Remove the box around those three lines and resize the three lines to match the size of the logo."* ⭐⭐ **WHY HE SAW IT AGAIN, WHICH IS THE LESSON:** D132 undressed the burger inside the PHONE QUERY ONLY, deliberately, to leave the frozen tablet alone — so the box was still live at 721–1120px and the resize half of the instruction was never done at all. ⭐ **THE BASE RULE CANNOT REACH THE FROZEN DESKTOP HERE:** `.nav-burger` is `display:none` in the base and only switched on at `max-width:1120px`, so the base rule and the tablet band are the same surface — verified `display:none` at 1440. ⭐ **"Match the size of the logo" is taken as the logo's HEIGHT, 34px** (`.brand-logo`, the single number that sizes the client's artwork): stripes now **34px wide in a 26px-tall block**, against 24 × 15.6. Stroke 1.6px → 2px, because a 24px stripe and a 34px stripe at the same weight read as the same mark stretched rather than a bigger one. ⚠️ The 44px box stays and is NOT decoration — it is the tap target. ⭐ D132's phone override was **DELETED rather than left to be out-specified**, this project's own reverting rule. |
| D183 | 13 Aug | ⭐ **THE MOBILE NAV BAR'S BOTTOM LINE TAKES THE HERO'S CURVE, MUCH SMALLER.** Client: *"the same curve we have at the bottom of the hero section, I want the nav bar on mobile to have that same curve to it, but much smaller. The curve just happens at the ends on that gold line at the bottom."* ⭐ **SAME MECHANISM, NOT A LOOKALIKE** — a 1px BORDER on a box carrying `border-bottom-left/right-radius`, hung out past the screen by `--navCurveOut` so the arc is cut at the edge mid-sweep, exactly as `.hero::after` does. A border follows `border-radius` and stays 1px doing it; a gradient can only ever draw a straight band. `--navCurveR:18px` against the hero's 48px, same out-hang-to-radius proportion, so the line is dead flat across the middle ~340px of a 375px screen and only turns in the last ~13px each end. ⛔ **THE GRADIENT RAMP IS THE PRICE AND IT IS PAID KNOWINGLY:** the straight version fades 0.08 → pale champagne → 0.08; a border takes ONE colour, so the ramp cannot survive. The colour chosen is the ramp's own midpoint so the line reads at the same weight. Keeping both needs two stacked pseudo-elements and the header has two already. ⚠️ Phone only; the 5px out-hang is safe because `html`/`body` carry `overflow-x:clip`. |
| D182 | 13 Aug | ⭐⭐ **THE "TWO LINES IN THE CORNER" WAS THE DIVIDER'S OWN TRAVELLING SHINE, PARKED.** Client, with a screenshot: *"the divider has this weird thing that's wrong on the side of it, in the corner. It's making these two lines one above one at the bottom."* ⭐⭐ **DIAGNOSED, NOT GUESSED — AND THREE WRONG CANDIDATES WERE RULED OUT FIRST.** A DOM scan for any line-like element within 18px of any of the eight dividers returned **nothing**, and the hero's bottom arc was screenshotted at 8× and is clean and continuous. The cause is `.sd-line::after`: it is `top:-2px;bottom:-2px` on a 2px rule, so it is **6px tall against a 2px line and blooms above and below on purpose**. Invisible mid-travel — but `--shine` is CLAMPED to [0,1], so **every divider below the fold sits at exactly 0% and every one above it at 100%**, and at those two values the 97.5px lozenge is parked half off the screen with its rounded blurred cap showing. A bright edge 2px above the line and another 2px below is exactly what he photographed, and it is permanent rather than transient. ⭐ Fixed at the cause with `--shineA`, an alpha written by the same IIFE that fades the highlight out before it reaches either end. **Verified: every parked divider now reads alpha 0.000 and the one mid-travel reads 1.000.** ⛔ **NOT `overflow:hidden` on the divider** — that clips the bloom for the whole travel, flattening the effect everywhere to fix it in two places. ⚠️ Applied at every width, `.strip-rule` included: the parked lozenge is a defect on desktop too and would only be reported again from another device. |
| D181 | 13 Aug | ⭐ **THE QUOTE BUTTON GOES FLAT.** Client: *"the get your free quote in the desktop nav bar and in the mobile sticky bar has that three d pattern on it. Remove the three dness of it and just make it two d again."* `.bar-cta` and `.mbar-cta` (and `.mn-cta` with them) drop the vertical `gold-hi → gold → #A98A48` metal ramp, the `inset 0 1px 0` top light with its matching `inset 0 -1px 0` bottom shade — a milled bevel — and the coloured drop-glow. Flat `var(--gold)` and the antique-gold edge remain. ⭐⭐ **ALL THREE HAD TO GO TOGETHER:** removing the bevel alone leaves a ramp that still shades top-to-bottom and still reads as a curved surface; removing the ramp alone leaves a lit edge on a flat fill, which reads as a mistake rather than a decision. ⭐ **The hover sweep STAYS** — a travelling highlight is a 2D animation on a 2D surface, and it is the button's only hover affordance. ⚠️ **He named the DESKTOP nav bar himself, which is what unfroze that base rule** (§2 rule 15). Desktop document height after: **14641**, the §1 baseline unchanged. |
| D180 | 13 Aug | ⛔⛔ **REVERSES D173: THE WHATSAPP BUTTON SITS IN THE BOTTOM CORNER AND RIDES THE STICKY BAR.** Client, with a screenshot: *"the WhatsApp button is way too high up. It needs to stay in the bottom corner. And then as the sticky bottom bar pops up, it stays just above that in its position."* ⭐ **THE OLD REASONING IS KEPT IN THE CODE ON PURPOSE.** D173 parked it at `bottom:73px` PERMANENTLY on the theory that a button which moved every time the bar left would read as a glitch. He looked at it and said the opposite, and he is right: with no bar under it the button floats in the middle of nothing and the corner reads as broken. ⭐ **IT NOW RIDES THE BAR'S OWN STATE AT NO COST** — `.mbar` already gets `.on` from the IIFE at the foot of the file and `.wa-fab` is its LATER SIBLING, so `.mbar.on ~ .wa-fab` reads that state directly: **no second scroll listener, no class on `<html>`, nothing for a media query to disagree with** (the D51/D59/D68/D78/D93/D105 family). ⛔ The two elements must stay siblings in that order or this rule dies silently. `bottom` is transitioned over the bar's own .5s so they move as one gesture. **Measured: 18px from the floor with the bar away, 73px with it in, and exactly 14px of clearance above the bar.** |
| D179 | 13 Aug | ⭐ **THE STONE SELECTOR NOW COMES BEFORE THE PROJECT GALLERY — ON THE PHONE.** Client: *"swop the chose your stone and project gallery section."* Painted order on the phone is now services · divider · **Choose your stone** · divider · **View our project gallery** · the estimator. ⛔ **THE MARKUP DID NOT MOVE, AND THAT IS THE WHOLE DESIGN OF THIS CHANGE.** §2 rule 15 freezes desktop and tablet, and a section swap is **NOT ADDITIVE** — unlike D173's floating button and D178's social row, which were built at every width because they re-compose nothing, reordering two sections rewrites a composition the client signed off at D91. So the four elements between the services divider and the brand strip (divider · `#gallery` · divider · `#stones`) are wrapped in **`.gs-swap`**, which is **`display:contents` in the base rule — an element that generates NO BOX AT ALL** — and the phone query turns it into `display:flex;flex-direction:column` and gives the four children `order:1..4`. ⭐ Same D120 idiom as `.strip-swap`: new markup exists at every width the moment it is added, so it defaults to a no-op and the phone opts in. **Proved by measurement, not by reasoning:** at 1440×900 document height **14641** and at 768×1024 **18418** and `.gal-scroll` **5632** — every one of them the §1 baseline to the pixel — with the wrapper computing to `display:contents`, all four `order`s at `0`, and `#gallery` still above `#stones`. Element count 2577 → **2578** at both widths, which is the wrapper div and nothing else. ⛔ **THE SEAM COMPENSATION HAD TO TRAVEL WITH THE GALLERY, AND THIS IS THE COST-STAYS-BEHIND TRAP FOR THE FIFTH TIME** (§7.2 of the START HERE; previously `animPx`, `--gold-lo`, the FAQ drawer and `#gallery`'s own `margin-top:-70px`). D158 had zeroed `#gallery + .section-divider`'s top margin because `.gal-mid` ends 83px after its last visible element — its bottom padding reserves the sticky bar — so that seam was paying for the gap twice. **That selector is DOM order, not paint order:** after the swap it names the divider ABOVE the gallery, where there is no reserve to double up with, and it would have pulled the stones → gallery seam 48.7px tight while the gallery's real bottom edge — now the brand strip's `.strip-swap` divider — paid the doubled gap instead. The rule moved to `.strip-band .strip-swap{margin-top:0}` and D158's comment carries the warning. **Measured after, at 375×812: 48 / 49 / 49 / 48 / 0 / 49px across the six seams** — every symmetric seam symmetric, and the one deliberate zero exactly where the gallery's own 83px reserve sits. ⚠️ **The brand strip stays where it is**, still the stones → estimator boundary in source; on the phone it is only the plain divider anyway (D143), so it reads as the gallery → estimator seam and needs nothing. ⭐ **The nav order was NOT changed** — Services · Projects · Stones · Estimate — because the top nav and the burger menu are the same markup at every width and desktop is frozen. Flagged to the client. ⭐ **To revert: delete `.gs-swap`'s two tags and its three CSS blocks.** Verified with no console errors, the wheel still stepping Azul Shimmer → Bianco Glacier, and a gallery card still opening and closing its project. |
| D178 | 13 Aug | ⭐⭐ **THE REAL WHATSAPP NUMBER IS IN, AND THE FOOTER GETS THE SOCIAL LINKS.** Client: *"07464940287, this is the WhatsApp number that's gonna be linked to the WhatsApp button, and anywhere else we'll be talking about WhatsApp. Then go find all of TopCat's social media or some sort of links and we add that to the footer. Then keep the landline number that's currently on the site as the call number for the call buttons."* ⛔ **TWO NUMBERS, ON PURPOSE, DO NOT TIDY THEM INTO ONE:** WhatsApp goes to the mobile `447464940287` (the leading 0 dropped for the country code, which is the only form `wa.me` accepts), and **all ten `tel:` links stay on the 0800 098 2812 landline** — verified by count after the change. This closes D173's open blocker. ⭐⭐ **THE SOCIAL LINKS CAME OFF TOPCAT'S OWN LIVE SITE, NOT OFF A SEARCH, AND THAT DISTINCTION IS THE POINT.** `topcatworktops.co.uk` links to **Instagram** (`instagram.com/topcatworktops/`) and **LinkedIn** (`linkedin.com/company/topcat-worktops/`) and nothing else; both were then checked and both return 200. ⛔ **A BRAND-NAME SEARCH IS ACTIVELY DANGEROUS HERE** — it surfaces Top Cat Furniture, Top Cat Media Group and several unrelated "topcat" handles, so a plausible-looking guess would have put a stranger's account on the client's footer. ⛔ **NOTHING WAS INVENTED TO FILL THE ROW**: `facebook.com/topcatworktops` returns 400 and does not exist; `tiktok.com/@topcatworktops` answers 200 but serves a 1.4KB stub with no profile and is not linked from their own site, so it was NOT trusted and NOT added. **Facebook, TikTok and YouTube have to come from him.** ⭐ WhatsApp rides along as a third icon since it is now a real channel. ⭐ The two profiles were also added to the JSON-LD as `sameAs`, which is where search engines actually read them; the block still validates and still carries no `reviewCount`/`aggregateRating` (§2 rule 4). | All widths — he named the footer and the row is additive. ⚠️ **It costs +33px of document height at 768** and nothing at 1440; **+17 elements**, fully accounted |
| D177 | 13 Aug | ⛔⛔ **THE TITLE COMES BACK UP AND THE BUTTONS STACK AGAIN — PARTLY REVERSING D174 AND FULLY REVERSING D171's SIDE-BY-SIDE, EACH ONE MESSAGE OLD.** Client: *"move the title back higher up so there's not so much space between the navbar and the title… let's stack the get a free quote above the call us again."* ⭐ D174 had pushed the title from 143 to **219** because removing the scroll arrow freed ~100px and he had just said the gaps were too big — so the space went above the title. That was too far: the fixed nav ends at 78px, leaving ~140px of empty ground under it. It is **80px** from the nav now. ⭐ **D174's EVEN GAPS ARE UNTOUCHED** — 35/34/35 is what he asked for and did not complain about; only the block's start moved, and stacking the buttons puts ~55px back into the content so the foot fills without the top holding it. ⚠️ **THE SHORT BUTTON LABELS WERE KEPT AND HE WAS TOLD.** He asked to change the arrangement, not the wording, and "Free quote" / "Call us" are his own words; the clipping constraint that forced them (224px of label in a 161px button) is gone with the row, so restoring the long labels is one line. ⚠️ `width:100%` rather than letting the 11 Aug derived width return — that expression sizes against the fact row, which has been `display:none` since D169. | Phone only |
| D176 | 13 Aug | ⭐ **THE SCORE MOVES DOWN BESIDE THE STARS: "Google" over "★★★★★ 5.0".** Client: *"I want the five point o to be next to the stars on the Google thing, so make the stars smaller."* Top line is the source, bottom line is the whole rating read as one phrase. Stars 11px → **9px** with tighter tracking, because they no longer own the line and the pair still has to fit inside the mark's 26px. ⚠️ `align-items:baseline`, not `center` — star glyphs and lining numerals have very different vertical centres, and centring leaves the score visibly riding high. ⭐ The mark and the stack still measure 26px and 26px, so D170's "all the same height as the Google icon" survives the change. | Phone only |
| D175 | 13 Aug | ⭐ **THE PHONE GETS ITS OWN HERO SUBTITLE: "Quartz, granite and marble worktops, chosen with you and fitted by our own team."** Client: *"change the subtitle to say something about stones and the services they offer, not anything specific, just talking about this is what they do, and make it very simple."* ⛔ **THE DESKTOP LINE IS UNTOUCHED** — he is looking at the phone, desktop is frozen (D91), and this file already has the idiom for exactly this in `.svc-sub-long` / `.svc-sub-short` (D96). ⚠️ **THE PHONE LINE DROPS THE PLACE, WHICH IS A REAL CHANGE AND NOT AN OVERSIGHT.** "across London and the Home Counties" is the region phrase §4b argues for, but a place is the most specific thing in the sentence and he asked for the opposite. It still appears in the footer, the FAQ, the schema and the page title, so nothing is lost to search — **say so if he wants it back**. ⭐ It also retires the `.nowrap` span on the phone and with it the 283px unbreakable phrase that never fitted a 320px screen, so the new line sets as two clean lines at 375 and needs no defending. ⛔ Claims only what is true: the materials and the service, templating and fitting are TopCat's (§2 rule 1), and there is no absolute in it (rule 12). | Phone only |
| D174 | 13 Aug | ⭐⭐ **THE SCROLL ARROW IS GONE FROM THE HERO AND THE GAPS COME BACK DOWN.** Client: *"remove the scroll arrow at the bottom of the hero section and optimise the spacing… move the surfaces worth building around down, and get some even spacing in there where things don't look cluttered, but right now the gaps in between is just too big."* ⭐⭐ **THE TWO HALVES ARE ONE FIX AND THE ARROW IS WHY — this is the third spacing pass and the first one that could actually do what he wanted.** D169 and D171 both had to spend their slack on the gaps because `.hero-cue` was a hard floor: an absolutely-positioned arrow at `bottom:30px` that content could not run into, so the only place to put ~100px of spare height was BETWEEN the elements — which is precisely the "gaps are too big" he is now objecting to. **Removing the arrow removes the floor**, so the space goes above the title instead: the block moves down (title top 143 → **219**) and the gaps tighten to an even **35 / 34 / 35**. ⭐ "Even" is built, not assumed — one value drives all three, with **+5px on the title** because a display serif's line box sits ~4.5px inside its own margin, measured across three passes. Equal margins render as a visibly smaller first gap. ⛔⛔ **AND A REAL BUG WAS FOUND DOING IT, TWICE, BOTH MINE: PROSE APPENDED AFTER A CLOSED `*/` SAT IN THE STYLESHEET AS BARE CSS.** The parser discarded it *and the rule immediately after it* — which is why `#hero .hero-cue{display:none}` silently did nothing while the rules further down applied normally. ⭐ **IT LOOKS EXACTLY LIKE A SPECIFICITY PROBLEM AND IS NOT ONE**; the tell is that the rule is absent from `document.styleSheets` altogether, not merely outranked. **The first instance had been live since D169.** ⭐ There is now a check for it — walk the `<style>` block counting `/*`/`*/` and braces; it takes seconds and it would have caught both. | Phone only. Frozen widths keep the arrow, the base padding and the base margins — verified |
| D173 | 13 Aug | ⭐ **A FLOATING WHATSAPP BUTTON, BLACK AND GOLD, BOTTOM-RIGHT.** Client: *"add a floating WhatsApp icon button in the black and gold style in the bottom corner of the site for people to click on it and go straight to WhatsApp."* ⛔⛔⛔ **THE NUMBER IS THE 0800 LANDLINE AND IS ALMOST CERTAINLY NOT A WHATSAPP ACCOUNT — THIS IS THE ONE UNFINISHED THING ON THE BUTTON AND IT BLOCKS GO-LIVE.** WhatsApp accounts sit on mobile numbers; a UK 0800 freephone cannot normally register one, so `wa.me/448000982812` will most likely open WhatsApp and report the number is not there, **which is worse than having no button**. It is wired to the only number this project holds so the thing is complete and testable — **swap the digits in the `href` for the real WhatsApp business number in full international form, no `+`, no spaces.** Nothing else changes. ⛔ **NOT WHATSAPP GREEN**, at his word ("the black and gold style") and for a reason worth keeping: the site has exactly one multi-colour element on purpose (the Google mark, D170) and a green badge would take that meaning away from it. The glyph's shape is unmodified, which is what WhatsApp's terms actually require. ⚠️ **IT IS ON EVERY WIDTH — a reading of "the site" — and desktop is frozen (D91), so it is deliberately the ADDITIVE kind of change**: a fixed overlay that displaces nothing and alters no frozen composition. One rule makes it phone-only if he prefers. ⛔ `z-index:38` is chosen: just under `.mbar` (39) so the two never argue about the corner, and therefore under the mobile nav (40), the project overlay (120) and the estimator modal (200). On the phone it clears the sticky bar by its real height + 14px + `env(safe-area-inset-bottom)` — verified 14px clear — and it stands down with everything else on `html.nav-open`. | All widths; **+4 elements** |
| D172 | 13 Aug | ⭐ **THE THREE REASONS COME BACK AS BUBBLES.** Client: *"then we have the more reasons, like, we will list it out in the same bubble kind of way… it's a seventy two hour aftercare, we will say quick turnaround time, all in the same bubble style as the Google and 10 year guarantee."* ⚠️ **THIS IS THE SAME CONTENT D169 REMOVED, IN A DIFFERENT DRESS, AND THAT IS THE POINT** — what he rejected was three icons each with a heading and a sub-line, not the facts. The icon row stays `display:none` and stays intact for desktop. ⚠️ **READ AS TWO BUBBLES, AND HE WAS TOLD**: "it's a 72-hour aftercare, we will say quick turnaround time" can be read as two reasons or as one re-worded reason. Two is the reversible reading — deleting one is a line — so two were built and the ambiguity was put to him. ⭐ They take `.chip` whole, which is what "the same bubble style" asks for literally; nothing new is styled. ⛔ Neither line states an absolute (§2 rule 12): "72-hour aftercare" is the existing live promise from the fact row, and "Quick turnaround time" is deliberately vague rather than a number we would have to keep. | Phone only via D120's default-hidden idiom; four bubbles wrap to two rows |
| D171 | 13 Aug | ⛔⛔ **THE HERO IS RE-ORDERED: PLAIN SUBTITLE, BUTTONS SIDE BY SIDE. REVERSES D169's SUBTITLE RING ONE MESSAGE AFTER ASKING FOR IT.** Client: *"we're gonna have the normal subtitle text with no border around it. Then we will have the free quote and call us buttons right next to each other."* ⭐ The ring's rules are DELETED rather than overridden, so the line falls back to the base `.hero-sub` exactly as it was — same reason D167's card revert deleted rather than out-specified. ⭐⭐ **AND THE LINE BREAKS BETTER WITHOUT THE BOX**: D169 had to defend a stranded "by us" through a 7px padding window because the pill cost the sentence 48px of measure; at the full 335px the whole first sentence fits and it sets as a clean two lines with nothing riding on Montserrat's metrics. **The problem D169 solved was one D169 created.** ⛔⛔ **THE BUTTON LABELS HAD TO SHORTEN AND THAT IS NOT A STYLE CHOICE.** Side by side, each button is ~161px; "Get your free quote" needs **224px** at its tracked size, and these buttons are `white-space:nowrap` inside `overflow:hidden` — **a label that does not fit is not a wrap, it is a silently clipped word** (the trap the 11 Aug comment on `.hero-ctas .btn-*` already records). The phone shows "Free quote" and "Call us", which are **the client's own words for these two buttons**, via D120's two-label idiom; every width above 720px keeps the full labels to the character. ⚠️ The 11 Aug derived width (`66.667% + --hfGap*0.667 + --hfIcon`) also had to go: it measured against the fact row, which has been `display:none` since D169, so it was already sizing to something invisible. | Phone only. Verified: buttons 161px each, neither clipped |
| D170 | 13 Aug | ⭐⭐ **THE GOOGLE CHIP IS REBUILT AROUND THE GOOGLE MARK, AND THE GUARANTEE SITS BESIDE IT.** Client: *"put the Google icon next to the five stars… the Google icon, then it will have the word Google on top and the five stars below, and it's all the same height as the Google icon"*, and *"have the ten year guarantee next to the Google review part."* ⭐ **"THE SAME HEIGHT" IS BUILT, NOT APPROXIMATED** — the mark is 26px and the stack is given the same 26px with `justify-content:space-between`, so "Google" and the stars land on the mark's own top and bottom edges however the two type sizes are later nudged. Verified: mark 26px, stack 26px, both chips 44px. Setting two font sizes and hoping they sum to 26 is what would drift. ⚠️ The pair were already siblings and already flex; what put the guarantee on its own line was `flex-wrap:wrap` at 375px, so `nowrap` plus tighter chip padding is the whole fix, and `align-items:stretch` keeps a two-line pill and a one-line pill the same height — two different heights side by side read as a mistake. ⛔⛔ **THE GOOGLE "G" IS IN ITS FOUR OFFICIAL COLOURS AND MUST NEVER BE RECOLOURED TO THE SITE'S GOLD.** It is Google's trademark, present to attribute the rating to its source — the one use that makes a third-party mark legitimate here. Recolouring is what Google's brand terms prohibit, and a gold "G" would also quietly stop reading as proof. **It is the only multi-colour element on the site, on purpose: that is what makes it somebody else's verdict rather than our decoration.** ⚠️ On the phone `.chip-legacy` ("5.0 on Google") is CLIPPED rather than `display:none`d, so it stays in the accessibility tree — a logo and five glyphs say nothing to a screen reader. | Phone only via D120's default-hidden idiom; **+9 elements at every width, fully accounted** (svg + 4 paths + stack + 2 spans + legacy span) |
| D169 | 13 Aug | ⭐ **THE HERO: THE SUBTITLE TAKES THE CHIPS' RING, THE THREE ICON FACTS COME OUT, AND THE SPACING IS RE-CUT.** Client: *"just like the five stars on Google, add that same border and golden ring around the subtitle"*, *"remove the three icons with the service second to none, every cutout, and the aftercare"*, *"optimise the spacing inside the hero section."* ⭐ The ring is `.chip`'s own four values, not a lookalike, so the two cannot drift apart; **only the radius could not be copied** — `999px` is a capsule on one line and a bulging lozenge on the two the subtitle always runs to, so it is 18px. ⚠️ The facts are HIDDEN, not deleted: the same `<ul>` is what every width above 720px shows, and the L-plan icon alone took five attempts (its comment records why it is an L and not a rectangle). ⛔⛔ **THE SPACING IS THE INTERESTING PART AND IT IS THE D157 LESSON AGAIN: removing the fact row left 101px of slack, and because `.hero-inner` is a block in normal flow it ALL pooled at the bottom as one hole.** Spent on the gaps rather than taken off `.hero{min-height:90vh}`, which is D97's and was tuned so the bevel and the top of the reviews both land on the first screen — shrinking the hero would have silently re-opened a question he has already answered. ⭐ The ceiling is the absolutely-positioned scroll cue at `bottom:30px`, so only ~60 of the 101 can be spent; ~21px is left clear. ⚠️ Centring `.hero-inner` was measured and rejected — its box is 629.5px in a 731px hero, so centring moves the content DOWN 50px, the opposite of the complaint. ⛔⛔ **AND PUTTING THE LINE IN A BOX COST IT ITS BREAK POINT**, which is worth keeping: `.nowrap` holds "across London and the Home Counties." together at **283px** (§4b — without it the place name broke as "London and the / Home Counties"), and "Chosen from the slab you approve, fitted" is **290px**, so the inner measure must land in **[283, 290)** or the line strands "by us" on its own. 24px a side puts it at 287. ⚠️ **`text-wrap:balance` WAS TRIED AND IS WRONG HERE** — it evens the lines it can break and cannot break the 283px phrase, producing 153/154/20/283, a ragged ascending wedge worse than the orphan; `pretty` changed nothing. ⚠️ **AND IT IS A 7px WINDOW — if Montserrat fails to load the break reverts to the orphan.** Cosmetic on a fallback font, not a broken layout, but build nothing else on those two numbers. | Phone only. ⚠️ **320px needed its own step**: 283px of unbreakable phrase against 280px of screen is arithmetic, so ≤360px takes the type to 13px — it was already spilling ~3px through the old borderless subtitle, where nothing drew the eye to it |
| D168 | 13 Aug | ⛔ **TABREZ CHAUDHRY'S REVIEW IS REMOVED. JHANZEB CHAUDHRY STAYS. THE RANGE IS 15 REVIEWS.** Client: *"remove the review from Tabrez because there is another one of his family members in the review as well. So Janzeb you can keep, and Tabrez you can take away."* One surname carrying two reviews reads as a family vouching for itself. ⛔⛔ **THE FIRST ATTEMPT WAS WRONG IN A WAY WORTH RECORDING: IT WAS ADDED TO THE EXISTING `.filter(r=>!/luke|copley|thadeus/i…)` INSTEAD OF DELETED.** That removed the card from the deck — 15 instead of 16, verified — **and left the review verbatim in the page source**, because `REVIEWS` is an inline array literal in `index.html` and the filter runs at load. ⭐ **"Not rendered" is not "removed": the text still shipped to every visitor, was still in view-source, and this repo is PUBLIC (§4).** The row is now deleted outright; `tabrez` stays in the filter as a guard so a wholesale re-import of the Google reviews cannot quietly restore it, and it now matches nothing. ⚠️ **HE SAID "JANZEB"; THE PAGE SAYS "JHANZEB" AND THAT WAS KEPT** — the spelling on the Google review, and a phonetic spelling in a voice note is not an instruction to rename a real person. **Same call as Rimsha/Remsha, which is still open (§10 q2).** ⛔ §2 rule 4 is unaffected and must stay that way: the count is never shown, and there is no `reviewCount` or `aggregateRating` in the schema, so the deck simply builds 15 cards and no number appears anywhere. | All widths — it is data, not design. Accounts for the **−49 elements** at both frozen widths |
| D167 | 13 Aug | ⛔⛔⛔ **THE PHONE'S REVIEW CARD IS BLACK AGAIN. REVERSES D116, AND WITH IT D156 AND D160, WHICH ONLY EXISTED TO REPAIR D116.** Client: *"let's revert the review cards back to what it was before. It had the darker stone design background, the lighter gold stars, and the white grey text that it had."* ⭐ **THE WHOLE LIGHT-CARD BLOCK WAS DELETED RATHER THAN OVERRIDDEN, AND THE PHONE NOW FALLS THROUGH TO THE BASE RULES** — `.rev-face` back to `linear-gradient(155deg,#15151b,#0e0e12)`, `.stars` back to `--gold`, `.quote` back to `--bone`, and the `.rev-stone::after` veil back to the near-black 0.55→0.72. Verified computed: card `rgb(21,21,27)→rgb(14,14,18)`, quote `rgb(244,241,234)`, stars `rgb(198,166,100)`. ⛔ Overriding an override would have left two competing descriptions of one card in the file, which is exactly how D106, D113 and D114 each lost a rule to a later one at equal specificity. ⛔⛔ **KEEP THIS ROW — THE REASON D116 EXISTED IS THE REASON NOBODY SHOULD REBUILD IT.** It was his own ask (*"give the reviews a cream white design… so that the review part stands out a little bit more"*), built exactly as described from the brand's own two colours swapped over, and it then needed **two further rounds of repair that were both real faults**: D156 (the only pure white on the page, read as paper not stone; the move also forced the champagne off `--gold-lo`, which measured **3.42:1** against the new base and would have shipped under AA) and D160 (`marbleSVG()`'s cream hairlines became the highest-contrast thing on a grey face and ran diagonally through the paragraph — body text measured **13.7:1** and was still hard to read). ⭐⭐ **THE LESSON SURVIVES THE REVERT AND IS GENERAL: a light card on this page has to fight the page's own stone texture, and a veil alpha is not portable between two base colours.** The dark card has neither problem — its veil was designed for this ground and has been running untouched on the frozen widths throughout. ⚠️ D119's gutter arrows, D145's removed entrance and D166's rasterised marble are separate rules and all stay; only the wash over the stone reverts. | Phone only — D116 was scoped to `.rev-solo` (`perPage()===1`, below 720px), so **nothing above 720px moves by one pixel.** Verified: `rev-solo` is false and the card is already dark at 1440 and 768 |
| D166 | 13 Aug | ⭐⭐⭐ **THE LAG AND THE SELF-RELOAD ARE ONE FAULT, THEY ARE NOT HIS PHONE, AND THE PAGE WAS MEASURED BEFORE ANYTHING WAS CHANGED.** Client: *"the review section, the animation, is extremely laggy. And then as you scroll down, some of the rest of the parts of the site is also laggy. And when I scroll too quickly, for some reason, it's just reloading the site on my side. It could be a problem on my phone, it could not, but something's just wrong."* ⭐ **HE IS RIGHT, AND HIS THREE CLAUSES ARE ONE ROOT CAUSE WITH THREE SYMPTOMS — the D157 lesson (do not collapse his clauses) inverted: this time they really did share a cause, and it was found by counting rather than by looking at any one section.** ⛔⛔ **MEASURED AT 375×812 BEFORE ANY EDIT: 431 elements carrying a live `filter`, of which 63 were `feTurbulence` and 42 `feGaussianBlur`; and 107 elements holding `will-change`, i.e. 107 permanently promoted compositing layers, 67 of them the stone wheel's slabs alone.** ⭐ **WHY THE REVIEWS ARE THE WORST OF IT, WHICH IS THE PART THAT NAMES THE BUG:** `marbleSVG()` stacks THREE `feTurbulence` (octaves 3, 3, 4) plus two displacement maps, and it is called with a DIFFERENT SEED for each of 16 review cards and 6 Why tiles, so nothing can be cached between them. A live SVG filter is re-evaluated whenever its element is re-rasterised, and **a transform re-rasterises** — so every frame of the review roll was re-computing per-pixel fractal noise for the card in flight. ⭐ **AND WHY THE TAB RELOADS:** iOS Safari kills and silently re-loads a tab whose renderer exceeds its memory budget, and a promoted layer costs `w × h × 4` bytes whether or not anything moves. It presents as "it reloaded", never as "it ran out of memory", which is why it has read as a phone problem for three rounds. ⭐⭐ **THE FIX CHANGES THE RENDERING PATH AND NOT ONE PIXEL OF THE DESIGN, WHICH WAS THE REQUIREMENT:** the same SVG string is handed to the image decoder as a `data:` URI instead of injected as live DOM, so it is rasterised ONCE and cached as a bitmap — same seeds, same 16 unique marbles, same veining. ⛔ **That mattered more than it looks: D156 and D160 tuned this texture BY EYE, twice. Anything that re-cut the noise would have quietly undone both.** Plus `will-change:auto` on the 67 slabs, the 6 Why tiles and the 5 project-hero slides — the move D133 already made and proved on `.hero-bg img`, sixty-seven times the saving. **Result, measured: `will-change` 107 → 18, `filter` 431 → 231, `feTurbulence` 63 → 0, `feGaussianBlur` 42 → 0, DOM 2,590 → 1,991 elements.** ⚠️ **THE LAYER AND FILTER COUNTS ARE MEASURED; THE EVICTION IS THE DOCUMENTED BEHAVIOUR THAT FITS THEM, AND IT IS NOT VERIFIED ON HIS DEVICE. ASK HIM.** ⛔ Phone only — the switch is `--stoneRaster` declared on `body` in the 720px block and read back by `marbleRasterOn()`, never a `matchMedia` in JS (D51/D59/D68/D78/D93/D105). | Verified frozen: 1440×900 and 768×1024 both still 2,590 elements, 63 `feTurbulence`, doc height 14641 / 18385, gallery cards `210.1/210.5/210.9/211.4` — the recorded baseline exactly |
| D165 | 13 Aug | ⭐ **NOTHING HAPPENS WHEN A FINGER TOUCHES A GALLERY CARD.** Client: *"any animation must not happen when someone's finger touches it. Only when they click in must it gloss over and go into that section."* ⛔⛔ **`:hover` IS THE BUG ON A TOUCH SCREEN AND THE RULE DOES NOT LOOK WRONG WHEN YOU READ IT.** iOS fires `:hover` on the first tap and then LEAVES IT LATCHED until something else is touched, so `.gal-set.settled .gal-card:hover img{filter:…}` and `:hover .sheen{transform:translateX(120%)}` both ran on finger-down and stayed run — the brightness lift and the polish sweep he is describing. They were written for a mouse, where the pointer leaving takes them off again. ⭐ The `.sheen` is hidden rather than re-timed: it is a full-card gradient layer that exists only to be transformed, so removing it is one fewer painted surface per card as well as one fewer thing that moves under a finger. ⭐ **THE OPEN IS UNTOUCHED AND WAS TESTED** — tapping card 2 still runs `openFocus()` and the project detail still transitions in (verified: "Waterfall Kitchen", Richmond, `aria-hidden` false). Only the touch-down states are gone. | Phone only, `#gallery.gal-static` |
| D164 | 13 Aug | ⭐ **THE GOLD OUTLINE COMES OFF THE GALLERY CARDS.** Client: *"remove the golden outline around the blocks in the view our project gallery."* ⚠️ **IT IS NOT A BORDER, WHICH IS WHY LOOKING FOR ONE FINDS NOTHING.** `.gal-door` carries `glow-card`, so the rim is the shared `::after`/`::before` pair — the cursor-reactive gold halo the review cards, process steps and services faces all use, driven by `attachGlow`. With no pointer on a touch screen it either sat on at full strength or lit under his finger, which is also half of D165. ⚠️ There was a SECOND gold edge: `render()` wrote an `inset 0 0 0 3px rgba(198,166,100,…)` box-shadow onto the two peeking cards while the pile was stacked — that one left with the engine (D163), so the string is still in the file and no longer runs. ⛔ The neutral `1px solid var(--hair-soft)` STAYS: it is what stops a dark photograph bleeding into a dark page with no edge at all. | Phone only |
| D163 | 13 Aug | ⭐⭐⭐ **THE PROJECT GALLERY'S ANIMATION IS GONE ENTIRELY ON THE PHONE, AND THE SECTION IS NOW A PLAIN NATIVELY-SCROLLED COLUMN. ⛔ THIS RETIRES D122, D125, D127, D128, D130, D142, D147 AND D157 ON THIS DEVICE.** Client: *"completely remove the animation from the view our project gallery. It's not working on mobile — well, it doesn't look good on mobile. So remove that."* And, separately, that this section lags worst. ⭐⭐ **THE TWO ARE ONE FIX, AND THAT IS WHY THE ENGINE IS STOPPED RATHER THAN PINNED AT `q=1`.** Pinning the accordion at its finished state would have removed the *animation* and left the *machine*: `.gal-pin` is `position:sticky`, `.gal-stage` is one viewport tall, and all eight cards are `position:absolute` moved by a scroll-derived `travel` written to eight promoted layers every frame — the whole section's scrolling hand-rolled in JS, which is what a phone is worst at. **That would have answered his words and not his complaint.** ⭐ So `measure()` returns early on a phone after adding `.gal-static`, and both `frame()` and `render()` bail — `render()` needed its own guard because it is called DIRECTLY at boot, and an inline transform beats a stylesheet rule, so the column was being laid out correctly and then transformed on top of itself. ⭐ **THE CARD KEEPS THE EXACT SIZE HE HAS BEEN LOOKING AT (331×145) AND NOT BY COPYING A NUMBER** — `--cw`/`--ch` are handed the already-multiplied size, because `.gal-name`, `.gal-place` and `.gal-meta`'s insets are all `calc(var(--cw) * k)` and passing the raw 157.5 would have shrunk every label to 47% and looked like a separate bug. ⭐ D113's split (title and subtitle above, buttons below) survives as flex `order` instead of a stretched absolute overlay, and **D147's gap survives as the same `clamp(18px,3vh,30px)` expression the services grid uses**, not as the ~24px it evaluates to (D107). ⛔⛔ **THE ONE LINE THAT IS NOT OBVIOUS: `.gal-stage` MUST COME OUT OF `position:absolute`.** Built without it the section collapsed to 35.7px and every card computed to `331 × 0` — which looks like a broken height rule and is not one: the stage was resolving `inset:0` against a `.gal-pin` that had just lost its `height:100vh`, and eight flex items with the default `flex-shrink:1` obediently shrank to nothing. ⚠️ The dead code — `colTop`, `travelMax`, `animPx`, `stackY`, the fan, `entryPos`'s phone branch — is LEFT IN PLACE deliberately. He has reversed himself on this section more than any other, and one `return` is the whole difference. ⛔⛔ **SHIPPED WITH A FAULT, FOUND BY HIS SCREENSHOT THE SAME DAY: THE SECTION DIVIDER RAN STRAIGHT THROUGH "View our project gallery".** `#gallery` carried `margin-top:calc(clamp(28px,4.6vh,48px) − var(--galNavH,78px) − clamp(22px,3.6vh,42px))` ≈ **−70px** from D129, whose only job was to cancel, in the flow, the nav reserve `.gal-mid` added inside the sticky stage. **This rebuild deleted both subtracted terms and left the subtraction**, so the pull had nothing left to cancel and dragged the section up over the divider — measured, gallery top 2130 against a divider at 2149–2151, a 21px overlap through the title. ⭐⭐ **THIS IS EXACTLY §7.2 — "when a change has a cost somewhere else, the cost stays behind; after changing a value, grep for what was derived from it" — for the fourth time (D142/`animPx`, D156/`--gold-lo`, D154/the FAQ drawer, now this).** ⚠️ **AND IT IS THE THIRD TIME A FAULT WAS INVISIBLE TO EVERY MEASUREMENT I TOOK**: all of them were inside the gallery, and this number describes the JOIN to the section above it. ⭐ Fixed as `#gallery.gal-static{margin-top:0}` plus `padding-top:clamp(28px,4.6vh,48px)` on the stage — **padding and not margin, which is load-bearing**: a positive `margin-top` beside the divider's 48.7px `margin-bottom` COLLAPSES to the larger of the two and would have given 48.7px, never the 86 the composition wants. Restores D124's symmetry exactly: 86.1px above the divider, 86.1px below. | Phone only. Desktop and tablet keep the pin, the runway, the accordion and the hallway — verified unchanged at both frozen widths |
| D162 | 13 Aug | ⭐ **THE SERVICE TILE PHOTOGRAPHS COME UP TO FULL BRIGHTNESS.** Client: *"in the surfaces for every space section there is some sort of dark radiant across each one of the blocks… I want the images to be full brightness, but the wording must still be clear on it."* ⛔⛔ **THE DARKENING WAS TWO SEPARATE THINGS AND ONLY ONE OF THEM LOOKS LIKE A GRADIENT — this is the D157 lesson again.** (1) `.face.front .stone{opacity:0.5}` — the PHOTOGRAPH ITSELF was half transparent over `.face`'s `background:var(--ink-2)` (measured rgb(21,21,26)), a flat 50% mix with near-black across the whole tile and the bigger of the two. It is not a gradient at all, which is why hunting for one finds only the veil. (2) `.face.front .veil` was still 0.15 opaque at the TOP of the tile. ⭐ **FIXING EITHER ALONE WOULD HAVE LOOKED LIKE NOTHING HAPPENED.** ⛔ The veil is re-aimed, not deleted, and the aim is measured: on a 146×106 tile the number sits 10→22px down (top 21%) and the name 71→85px down (67→80%), so the scrim is opaque only in those two bands and **fully transparent from 44% to 76%** — the middle of the frame, where the subject of all six photographs actually is. ⭐ The type carries its own contrast now (the `.hx-name` shadow pair), which is what buys the middle its transparency. ⚠️ **AND THE NUMBER NEEDED MORE THAN THE NAME, WHICH ONLY SHOWED UP BY LOOKING** — 9.5px champagne in the near-clear top band nearly vanished on Bespoke Worktops and Outdoor Kitchens, so it takes a third, tight, opaque halo stop rather than more scrim. | Phone only, verified at 2.3× zoom |
| D161 | 12 Aug | ⛔⛔ **REVERSED THE SAME DAY, ON SIGHT. THE LIGHT STONE BANDS WERE BUILT, SHOWN AND REMOVED — "revert it".** ⭐⭐ **KEEP THIS ROW: IT IS THE RECORD THAT THE LIGHT-SECTION IDEA WAS TRIED PROPERLY AND REJECTED, SO NOBODY PROPOSES IT AGAIN AS A FRESH THOUGHT.** ⛔ **DO NOT REBUILD IT WITHOUT HIM ASKING TWICE.** He commissioned it himself, with the escape hatch stated up front — *"I just want it to look good, make sure it's on brand. If it doesn't look good, I want to be able to revert it"* — looked at the result and took the hatch. **That is the design failing on his eye, not a build fault**: it was measured on brand (the site's own slab, its own champagne ramp re-cut for contrast), it passed the freeze at both frozen widths, and every fault found during the build was fixed before he saw it. ⭐ **WHAT SURVIVES THE REVERT, AND IT IS THE VALUABLE PART:** the two failure modes below are general, and both were found by looking rather than calculating. ⚠️ **WHAT WAS REVERTED IS ONLY THE FENCED CSS BLOCK.** D160's review-card readability fix, made in the same message, was deliberately KEPT — that was a fault he reported, not part of the trial. ⚠️ The removal is a clean 102-line deletion between two banner comments; `Website Demo/index.html.pre-stone-sections.bak` remains if it is ever wanted back. Verified after: all four grounds back to `none`, titles back to `--bone`, the em ramp back to the original light champagne, the FAQ drawer back to light frosted, document height back to 14318. **Original entry follows.** — Client: *"I want the current background of the slab that we have to turn into a parallax. So the surfaces for every space to have a new neutral stone faded background colour that's still on brand… maybe a grey or white stone, and then we just change the text to black and gold. And then the view our project gallery to have the same background as it has now… the current black background almost works like a parallax. And then that same stone background for the what would it cost section, and the about us section, and the answers set in stone section. I just want it to look good, make sure it's on brand. If it doesn't look good, I want to be able to revert it."* ⭐ **THE PARALLAX ALREADY EXISTED AND HE SPOTTED IT** — `body::before` is `position:fixed`, one slab held still under a scrolling page. **Alternating light and dark BANDS is what makes that legible**, because a fixed texture only reads as fixed once something moving crosses it. ⭐ **THE LIGHT BANDS ARE THE SAME SLAB, NOT A NEW IMAGE**: `--marbleBG` under a light wash (`rgba(239,236,229,0.90)`) instead of the floor's dark one, so it is on brand by construction and there is no second asset to keep in step. Applied to **#services, #estimator, #about, #faq**; **#gallery, #stones, #reviews, #process, #why, #cta untouched**, exactly as he listed. ⚠️⚠️ **`background-attachment:fixed` IS THE PARALLAX AND IT IS THE ONE THING THAT CANNOT BE VERIFIED FROM THIS MACHINE** — iOS Safari has never honoured it reliably and commonly paints it as `scroll`. **If it degrades the sections still show light stone, they simply scroll instead of holding still: the look survives, only the motion is at risk.** Told to him plainly, the same way D133 was. ⛔ **A FIXED PSEUDO-ELEMENT CANNOT SUBSTITUTE**: clipping one to its section needs `contain:paint` or a transform, and both make the section the containing block, which is exactly what stops it being viewport-fixed. ⚠️ **AND A LIGHT WASH OVER THE EXISTING FLOOR WOULD NOT HAVE WORKED** — the floor is already veiled to median 7.9/255 (D123), so a 0.90 wash over it leaves ~1.7 levels of variation, a flat panel with no texture. The bands need their own lighter-veiled copy. ⭐ Band edges are FEATHERED with a 36px mask top and bottom; a hard 1px meeting with the near-black floor reads as a mistake. ⛔⛔ **TWO THINGS BROKE AND BOTH ARE THE SAME LESSON — A COLOUR THAT IS A FUNCTION OF ITS BACKGROUND CANNOT SURVIVE THE BACKGROUND CHANGING.** (1) **`.section-title em` IS PAINTED BY A GRADIENT WITH `background-clip:text`, so setting `color` does nothing and looks like it worked** — the champagne ramp measured **2.42:1 at its lightest stop** on the light ground, on the largest type in the section. Re-cut to the dark end of the same ramp (#96702F → #6F5327 → #5E4520): **3.08:1 at the lightest stop, 4.87 at the middle**, above the 3:1 large-text floor throughout, and still a lit metal rather than a flat brown. (2) **The FAQ drawer was a LIGHT translucent panel with white text** (D154), correct hanging off a dark row on a dark page and near-invisible on light stone — flipped to `rgba(16,16,20,0.86)` so it welds to its dark row again and keeps the glass. ⭐ **EVERY OTHER CARD AND PANEL IS DELIBERATELY LEFT ALONE** — dark panels on light stone is the look, and it is what keeps this small enough to revert. ⚠️ **The rule that falls out: an OPAQUE panel can be left alone when you invert a ground; a TRANSLUCENT one is by definition a function of what is behind it. Check every `rgba()` surface.** ⛔ **PHONE ONLY** — he did not name desktop and rule 15 stands; freeze verified at 1440 and 768 with all four grounds computing `none`, titles still `--bone` and the em ramp still the original light champagne |
| D160 | 12 Aug | ⛔ **THE REVIEW CARD'S VEIL GOES BACK UP — D156 OPENED IT TOO FAR AND THE VEINS LANDED ON THE WORDS** | Client, on the D156 build: *"the newer grey card makes the text in the review card hard to read, so you need to fix that."* ⭐⭐ **THE CAUSE IS NOT THE CONTRAST RATIO, WHICH IS WHY THIS NEEDED LOOKING AT RATHER THAN CALCULATING.** Body text measured **13.7:1** on the D156 card — comfortably AAA, and D156 had checked exactly that number. What he is reading is `marbleSVG()`'s hairlines running diagonally THROUGH the paragraph: local light and dark inside the letterforms, which breaks up a line of type however good the average is. ⛔⛔ **AND IT BITES HARDER ON A GREY CARD THAN IT EVER DID ON A WHITE ONE.** The veins are cream; against the old near-white face they were nearly invisible, so 0.74 "felt" like a small step from 0.88. Against a grey face the same veins are the highest-contrast thing on the card. **A veil alpha is not portable between two base colours — re-judge it whenever the base moves.** ⭐ Veil 0.74/0.82 → **0.90/0.94**, back to a whisper. ⚠️ **THE GREY BASE STAYS** — that was the half of D156 he asked for and has not complained about; only the texture is walked back. Base lifted one step to #EFECE5→#E0DCD3, which also takes the champagne from 4.97:1 to **5.22:1** |
| D159 | 12 Aug | ⭐ **THE FOOTER, SECOND PASS: THE TWO MARKS BECOME HERO-STYLE BADGES, THE PHONE AND EMAIL LEAVE THE COLUMN, AND EXPLORE/BROWSE ARE CENTRED AS A PAIR. ⛔ THIS REVERSES D153's OWN REASONING ON THE BADGE** | Client, on the D153 build: *"we need to have the five star Google reviews and the ten year guarantee as having like a block or a border, like the hero section, because currently it just looks like too much text stacked on top of each other. Then the email and phone number maybe needs to stand out in a different way, because currently the email takes up two lines on mobile. And then the explore and browse don't feel centred — the explore feels too close to the side of the screen, the browse feels too far away from the other side."* ⛔ **D153 DELIBERATELY CHOSE THE QUIET `.foot-stars` TREATMENT AND ARGUED AGAINST THE HERO PILL** — "a bordered badge is hero furniture and would be the loudest thing down here". **He looked at it and said the opposite, and he is right for a reason that round missed: two unbordered lines of small type, one directly under the other, do not read as two marks at all — they read as a paragraph. The border is what makes each one an object.** ⭐ It is the hero's `.chip` verbatim (same radius, border, glass, tracking) rather than a new footer badge, because he asked for "like the hero section" and because a second nearly-identical pill style is how a palette drifts. ⚠️ They stack rather than sitting side by side — measured, the pair needs ~397px against 335px of footer; what he objected to was untreated stacked TEXT, and two centred pills is still the fix. ⭐⭐ **THE PHONE AND EMAIL LEAVE THE HALF-COLUMN ENTIRELY, AND THAT IS THE REAL FIX FOR THE WRAP.** D153 got the address onto one line by dropping it to 12px in a 167px column; he still saw two lines because **his handset is narrower than the one it was measured on** — a 160px column at 375px. ⛔ **A number and an address are not a nav list and do not belong in a nav column.** Full width, centred, shaped as the same badge, `white-space:nowrap` now a guarantee rather than a hope (the address has ~200px of room it does not need). The "Contact" heading and the "Phone"/"Email" micro-labels go with them — they were structure for a column that no longer exists, and three stacked labels is the thing he objected to in the same message. ⭐ **EXPLORE/BROWSE: each list is shrink-wrapped and centred INSIDE ITS OWN HALF**, which gives both the same inset from their outer edge whatever the longest link happens to be — measured **0px of air left / 32px right → 34.8 / 24.3**. ⚠️ The text stays LEFT-aligned inside each list; a centred nav list is unreadable, it is the block that is centred, not the words. ⚠️ Not perfectly equal because the two lists are different widths; centring the pair as a unit would be exact but would shift every time a link's text changed |
| D158 | 12 Aug | ⭐ **THE GALLERY'S FOOT LOSES ITS DOUBLED-UP AIR. ⛔ PARTLY REVERSES D129, AND THE REASON IS WORTH READING** | Client: *"the get in touch and the cards and the border or the section divider have too much space in between them."* ⚠️ **D129 SOLVED FOR THE CTA SITTING EXACTLY HALFWAY** between the last card and the section's end (`F = barH + 48 = 107`), because that is what he asked for then: "in the middle between the last card and the divider". **Measured, it delivered precisely that — 82.4px above and 83.0px below — and the total was still too much.** Centring was the right answer to the question he asked then; it is not a reason to keep the total. Air term 0.13vh → **0.082vh**, so the gap above the CTA drops **82.4 → 43.4** and the column gets ~38px shorter. ⛔ The `barH` term is untouched: it stops the CTA sitting under the sticky bar at the instant the column finishes travelling, which is functional clearance, not spacing. ⭐⭐ **AND THE SEAM ITSELF WAS PAYING FOR THE GAP TWICE.** Measured across the gallery → stones divider: **131.7px above the rule against 64.2px below** — yet `margin:48.72px 0` is equal on both sides. The difference is what each section leaves at its own edge: `#stones` starts its title 20px in, while `.gal-mid` ends **83px** after its last visible element because of that sticky-bar reservation, which already does exactly the job a divider margin does. Dropping this ONE divider's top margin leaves 83px above against ~69px below — balanced, and 48.7px shorter. ⛔ **SCOPED `#gallery + .section-divider`, NEVER THE SHARED RULE** — there are eight dividers and the other seven sit between sections that reserve no sticky bar; D149 had just made the estimator → process seam symmetric on exactly this principle. **Total last card → divider: 256.6 → 168.9px** |
| D157 | 12 Aug | ⭐⭐ **THE GALLERY IS UNSTUCK: THE HELD RUN GOES TO ZERO AND THE PHONE'S PLAYHEAD STOPS BEING DAMPED. ⛔ TWO SEPARATE CAUSES, AND HE FELT BOTH** | Client: *"the view our project gallery section just feels like it's stuck somehow. As you scroll up the animation plays, which is fine, but I can still feel like something is tied to the scroll. I have to scroll three times before it actually starts letting me go through that section. And then even after going through that section, it feels like it's still moving up, so something feels like it's still tied to the scroll there, which is wrong."* ⭐⭐ **CAUSE ONE — `animPx` WAS THE OTHER HALF OF D142 AND SHOULD HAVE GONE WITH IT.** `animPx` is scroll reserved for the animation to be SCRUBBED against; it existed only so `q` had a runway to be read off. D142 moved the accordion onto a wall clock, so nothing reads that runway any more — **but it stayed: `0.85 × 812` = 690px of scrolling during which the page is pinned and, by construction, nothing moves at all.** Three flicks that do nothing, which is exactly the count he reports. At 0, `travel` becomes `p × travelMax` and the column moves the instant the finger does. **Measured after: every 100px of scroll moves the column exactly 100px, from the first step.** ⚠️ What this gives up: D127's promise that the accordion finishes on the frame showing the FIRST card at the top. It cannot survive a page free to scroll during the animation, and he has asked for the freedom. ⭐⭐ **CAUSE TWO — `SCRUB` IS A MOUSE-WHEEL BEHAVIOUR ON A TOUCH SCREEN.** Its own comment says it is "low on purpose: after the wheel stops, the scene should [keep easing]". At 0.045 the playhead needs ~50 frames to close 90% of a gap, so **the column keeps drifting for the best part of a second after the finger lifts** — his "it's still moving up". `travel` is documented (D127) as sliding the scene "by exactly the scroll distance… so it reads as ordinary scrolling"; **damping is what stopped that being true.** `pCur=pTarget` on the phone only; `gCur` keeps its damping and above 720px both are untouched. **Measured: full 200px on the very next frame, zero drift after.** ⭐ D142's clock re-proven intact — with scrollY frozen the card gap still grows 11.7 → 160 over ~1400ms |
| D156 | 12 Aug | ⭐ **THE REVIEW CARD COMES OFF WHITE AND ONTO STONE. ⚠️ A REFINEMENT OF D116, NOT A REVERSAL — the cream card stays, it simply stops being paper** | Client: *"I don't want the review cards to be such a sharp white, it currently stands out a bit too much… maybe we need to add some sort of design, but more stone, more grey. It's just very shocking white right now, and nothing else on the side is that shocking white."* ⭐ **HE IS RIGHT ABOUT THE COMPARISON AND THAT IS THE POINT.** D116 asked for the strongest contrast the palette could make (`--bone` on `--ink`) and delivered exactly that — the card was #FBF9F4→#EFEBE1, **brighter than `--bone` itself**, on a page whose next lightest element is muted limestone at 55% alpha. It was the only pure white on the page. Now **#EDEAE3→#DBD7CD**. ⭐⭐ **AND THE VEIL OPENS, WHICH IS THE "MORE STONE" HALF.** At 0.88/0.94 the wash was all but opaque: `marbleSVG()` was there, paid for in DOM, and invisible — the card was a flat tint, which is a large part of why it read as paper. At **0.74/0.82** the veining comes through as faint mottling. The wash now carries the CARD'S OWN two stops so the two cannot drift apart. ⛔⛔ **THE CHAMPAGNE HAD TO COME DOWN WITH IT AND THIS WOULD HAVE SHIPPED BROKEN OTHERWISE.** D116's header records that `--gold-lo` (#8C6B34) was chosen over `--gold` because it carries ~4.6:1 against the old card. **Against the new one it is 3.42:1 — under AA**, because the card came down and the ink did not. #6F5327 restores it to **4.97:1**; body text is 13.68:1. One token (`--revChampagne`) so the stars, the seal and "Read more" cannot drift apart. ⛔ Do not darken the card further without re-checking that number |
| D155 | 12 Aug | ⛔⛔ **THE "ADD A STONE" PICKER WAS COMPLETELY BROKEN AND IT WAS A GRID-SIZING BUG, NOT STYLING. `aspect-ratio` GAVE THE TILE A HEIGHT BUT CONTRIBUTED NOTHING TO THE ROW** | Client, with a screenshot of 132 stones stacked on top of one another: *"the add stone is completely broken, doesn't matter what option you click on, it's completely broken."* ⭐ **THE DIAGNOSIS:** every child of `.cmp-pick-tile` is `position:absolute` (image, veil, meta), so the tile's in-flow content height is 0 and its intrinsic contribution to an auto-sized grid row was **its borders alone**. Measured: **rows resolved to 2px while the tiles themselves resolved to 129.2px from the ratio**, so every row overflowed 117px into the row below. ⭐⭐ **THE PRECISE CONDITION, AND IT IS WHY THE COLLECTION PAGE IS FINE:** an aspect-ratio only yields a definite block size — and therefore a row contribution — if the item's INLINE size is definite before rows are sized. `.st-grid` uses a fixed `335px` column, so it is; the picker uses `minmax(…,1fr)`, a FLEXIBLE track whose width is not resolved at row-sizing time. **`.stile` and `.stp-slab` have the same five-absolute-children construction and are both fine — only the one in a `1fr` track collapses.** ⚠️ **`align-items:start` / `align-content:start` DO NOT FIX IT — both measured, rows stayed 2px.** Nor does moving the image into flow. ⭐ Fixed with `height:0; padding:0 0 120%`: padding is part of the border box so it IS counted in the intrinsic contribution, and a percentage resolves against the item's own inline size, so the shape tracks the column at every width — which `grid-auto-rows:129px` (also measured working) would not. ⚠️ **THIS BUG HAD BEEN LIVE SINCE THE PAGE WAS BUILT ON 12 Aug** and at every width; §8 records that nobody had seen the stones desktop. Verified after: rows 131.195px, 10px gaps, no overlap anywhere, all four tabs (132/67/45/20), search "white" 50, and adding a stone builds the card |
| D154 | 12 Aug | ⭐ **THE FAQ ROWS TAKE THE ENQUIRY FORM'S GREY, THE DRAWER BECOMES FROSTED GLASS, AND NOTHING IS OPEN ON ARRIVAL** | Client: *"currently, the first one stays open. I actually wanted to have the same grey drop down colour as the one visit, one price, one team form section. So currently it has that see through top part, and then I want the drop down to have the see through frosted glass version, and then I want the top to have that grey colour from the form."* ⛔ **THE ROW HAD TO STOP BEING TRANSLUCENT AND THAT IS THE WHOLE CHANGE.** It was `rgba(244,241,234,0.055)` — 5.5% white — which over `.cta-card`'s panel grey renders as a grey, but over the FAQ's own backdrop (the page's marble floor) renders as **see-through marble**. Same declaration, two completely different results; his words were "that see through top part". ⭐ **SO IT IS BUILT FROM THE FORM'S OWN TWO LAYERS RATHER THAN A SAMPLED HEX** — `rgba(255,255,255,0.045)` stated literally over `var(--ink-2)`, exactly as `.cta-form input` sits on `.cta-card`. It composites to ≈rgb(32,32,36) **and follows `--ink-2` if that token is ever retuned**. ⭐ The drawer takes `backdrop-filter:blur(16px) saturate(1.25)`, which is what makes it glass rather than merely transparent: the marble floor stays visible through it but blurred, so the answer sits on softened stone. ⭐ **CLOSED ON ARRIVAL AND CLOSABLE** — this answers the open question D152 left ("he described opening, not closing"). `deselect()` writes `.faq-shut` on `#faq`; a re-tap on the live row closes it. ⛔ **DESKTOP MUST NEVER REACH `deselect()`** — the plate there is a fixed-height panel with `min-height` locked to the tallest answer, so closing it would leave a ~200px hole in a signed-off composition; a resize off the phone with nothing open re-opens question one. ⚠️ **`--faqMode` IS DECLARED IN CSS AND READ BACK, and that is not redundant with the module's existing `narrow` matchMedia**: 760px is where the plate moves inline (the index's one-column breakpoint), 720px is where the phone build starts. **Two different questions with two different answers** — answering the second with a second matchMedia is the D51/D93 disagreement bug |
| D153 | 12 Aug | ⭐ **THE FOOTER: A CENTRED HEAD, TWO PACKED COLUMNS AND A CENTRED FOOT ON THE PHONE — and the ten year guarantee joins the Google rating down there** | Client: *"for the footer, the footer parts should be centre. So the logo should be centre, and then the subtitle also centre, then the Google reviews and ten year guarantee also there. And then we should put browse and contact next to explore, so it fits in one of the parts, not all below each other. And then also contact the phone and the email below browse next to explore. Then we can have the location and the hours centre, as well as the last part, all rights reserved, sitemap privacy terms and cookies, also centred."* ⛔ **WHAT HE IS DESCRIBING IS `@media(max-width:560px)` COLLAPSING THE FOOTER TO ONE COLUMN** — brand, then Explore, then Browse, then Contact, four stacked blocks. His fix is precise and is what was built: two columns, Explore one side, Browse with Contact beneath it on the other, and three centred full-width bands (head, location/hours, legal). ⭐⭐ **`display:contents` ON `.foot-contact` IS THE LOAD-BEARING TRICK AND THE ONLY REASON THIS IS POSSIBLE WITHOUT MOVING MARKUP.** Area and Hours have to leave their column and centre full-width while Phone and Email stay in column 2 under Browse — but all four are children of one div, so while that div is a grid item they can only go where it goes. `display:contents` dissolves the wrapper and promotes its five children to grid items in their own right. ⚠️ `.foot-contact div{…}` still matches them afterwards (the selector never cared about the parent's display), so the label-over-value stacking is inherited unchanged. ⚠️ The area map gives each contact line its own ROW rather than sharing one "contact" cell — several items in one grid area OVERLAP rather than stack. ⭐ **THE TEN YEAR GUARANTEE MARK IS NEW MARKUP, NOT A NEW CLAIM**: the pair has lived in the hero (`.hero-chips`) since the start and only the Google half was ever in the footer. It takes the footer's own quiet `.foot-stars` treatment, **not** the hero's bordered `.chip` pill, which would be the loudest thing down there. ⚠️ Its `<b>` needs `letter-spacing:normal` — the 3px tracking on `.foot-stars b` is for the five star glyphs and reads as a trailing space on "10". ⚠️ The email is the longest unbroken string in the footer and now sits in a 167px column: `overflow-wrap:anywhere` alone broke it after "…co." and left "uk" on its own line, which reads as a rendering fault. At 12px the address measures ~136px and fits on one line, so the wrap guard never fires. ⛔ Phone-scoped. `foot-explore` / `foot-browse` / `foot-c-*` are NAME HOOKS ONLY, referenced by no base rule, added in preference to positional `:nth-child()` against a markup order nobody would know was load-bearing. Verified at 1440 and 768: four-column and two-column grids unchanged, `gridTemplateAreas:none`, `.foot-contact` still `block`, the guarantee mark `display:none` |
| D152 | 12 Aug | ⭐⭐ **"ANSWERS, SET IN STONE" BECOMES A PLAIN ACCORDION ON THE PHONE — and the fault was never the styling, it was that ONE QUESTION WORE THREE DIFFERENT NAMES** | Client: *"for mobile only, we have to completely redesign the way the answers set in stone section functions. It looks terrible now. So we can have it in a more basic row with the things as a dropdown that all makes it part of it. So we can make it grey with a name in it, and then it just drops down to show that part. But this doesn't have to be centre."* ⛔⛔ **THE DIAGNOSIS IS THE WHOLE ROW.** D-7 Aug built a CONTENTS PAGE for a wide screen: the row shows a short scannable label ("What it costs"), and a framed plate beside it carries an eyebrow ("Pricing") plus the real question ("How much does a stone worktop cost?"). Three labels for one item is fine in separate columns where it reads as index-then-detail. Stacked on a phone the plate lands directly under the row, so **the same item announces itself three times in a vertical column** and the framed card reads as a floating panel rather than as that row's answer. ⭐ **THE MECHANISM WAS ALREADY RIGHT — `place()` has moved the plate inline below 760px since the day it was built. It was only ever wearing desktop clothes.** ⭐ **SO: THE ROW TAKES THE QUESTION AND THE PLATE TAKES ONLY THE ANSWER.** One name per item, the row grey, the panel welded to its bottom edge at `margin-top:-1px` so the pair reads as one control; `.fp-head` (eyebrow + repeated question + rule) is hidden outright. ⚠️ Safe for assistive tech: the panel is `aria-labelledby` the row's button, which carries the full question as its accessible name. ⭐ A new `.faq-qf` span carries the full question in every row's markup at every width, `display:none` in the base rule (D120) so the desktop index keeps its short labels. ⭐ `::after` is REPURPOSED as the chevron rather than adding an element — it was the gold left edge, which is going anyway; it needs `position:static` to become a flex item. ⚠️ **A CHEVRON READING MEASURED 1.2s AFTER A TAP IS STILL IN FLIGHT AND WILL LOOK INVERTED** — the base rule's `.55s` transition on `--ease` has a long tail. Settled: closed rows 45°, open row −135°. ⚠️ **ONE ROW IS ALWAYS OPEN and that is deliberately unchanged** — `select()` returns early on a re-click of the live row. He described opening, not closing, and arriving with the first answer showing is what teaches the pattern. Tap-to-close is a change to the selection logic, not a style, and has not been made. ⚠️ Group headings KEPT — real structure, and he did not ask for them to go |
| D151 | 12 Aug | ⭐ **THE ABOUT STORY IS CENTRED ON THE PHONE — title, both paragraphs and the CTA** | Client: *"the title for about us and the text below must also be centre, as well as the CTA."* ⚠️⚠️ **PHONE ONLY, AND THAT IS A DELIBERATE READING OF A GENUINELY AMBIGUOUS INSTRUCTION — DO NOT ASSUME IT WAS AN OVERSIGHT.** In the same message he said the About change *"also applies to the desktop version, but nothing else that I mentioned here on the other changes applies to desktop, only the about us section part"* — and the About change he had just described was cutting Ali (D150). The centring came AFTER that sentence, so whether "only the about us section part" reaches it is unclear. Desktop is frozen and only the client unfreezes it (§2 rule 15), so the reversible reading won: **the removal is at every width because he named it, the centring is phone-scoped until he says otherwise.** ⭐ He was told this explicitly in the reply rather than left to find it. If he confirms, move three rules out of the phone block; nothing else changes. ⛔ **`text-align` ALONE IS NOT ENOUGH, AND THIS IS THE PART THAT WOULD HAVE LOOKED FINE ON THIS HANDSET AND WRONG ON A NARROWER ONE.** `.about-copy` is a flex COLUMN, so each child is a flex item: `text-align` centres the words inside each box, but the boxes are placed by flex. The title carries `max-width:22ch` and the copy `52ch`, and 22ch computes to ~350px against exactly 350px of room at 390px — **it is on the very edge here and binds below it**, at which point the box shrinks and flex parks it at the START. Auto margins centre the box, text-align centres the line within it; both are needed. ⚠️ The CTA needs neither — it is a flex item with `align-self:flex-start`, so that is the property that had to change |
| D150 | 12 Aug | ⭐⭐ **THE TEAM IS NICK AND RIMSHA. ALI IS OUT, THE COLLAGE GOES TO SIX COLUMNS, AND THIS IS THE ONE CHANGE OF THE ROUND THAT APPLIES TO DESKTOP** | Client: *"this part that I'm talking about right now also applies to the desktop version — we are only gonna have Nick and Remsha, we're not gonna have Ali, so you can divide that into two sections instead of the three that are there. So that also applies to desktop, but nothing else that I mentioned here on the other changes applies to desktop."* ⚠️ He says "Remsha"; the page has always said **Rimsha** and that spelling was kept — a phonetic spelling in a voice note is not an instruction to rename a real person. **Worth confirming with him.** ⭐⭐ **SIX COLUMNS RATHER THAN THREE, AND THE SWITCH IS FREE — THIS IS THE PART WORTH KNOWING.** Two equal tiles do not divide into three columns, so the track count doubles, and doubling changes NOTHING about the work band below. With W the width and g the gap: at 3 columns w1 spanned `2col+g = (2W−g)/3` and w2 spanned `col = (W−2g)/3`; at 6 columns w1 spans `4col+3g` and w2 `2col+g`, which reduce to **exactly the same two expressions**. Measured at 1440: work tiles 326.5 / 156.3 / 211.5 before and after, identical. Only the top band changes, thirds → halves at `3col+2g = (W−g)/2`. ⭐ **THE PORTRAIT CROP CHANGES WITH IT AND THAT IS AN IMPROVEMENT**: each was 1 column by 3 rows, i.e. **1:2**, a very tall narrow standing crop; half the width over the same 3 rows is the collage's own **3/4**. Measured 241.4×324.2 at 1440, 162.7×218.6 on the phone. A pair at 1:2 would have been two slivers with a hole where the third used to be. ⛔ **THREE THINGS ARE INDEXED BY DOM POSITION AND MUST AGREE — the figure, the grid's column count, and `HINGE` in the ABOUT COLLAGE IIFE.** All three were changed. ⭐ The top band is a PAIR now, not a triptych, so it opens like two doors from the centre: left tile off its left edge, right tile off its right. Rimsha's used to fall off its TOP edge because as the middle of three it had no outward side to open on. ⚠️ `scrollSequence`'s bound is `(n−1)*step + span < 1`; at five tiles that is 4×0.112 + 0.42 = 0.868, so there is MORE headroom than at six and the timings are untouched. ⚠️ `picks` needed no change — the portraits carry no `<img>` at all, so `querySelectorAll('.ac-tile img')` never matched one. ⚠️ `.acp-role{min-height:2.6em}` KEPT even though the wider tiles now fit "Operations Director" on one line: it is what keeps the pair robust to a role title nobody has thought of yet, and costs nothing unused |
| D149 | 12 Aug | ⭐ **THE PROCESS TITLE SITS AS FAR BELOW ITS DIVIDER AS THE ESTIMATOR'S LAST LINE SITS ABOVE IT — and the whole thing reduces to "give the two sections the same padding"** | Client: *"the process without surprises section, the title is too far away from the divider. It should be the same distance away as the last words in the section above."* ⭐⭐ **`.section-divider` HAS A SYMMETRIC MARGIN — `clamp(26px,6vh,74px)`, 50.64px top AND bottom on an 844px screen.** So the gap above it is `#estimator`'s padding-bottom + 50.64 and the gap below it is 50.64 + `#process`'s padding-top. **Equal paddings, equal gaps, and no arithmetic left to get wrong.** ⛔ `#process` was the odd one out for a DESKTOP reason: it carries `padding-top:clamp(64px,10vh,104px)` against `.section`'s own `clamp(30px,4.6vh,84px)` — 84.4px against 38.8 — because it is a `min-height:100vh` composition that centres itself and wanted headroom on a wide screen. **Measured on the phone before: 89.5px above the divider, 135px below. After: 89.5 and 89.5, delta 0.** ⛔ **DO NOT REPLACE THE CLAMP WITH THE 39px IT EVALUATES TO** — it is `.section`'s own expression, so the two track each other at every screen height. ⚠️ Only the TOP changed; `padding-bottom`, the `min-height:100vh` and the centring are untouched. ⚠️ Measure this with `.rise` SETTLED (`transform:none`, class has `in`) or the head's 34px entrance offset is in your number — a first pass read 501px where the padding said 493.6 |
| D148 | 12 Aug | ⭐⭐ **THE ESTIMATOR IS REBUILT FOR THE PHONE, AND NEARLY EVERY FAULT IN IT WAS ONE FAULT REPEATED: A LABEL AND ITS HINT SHARING A LINE** | Client: *"the estimator, what would yours cost, we're gonna have to redesign it for mobile so that it looks better because we have so much text. Some text is next to each other, like the word width mm, and the word length mm is below. So we have to make the whole thing look neater and function better for mobile, because they can put all their settings there, and then they have to scroll down and see what they're actually doing there and the actual price."* ⛔⛔ **THE PANEL IS BUILT OUT OF `justify-content:space-between` PAIRS — caption left, explanation right — WHICH IS A DESKTOP IDIOM THAT WANTS ~420px AND COLLAPSES AT THE 294px OF PANEL A 390px HANDSET GIVES. Six of them do it**: `.est-labelrow` (×3), `.est-rowfoot`, `.est-bhead`, and the piece list's shared header. Stacking each one fixes it outright. ⭐ **THE ANSWER IS FEWER THINGS PER LINE, NOT SMALLER TYPE** — shrinking captions to fit is how this arrived at 9.5px on .18em tracking, and another pass of that would read as fine print and still not fit "Thickness mm" into an 88px column. ⭐⭐ **THE LINE HE POINTED AT: `.est-rowhead` had to fit three uppercase captions across two 1fr columns and an 88px one — 67px a column against the ~75px "Length mm" needs.** It is deleted on the phone and **one piece is one card with every field labelled where it stands**: columns `24px 1fr 1fr 24px`, areas `pl use x / flen flen fwid fwid / len len wid wid / fth fth seg seg`. The field pairs span 1-2 and 3-4 so the two inputs come out exactly equal (24+gap+1fr against 1fr+gap+24) while the letter and the × keep narrow gutters — a plain two-column template would have made the letter's column as wide as an input. ⭐ The letter and "what it is for" share the top line, which is how a cut-list reads out loud: "A, worktop run". ⭐ `.est-stats`' three figures become three caption-left/figure-right ROWS, not three boxes: `repeat(3,1fr)` gives 91px a column and wraps "Slab needed", and the `max-width:420px` fallback avoids that by going 2+1 and leaving the third figure stranded full-width, which is what he was looking at. `row-reverse` rather than reordering the markup — the DOM order is what a screen reader hears and it is already right. ⭐ Three `.est-fl` label spans are emitted per row at every width and are `display:none` in the base rule; **a `display:none` grid item is not a grid item at all**, so the six-column desktop template still receives exactly six items (verified byte-identical at 1440 and 768). ⛔ Selectors carry `#estimator` because `@media(max-width:620px)` sets both a five-column template and `.est-use{grid-column:2/5}`, and an id is what outranks it; `grid-area` also resets all four placement longhands, which is what clears that span. ⛔ **THE ORDER WAS ALREADY RIGHT AND IS UNTOUCHED** — `@media(max-width:980px)` already puts the settings above the cutting plan and the price, which is the flow he describes rather than a change he is asking for. ⚠️ **NO "the price is below" CUE WAS ADDED**: he raised one and answered himself — *"or we don't have to say anything, I'm sure the user will know to scroll further down and see"* — and the panel already opens on "Your cutting plan" and closes on "Your estimate" in gold. One line to add if he asks. ⭐ Re-tested live after the rebuild: shapes prefill, + Island moves the price, length edits recompute, thickness toggles, add and remove work, four rows with no overlaps and no wrapped labels, no console errors |
| D147 | 12 Aug | ⭐⭐ **THE GALLERY'S FIRST CARD SITS THE SAME DISTANCE BELOW THE SUBTITLE AS THE SERVICE TILES DO — and the 42.8px that had to go was not spacing at all, it was a reservation for the pile** | Client: *"the project gallery section, the first card, the book matched island should be slightly closer to the subtitle like on the surfaces for every space section where those cards are also close to the subtitle. Same distance."* ⭐ **IT IS THE SAME EXPRESSION, NOT A COPIED NUMBER**: `#services .services-grid` carries `margin-top:clamp(18px,3vh,30px)`, so `colAir` is that clamp written in JS. Measured **25.3px in both places** on an 844px screen, and the two now track each other at every screen height instead of agreeing once. ⛔ Do not replace it with the 25 it evaluates to. ⛔⛔ **THE REAL FIND: D128 ADDED `fan/2` TO `colTop` BECAUSE THE PILE IS CENTRED ON SLOT 0 AND THEREFORE REACHES `fan/2` ABOVE IT** — that is what put the back of the deck through the subtitle in his earlier screenshot. Measured before: card 0 started **35.4px** under the copy and drifted DOWN to **78.2px** as the deal played. So the settled gap could not come down while the pile was centred on it. ⭐⭐ **THE FIX IS TO HANG THE FAN ENTIRELY BELOW SLOT 0 RATHER THAN CENTRING IT THERE** — `stackY` drops by the WHOLE fan instead of half, putting the pile's topmost card exactly ON slot 0, so nothing in the pile can ever reach above where card 0 will live and **nothing has to be reserved above it**. D128 bought clearance by moving the column down forever to make room for a pile that exists for a fraction of a second; this buys the same clearance for free. ⭐ **IT ALSO MAKES THE MOVE MORE LITERALLY WHAT D142 ASKED FOR**: card 0 now starts on its slot and never moves at all while cards 2-8 slide down out from under it — *"it will just go straight down"*. **Measured after: card 0 at 25.2px, movement during the animation 0.0px, minimum clearance across the whole 1600ms 25.2px, spread still growing 85.7 → 1169.7.** ⚠️ Nothing about the pile's SHAPE changes — same peek, same depth order, same z-order, only where the stack is anchored. ⛔ Both edits are inside `if(phone){…}`; `--galMode` is unset above 720px, verified |
| D146 | 12 Aug | ⭐ **THE SERVICE TILES RUN TO THE SUBTITLE'S OWN EDGES — the grid was carrying a second inset on top of the section's** | Client: *"the surfaces for every space section, I want you to make those cards wider so that they match up with the widest part of the subtitle."* ⛔ `.section` already pads 20px a side, which is what puts the head at 20…370 on a 390px handset; `#services .services-grid` then added `5vw` (19.5px) INSIDE that, so the tiles sat at 39.5…350.5 — **a 19.5px ledge either side that nothing else on the page shares**. Measured: subtitle box 350 wide, tile row 311. ⭐ **MATCH THE SUBTITLE'S BOX, NOT ITS LONGEST LINE.** Its widest line happens to run 22.9…367.1, but that is where this particular sentence wraps at this particular width — align to it and the tiles move the next time a word changes. The box is the real edge and the longest line sits 3px inside it, which is what "match up with" looks like. ⚠️ Columns go 150.5 → 170 and the `1.38/1` ratio takes the tiles 109 → 123 tall with them, which is MORE room for "Outdoor Kitchens" on two lines, not less. ⚠️ `.svc-catchall` keeps its own `5vw` and was left alone — it is centred text, so its inset does not read, and he did not mention it |
| D145 | 12 Aug | ⛔⛔ **THE REVIEW CARDS' ENTRANCE IS GONE. D134 ANSWERED THIS EXACT COMPLAINT AND ONLY GOT HALF OF IT, WHICH IS WHY HE ASKED TWICE** | Client: *"I don't want the 'hear it from your neighbours' review cards to have an animation that comes up when the user scrolls to that section. It must already be there and then just function the exact same way that it does now."* ⛔⛔ **D134 MEASURED THE SECTION, FOUND THE DECK AND ALL SIXTEEN CARDS ALREADY AT `opacity:1` WITH NO TRANSFORM, AND CONCLUDED THE ENTRANCE WAS THE SECTION HEAD ALONE (`.rise`). That reading was right about the CSS and wrong about the section.** The cards are not dressed by a stylesheet at all — they are positioned by `soloRender()`, whose `!revEntered` branch parks all three ~400px off-screen until a scroll watcher (`checkRevSequence`) flips the flag. **Nothing in the computed style says so, which is exactly how it survived a measurement.** ⭐ **THE LESSON: a computed-style sweep cannot see an entrance driven by inline transforms written per frame. Look for the flag, not the class.** ⭐ **THE PRECEDENT WAS ALREADY IN THE FILE AND WAS REUSED RATHER THAN INVENTED** — `reduceRev` has always meant "skip the entrance, start seated"; a phone now means the same thing, via `revNoEntrance() = reduceRev || revSolo()`. ⚠️ **THE TEST IS `revSolo()`, NOT A MEDIA QUERY, AND THAT IS D93's RULE** — this module already decides what a phone is in exactly one place (`perPage()===1`, which also sets the `.rev-solo` class the stylesheet dresses off), and the two already disagree at exactly 720px. ⛔ **THREE EDITS, AND MISSING ANY ONE LEAVES A VISIBLE ANIMATION.** (1) `revEntered` initialises seated. (2) `checkRevSequence` early-returns on the phone — **without this the EXIT half still runs and a scroll back up past `REV_EXIT_LINE` throws the three cards off-screen again**, so the section would be empty on the way back down; it also seats rather than merely returning, because a resize from a desktop width arrives with `revEntered` false. (3) `soloPlace` skips the CSS-borrow branch, so `.rev-live` goes on SYNCHRONOUSLY and `transition:none` applies — leave that branch in and the first seat eases the cards from identity into their slots on load, which is a different animation in the same place rather than no animation. ⭐ **PROVEN: six samples — before the section is on screen, scrolling in, sitting in it, and after scrolling back up past the old exit line — all byte-identical, `transition:none` throughout, `rev-live` on at load.** Paging still rolls through intermediate frames and returns exactly to its start |
| D144 | 12 Aug | ⭐⭐ **THE HERO'S GOLD LINE RUNS OFF THE SCREEN. THE "CORNER" HE COULD SEE WAS THE ARC'S TERMINUS, NOT A CORNER AT ALL** | Client, with a screenshot of both corners: *"the gold line should fold or go almost out of the screen. Shouldn't just go straight up so that the corners aren't visible."* ⛔⛔ **A QUARTER CIRCLE TURNS THROUGH A FULL 90°, SO ITS LAST POINT HAS A VERTICAL TANGENT — and at `curveOut:0` that point sits exactly on the screen edge, where the gold line stops dead in mid-air against the dark hero. That stop is the mark he was pointing at, and no amount of tuning R moves it: a bigger R only makes the vertical stub taller.** ⭐ **SO THE BOX HANGS PAST THE VIEWPORT AND THE TERMINUS GOES WITH IT.** `--curveOut:14px`, `--curveR:48px` (was 26): with the arc's flat-tangent point at `R − curveOut` from the screen edge, the line crossing x=0 is still mid-turn — `sin φ = 1 − curveOut/R` puts it at **45.1°, 14.1px above the bottom**, climbing as it leaves the frame. `.hero` is `overflow:hidden` in the base rule so the rest is cut precisely at the screen edge, and `html`/`body` already carry `overflow-x:clip` so there is no sideways scroll to introduce. **Verified at 7× magnification: one continuous curve leaving the frame, no terminus, no stub.** ⭐ **THE FLAT RUN SURVIVES, WHICH IS THE CONSTRAINT THAT MATTERS**: visible flat is `390 − 2(R − curveOut)` = **322px of 390**, against 338px at R:26 — 16px. ⛔ **THAT 16px IS THE WHOLE SAFETY MARGIN.** This edge was a full-width ellipse (`border-radius:50% 30px`) until 11 Aug and the client threw it out (D97) precisely because it curved the ENTIRE bottom edge with no flat run. A soft corner and that ellipse differ only by how much flat is left. ⛔ **THE BOX IS WIDENED RATHER THAN THE CLIP BEING GIVEN NEGATIVE INSETS, AND THAT IS A REAL CHOICE**: `inset(0 -14px …)` does parse and apply (checked), but D133 put a SECOND clip on `.hero-bg` — `overflow:hidden` plus a matching `border-radius`, the mechanism that actually cuts the corner on his iPhone — and a border-radius can only round the element's OWN box. Left at `inset:0` it would round at x=0 while the clip-path rounded at x=−14, and the tighter wins: the photograph would be bitten 48px up at the screen edge along an arc the hairline does not trace. ⭐⭐ **AND D133 IS ANSWERED BY THE SAME SCREENSHOT — his phone shows the stone floor through both corners with no black square.** §9.1 can be closed: the iOS compositing leak was the cause and the three mechanisms held |
| D143 | 12 Aug | ⛔ **THE BRAND MARQUEE IS REPLACED BY A PLAIN DIVIDER ON THE PHONE — because the names he wanted in it cannot go on a public page** | Client: *"in that rolling bar where it says brands we work with, remove the words, and add the names from all the supplier links I sent you."* ⛔ **§2a's list is TopCat's BUYING LIST and §2 rule 9 / D8 keep it off the site** — naming Nile Stone and Next Stone Slabs tells a customer where to go direct. Put to him, and his answer: *"if we cannot name the suppliers, then remove that rolling bar divider completely and just make it a regular divider."* ⭐⭐ **BUT THE STRIP WAS NEVER HIS BUYING LIST, AND ANYONE THINKING OF RESTORING IT SHOULD KNOW THAT.** `SUPPLIERS` in the script is a misleading variable name: it held twelve MANUFACTURER BRANDS — Silestone, Caesarstone, Technistone, Unistone, Compac, Cimstone, Ceralsio, Noble Stone, iStone, CQ Stone, Dekton, Neolith — every one safe to name and never in tension with rule 9. He was told this and chose to drop the band rather than run it with brands alone; the option to bring it back with brands only is still open. ⚠️ **THE BAND WAS THE SECTION BOUNDARY BY DESIGN** (client, 30 July): there is deliberately NO `.section-divider` after the stones section because the strip itself was the seam, so hiding it without a replacement runs stones straight into the estimator. That is why this is a SWAP, not a deletion. ⭐ The band's own `padding-top` drops to 0 on the phone so the divider's symmetric `clamp(26px,6vh,74px)` is the only spacing left — **measured 49px above and 49px below**, which is what "perfectly in between the two sections" means. ⛔ **PHONE-SCOPED, AND DELIBERATELY NOT REMOVED ABOVE 720px** — the strip is an index.html element at every width and desktop is frozen (rule 15, his own words: "everything else stays exactly as it is on the other devices"). ⚠️ New markup cannot be scoped by a media query (D120), so the divider is `display:none` in the base rule and the phone opts in. Verified at 1440: marquee still shown, divider hidden, document height unmoved at 14641, element count +2 — markup present, taking no space |
| D142 | 12 Aug | ⭐⭐ **THE GALLERY ACCORDION RUNS ON A CLOCK, NOT ON THE SCROLL. ⛔ THIS IS THE FIFTH DIRECTION CHANGE ON THIS SECTION AND IT RETIRES THE SCRUB** | Client, twice, the second time because the first pass was not literal enough: *"as soon as the view our project gallery sets in place, the animation will play, and it will just go straight down. The scroll does not change the speed or the up or the down. The animation just plays. So it's not tied to scroll at all."* ⭐ **`q` WAS `p/animFrac` — SCROLL POSITION, SCRUBBED. IT IS NOW `animQ`, WALL-CLOCK TIME**: `ANIM_MS` 1600 from the moment the pin engages, and it only ever increases, so a flick upward cannot rewind the deal and a fast scroll cannot fast-forward it. ⭐ **THE TRIGGER IS `r.top <= 0`, THE INSTANT THE PIN ENGAGES**, which is "sets in place" — an IntersectionObserver threshold would have fired when a pixel appeared at the bottom of the screen, which is a different moment and the wrong one. ⚠️ **`travel` IS DELIBERATELY STILL SCROLL-DERIVED AND THAT IS NOT AN INCONSISTENCY**: travel is the column moving up the screen, which IS the page scrolling and must follow the finger. He asked to untie the ANIMATION, not the page. ⛔⛔ **THE LOOP HAD TO BE TAUGHT TO STAY AWAKE, AND MISSING THIS WOULD HAVE SHIPPED A FREEZE.** `frame()` shut itself off the moment the playhead caught the scroll — safe while everything was scroll-derived, because nothing could change without a scroll event to restart it. A clock can, so `playing` now holds the loop open; without it the deal stops dead the instant the finger lifts, which is the exact opposite of the ask. ⭐ **PROVEN WITH THE SCROLL FROZEN**: parked at the pin and sampled every 160ms — scrollY constant at 2119 from 1120ms onward while the card gap went **12 → 13 → 29 → 59 → 94 → 127 → 152**. The animation completed with no scrolling at all. ⭐ It plays ONCE and stays played, the D26/viewSequence precedent. ⛔ Freeze proven at 1440x900 and 768x1024: all eight card sizes, document height, element count, pin and runway identical — `phone` guards the clock and `animT0` is never set above 720px |
| D141 | 12 Aug | ⭐⭐ **A COMPARE PAGE AT `/stones/compare.html`, MULTI-STONE, WITH THE COLLECTION AS ITS PICKER** | ⛔ **CUT BACK TO A LOOK, NOT A DATASHEET, THE SAME DAY IT WAS BUILT.** Client on the first version: *"what I meant by compare the stones is only the VIEW of the stone — two stones next to each other and slightly bigger, and when they add another it goes below. Then only the name and the material, but not the other details."* The ten-row spec table, the horizontal scroller, the sticky label column and the difference markers are all **gone**: two cards across that WRAP need no scroller at all, and the whole apparatus existed only to fit fact rows across n columns. Slab 133x177 → **162x200**. ⭐ **THE FACTS WERE NEVER MISSING — they are on the stone page, one tap away.** ⚠️ The lede was rewritten with it: it promised "the finish, the colour and how each one lives", which was true of a page with ten fact rows and a plain untruth on a page showing slabs and names. **Copy describing a previous version of the design is the quietest way to mislead someone.** ⛔ **AND THE "GIGANTIC OPEN SPACE BELOW THE NAV" HE REPORTED WAS A TYPO, NOT A SPACING CHOICE**: the page shipped `<nav class="crumbs">` where every other generated page says `class="crumb"`, so the breadcrumb was UNSTYLED — `ol` fell back to `display:block` and the three items stacked vertically at **77px instead of a 26px row**. It also had no `.crumb-back` arrow, which rides with the breadcrumb on all 160 generated pages. ⚠️ **A misspelt class does not error, it just silently renders the browser's defaults** — and those defaults look enough like a layout decision to be argued about. Nav-to-title 132px → 97px. Client: *"build out a compare stones page with a world class design where you can select multiple stones and see them next to each other. You can always add a new stone onto that page, and then it takes you to the part where you can see all the stones or select them. And there must be an enquiry and all the different things needed on that page to convert."* ⚠️ **HE ASKED FIRST WHETHER IT WAS WORTH BUILDING WITHOUT A VISUALISER, AND THE ANSWER ON RECORD IS THAT THE TWO ARE UNRELATED**: compare answers "which of these two", a visualiser answers "how does it look in my kitchen". ⭐ Compare is also the SAFER of the two here — it only ever shows two supplier photographs side by side, where a visualiser puts a stone into a room that is not the customer's, which runs straight at the integrity rule (§3). ⛔ **It was also said plainly that this is not the highest-value item — the enquiry form still has no backend** — and he chose to build compare anyway, which is his call and is recorded as such. ⭐ **ONE PAGE READING THE QUERY STRING, NOT 132x132 PAGES.** `?s=a,b,c` means a comparison is a LINK, which is the half of this with a business case: TopCat have no showroom (D5), so the one thing a showroom does that this site could not is hold samples side by side — and Nick can now send that view to a customer in a text. ⛔ **THE LAYOUT IS ONE CSS GRID INSIDE ONE HORIZONTAL SCROLLER, AND THE ALTERNATIVE IS THE TRAP.** The obvious build — stone columns with fact labels down the left, each fact its own flex line — makes the labels and the columns two separate scrollers that script must keep in sync every frame, on the device already complaining about smoothness (§9.3). One grid in one scroller moves as a unit for free and `position:sticky;left:0` holds the labels while the stones slide under them. **No JS touches scroll at all.** ⚠️ **SIZE THE COLUMNS AGAINST THE WRAP'S CONTENT BOX, NOT THE VIEWPORT** — got this wrong once and two stones scrolled 33px for no reason: a 375px screen gives `.wrap` **335px**. At `68 + 2x124` two always fit and the 1fr spreads them; three is 440 and scrolls, which the hint line announces. ⭐ **THE PICKER IS THE COLLECTION, NOT A LIST OF NAMES** — same photographed tiles, same material tabs, and **the same search**: `_haystacks()` was factored out so the grid and the picker are literally one function, and `SCOPED` is the same emitted vocabulary (D139). Two copies would be D51 in its purest form. ⭐ Rows where every stone says the same thing are unmarked; the ones that DIFFER carry a gold dot, which is the whole job of the table. A row no stone has a value for is dropped rather than printed as a line of em-dashes — `Range` is empty on every quartz and granite by design. ⚠️ **`?stones=` RIDES THE ENQUIRY LINK AND NOTHING READS IT YET.** It is carried so the day the form is wired the shortlist arrives with the customer. **Do not mistake it for a working handover.** ⛔ **`verify.py` FIRED THE MOMENT THE PAGE WAS BUILT** — check 5 counts every `.html` in `/stones/` as a stone page. The fix was to name `compare.html` in an explicit `NOT_A_STONE` set, not to loosen the check: every name excused there is a name it can no longer catch. **Rebuilt and gated: ✅ PASS.** ⚠️ Entry points are on the stone pages (with the "more to consider" strip, deliberately not beside "Get an estimate") and the collection controls, at every width — the stones pages are not part of index.html's frozen composition, and §8 records that he has not seen their desktop at all |
| D140 | 12 Aug | ⛔ **`URL is not a constructor` ON EVERY STONE PAGE WAS REAL, AND A PREVIOUS SESSION DISMISSED IT AS A STALE CONSOLE ENTRY** | The back crumb's inline handler read `new URL(document.referrer,location)`. ⭐ **INSIDE AN INLINE `onclick` THE SCOPE CHAIN INCLUDES THE ELEMENT AND THE DOCUMENT**, so the bare identifier `URL` resolves to `document.URL` — a STRING — long before it reaches `window.URL`. The handler threw on the collection page and on all 132 stone pages. ⚠️ **IT LOOKED FINE, WHICH IS WHY IT SURVIVED**: the throw meant `history.back()` never ran, the click fell through to the `href`, and the page navigated anyway — just always as a fresh load rather than a real back. The only symptom was a console error. ⛔ **§7 of the START HERE cites this exact error as an example of the console REPLAYING STALE ENTRIES from another page** — the replay is real, but it was replaying a genuine error, and "the console lies" was used to dismiss it. ⭐ **The check that settles it costs nothing: open a NEW TAB, load the page, read the console.** A fresh tab has nothing to replay. Fixed to `new window.URL(...)`, verified by executing the handler directly (throws nothing) and by a clean-tab load (no errors) |
| D139 | 12 Aug | ⭐⭐ **THE COLLECTION SEARCH WAS FIRING ALL ALONG — IT WAS ANSWERING WITH MOST OF THE COLLECTION. A COLOUR OR MATERIAL WORD IS NOW ANSWERED BY THE CLASSIFIER, NOT THE PROSE** | Client: *"the stone search isn't actually working. When I type white, it's not only showing all the white ones. Nothing changes when I type anything in on mobile."* ⭐ **HE READ THE SYMPTOM EXACTLY RIGHT AND THE CAUSE WAS NOT WHAT IT LOOKED LIKE.** Measured on the built page: `white` returned **79 of 132 and 29 were not white stones**, `marble` **112 of 132**, `grey` 80, `veining` 79. Specific words were fine — `calacatta` 20, `gold` 24, `matt` 12. **A filter that returns 60–85% of a 132-tile grid, on a phone's single column, is indistinguishable from one that does nothing.** ⛔⛔ **TWO INDEPENDENT FAULTS, AND FIXING EITHER ALONE LEAVES HIM STILL RIGHT.** (1) **The description is in the haystack** — deliberate on 10 Aug so "sparkle", "pebbles" and "copper" would find anything, and still right for those, but it means Marquina, a BLACK stone described as having "white veining", answers a search for white. A colour word in the prose describes a DETAIL; a colour word in `hue` describes the STONE. (2) **The match is a SUBSTRING** — `indexOf("marble")` finds it inside `marbleeffect` and `marblelook`, the keywords every quartz carries, so all 67 quartz answered "marble". No amount of cleaning the prose would have touched that half. ⭐ **THE RULE: a word that NAMES one of our own classifiers is matched token-exact against a new `data-attr` (the facts only, no prose); everything else keeps the loose substring search over `data-find`,** so the 10 Aug behaviour is untouched for the words it was built for. ⭐ **THE VOCABULARY IS DERIVED FROM THE CATALOGUE AND WRITTEN INTO THE PAGE BY THE BUILDER** (`scoped_words()`, 35 words) — D51's lesson: a hand-kept list beside data that also lives in the data does not error when it goes stale, it just quietly stops working. Add a hue and the page learns it at the next build. ⚠️ Deliberately NOT scoped: "warm", "bright", "natural", "soft", "plain" and the durability words — they read as adjectives, a customer typing them means something fuzzy, and scoping them would narrow honest results. **Measured after: white 50 (0 wrong), black 16, marble 45, quartz 67, granite 20 — exactly the catalogue's own distribution** — while sparkle 41, copper 5, pebbles 1, calacatta 20, gold 24 and matt 12 are unchanged, and "white marble" 19 and "light grey" 13 now narrow properly across two terms. ⚠️ The three extras that survive are correct and must: **Arabescato Grey** and **Calacatta Grey Classic** are classified white but NAMED grey, and **Arctic Cream** is classified grey — someone typing the name has to find them. ⭐ **THE WHEEL ON index.html WAS ALREADY RIGHT** and needed no change: `stoneHaystack()` indexes name, kind, tone, hue, vein, finish and silica and has never carried the prose, so this closes a gap between the two surfaces rather than opening one. ⛔ Rebuilt and re-gated: **all 132 stone pages regenerated byte-identical** (only the collection page changed), `verify.py` ✅ PASS |
| D138 | 12 Aug | ⭐ **SOME CURVE COMES BACK (bend 22 → 30), AND THE WHEEL'S HEIGHT CAP DROPS 450 → 390 TO CLOSE BOTH GAPS. ⛔ NEITHER GAP WAS A MARGIN** | Client, on the D137 build: *"add a little bit more of that curve back. So I don't want you to revert back to what it was, but I want to see slightly more of that half moon curve than there is now."* And: *"move the name of the stone closer, where it says quartz, and the buttons also go closer… and move it slightly closer to the granite and all stones buttons. So just the gap in between, make it slightly smaller."* ⭐ **22 HAD GONE TOO FLAT AND READ AS A ROW, NOT A FAN.** At 30 the second ring hangs **3px** below the centre card instead of −1 — still level to the eye, still no protruding corner — and the outermost is 20px down and off-screen. Re-ran D137's corner hit-test at 26, 30 and 34: **the exposed set does not change**, so the curve can be tuned in this band without reopening the corner complaint. ⛔⛔ **BOTH SPACING ASKS WERE ONE FAULT, AND IT WAS NOT IN ANY MARGIN — REACHING FOR THE MARGINS WOULD HAVE FIXED NEITHER.** The wheel is a FIXED-HEIGHT box with the card centred in it, so taking 20% off the card in D137 left the difference as dead air split evenly above and below: measured **68px over the cards and 74px under them, inside the box**, against margins of only 18px and 8px. Pulling the readout up by its margin would have moved the text and left the hole; the top gap had no margin to take at all. Dropping the CAP to 390 closes both at once: **86/82 → 55/41 measured**, and the section gets 60px shorter. ⭐ **ONLY THE CAP CHANGES** — the `clamp(272px, 100vh − 310px, …)` shape is untouched, so the short-screen protection is exactly as it was; verified at 375×667, where the clamp binds at 357 and the card follows it down to 155×252 through the `--wheelH` arm of `--stoneW`'s `min()`, cards still inside the box. ⚠️ **THE PHONE'S SIZE CONSTANTS HAD TO STOP BEING THE BASE 0.76/0.583/56vw**, and this is the trap: `--stoneScale` multiplies the vw arm too, and that arm does not track `--wheelH`, so shrinking the box while holding the scale would have made the card WIDER. The phone now carries its own three constants (0.705 / 0.433 / 45vw) chosen so both `min()` arms are equal at 375×812, and `--stoneScale` stays the single knob for "smaller". Card unchanged at **169×275** |
| D137 | 12 Aug | ⛔⛔ **THE WHEEL'S VISIBLE BOTTOM CORNER IS THE ROTATION, NOT THE SAG — AND THAT IS WHY D135's FIRST PASS DID NOT FIX IT. Bend goes 64 → 22 and the card to 0.80** | Client, on the D135 build, with a screenshot: *"on the third slab, the one that's just going off the screen, arabescato grey, and then the one on the other side that's also going off the screen, you can see their bottom corners. I don't actually want the bottom corners of it to be visible. So I want you to make the half moon curve even less, and you can make the main stone even slightly smaller."* ⭐ **HE NAMED THE THIRD SLAB, AND THE THIRD SLAB IS A ROTATION PROBLEM.** Its CENTRE is already off-screen at x=379 on a 375px phone — but the card is tilted `asin(ax/R)`, and at 37.7° its bottom corner swings back INWARD to x=363, on screen. Raising R drops the tilt and swings the corner back out past the edge. **Measured off-screen at bend 44 and below.** Flattening works here for a reason that has nothing to do with the sag everyone reaches for first. ⚠️ **THE SECOND RING CANNOT BE SOLVED THAT WAY, AND THE NEXT PERSON WILL WASTE A ROUND FLATTENING FURTHER TRYING.** Its half-width is smaller than its distance to the screen edge, so its outer bottom corner is on-screen at EVERY bend — it is exposed by construction, because z-order runs centre-highest and the card outboard of it sits BEHIND, so nothing can cover it. ⭐ What CAN be fixed is how far it hangs: measured below the centre card's bottom it drops **9px at bend 44, 2px at 28, −1px at 22**, and at 22 it comes level so the foot of the fan reads as one continuous line with the centre card proud of it rather than a row of points. ⛔ **22 IS ALSO THE FLOOR**: by bend 12 the outermost card sits 4px ABOVE the centre and it stops being a fan at all, losing the "almost like a half moon" he has asked to keep in both messages. ⭐ **THE TEST THAT SETTLED IT IS WORTH KEEPING**: compute each slab's four corners through its own `DOMMatrix` (a bounding rect is useless on a rotated card), inset each bottom corner 7px toward the card centre, and `elementFromPoint` it — it returns `offscreen`, `covered` or `VISIBLE` per corner and turns "I can see the corners" into a number. ⭐ Card scaled again to **0.80 of base, 210×342 → 169×275**, and it is `--stoneScale` now, one token multiplying all three base numbers, because he has asked for "smaller" twice. ⚠️ Card size feeds `step` (`cw*0.52`), so the ring closed from ±192 to ±175; five stones still show, which is D48's decrowd. ⛔ Freeze re-checked at both frozen widths: `--wheelBend` and `--stoneScale` unset, slab back at its base 262×342, document height and element count unmoved |
| D136 | 12 Aug | ⭐ **THE STONE WHEEL'S ARROWS LOSE THE DISC AND BECOME GOLD-GRADIENT CHEVRONS** | Client: *"we can remove the circle around those arrows and just make the arrows there as they are just with the arrow part and make that gold or gold gradient arrows."* ⭐ **THE SAME MOVE D119 MADE ON THE REVIEW ARROWS, NOW ON THE WHEEL'S** — a 52px ringed disc is a CONTROL, and he has now twice asked for a mark instead. The border, the fill and the hover fill go; the 52px box STAYS because it is the tap target and not decoration. ⭐ **NO NEW COLOUR WAS ADDED**: the stroke takes `url(#tcGoldSolid)`, the paint server already sitting in the `.tc-defs` sprite, so the chevron is lit champagne-to-antique exactly like every other gold mark on the page. It resolves against each arrow's own bounding box, so the two light identically. ⚠️ The glyph goes 18px → 26px: without its disc an 18px chevron has nothing giving it presence and simply reads as small — the same "the number was right for the old container" fault as D126. ⛔ Phone-scoped; `.wbtn` dresses the FROZEN tablet's wheel too |
| D135 | 12 Aug | ⭐⭐ **THE STONE WHEEL'S CURVE IS A PHONE NUMBER NOW, AND THE CARD IS SCALED TO 0.88. ⛔ THE PHONE HAD BEEN CARRYING FOUR TIMES THE DESKTOP'S CURVE FROM A CONSTANT NOBODY SET** | ⛔ **THE MECHANISM HERE STANDS; THE TWO VALUES DO NOT — bend 64 and scale 0.88 were superseded the same evening by D137, which is where the working numbers and the real diagnosis are.** ⚠️ Keep this row: it is the record that halving the sag was not enough, and WHY — the corner he could see was put there by the tilt, not the drop, and this pass fixed the drop. Client: *"in the choose your stone section it's very curved where you can see the bottom corners of the design. Make the blocks, the actual centre block, slightly smaller and have the sides go less curved. So still almost like a half moon kind of shape, but going out of the screen, not curving down like it currently is, slightly more towards the middle of how it looks on desktop versus how it looks now."* ⭐ **THE BEND IS A SAGITTA AND IT WAS THE ONLY THING WRONG.** `R=(H²+B²)/2B`, so at the edge of the fan the arc term is exactly `B` — the sag of the outermost lit card below the centre one — and the tilt, `asin(ax/R)`, falls out of the same circle. ⛔ **`BEND_PX` IS ABSOLUTE AND `H` IS NOT.** 120px of sag over a 720px half-width on a 1440 desktop is a ratio of 0.17; the same 120px over a 187px half-width on a 375 phone is **0.64**, very nearly four times the curve, and nobody ever chose it. ⭐ **MEASURED, AND IT IS EXACTLY WHAT HE DESCRIBED**: at 120 the outer cards sagged 128px and their bottoms hung **21px PAST THE BOTTOM OF THE WHEEL'S OWN BOX** — the "bottom corners of the design" he can see. At **64px** the sag is 72px and the lowest card sits 10px INSIDE it; ratio 0.34, against desktop's 0.17 and the old 0.64, which is the middle he asked for, nearer the desktop end. ⭐ Declared in CSS as `--wheelBend` and read back by `metrics()` — the `--hxMode`/`--galMode`/`--svcMode` idiom (D96, D105, D117) — declared ONLY inside `@media(max-width:720px)`, so desktop and tablet read nothing and fall back to `BEND_PX` exactly. ⚠️ **CACHED, AND THAT IS NOT A MICRO-OPTIMISATION**: `metrics()` is called from `layout()`, which runs on EVERY rAF frame, and `getComputedStyle()` forces a style resolve — a per-frame style read on the wheel is the last thing to add to the device whose only open complaint is that the animations are not smooth (§9.3). Invalidated on resize, the one moment the query can flip. ⭐ **THE CARD IS SCALED, NOT RESHAPED**: both tokens take the same 0.88, so the slab keeps its proportion and `object-fit` re-crops the photograph rather than squashing it — **210×342 → 184×302** (D125's lesson from the gallery). ⛔ **FREEZE PROVEN**: the script change runs at every width, so all 67 slab transforms were hashed at 1440×900 and 768×1024 against the pre-round file — identical at both, along with document height and element count |
| D134 | 12 Aug | ⭐ **THE REVIEWS SECTION NO LONGER FADES IN ON THE PHONE** | Client: *"remove the animation of the 'hear it from your neighbours' coming in. I want it just to already be there."* ⭐ **MEASURED BEFORE CHANGING ANYTHING, AND ONLY ONE THING IN THE SECTION ACTUALLY MOVES.** The deck and all sixteen cards are already `opacity:1` with no transform before the section is ever reached — the entrance is the section head alone, `.rise`, holding `opacity:0` and a 34px offset until the shared IntersectionObserver adds `.in`. So `#reviews .section-head.rise{opacity:1;transform:none}` is the whole change, and there was no second animation to hunt. ⛔ **SCOPED TO `#reviews`**: `.rise` dresses every section head on the page, so killing it globally would strip the reveal off About, Why, the FAQ and the rest. ⚠️ He called it "the collage", which is the ABOUT section's word — if he meant that one instead, it is a different (scroll-driven) engine and a different change |
| D133 | 12 Aug | ⛔⛔ **THE HERO'S ROUNDED CORNER WAS NEVER GEOMETRICALLY WRONG — IT IS AN iOS COMPOSITING LEAK, AND THAT IS WHY D115 SHIPPED LOOKING CORRECT** | Client: *"I can still see the black corners going through the divider. The rounded corner should be perfect with no black corners past the roundedness. You should only see the stone behind it."* ⭐ **VERIFIED AT 6× MAGNIFICATION IN CHROME, WITH THE PAGE FLOOR FLOODED FLAT RED TO SEPARATE THE LAYERS: the arc is clean, the hairline traces it, and the floor shows through outside it. Nothing inside the hero paints past the corner.** ⛔ **THE CAUSE IS THAT `.hero-bg img` CARRIES `will-change:transform` AND A `scale(1.08)`**, which promotes it to its own compositing layer — and a promoted child is composited AFTER its parent's `clip-path` is rasterised, so on iOS Safari it paints past the corner. The photograph is near-black at its foot, so what escapes is exactly the black square corner he can see and this Mac cannot. ⭐ **THREE INDEPENDENT MECHANISMS, BECAUSE THE FAILURE IS INVISIBLE FROM HERE**: `overflow:hidden` plus a matching `border-radius` clip the box in the normal paint pass, `translateZ(0)` makes `.hero-bg` own the layer so the corner is cut in the compositor where the img actually lives, and dropping the child's `will-change` removes the promotion that causes it at all — it only ever bought a one-shot 3s load zoom, which is not worth a permanent layer on the device already complaining about smoothness. ⚠️ Adding a transform to `.hero-bg` changes no containing block: `.hero-shade` is `position:absolute` and `.hero-bg` was already its nearest positioned ancestor. ⚠️⚠️ **NOT VERIFIABLE ON THIS MACHINE — IT NEEDS HIS EYE ON THE PHONE.** ⭐ If the corners still read black after this, the leak was not the cause and the next lever is the floor's brightness at the top of the page, which is D123's number: outside the arc the corner shows the page floor, and at the top of the page that stone is very dark |
| D132 | 12 Aug | ⭐ **THE BURGER LOSES ITS BOX ON THE PHONE — THREE STRIPES, NO SQUARE** | Client: *"on the navbar, I don't like the current design where it has a square around the three stripes for the menu options. I just want the stripes by itself and just make it fit nicely and look nice within the design."* ⛔ **THE BASE RULE IS NOT TOUCHED, AND THAT IS THE WHOLE CARE HERE**: `.nav-burger` is switched on at `max-width:1120px`, so undressing it in the base rule would strip the box off the FROZEN tablet too, where the header still carries its CTA beside it. ⭐ **THE 42px BOX STAYS** — it is the TAP TARGET, and it is the one thing in that rule that is not decoration. Only the border, the fill and the backdrop blur go. ⚠️ The stripes were 18px wide inside a 42px box, drawn to sit comfortably INSIDE a border; with the border gone that reads as the leftovers of a button rather than a mark, so they run to **24px** and the pitch opens 6px → 7px to keep the block square-ish. Same family as D126 — a number that was right for one container is rarely right once the container goes |
| D130 | 12 Aug | ⭐⭐ **THE CARDS RIFFLE IN FROM THE SIDES. ⛔ REVERSES D105's "FIREWORK FROM BELOW"** | Client: *"the animation is not going to come up from the bottom any more. It's going to almost shuffle, like two sides of a deck of cards, shuffling into each other from the sides."* ⭐ **ALTERNATING ON PILE DEPTH IS WHAT MAKES IT A RIFFLE RATHER THAN TWO QUEUES.** The cards already arrive strictly back to front (D128), so alternating on depth means each consecutive arrival comes from the opposite side and the halves bridge into each other; alternating on the SET or the DOM index would deal four from one side then four from the other. ⛔ **D105 REJECTED A SIDE ENTRY FOR A REAL REASON AND THAT REASON IS GONE — do not read this as the old bug returning.** Then the cards were small and centred, so a stage width out plus most of a card width was ~450px off a 375px screen and most of every flight happened where nobody could see it. The card is now full-bleed, so `(stage + card)/2` = **361px** is exactly where its trailing edge clears the frame and every moment of the flight is on screen. ⚠️ `y` and `sc` are the pile's own, untouched — the cards arrive at full size (D127) and slide in flat. ⭐ **Verified mid-shuffle**, centres in arrival order against a stage centre of 188: 188 · 166 · 252 · 66 · 373 · −61 · 491 · −156 |
| D129 | 12 Aug | ⭐ **THE TITLE GAP, THE DEAL DIRECTION AND THE CTA'S PLACE — the title gap is the one I got wrong TWICE** | ⛔⛔ **THE TWO TITLE ASKS ARE NOT A CHOICE, THEY LIVE IN DIFFERENT FRAMES, AND TREATING THEM AS A CHOICE COST TWO ROUNDS.** `padding-top` is measured from the SECTION, so once pinned it holds the title clear of the fixed nav (D128) and must stay. Before the pin engages that same padding is dead space in the FLOW, on top of the divider's 49px margin: 49 + 107 = **156px** against the 86px he asked for twice. ⭐ **Pulling the section up by exactly the reserve it adds cancels it in the flow and changes nothing once pinned**, because a pinned element positions against the viewport, not its own margin: 37 − 78 − 29 = −70, and 156 − 70 = 86. Written in the same tokens as the things it cancels so it cannot drift. **Measured 86/86.** ⭐ **THE DEAL RUNS FROM THE FRONT OF THE PILE** (*"one by one from the front card… and the one at the back stays there"*) — the physically honest order: the front card is the LAST column slot with the whole column to travel, the back card is already at slot 0. Dealing from the back sent the shortest journey first and left the visible card still. ⭐ **Get in touch centred between the last card and the section end**: the gap above is `F − 24`, below is `barH + 24` = 83, so `F = 107`. **Measured 83/83**, from 25/83 |
| D128 | 12 Aug | ⭐ **THE NAV RESERVE RETURNS, THE NAMES SHOW, AND THE PILE AND COLUMN BECOME ONE ORDER** | ⛔ **D124 WAS RIGHT WHEN WRITTEN AND WRONG THE MOMENT THE PIN CAME BACK** — pinned, the copy sits at the top of the viewport for the whole animation and the fixed 78px nav covers it, which is the cut-off "GALLERY" in his screenshot. ⭐ **THE PILE'S DEPTH IS NOW THE COLUMN SLOT COUNTED BACKWARDS** (client: *"the top card should be the one at the back, and the one in front must go all the way down; the second is second last"*) — a deck being dealt, so arrival, stacking and dealing all read off one function instead of the column using project order and the pile a desktop dealing order with no relation to it. ⛔ **`.gal-meta` WAS `opacity:0` UNTIL `.gal-set.settled`**, which on a phone needs the accordion finished — so every card was anonymous through the one beat you look straight at them. Turning them all on then printed eight captions over each other in a pile 11px apart, so each caption now arrives with its own card and only the front card's shows in the pile. ⛔⛔ **AND THE PLANE Z-ORDER HAD TO INVERT WITH THE DEPTH MAPPING — I MISSED IT AND IT SHIPPED**: the front card moved into set 1 while set 0 kept the higher z, so the deepest cards painted OVER the card you were looking at (wrong photograph, right name underneath). **Second time in two days (D125). Whenever the depth mapping changes, change that line with it.** |
| D127 | 12 Aug | ⛔⛔ **THE PIN IS BACK, REVERSING D122's RELEASE — AND THE CARDS ARRIVE AT FULL SIZE** | Client: *"the title must not move from its finished position… the screen is gonna stay on the exact same position so they can see the first one, and it doesn't scroll past."* ⭐ **HE WAS DESCRIBING THE UNAVOIDABLE COST OF AN UNPINNED ANIMATION.** If the page really scrolls while the cards animate, then by the time the last one lands the viewport has travelled the animation's whole length — the title is gone and you are looking at the END of the column. Holding the screen still while a scroll-driven animation plays IS a pin. ⚠️ **This does not undo D122's reasoning, it splits it in two:** phase 1 holds everything still and plays the animation; phase 2 translates cards and copy together by exactly the scroll distance, which is what the browser would have done, so it still reads as ordinary scrolling. ⭐ **`travel` is 0 at the moment the animation ends** — the frame the accordion finishes on is the frame showing the FIRST card, which is the actual requirement. ⚠️ `.gal-mid` takes the COLUMN's height, not the stage's, or `margin-top:auto` parks Get in touch at the bottom of the SCREEN for the whole scroll. ⭐ Client also: *"the stack should already be in its full size, not a smaller version"* — the per-depth shrink goes; every card is `colSc` and only offset |
| D126 | 12 Aug | ⭐ **THE SERVICE TILES' CORNERS ARE SQUARER** | Client: *"all four corners of each block is rounded just a bit too much. Make the roundness a bit less — almost square with a little bit of a rounded corner, but not too square."* 14px → **7px**, phone only. ⚠️ The number was never wrong, the CARD changed under it: `.face`'s 14px was drawn for a 300px-plus desktop card, and on a 129px tile the same radius is more than twice the proportion of the edge — which is why he sees it here and not on desktop. A radius set once for one card size will always drift when the card is resized (D118 took these to 146×106, D125 shrank them again) |
| D125 | 12 Aug | ⭐⭐ **THE GALLERY: SLOWER ARRIVAL, AN EVEN STACK, AN ACCORDION OPENING, AND CARDS A THIRD SHORTER. ⛔ ONE OF THESE WAS A BUG I SHIPPED IN D122** | Client: *"the animation must follow slower… delay when the first card comes in… some of it is stacking in an uneven way, we need it to stack evenly so you can see the cards behind the first one… then slowly one by one, just like an accordion, filtered down, because currently it's just jumping into position… make them one third smaller only on the height, not the width."* ⛔⛔ **THE UNEVEN STACK WAS THE REAR FOUR PAINTING OVER THE FRONT FOUR, AND D122 CAUSED IT.** The set planes used to be ordered `10 + u*5` — set 0 at z 10, set 1 at z 5. Flattening both to a constant 10 for the column handed the paint order to the DOM, and set 1 comes last, so the cards meant to peek out BEHIND the front card were drawn ON TOP of it. ⚠️ **It looks like a spacing fault and is not one** — every card's `x` is 0 and the offsets were always right. Measure the paint order, not the geometry. Now `10 − s` on a phone. ⭐ **AND THE PEEK HAD SILENTLY SHRUNK BY A THIRD**, because it is a fraction of card HEIGHT and this same round took a third off the height: 6.4px → 4.3px, and the pile closed into what looked like one card. `PEEK_PHONE` 0.17 restores ~12px a step. ⚠️ The fan hangs UPWARD, so a pile whose front card sits on slot 0 reaches `7 × peek` = 82px higher and put the back of the deck through the subtitle — the pile is dropped by half the fan to centre it. ⭐ **ONE THIRD OFF THE HEIGHT IS THE CARD'S OWN RATIO, NEVER A `scaleY`**: a non-uniform transform squashes the PHOTOGRAPH with the card, visibly stretching kitchens; changing the ratio leaves `object-fit:cover` to re-crop the same picture. 331×219 → **331×145**, width untouched. ⭐ The accordion slices the RAW linear spread per card (`COL_STAG` 0.58) and eases each slice — easing first and slicing after would compress every card into the same curve and undo the stagger. ⚠️ The stagger runs on the COLUMN ordinal, not the gather ordinal. Gather delayed 0.02→**0.30** of the approach and stretched to 0.98, so no card moves until the heading is most of the way up |
| D124 | 12 Aug | ⭐ **THE GALLERY TITLE SITS THE SAME DISTANCE BELOW THE DIVIDER AS THE SERVICES COPY SITS ABOVE IT** | Client: *"that title is too far away from the divider. Look where it says not seeing your space here, and how far that is from the divider — the view our project gallery should be the same distance away."* ⛔ **IT WAS 102px OF DEAD SPACE LEFT BEHIND BY THE PIN.** `.gal-mid`'s `padding-top:calc(--galNavH + 24px)` existed because the block sat in a 100vh sticky window welded to the top of the screen, where the fixed nav really did cover its first 78px. D122 released that pin the day before, so the section scrolls under the nav like every other one and needs no reserve — but the padding stayed. **Measured 86px above the divider, 151px below; now 86/86.** ⭐ 4.6vh rather than 37px, so the pair stays matched as the viewport changes. ⚠️ **AND `colTop` HAD TO STOP RE-DERIVING THE SAME DISTANCE FROM PARTS** — it was `navH + copyH + air`, which silently assumed the old padding and would have left a 41px hole between the subtitle and the first card. It now reads the subtitle's real `offsetTop + offsetHeight`. Two numbers describing one distance drift the moment either is tuned (D107) |
| D123 | 12 Aug | ⭐ **THE STONE FLOOR IS LIFTED ON THE PHONE — MORE VISIBLE THERE THAN ON DESKTOP. ⛔ A DEVICE-SCOPED REVERSAL OF THE 9 AUG DARKENING** | Client: *"the stone background with the gradient is almost completely not visible on mobile. It needs to be more visible on mobile than on desktop, so we can see there's an actual background and it's not just black. Some sections just look black."* ⭐ **MEASURED, AND HE IS EXACTLY RIGHT.** Sampling the real `cover` crop at 375×812: the graded slab is median 12.9 / p90 19.9 / max 44.9, but under the 0.70 veil the phone rendered **median 5.4 / p90 7.5 / max 15**. Site ink is luminance **11.3**, so the brightest tenth of the whole background still sat BELOW the colour of the sections on top of it and only **0.1%** out-brightened them. It was not a subtle background, it was one hidden under the black. ⚠️ **THE CROP IS NOT THE PROBLEM AND WAS THE OBVIOUS WRONG SUSPECT**: `cover` at this width DOWN-scales the 2048px slab to 0.705 and shows a 26% slice, so the grain is at full resolution. Reaching for `background-size` would only tile a photographed slab and run seams down the page. ⭐ **0.46 IS MEASURED: median 7.9 / p90 11.7 / max 25.2, 11% above ink.** ⛔ Do not go below ~0.40 without re-measuring — at 0.34 it is 23% above ink and the sections stop standing proud, which is the exact complaint the 0.70 veil was introduced to fix. ⚠️ The two corner glows were lifted out of the `background` shorthand into `--floorGlowA/B` purely so the phone could raise them; desktop values are unchanged to the digit and the resolved background string is byte-identical at both frozen widths |
| D122 | 12 Aug | ⭐⭐ **THE PHONE GALLERY IS ONE SCROLLING COLUMN OF FULL-WIDTH CARDS. THE PIN IS RELEASED AND THE HALLWAY WALK IS GONE** | Client: *"it's still gonna come up and stack the same way that it currently does, but after it comes out of the stack… all the projects you will see in one long line. So one of the projects will go from the left to the right in a rectangular shape, and then the next one will be below that, and eight of them."* ⭐ **THE FORK WAS PUT TO HIM AND ANSWERED: THE LINE SCROLLS.** Eight full-width cards is ~1,800px against an 812px screen, so either the line scrolls or the cards compress to ~55px bands. He chose **the pin lets go and you travel down the column as normal page content, ~200px a card, Get in touch at the foot.** ⭐ **BUILT AND VERIFIED THE SAME DAY.** Measured: eight cards at **331 × 219**, all at x=22, gaps 14–15px, column running 240→2094 inside a 2245px stage, ending on Get in touch. ⛔ **THE ONE THAT WOULD HAVE SHIPPED BROKEN AND LOOKED PERFECT: `settled`.** Clickability was gated on `Math.abs(u)<0.05 && open>0.96`, and `u = walk − s` — with the walk pinned at 0 the REAR set sits at u = −1 for ever, so projects 5–8 would have rendered flawlessly and refused every tap. Verified after the fix: both sets `settled`, all eight `pointer-events:auto`, hit-tests reaching the card, and the eighth opening its story. ⚠️ Two more of the same shape: `M.h` must stay the VIEWPORT (every other function reads it as "one screen", and entryPos launches a card `M.h*0.62` below the pile — against a 2245px stage the whole gather would have played out off-screen), and the set PLANES must be forced flat and opaque (the rear plane's z/opacity are hallway terms, and at u = −1 they would have hidden half the column). ⭐ **RELEASING THE PIN ALSO ATTACKS AN OPEN COMPLAINT**: "the animations are not smooth on my iPhone" (§9.5, raised twice, never diagnosed). A pinned section drives eight large composited layers from a rAF playhead every frame; once these cards land they cost nothing per frame at all, and the scrolling is the browser's own. ⚠️ `p` had to be re-based: measured against the section's full height the opening would have stretched across all 1,900px of column, so on a phone it is `-r.top/(vh*0.55)`. ⚠️ The column is ordered `s*PER_SET+i`, NOT `SLOTS[i].ord` — that is the GATHER order (rear set first) and would have printed the projects down the page in the sequence they flew in. **This was a rebuild of a PINNED 3D component into scrolling content:** `.gal-pin` is `position:sticky;height:100vh;overflow:hidden`, `.gal-stage` is `inset:0` inside it, and every card is absolutely positioned and driven by `lerp(lerp(entryPos,stackPos,g), sidePos, spread)` plus a `walk` phase. Releasing the pin means the stage must go tall and unclipped, `sidePos` must be replaced by a `columnPos`, the walk must switch off on the phone, and the runway must be resized — several interlocking changes on the most history-laden component in the file, all of them behind a `phone` guard so the frozen widths keep the hallway. ⭐ **THE ZOOM IS THE SAME ELEMENT, NOT A SECOND ONE**: the card is built `cw` wide and every column slot is that card scaled by `colW/cw`, which is literally the "zoom and extend" he asked for and is why no new art was needed. ⛔ **Freeze proven at 1440×900 and 768×1024: identical, including ALL EIGHT card sizes** — the strongest available signal that the hallway composition never moved — plus pin/stage/runway and the reviews. Element count is +2 at both, which is D120's catch-all line, and document height does not move |
| D121 | 12 Aug | ⭐ **"VIEW AS GRID" IS GONE FROM THE PHONE GALLERY** | Client: *"we will remove the viewer's grid CTA."* That is `.gal-grid-btn`, the ghost button paired with "Get in touch" in `.gal-mid-actions`. ⭐ **HIDDEN, NOT DELETED, AND THE HANDLER IS THE REASON**: `openGrid()` is bound at load to EVERY `.gal-grid-btn` on the page and the grid overlay is reachable from elsewhere, so removing the element from the markup would take it off the frozen desktop too. `display:none` inside the phone query leaves the desktop pairing intact. ⚠️ This leaves `.gal-mid-actions` a ONE-child column on the phone — safe because it is `flex-direction:column`, but re-read D113 before adding a second control back: that same block put the subtitle in the middle of the screen when `space-between` met three children instead of two |
| D120 | 12 Aug | ⭐ **A CATCH-ALL LINE UNDER THE SIX SERVICE TILES: "Not seeing your space here? Contact us"** | Client's own words, with *"that contact us underlined as well."* Links to `#cta`, the same enquiry block the sticky bar's Get a quote points at. ⛔ **THE SCOPING LESSON, WHICH IS DIFFERENT FROM EVERY OTHER MOBILE CHANGE IN THIS REGISTER: this is new MARKUP, and a media query cannot scope an ELEMENT.** A new rule only applies where its query matches, but a new element exists at every width the moment it is added — so the default must be `display:none` in the base rule, with the phone query opting in. ⚠️ Verified at both frozen widths: element count rises by 2 and document height does not move at all, which is the signature of markup that is present but takes no space |
| D119 | 12 Aug | ⭐ **THE REVIEW ARROWS MOVE OUT OF THE BOTTOM BAND AND INTO THE GUTTERS BESIDE THE CARD, AND THE STRAP LINE COMES UP TO MATCH THE SUBTITLE'S GAP** | Client: *"remove the arrows from the bottom of the cards, then move the CTA part a little bit closer up, so it's the same length the way as the subtitle. Then add the arrows in between the gaps of the cards with some transparency, making them gold… without it being too obvious."* ⭐ **THE THREE ASKS ARE ONE CHANGE.** `--revPagerH` is a RESERVED BAND, not decoration — `.rev-stage` grows by exactly that much to hold the arrows under the card — so taking the arrows out of the bottom hands back 50px on its own and the strap rises by that much before a margin is touched. ⛔ Pulling the strap up with a negative margin instead would have left it sitting inside a 50px band of nothing. ⭐ **"THE SAME LENGTH AS THE SUBTITLE" IS NOW THE SAME NUMBER**: the gap above the card is `.section-head`'s 14px plus half of `--revAir` = 30px, and with the band gone the strap needs the same 14px to land at the same 30px. **Measured 30px above / 110px below before, 30/30 after.** ⚠️ The two 14s are a matched pair. ⛔ **THE GUTTER IS 22px AND IT IS NOT A FIXED NUMBER** — it is the peek, and it falls out of `--revScale`, which gridLayout() measures and publishes: the card is transform-scaled inside the deck, so it sits `deckW × (1 − scale) / 2` in from each edge. A hard `left:20px` was correct at 375px and lands ON the card at 320px, so the arrow is placed by calc from the deck offset plus half that inset. ⭐ All the button chrome goes — the base rule is a 46px filled disc with a hairline and a backdrop blur, which is a control; he asked for a hint, so what is left is a 15px champagne chevron at 0.42 |
| D118 | 12 Aug | ⭐ **THE PHONE'S SERVICE TILES ARE LANDSCAPE, NOT SQUARE, AND THEY WEAR A GOLD RIM** | Client: *"add a gold border around the surfaces for every space cards, and I want them to be a little bit thinner instead of being actual blocks, make them more rectangular shapes each of the blocks."* ⚠️ **"THINNER" MEANS SHORTER, NOT NARROWER** — the columns are fixed by the 2-up grid, so height is the only dimension a tile can give up. `aspect-ratio:1.38/1` takes the tile from a 146px square to **146 × 106** (measured 129.4 × 93.7 at 375px, after the grid's 5vw padding and 10px gap). ⛔ **`min-height:0 !important` HAD TO STAY AND THE REASON GETS WORSE WITH A WIDER RATIO** — D114's trap is that `@media(max-width:600px){.svc{aspect-ratio:auto;min-height:420px}}` derives the WIDTH from the height, and at 1.38 that would compute 420 × 1.38 = **580px** of card in a 146px column, against D114's 375px. The wider the tile, the worse the fault it is holding back. ⭐ The rim is the whole border at `rgba(198,166,100,0.62)`, carried above `--hair` (0.34), which disappears at tile size over a photograph. ⛔ **The WHOLE border, never a top edge alone** — §2 rule 10, built by mistake twice before. Reverses D114's square tile ("almost like an ice block grid"), on the client's own instruction |
| D117 | 12 Aug | ⭐⭐ **THE PHONE LISTS SERVICES MOST-POPULAR FIRST, AND EACH TILE IS NOW A LINK STRAIGHT TO ITS PAGE — NO MORE "CLICK FOR DETAILS"** | Client: *"in the surfaces for every space, we're gonna list them from most popular to least popular service. And those cards won't say click for details. It will then go to that specific page."* Order confirmed by him: **Worktops, Islands, Bathrooms, Splashbacks, Outdoor, Commercial**. ⛔ **RE-ORDERING THE `SERVICES` ARRAY IS THE OBVIOUS MOVE AND IT IS WRONG — TWO OTHER THINGS INDEX IT BY POSITION.** The desktop helix spins in `ORDER=[1,0,2,3,5,4]` and the project gallery captions its first four photographs from `SERVICES[0..3]`. Moving the array would silently re-spin a FROZEN desktop helix (§2 rule 15) and swap the gallery's pictures out from under their captions — two regressions, neither of which looks like a bug in the thing that was changed. **The array is data; the order is presentation; they are kept apart.** ⛔ **AND IT IS NOT CSS `order` EITHER, WHICH WAS THE NEXT OBVIOUS MOVE**: `buildCards` stamps a literal `01`…`06` from the build index, so a purely visual re-order runs the numbers **01, 02, 04, 03, 06, 05** down the page. The DOM moves instead and the labels move with it, which also keeps the screen-reader order equal to the visual order and keeps the reveal's `:nth-child` row delays and column offsets addressing the right tiles. ⭐ Scoped by `--svcMode`, declared in the stylesheet and read back by the script — the `--hxMode`/`--galMode` idiom (D96, D105), so no second opinion about what a phone is. **No base rule was touched at all**: the property is declared only inside the phone query and the script falls back to `desktop` when it is unset. ⛔ **`arrange()` BAILS ENTIRELY WHEN THE DOM ALREADY READS CORRECTLY** — on a frozen width the wanted order IS the built order, but `appendChild` still removes and re-inserts every node, restarting transitions and re-entering the reveal. Re-running an identical arrangement is not the same as leaving it alone. ⭐ The tap is intercepted in the CAPTURE phase on the grid, so `buildCards`' flip never fires and the card cannot half-flip on its way out; `buildCards` itself is untouched because the same function builds the tablet grid and the process tiles. ⚠️ The tile keeps `tabIndex=0` but stops being a flip control, so it takes `role="link"` — otherwise a screen reader still offers a hint that is now hidden. **Verified: all six tiles resolve name → correct page** |
| D116 | 12 Aug | ⭐ **THE PHONE'S REVIEW CARD IS CREAM WITH BLACK TYPE, NOT BLACK WITH CREAM TYPE** | Client: *"give the reviews a cream white design instead of the black design, so it'll have black text and the cream white card… just so that the review part stands out a little bit more."* ⭐ **NO NEW COLOUR WAS INVENTED — IT IS THE BRAND'S OWN TWO, SWAPPED.** The card takes `--bone` (#F4F1EA) and the type takes `--ink` (#0B0B0D). ⛔ **SCOPED BY `.rev-solo`, NOT A MEDIA QUERY (D93's rule).** `.rev-solo` is set from `perPage()===1` (`innerWidth<720`), the same test the layout maths uses; a media query here would be a second opinion about what a phone is, and the two already disagree at exactly 720px. It also guarantees tablet cannot catch it — `perPage()` returns 3 from 720px up. ⚠️ **EVERY OVERRIDE IS THERE BECAUSE THE TOKEN WOULD OTHERWISE VANISH, NOT BECAUSE IT LOOKED BETTER.** `--muted`, `--faint` and `--hair-soft` are all WHITE at low alpha — on cream they are invisible, not subtle. Same for `--gold` (#C6A664) on anything that must be READ: ~1.9:1 against #F4F1EA. The stars, the seal and "Read more" move to `--gold-lo` (#8C6B34, ~4.6:1), which is the palette's own antique champagne. ⛔ Do not "restore" --gold here. ⭐ **THE STONE STAYS AND ITS VEIL FLIPS**: `.rev-stone` is marbleSVG() at card scale, the thing that stops a card reading as a plain rectangle (§4); its near-black wash was tuned for a DARK face and left standing would grey the cream card into putty, so it is inverted to a cream wash doing the same job, and the veining reads as faint mottling instead |
| D115 | 12 Aug | ⭐ **THE HERO DIVIDER HAS ROUNDED CORNERS AND A LONGER FLAT RUN. ⛔ REVERSES D97's 30° BEVEL** | Client: *"the Hero section divider that has those sharp corners now — move the flat part even further and then just round the corners near the edge of the screen. So we're gonna make it round corners instead of sharp corners."* ⭐ **THE FLAT RUN AND THE CORNER ARE ONE NUMBER NOW, WHERE THE BEVEL NEEDED TWO.** The bevel's ratio WAS its angle (22/38 = tan 30°), so `--bevX` and `--bevY` could never move independently without tilting the cut away from the hairline tracing it. A quarter-circle has no angle to hold: `--curveR:26px` is the whole shape, and it leaves **323px flat of a 375px screen against the bevel's 299px** — the "even further" he asked for arrives as a consequence of the softer corner rather than in tension with it. ⛔ **THE HAIRLINE HAD TO STOP BEING GRADIENTS AND BECOME A BORDER, AND THAT IS THE REAL LESSON.** D97 drew each diagonal with a `linear-gradient` and derived the gradient angle from the bevel angle. **A gradient can only ever draw a straight band — an arc has no single angle, so no gradient traces this shape.** A border follows `border-radius` exactly and stays 1px doing it. The box is `R + 1px` tall so the straight part of each side border is zero; the left and right borders exist ONLY to paint their half of the arc, because a corner arc is split between its two adjacent borders and leaving the sides transparent draws half a gold arc fading into nothing. ⭐ `clip-path:inset(0 round …)` rather than `border-radius` on `.hero-bg`, because clip-path clips DESCENDANTS and the photo is an `<img>` transform-scaled to 1.08 on load; `border-radius` would have needed `overflow:hidden` added to a base rule desktop shares. ⚠️ **HISTORY, SO NOBODY REVIVES THE WRONG ONE**: this edge was `border-radius:50% 30px` — an ellipse across the whole width with NO flat run — until 11 Aug, and that is the thing the client rejected. A rounded corner is not that ellipse. Keep 2R well under the screen width |
| D114 | 11 Aug | ⛔⛔ **THE HELIX IS GONE ON MOBILE. Six cards in a two-column grid that swing in like double doors. ⭐ THE HELIX STAYS ON DESKTOP** | Client: *"For mobile, we're no longer going to do the helix. I want it to be a simple two by two grid, next to each other"*, then, after a first attempt came out wrong: *"clear small blocks where you can see the full block on each side next to each other… maybe flipping in, like doors closing, from each side."* ⚠️ **THE AGENT WAS TOLD THIS AND DID THE GALLERY INSTEAD, THEN REPORTED THE HELIX AS "NEXT".** That is the fault worth recording: the instruction was explicit and unambiguous and it was deferred without being asked. ⭐ **WHAT THIS DELETES IS AS IMPORTANT AS WHAT IT ADDS.** `--hxMode` is no longer set to `phone`, so `hxPhone()` is false and the phone's entire gesture path switches itself off — `attachSwipe`'s `enabled` gate never opens, the stage's `touch-action:none` stops applying, and the page scrolls over this section the way it scrolls everything else. **D102, D103 and D112 all existed to make a hand-rolled scroll behave on this one component; on a phone there is now nothing to hand-roll.** ⛔ **THE BUG THAT MADE THE FIRST ATTEMPT UNUSABLE, AND IT IS NOT OBVIOUS**: `@media(max-width:600px){.svc{aspect-ratio:auto;min-height:420px}}` was written for a ONE-column phone layout. Left standing it combines with an `aspect-ratio` to solve the card the wrong way round — the ratio is honoured, the 420px floor is honoured, and the WIDTH comes out at 420 ÷ 1.12 = **375px, a full-screen-wide card jammed into a 146px column**, hanging off both edges. `min-height:0` is the whole fix. ⚠️ Two columns also has to out-rank that same `max-width:600px` block's `grid-template-columns:1fr`; it does only because this block is LATER at equal specificity — the third time that ordering rule has bitten (D106, D113). ⭐ **THE DOORS**: each ROW is a pair, left card hinged on its LEFT edge, right card on its RIGHT, swinging in to meet. ⚠️ The delay is per ROW, not per card — `--si` is the DOM index, so a `--si × n` stagger would land one door before its partner and the pair would never meet. ⛔ `perspective` goes on the GRID, never in a transform on `.svc` itself: the flip lives on `.svc-inner` and giving the shell its own 3D context is what flattens a child into painting its back face. ⭐ **FREEZE PROVEN BY TARGETED COMPARISON, NOT BY THE WHOLE-PAGE PROBE** — at 1440 and at 768 every property this round touched (helix display, stage perspective/height/touch-action/mask, card geometry, grid display/columns/gap/max-width, card aspect-ratio/min-height/transform-origin) reads **byte-for-byte identical** before and after. ⚠️ The whole-page probe returned 653 diffs and was **not usable**: the stone wheel alone accounts for 335 of them and the gallery for most of the rest, both being scroll-driven scenes at a different phase. **When the probe is this noisy, compare the specific computed properties you changed — that is deterministic and the probe is not.** ⚠️ Still open from this round: the gallery's one-swipe-to-eight and its "more cards" affordance, the pre-arrival gap, and the divider the client says looks wrong |
| D113 | 11 Aug | ⭐ **THE PHONE'S GALLERY COPY IS SPLIT: TITLE AND SUBTITLE ABOVE THE CARDS, BUTTONS BELOW. ⚠️ AND THE FREEZE PROBE'S 2.6s SETTLE IS NOT ENOUGH — IT NEEDS 8s** | Client: *"I still want the title and the subtitle to be above, and then the cards to pretty much immediately follow after that… after the title settles, the last few cards come in… and then has the buttons below."* ⛔ **THE BLOCK IS STRETCHED, NOT MOVED.** Anchoring `.gal-mid-actions` to a `bottom:` of its own cannot work — `.gal-mid` is `position:absolute`, so it becomes the containing block and the buttons would hang off the BOTTOM OF THE COPY, not the stage. Instead `.gal-mid` spans the whole stage (`inset:0`) and lays its children out in a column; the cards fly over the gap between. ⚠️ `pointer-events:none` on it is now LOAD-BEARING — it covers the stage, so without it the cards stop being tappable. ⛔ **TWO CSS MISTAKES MADE AND MEASURED, BOTH OF WHICH LOOKED PLAUSIBLE:** (1) `justify-content:space-between` has THREE children here, so it put the SUBTITLE in the middle of the screen — the fix is `flex-start` plus `margin-top:auto` on the actions alone. (2) That `margin-top:auto` then LOST, because the base `.gal-mid-actions{margin-top:clamp(...)}` rule sits AFTER the media query at equal specificity; `.gal-mid .gal-mid-actions` outranks it without moving code (D106's ordering lesson, second time). ⭐ The two halves also now fade on DIFFERENT clocks: the heading leads on the gather (`gRaw`), the buttons wait for the spread, because on a phone the copy is above the cards and cannot be covered by one — which is exactly what makes leading with it safe, and why desktop keeps waiting. Measured: title 24px under the nav, subtitle 12px under the title, 407px of clear band for the cards, buttons 24px above the sticky bar. ⚠️⚠️ **THE PROCEDURE CORRECTION THAT MATTERS MORE THAN THE FEATURE: the freeze probe came back with 415 diffs and it was NOT a regression.** §0 of the START HERE says settle ~2.6s; the STONE WHEEL's one-shot entrance is still running well past that, and 335 of the 415 were its 67 slabs at a different rotation. **At 8s the same comparison gives 31.** ⛔ Do not read a large diff as a fault until the wheel has finished — and do not lower the settle back. ⭐ An idle noise-floor check (grab the same page twice, 3s apart, touching nothing) returns 29 and is the honest baseline to compare against. ⚠️ **ALSO A LIVE ENVIRONMENT TRAP: `requestAnimationFrame` DOES NOT RUN WHILE THE BROWSER PANE IS HIDDEN**, and this gallery's loop shuts itself off when caught up and restarts only from a scroll event — so a hidden pane leaves it frozen and every card reads as stuck. It cost a full false diagnosis of "I broke the gallery". Check `document.visibilityState` before believing a still scene |
| D112 | 11 Aug | ⛔ **"IT JUST JUMPS BETWEEN SECTIONS" WAS A GLIDE THE CUSTOMER COULD NOT STOP, PLUS A RELOAD THAT THREW HIM BACK TO THE TOP. FOUR FAULTS** | Client, on the services helix: *"when I put my thumb down… whenever I try to scroll in that whole section, everything glitches out and it takes me right back to the hero. It's jumping between sections. I'm seeing every section at hyper speed with my thumb barely moving."* ⭐ **(1) THE GLIDE WAS UNSTOPPABLE, AND THAT IS THE WHOLE BUG.** `stopGlide()` was reachable only from a `pointerdown` ON THE STAGE, so the one thing anybody does to halt a moving page — put a thumb down — did nothing. Measured: **406px of further travel after the touch**. Worse, the native scroll that touch then begins runs AT THE SAME TIME as the hand-rolled glide, and two things drive `window.scrollTop` sixty times a second. ⛔ That is not a feel complaint, it is a race. Now killed by a capture-phase `pointerdown`/`touchstart`/`wheel` on the WINDOW. **(2) THE RELEASE SPEED WAS ONE SAMPLE.** `vy=step/dt` with dt floored at 1ms, and a phone delivers pointermove every 4–8ms, so an ordinary final sample became 5px/ms — a **40px flick threw the page 309px**, which is precisely "my thumb barely moving". Now averaged over a 70ms window, which also means a pause-then-lift does not fling at all (verified: moved exactly the finger's 100px). **(3) MOMENTUM WAS TOO CHEAP TO SPEND**: total travel is `v0/(1-decay)`, so 0.94 spent 16.7 frames of launch speed; 0.90 with a 2.6px/ms cap spends 10. Hard flick **1.43 viewports → 0.59**. **(4) `fling()` DID NOT CANCEL AN EXISTING GLIDE** — each rAF chain re-registers itself, so a second would have ADDED to the first, not replaced it. ⭐⭐ **AND THE ONE NOBODY WOULD HAVE GUESSED: the live-reload was firing on every save while he was reviewing.** Twelve reloads in one working session, each dumping him at the hero — from the far end of a phone that is indistinguishable from the page glitching. The reload now saves and restores scroll position (verified: parked at 6000, reloaded, still 6000). ⚠️ **`.hx-front` IS A CARD FACE, NOT A POSITION** — a first attempt to prove the horizontal swipe still worked read it as "which card is in front" and reported failure on the UNCHANGED baseline too. The helix rotates by transform; measure card RECTS. Horizontal swipe verified working after the fix (six cards moved, centre went Kitchen Islands → Splashbacks, page did not scroll), reviews verified paging on a flick. ⛔ Freeze probe: 30 diffs at 1440×900 and 30 at 768×1024, all marquee/scroll-cue, counts and heights identical. ⚠️ **STILL OPEN: the client wants the gallery copy SPLIT — title and subtitle ABOVE the cards, buttons BELOW — the pre-arrival gap closed, and reports the section divider looks wrong. None of that is done** |
| D111 | 11 Aug | ⛔ **`no-store` WAS KILLING THE PHONE'S BACKGROUNDED TAB. The dev server is now detached, compressed and cacheable** | Client: *"the hosting thing keeps closing. Every time I go out of it for a short amount of time it says can't open this page"*, and then the correction that matters: *"It's not my Mac that's sleeping. It's when I go off of Chrome on my phone."* ⭐ **Three causes.** (1) ⛔ **The agent kept killing the server** — `preview_stop`, a `kill`, and at one check NOTHING was listening. It now runs `nohup`'d with PPID 1 so the session's tooling cannot take it down, under `caffeinate` as insurance. (2) ⭐ **`Cache-Control:no-store` DOES NOT MERELY SKIP THE CACHE — IT MAKES A PAGE INELIGIBLE FOR THE BACK/FORWARD CACHE.** Chrome keeps a backgrounded tab in memory and restores it instantly; `no-store` makes it discard the tab, so returning means a fresh fetch — the one that fails while a phone's Wi-Fi is re-associating. ⚠️ The names mislead: `no-cache` means "revalidate", `no-store` means "never keep it". Only the second breaks the phone. Now `no-cache` + an ETag over the served body, so revalidation is a 0-byte 304. (3) **The live-reload snippet reloaded on any stream error**, throwing away a good page to request one that was not there; it now backs off to 15s and holds what it has (verified: server killed for six seconds, page stayed fully rendered), releases the stream on `pagehide` so it does not itself block bfcache, and reconnects on `pageshow`. ⚠️ **The whole of this is DEV-SERVER only and does not travel to production** — a host must be chosen and given the same compression and cache headers, and that is still open |
| D110 | 11 Aug | ⭐ **THE PHONE'S PROJECT GALLERY: PILE DEAD CENTRE, ARRIVING EARLIER, CARDS AND COPY BOTH HIGHER AND CLOSER TOGETHER** | Client, on the stacked beat: *"the first card is slightly lower than the middle of the screen, we need to move that card exactly to the center… the cards also need to come up earlier in the scroll before the border of the above section goes through… the words 'view our project gallery' should be closer to the actual cards, and the cards themselves slightly higher closer to the nav bar."* ⭐ **THE 50px HE COULD SEE WAS TWO FAULTS ADDING UP, AND ONLY ONE IS OBVIOUS.** (1) ⛔ **NOTHING IN THIS SECTION KNEW ABOUT THE STICKY BOTTOM BAR (D99).** Every "centre" was computed on a band running from under the nav to the very bottom of the viewport — i.e. 59px of it behind a bar the customer cannot see through. (2) **Centring the pile's EXTENT is not centring the card you look at**: the peek nudges run upward, so the eight-card extent's midpoint sits well above the front card, which is the biggest and hangs lowest. On a phone only the front card really reads, so `stackY=(navH-barH)/2` now puts *that* on the visible centre — measured 0px off, from +50. ⛔ Desktop keeps extent-centring. ⭐ **EARLIER WAS ABOUT THE WINDOW'S END, NOT ITS START**: 0.16 already began the flight 13% into the section, but STAGGER spreads eight arrivals across the whole window and the last did not land until 1.62, which is 0.62 of a viewport INTO the pin. GATHER_TO_P 1.62 → 0.72 lands the eighth card before the pin, so the whole settle happens while the border above is still crossing — measured, cards are visibly arriving with the border at 116px and 16px down the screen. ⚠️ **Pulling only the gather forward would have left the pile sitting still for ~1.8 viewports**, so the break-out came with it: SPREAD_START_P/END_P 0.08/0.28 for the phone against desktop's 0.27/0.47. ⭐ **THE CARDS AND THE COPY DELIBERATELY DO NOT MOVE TOGETHER THIS TIME** — D105 moved them in step to stop the cards landing on the words, but he asked for the gap CLOSED, so the copy moves further: cards `yShift` 0.22 → 0.25 of the stage, copy `bottom` 12% → 18% of the pin. Measured: nav-to-cards 75px → 51px, cards-to-copy 79px → 41px. ⚠️ Safe because `.gal-mid` is at opacity 0 for the entire gather and only fades in as the spread completes, so a higher copy block cannot collide with a higher pile. ⛔ Freeze probe against the pre-round file: **30 diffs at 1440×900, 28 at 768×1024, every one the animating marquee or the scroll cue**, geometry and opacity only; element count and document height identical at both. ⚠️ **A `barH is not defined` error in the console was STALE** — it came from the gap between two saves and a fresh instrumented run driving `measure()` twice and the whole gallery returned zero errors. The console replay trap, again. ⚠️ **Still open: the client reports the animations are not smooth on his iPhone.** He parked it; it is NOT diagnosed. Frames measure 8ms p50 on the Mac, so it is device-side — the coasting rAF playhead and the composited card layers are the first places to look |
| D109 | 11 Aug | ⭐ **THE PAGE WAS 3 MB ON THE FIRST SCREEN AND IS NOW 286 KB. Nothing about it looks different, proven by probe at both frozen widths** | Client: *"the website is currently glitching and jumping around and taking a second to load… we need to have super fast load speeds even with all the images and the video that's going to come in… keep the site looking great, don't change anything about the way it looks."* ⭐ **FOUR SEPARATE FAULTS, AND THE FIRST WAS THE LINK HE WAS TESTING ON.** (1) `dev-server.js` sent everything **uncompressed** and stamped `Cache-Control:no-store` on every file, so his phone re-pulled the entire site on every load AND every live-reload. Now brotli/gzip on text and ETag revalidation on assets: HTML **1,084 KB → 205 KB**, a repeat photo fetch **282 KB → 0 bytes (304)**. ⛔ Delivery only, not one byte on disk changed. (2) **36% of index.html was seven photographs pasted in as base64** — 391 KB of text that cannot be cached, lazy-loaded, converted or meaningfully compressed. Extracted to files by `build_images.py`. (3) **The photographs were serving at up to 24× the pixels their box uses**, and decoded to **99 MB of image memory on a phone** — quarry.jpg alone was 546 KB and 16.5 MB of RAM for a box 105×208. **That memory figure is the "glitching", not the download**: an iPhone holding 99 MB of bitmap for pictures it is showing at postage-stamp size stutters and jumps. (4) 18 `<img>` tags carried **one** `loading=lazy` and **two** width/height between them. ⚠️ **THE TRAP THAT NEARLY MIS-SIZED HALF OF THEM: every one of these sits under `object-fit:cover`, mostly in boxes TALLER than they are wide, so the binding constraint is the box's HEIGHT and the browser crops the sides.** Judging by width alone says cta-slab.jpg is 6× oversized when it is actually **under**-sized, and makes `sizes` select a rung far too small. ⛔ Requirements are `natural_width × cover_scale × 2`, measured by scrolling the whole page at 375px and 1440px. ⚠️ **PSNR IS A LIAR HERE and was nearly believed twice**: quarry scored 33 dB at 764px and 41 dB at 1000px — the same picture, a one-pixel crop offset apart. A 4× zoom on the finest detail showed 764px, 1000px and the original as identical, which is why WebP q85 was kept over q88 and saved 169 KB. ⭐ **The originals are never touched or deleted** — kitchen-day.jpg is referenced by 20 other files — so no other page can be affected. ⛔ **Freeze probe run LAST (D108's lesson): 27 diffs at 1440×900 and 27 at 768×1024, every one of them the animating marquee or the bouncing scroll cue, geometry and opacity only. Element count and document height identical at both.** ⚠️ Still open: no production host is chosen, and compression/caching in production is the HOST's job — the dev-server fix does not travel |
| D108 | 11 Aug | ⛔ **AN UNCLOSED HTML COMMENT PUT 48px OF EDITOR'S NOTES AT THE TOP OF EVERY PAGE, AT EVERY WIDTH. Caught by the freeze probe, not by eye** | Kept because the failure mode is the lesson, not the typo. A note was added to the sticky bar's comment block by prepending it to the `<div>` — with its own `-->`, while the block above still had one. The three lines between the two became a **text node in `<body>`**, and the whole page moved down 48px on desktop, tablet and mobile alike. ⭐ **NOTHING ABOUT IT LOOKED BROKEN**: the JS was fine, `node --check` was never going to see it, the section it belonged to worked perfectly, and the shift was uniform, so every screenshot still looked like the site. **It was found only because the desktop/tablet probe came back with 457 diffs where the previous run had 18** — the pattern §0 of the START HERE describes, doing exactly the job it is there for. ⚠️ **THE POINT: the freeze probe is not only a check on desktop.** It is the one thing in this project that reads the whole document rather than the part being worked on, and here it caught a fault on the very device that WAS in scope. ⛔ Run it after the last edit, not after the interesting one |
| D107 | 11 Aug | ⛔ **THE ROLL COULD NOT BE INTERRUPTED, WHICH READ AS "IT WON'T LET ME SWIPE QUICKLY". TWO SEPARATE CAUSES** | Client, on the rebuilt carousel: "when I try and swipe quickly on the reviews, it's not letting me swipe quickly", and note **"it only happens when I view it on my phone, not on the preview on the MacBook"**. **(1) A CSS TRANSITION CANNOT BE PICKED UP MID-FLIGHT.** The inline transform already holds the DESTINATION while the compositor is still easing toward it, so the instant a new drag turned the transition off, the belt **snapped forward to that destination** and only then began following the finger. One swipe looked right; two in a row jumped. ⭐ **The roll is now driven frame by frame from ONE number** — `soloAnim`, the drum's offset in steps from the current page — so a new gesture simply continues from wherever the eye last saw it. ⚠️ `.rev-live` is the switch between "the loop owns the cards" and "the stylesheet does"; the section's ride-in from off-screen still borrows CSS for one beat, and ⛔ **taking the cards over on the same call that seats them turned that entrance into a teleport** (measured: x −214 → 187 with no frame between). **(2) A DISTANCE-ONLY THRESHOLD REJECTS THE FASTEST GESTURE.** A flick leaves the glass after 25–40px, under the 48px throw, so the belt rolled back — the control ignored exactly the input a confident user reaches for. ⭐ **The release now carries its SPEED as well as its distance** (>0.45px/ms, with a 10px floor so a tap's drift cannot page). ⚠️ Both faults are invisible on a mouse, which is why the client only saw them on the phone |
| D106 | 11 Aug | ⭐ **THE PHONE'S NAV BAR IS ALREADY FORMED AT THE TOP OF THE PAGE, AND THE STICKY BAR LEADS WITH THE QUOTE** | Client: "the nav bar should already be formed, so already a gold line below it and everything, from the top. Not after you start scrolling — immediately it must already be there", and "the call button must be switched with the get a quote button." ⚠️ **On desktop the bar deliberately floats transparent over the hero and pours its glass in as you leave the top — that is FROZEN (D91) and is untouched.** On a phone it reads differently: the bar is ~66px of a small screen, the logo sits straight on a busy photograph, and a hairline arriving late looks like something still loading. ⛔ **The flare goes with it**: `.bar-flare` is the shine that rides the hairline out from its centre AS IT FORMS (D33), and a bar that is already formed has nothing to form — left armed it would flash across a finished line 40px into the first scroll. ⚠️ Both rules must sit AFTER the base ones; same specificity, so order decides |
| D105 | 11 Aug | ⭐ **THE PROJECT GALLERY ARRIVES EARLIER, RISES FROM BELOW LIKE A FIREWORK, WEARS A GOLD RIM, AND SITS HIGHER UP THE SCREEN. Mobile only** | Client: "for the project gallery section I want the cards to come in slightly earlier, and I also want them all to pop up from the bottom and then lay in a stack. So they all pop up directly up like it's a firework shooting up, and then they all stack there. Instead of coming in from the sides. And then they spread out to the way that it currently is. I just wanted to have a golden rim around it. And I also want the four cards that are showing to be slightly higher… then move the title, subtitle and button slightly higher in that section." **(1) EARLIER**: the gather window is `0.16 → 1.62` of a viewport on a phone against the desktop's `0.40 → 1.86`, so the whole arrival happens while the section is still coming up the screen rather than after it has landed. **(2) FROM BELOW**: ⛔ **A SIDE ENTRY IS THE ONE THING A PHONE HAS NO ROOM FOR** — each card used to start a full stage width out PLUS most of its own width, i.e. ~450px off a 375px screen, so most of every flight happened where nobody could see it. They now start a card-height below the stage floor and rise. ⚠️ **The lateral offset is small but NOT zero** (0.10 of the stage at most): eight cards on one perfectly vertical line overlap exactly and read as a single card rising. **(3) THE RIM** is the card's whole border in the site's own champagne, carried a little stronger — ⛔ **NOT a top-edge seam, which §2 rule 10 forbids and which has been built by mistake twice.** **(4) HIGHER**: the walls' phone shift goes −0.16 → −0.22 of the stage and the copy block goes 4% → 12% off the floor of the pin. ⚠️ **Those two move TOGETHER** — shift the cards without the copy and they land on the words. ⭐ Also: `--galMode` now declares the phone in the STYLESHEET and the script reads it, replacing three separate `M.w<720` tests that disagreed with the 720px query at exactly 720px (D93's lesson, found sitting in the file) |
| D104 | 11 Aug | ⛔ **iOS DOUBLE-TAP ZOOM IS OFF ON EVERY CONTROL, PHONE ONLY** | Client: "if I tap the buttons too quickly it automatically zooms in my phone, because double tap on iPhone means zoom in. So we have to be careful of that." ⚠️ **This is not a mis-tap to design around**: zoom-on-double-tap is the DEFAULT on every element that has not opted out, so paging a carousel or walking a stepper at any speed fires it. `touch-action:manipulation` opts the control out and leaves everything else — pinch zoom included — exactly as it was, so the page is still zoomable for anyone who needs it. ⭐ **Deliberately a blanket rule** over `a, button, [role=button], label, summary, input, select, textarea` rather than a list of today's buttons, which would not cover the next one somebody adds. ⚠️ Where an element also needs the page not to pan (the review stage, the helix), that `none` is on an ancestor and still wins — the effective value is the INTERSECTION down the chain, not the nearest declaration |
| D103 | 11 Aug | ⛔ **THE HELIX NO LONGER DRAGS THE PAGE WITH IT. A diagonal swipe turns the spiral and nothing else** | Client: "the helix section is also laggy or completely glitched. As I'm swiping it diagonally down or diagonally up, the screen is also moving diagonally down and diagonally up. So we need to make it if they're touching the cards, the screen basically stays still unless they're swiping directly up or directly down on that section." ⛔ **He is describing a real fault, not a preference**: the helix's touch handlers were bound `{passive:true}` with NO `touch-action` on the stage, so they could never stop a scroll — the browser panned on the gesture's vertical component while the script turned the spiral on its horizontal one, at the same time. ⭐ Fixed by `touch-action:none` on the phone stage and the shared arbiter in D102. ⛔ **THE DESKTOP PATH IS UNTOUCHED AND STILL ITS OWN CODE** — the old mouse and touch handlers simply stand aside when `--hxMode` says phone, because desktop is frozen (D91) and a rewrite of a frozen surface's input is still a change to it |
| D102 | 11 Aug | ⛔ **THE REVIEW SWIPE DID NOT WORK ON THE CLIENT'S iPHONE — THIRD BUILD, AND THE FIRST ONE THAT FIXES THE RIGHT THING** | Client: "the review section is completely glitched out, if I try and swipe on it, it's not working." ⛔ **D93 shipped touch-only and failed on his MacBook; D94 bolted a mouse path beside it; and the touch path then failed on his phone. Three builds, one root cause: the gesture was arbitrated in three places, and on a phone the BROWSER was arbitrating first.** ⭐ **`touch-action:pan-y` HANDS THE FIRST DECISION TO THE BROWSER**, which takes any drag with a real vertical component as a page scroll before a single handler runs — and a thumb swipe is never perfectly horizontal. Nothing in the handler was reachable. ⭐ **So the page is taken out of the argument**: `touch-action:none`, ONE arbiter (`attachSwipe`) shared with the helix, axis decided once at 5px, ties going to the carousel. ⛔ **THE PRICE, PAID DELIBERATELY**: `none` also kills the native vertical scroll over that element, so a near-vertical drag is scrolled BY HAND, 1:1, with a decaying glide on release — that is the client's "unless they're swiping directly up or directly down". ⚠️ **`html` carries `scroll-behavior:smooth` and it poisons a hand-rolled scroll** — every `scrollBy` animates toward its target and sixty a second each restart from where the last got to; measured, 120px of finger moved the page 45. An inline `auto` is set for as long as the gesture owns the page. ⚠️ Bound to the STAGE, not the deck: the deck is only the centre card's box, and a thumb landing on a peeking neighbour was landing outside the one element that listened |
| D101 | 11 Aug | ⭐ **THE REVIEWS ROLL LIKE A DRUM NOW. ⛔ The old motion was not missing, it was FRONT-LOADED** | Client: "when I swipe or click the buttons there's barely any animation, it just flicks over to the next one, it's an instant switch between them. I almost want it to feel like a rotating wheel — imagine a roll of duct tape laying down flat, and you're just rolling the reviews in, and the other one rolls out." ⭐ **THE DIAGNOSIS IS THE KEEPER.** The page change already ran 0.95s — but on `--ease`, `cubic-bezier(0.16,1,0.3,1)`, an expo-out that covers ~90% of the distance in the first quarter of its time and crawls the rest. Measured mid-flight, the card was already home. ⚠️ **A longer duration on that curve changes nothing you can see; the CURVE is the fault.** Now 0.72s on a curve with real acceleration at both ends. ⭐ The reviews sit on the rim of a cylinder with a VERTICAL axis — the client's roll of tape — as `translateZ(−R) rotateY(θ) translateZ(R)`, in that order, which puts the drum's axis at the screen plane so the front card stays at its true size and only the ones turning away recede. ⭐ **R IS SOLVED FROM THE PEEK, AND FROM ITS INNER EDGE, NOT ITS CENTRE**: ⛔ the first version held the neighbour's centre where D93 put it and the peek collapsed from 44px of visible card to 4 — a card turned 32° is FORESHORTENED to cos(32°) of its width, so holding the centre pushes the inner edge 70px further out. ⭐ **Every card is on the rim; there is no parked state left.** The old build teleported anything more than one page away with `transition:none`, which is why the outgoing neighbour VANISHED instead of rolling out. ⚠️ `.rev.entering` set `transition:transform` alone, silently dropping the fade the base rule carried — opacity is back in the list |
| D100 | 11 Aug | ⭐ **THE HERO'S TWO BUTTONS ARE AS WIDE AS THE ICON ROW, EDGE TO EDGE** | Client: "make the get your free quote and give us a call slightly thinner, not as wide. I want to basically match the size of the side of the icons." They were `width:100%`, reaching the screen's own margins while the composition above them stopped well short, so the pair read as two bars rather than as part of the same centred stack. ⭐ **THE WIDTH IS DERIVED, NOT TYPED**: the three reasons are equal thirds and each icon is centred in its own third, so first-icon-left to last-icon-right is `(2/3)·row + (2/3)·gap + icon` — 256px on a 375px handset, measured against icons at 59 and 316. ⚠️ Both terms are CSS variables shared with the icon row, which is the point: type 257px instead and the buttons stop lining up the moment the gap clamp moves at another width. ⚠️ **The type size had to come in with the width and had to be vw-led** — the label is `nowrap` inside an `overflow:hidden` button, so a label that does not fit is not a wrap, it is a silently clipped word, and the button is 246px at 360 and 219 at 320 |
| D99 | 11 Aug | ⭐ **THE PHONE GETS A STICKY BOTTOM ACTION BAR: CALL, EMAIL, GET A QUOTE** | Client: "mobile should have a nav bar and a sticky bottom nav bar. The top nav bar will be visible from the start on mobile, then as you scroll past the two CTAs in the hero section the sticky bottom nav will pop up with the email and phone and get an estimate or whatever buttons are necessary." ⚠️ The top bar already behaves that way and was left alone. ⭐ **THE TRIGGER IS THE CTA ROW'S OWN RECT, NOT A SCROLL NUMBER** — `scrollY > 600` would be a second description of where the hero's buttons are and would be wrong the moment the title wraps to another line on another handset. It rises when the row passes behind the fixed header. ⛔ `display:none` above 720px, so desktop and tablet render identically — proven by probe, not asserted. ⚠️ **The top edge is the BONE hairline, not gold**: §2 rule 10 forbids a bright or gold line across the top of a card or section and this is the top edge of a bar. ⚠️ `visibility:hidden` while parked, not just a transform, or three links sit invisibly in the tab order. ⚠️ z-index 39 — under the burger menu at 40 so the sheet covers it, far under the estimator modal and the project overlay. ⚠️ `body` takes 66px of bottom padding or the footer's last line lives under the bar. ⭐ Three actions, all already on the page (the hero's call button, the contact block's address, the quote CTA) — no new promise and no new number. ⚠️ **"Get an estimate" was read as the quote CTA**, not the estimator tool; say if the estimator is what he meant |
| D98 | 11 Aug | ⛔ **THE FIFTH HELIX CARD WAS INVISIBLE, AND THE BEVEL'S CORNERS WERE TOO LONG. Both fixed, and the mask lesson behind the first one is the keeper** | Client: "I can only see the one at the bottom, the back of the card, but I cannot see the top one. So now there's four on the screen when there should be five", plus "in the hero I want that flat bottom part to be closer to the sides, so there's only a little bit of the angle that's visible." ⛔ **THE FIFTH CARD FAILED FOR THREE REASONS AT ONCE, WHICH IS WHY ONE TWEAK WOULD NOT HAVE FOUND IT.** **(1)** The stage overlapped the copy by 118px, so the top ghost sat ENTIRELY inside the band the mask holds at 0.10 for the text's sake — 70px now, and only ~15px of the card tucks behind the copy. **(2)** ⭐ **THE MASK'S TOP STOPS WERE PERCENTAGES, AND THEY HAD TO BE PIXELS.** A percentage scales with the STAGE; the thing this stop must line up with is the COPY. The copy's bottom sits a fixed ~46px below the stage top (the −70px margin less the head's own ~21px margin) and **that distance does not move when the copy rewraps at another width**, so the stops are `0.10 → 46px, solid by 64px`. As percentages the ramp ran on past the text and the only part of the ghost a customer can see was still climbing out of the fade. **(3)** The ghost opacity was 0.22 and the mask multiplied it to nothing; 0.32 now, which behind the copy still lands at 0.032. ⭐ **Measured after: top ghost 32px of exposed sliver at 0.312 effective brightness, bottom ghost 43px at 0.285 — balanced, where before the top one was invisible and the bottom one was not.** ⚠️ Room for the fifth slot came from the CARDS, not from the section: card width 0.76 → 0.70 of the stage (its height is 0.66 of its width, so trimming width is what buys vertical room) and STEP 0.20 → 0.175. ⭐ **Sanity check for anyone retuning this: the spiral spans `4·STEP + --hxH`. Keep that within about a card's height of the stage or a card the client asked to SEE stops being visible.** The section is 701px, up 72px from three cards to five. ⭐ **THE BEVEL'S CUTS ARE SHORT NOW**: `--bevX` 64 → 38, `--bevY` 37 → 22, so the flat run is **299px of a 375px screen, 80%**, against 247px before. ⚠️ **22/38 = 0.579 and tan(30°) = 0.577** — the ratio is the angle, and the hairline's `30deg` gradient only lies on the cut while it holds. Measured after: 30.07°. ⛔ **Desktop 1440×900 and tablet 768×1024 probed identical** — hero height, clip, border, cue span, inner padding, stage, mask, helix margin, card width and height, the grid's columns and rect, and all six card positions, opacities, pointer-events and ghost flags. Error probe clean, and no ghost is left clickable. |
| D97 | 11 Aug | ⭐ **FIVE CARDS ON THE MOBILE HELIX WITH TWO INERT GHOSTS, THE SPIRAL RUNNING OFF BOTH EDGES, AND THE HERO'S BEVEL DOWN TO 30° WITH ITS EDGE RAISED** | Four client changes in one round, all mobile. **(1) THE GHOST PAIR.** "I still want to see one more on the top and one more at the bottom, but very faded, and you can't interact with that block. You can interact with the centre block, the block above that, and the block below that", and then: **"that one more on the top goes behind the text, but must be so faded that the text is not obstructed at all and it's still easy to read."** ⭐ **The stage now reaches UP under the intro on a −118px margin**, which is what buys the fifth card its room without the section growing — measured, the section went 649px → **629px while gaining two cards**. ⛔ **The copy had to be lifted above the helix**, which comes after it in the DOM and would otherwise paint over the words. ⭐ **THE MASK'S TOP STOP IS 0.10 ALPHA, NOT 0** — a mask reads alpha, so an intermediate stop is a partial reveal: measured, the ghost renders at **0.022 behind the first line of copy and 0.08 behind the last**, present as depth and unable to compete with body text. ⚠️ Below the copy the mask opens to solid so the same card resolves as it descends. ⛔ **INTERACTIVITY IS DECIDED BY POSITION, NOT OPACITY**: the existing `o<0.1` test would have left the ghosts clickable at 0.22, and a card you can tap but cannot see is the worst of both. ⚠️ **And pointer-events was not enough on its own** — a dispatched click bypasses hit-testing, so `.hx-ghost` is set by render() and the click listener refuses it outright. Proven: a click on a ghost does not move the spiral; a click on the card one step out does. ⚠️ The phone fade is a RAMP, not a step, because `d` is fractional throughout a spin and a threshold would make a card blink as it crossed. **(2) OFF THE EDGES.** "I wanted it to go all the way to the side of the screen, so it goes out of the screen essentially." ⛔ **0.62 of the stage was too far and the reason is worth knowing: sin is symmetric about 90°, so the card ONE step out and the ghost TWO steps out sit at the SAME sideways offset** — push them wide and both collapse into one 77px sliver, losing the tap target the client explicitly asked to keep. **0.56 crosses the edge and still leaves 94px of card.** **(3) THE SCROLL CUE LOSES ITS WORD** and the hero's bottom edge comes up with it: "I don't want it to say scroll, and that means we can also move that divider or bottom border higher up." ⚠️ **min-height ALONE would have done nothing** — the content is taller than 90vh, so the bottom padding had to come down with it. Measured: hero **812 → 755px**, so the bevel and 57px of the reviews are both on the first screen. **(4) THE BEVEL IS 30°, NOT 45°.** The RISE is kept and the RUN lengthened, so it reads as a shallower, longer cut rather than a smaller one: `--bevX:64px`, `--bevY:37px`, **tan⁻¹(37/64) = 30.03°**, flat middle 247px of 375. ⛔ **THE HAIRLINE'S GRADIENT ANGLE HAD TO CHANGE WITH IT, AND THIS IS THE TRAP**: `to top right` traced the cut only because the old box was SQUARE. A gradient's bands run perpendicular to its gradient line, so lying along a cut of (run, rise) requires `tan(A)=rise/run` — **A is exactly the bevel's angle from the horizontal**, 30deg and −30deg. Keep the corner keyword in a non-square box and the hairline drifts off the cut it is supposed to trace. ⭐ **Verified**: five cards at 375 with the ±1 pair interactive at 94px visible and the ±2 pair inert at 0.22; next, prev, mouse drag, touch swipe and trackpad wheel all turn it; no horizontal scroll; error probe clean. ⛔ **Desktop 1440×900 and tablet 768×1024 probed identical** — hero height, min-height, clip, border, cue span, cue rect, inner padding, stage, mask, helix margin, the grid's columns and rect, and all six card positions, opacities, pointer-events and ghost flags. |
| D96 | 11 Aug | ⭐ **THE SERVICES HELIX NOW RUNS ON MOBILE, AND THE SECTION IS 2,290px SHORTER FOR IT** | Client: "on mobile for the surfaces for every space section, I still want the same helix like we do on desktop, but we need to optimise it to work for the mobile version. Without the user having to scroll too far down — I want the helix to maybe fade out behind the subtitle text, or we make the subtitle half the text and create the helix in the best possible way for mobile." ⭐ **BOTH of his suggestions were taken, and the result is far SHORTER, not longer**: the helix replaces the six stacked flip cards, which were the tallest block on the phone. **Measured at 375×812: the section was 2,939px and is now 649px.** ⛔ **THE CARD CSS WAS LIFTED, NOT COPIED.** Everything from `.helix-stage` to `.helix-ui` used to live inside `@media(min-width:1121px)`; it now sits in a shared `@media(min-width:1121px),(max-width:720px)` block. **A second copy of it would have been this project's signature bug for the fifth time** (D51, D59, D68, D78, D93). ⚠️ It was moved to sit immediately AFTER the desktop block, not before, so its order against anything else touching those selectors is unchanged — desktop probed identical. ⭐ **`--hxMode` is set by the media query and READ by the script**, so the breakpoint is declared once, in the CSS; a `matchMedia` test in the helix would have been a second opinion about what a phone is. ⛔ **THE DESKTOP FLOORS ARE THE TRAP**: card 300px, R 210px, STEP 96px are all wider than a phone can give, so the desktop branch would hand a 335px stage a 300px card and a 210px radius and throw the spiral off both edges. The phone branch is card 0.76·W (255px), R 0.47·W, STEP 0.30·H. ⭐ **STEP IS THE EDIT, NOT JUST THE RISE.** At 0.22 the ±2 cards still sat inside the frame and five overlapping cards on a 375px screen read as clutter; at 0.30 that pair climbs clear of the stage and the mask takes it, so a phone shows **three** cards — the one you are reading and a hint either side. ⚠️ **The section does not grow for it**: the stage height is fixed and the spiral simply runs out of it, which is the client's "fade out". ⭐ **The mask goes on the STAGE, never on a card** — a mask, filter or opacity on the card shell flattens its 3D and the back face paints as a mirrored front. Verified after: `transform-style` on the shell is still `preserve-3d`. ⚠️ **The limestone REVERSE is effectively not seen on a phone**, because only cards two steps out have turned past 90° and those are the ones the mask removes. A deliberate trade for legibility; say so if the client asks. ⚠️ **The card's furniture had to come down with the card**: at the shared sizes the name wrapped to two lines and "View this service" was 197px of button in 176px of room. Now 19px name, 10px label at 0.18em — both measured to one line. ⚠️ **Drag distance too**: the desktop floor of 180px per card is half a phone screen, so a full swipe would not reach one card. ⭐ **The intro is HALVED on the phone** — two spans in the markup, exactly one displayed, so desktop keeps its paragraph unchanged to the character. Both say the same true things: templating and fitting are TopCat's, the cutting is the workshops' (§2 rule 1). ⛔ **TABLET (721–1120px) KEEPS THE FLIP-CARD GRID** and is deliberately not in the shared query, because tablet is frozen until the client says otherwise (D91). Widen it to `(min-width:721px)` when the tablet round comes. ⭐ **Verified**: next, prev, mouse drag, touch swipe and trackpad wheel all turn the spiral; no horizontal scroll; error probe clean; **desktop 1440×900 identical including the subtitle rect, the stage, and all six card positions, sizes and opacities**; tablet 768×1024 identical including the grid columns and card geometry. |
| D95 | 11 Aug | ⭐ **THE MOBILE REVIEW SPACING IS REBUILT ON THE CARD'S REAL HEIGHT. The gap was mostly INVISIBLE SLACK, not a spacing choice** | Client: "on mobile the spacing between the subtitle and the buttons is too big. Move the buttons slightly up close to the card, and then move the card closer up to the subtitle. Optimise the spacing." ⛔ **THE PART WORTH UNDERSTANDING**: the stage was sized from `--revCardH`, which is the deck's UNSCALED height — but the card is transform-scaled to ~0.84 inside it, so `(1-scale)/2` of that height was dead space above the card and the same again below. **27px a side on a 397px handset, named by no rule at all.** Of the 64px between the subtitle and the card, 27 was slack, 22 was the section head's margin and 15 was stage padding. ⚠️ **Tuning the numbers that did exist would only ever have chased it** — that is why this is a rebuild rather than two smaller values. ⭐ `gridLayout()` now writes the measured scale out as **`--revScale`**, so the stage height is `card height × scale + air + pager band` and every number in the rule means what it says. ⛔ **Solo must therefore SKIP the stage-height cap on the scale** (`GS=Math.min(GS,(stageH*0.94)/ch,1)`) — the stage is derived from the scale, so capping the scale against the stage is circular. It is also unnecessary: a card cannot overflow a stage measured from it. ⚠️ **The deck box is still the unscaled height and so is taller than the stage's content box.** It overflows a few px each way, which is invisible — it paints nothing — and keeps the scaled card centred. ⛔ Do not "fix" that by shrinking the deck; the card is `inset:0` within it. ⭐ **Measured, 375×812 — subtitle→card 64 → 30, card→buttons 54 → 20, buttons→strap 18 unchanged; the section is 62px shorter and the card, the 40px peeks and the 64px button spacing are all untouched.** ⚠️ **The pager band is 50px and the button is 46px of it, so the card-to-button gap is `--revAir/2 + (band − 46)`.** The strap line sits a fixed ~19px under the stage, so a band that leaves the buttons further from the card than that makes them read as the strap's controls rather than the card's. ⛔ **HALF THE AIR HAD TO BE ADDED TO THE OPEN-CARD PAGER DROP TOO, and it was missed first time**: an open card put the buttons **4px** below it where a closed one is 20, because the drop counted the band but not the half-air the centring puts under the card. It reads `--revAir` from the stylesheet rather than hard-coding it, so the two cannot drift when the spacing is next tuned. Open and closed now both measure 20px. ⛔ **Desktop 1440×900 and tablet 768×1024 re-probed: head rect, head margin, stage, deck, pager, all 16 cards and the shared expand path identical.** ⚠️ **Two measurement traps cost time here and will again**: an open card's height ANIMATES over .45s, so a probe that reads too early reports the collapsed height and a nonsense pager gap; and **the card entrance depends on SCROLL HISTORY, not on scroll position** — a file loaded fresh shows every card parked where one already scrolled shows them seated. Give both files the same history before calling a difference a regression. |
| D94 | 11 Aug | ⛔ **THE SWIPE D93 SHIPPED ONLY WORKED ON A TOUCHSCREEN, SO IT DID NOT WORK FOR THE CLIENT AT ALL. Plus: an open review now shows the WHOLE review in one go** | Client, testing it: "I am trying to swipe on the review card now, but for some reason it's not letting me, whether I swipe with my MacBook with two fingers or whether I click and hold and swipe. It's not working." ⛔ **He is right and the cause is embarrassing in hindsight**: the handler was `touchstart`/`touchmove`/`touchend` ONLY. A laptop narrowed to a phone width has no touch hardware, fires no touch events, and there was nothing to catch a drag. ⭐ **THE LESSON, AND IT GENERALISES TO THE WHOLE MOBILE ROUND: a phone layout is looked at on a desktop browser far more often than on a phone during a build — including by the client. A phone gesture that only answers to touch events is untested and unusable for everyone reviewing it.** ⚠️ It passed its own verification because the tests SYNTHESISED touch events; they proved the handler worked, not that a person could reach it. ⭐ **Two input paths added beside the touch one.** **(1) Mouse drag**, via pointer events filtered to `pointerType==='mouse'` so touch keeps its own handlers. ⚠️ move and up bind to the WINDOW, not the deck — a drag that leaves the deck still has to finish, or the belt sticks mid-gesture with the button already up. ⚠️ `user-select:none` while dragging only, or dragging across a card of text selects the text and the belt appears frozen. **(2) A two-finger trackpad swipe, which is a `wheel` event with deltaX and fires nothing either drag handler listens for** — this is the gesture a Mac user reaches for first. ⚠️ Acted on only when `|deltaX| > |deltaY|`, so a two-finger scroll DOWN the page still scrolls the page; almost no trackpad scroll is perfectly vertical. ⚠️ **Momentum keeps firing wheel events for about a second after the fingers lift**, which paged several times from one flick — a lock holds until the stream has been quiet for 140ms, so a flick turns exactly one review. ⭐ **THE OPEN CARD NOW SHOWS THE WHOLE REVIEW, NO INNER SCROLLBAR.** Client: "if someone taps on the review they can read the whole review… everything should be visible in one go", and tapping again closes it. `capR` fits an open card to ONE SCREEN, which is right on desktop and wrong on a phone: it left a 925-character review scrolling inside the card, so a customer had a scrollbar inside a scrolling page and no sense of how much was left. On a phone the card takes its full content height and the PAGE scrolls — the "dragging to read" he described. ⚠️ `overflow` had to be set on BOTH axes: `visible` on one axis beside `hidden` on the other computes back to `auto` and the scrollbar returns. ⭐ **Measured after, at 375 and 430**: mouse drag left/right, touch swipe, trackpad flick both ways, both buttons, neighbour tap, tap-to-open and tap-to-close all driven and confirmed; a vertical drag and a vertical wheel are left uncancelled so the page still scrolls; a short drag settles back and its trailing click does NOT also expand; the 925-character review renders complete with no inner scroll and nothing overlapping the pager or the strap line. ⛔ **Desktop 1440×900 and tablet 768×1024 re-probed against `index.html.pre-mobile-reviews.bak` — stage, deck, pager, all 16 cards AND the shared expand path (343→564, 227px stage margin, quote still scrolling inside as designed) identical.** Error probe clean after driving every control. |
| D93 | 11 Aug | ⭐ **THE MOBILE REVIEWS ARE A SWIPEABLE CAROUSEL: NEIGHBOURS PEEKING EITHER SIDE, PAGER BELOW** | Client: "move the two buttons below, spaced out next to each other well. And on the left and the right of the review card I would like to see the other cards, so that the user can swipe it to the left or the right and then go to the other card… make the main center review card slightly smaller, and then show the other two on the side of it, so it's obvious that there's a review on either side that they can swipe to or click the buttons left or right below." ⛔ **MOBILE ONLY, and desktop + tablet were probed against the pre-change file and came back identical** — see the measurements below. ⭐ **THE SCOPE IS A CLASS, `.rev-solo`, NOT A MEDIA QUERY, AND THAT IS THE PART TO KEEP.** It is set by `gridLayout()` from `perPage()===1` — the same test the layout maths uses — so the stylesheet can never disagree with the JS about what a phone is. ⚠️ The two ALREADY disagreed: the old `@media(max-width:720px)` arrow rule fires at exactly 720px where `perPage()` still returns 3. **A second source of truth for the same fact is this project's most repeated bug** (D51, D59, D68, D78) and a media query here would have been the fifth. ⭐ **The card got SMALLER on purpose**: `availW` was `innerWidth-96`, reserving 48px a side for arrows sitting in the gutters. The arrows moved below, and rather than handing that width back to the card it goes to the neighbours — 72% of the screen, **270px of card on a 375px handset against 279px before**, with each neighbour showing exactly **40px**. ⭐ **The neighbours are the whole affordance**, not decoration: one centred card with two arrows gives a customer no reason to believe there is anything else to read, which is why he asked. They sit at 0.86 scale and 0.5 opacity, and **tapping one pages to it**. ⭐ **`soloDist()` is why the wrap is free**: each card's SIGNED, shortest-way-round distance from the current page decides its slot, so a page turn is "everything moves one slot" and the last review's next is the first. ⛔ **The park-and-recycle dance `goPage()` does for the 3-up wall is not needed and must not be copied here** — the neighbours are already on screen. ⭐ **The swipe tracks the finger** and settles or turns at 48px. ⚠️ **The axis is decided ONCE, on the first real movement, and held** — re-deciding per frame makes a diagonal drag flicker between scrolling the page and moving the belt. Vertical drags are left alone and are NOT preventDefaulted, so the page still scrolls; measured. ⚠️ **A swipe ends in a click on most engines**, so exactly one click is swallowed after a drag or every page turn would also expand whichever card the finger landed on. ⭐ **The pager RIDES DOWN with an open card.** First attempt capped the card at the pager's top instead, and that was wrong: it left a long review scrolling inside ~290px on an 812px phone. Now the band drops with it — measured open: card 484px tall, buttons 12px under it, strap line 24px under them, nothing overlapping. ⚠️ **The band height was measured, not picked**: the strap line sits a fixed ~29px under the stage, so `--revPagerH` only moves the card-to-button gap. At 84px the buttons were 77px below the card and 29px above the strap and read as part of the strap line; at 58px they sit 51px under the card and 18px above the strap. ⭐ **Measured after, at 375×812**: centre card 270px centred on 187.5, both peeks exactly 40px, buttons 64px apart centred on 188, no horizontal scroll, and every one of prev / next / swipe-left / swipe-right / neighbour-tap / expand / collapse driven and confirmed. ⛔ **Desktop 1440×900 and tablet 768×1024 probed against `index.html.pre-mobile-reviews.bak` served side by side: section, stage, deck, pager and ALL 16 card positions, widths, opacities and z-indexes identical**, and the desktop cartwheel pager still shifts 0,1,2 → 1,2,3. ⚠️ **The live console showed `revSolo is not defined` and it was STALE** — the dev server reloaded between the edit that added the call and the edit that added the function. An instrumented `_debug.html` with an error probe in `<head>` returned **zero** errors on a clean load and after driving every control. §10 warns about exactly this; trust the probe, not the replay. D91, §2 rule 15 |
| D92 | 11 Aug | ⭐ **THE MOBILE HERO IS CENTRED, AND ITS BOTTOM EDGE IS A MATCHED BEVEL. First change of the mobile round** | Client: "for the mobile hero section, most of the things are going to be centered… the title, the subtitle, and then the three icons are all going to be center with the icon on top and the text below, and they're all three going to be in line", and "the divider between the hero and the reviews section is currently just a curved line… change that to a matched bevel design, mostly flat in the centre and then just the corners cut in, reaching the border… the middle will be longer, and then the forty five degree angle cut on the two sides." ⛔ **ALL OF IT IS INSIDE `@media(max-width:720px)` — desktop and tablet are byte-identical, proven rather than assumed** (see below). ⭐ **The three reasons go back to a ROW of three**, icon over label, each centred in an equal third — this deliberately overrides the `max-width:760px` rule that stacks them one per row, which still governs the untouched 721–760px band. ⚠️ **Icons are TOP-aligned, not centred on the tallest label**: the three labels wrap to different line counts, and centring them vertically puts the three icons at three different heights, which is the one thing "all three in line" rules out. Measured at 375px: three 106px columns, all three icons at y=341 and all three labels at y=377. ⚠️ **THE TRUST LINE LOSES ITS GOLD LEFT RULE ON MOBILE, and the client should be told**: `.hero-sub::before` is a LEFT-EDGE mark (§4b, his own "stand out in a different way" brief) and against centred text it strands itself at the far left of a full-width block. It stands down at ≤720px only. ⛔ **Do not replace it with a short gold rule above the copy — that is the top-edge hairline §2 rule 10 forbids and has rejected twice.** ⭐ **THE BEVEL, and the two things that make it work.** It was `border-bottom-left/right-radius:50% 30px`, an ellipse across the whole width. Now `--bev:34px` is BOTH the horizontal run and the vertical rise of each cut, which is what makes the angle exactly 45° — change the one number and it stays true; change one axis and it stops being a bevel. On a 375px handset the flat middle is 307px against two 34px cuts. **(1)** The clip is on **`.hero-bg`**, not on `.hero` — `.hero-shade` lives inside `.hero-bg` so one clip takes the photograph and its shading together, and ⛔ putting it on `.hero` would have clipped the hairline pseudo-element in half, because a pseudo-element is clipped by its own parent's clip-path. **(2)** The hairline is **three background gradients, not an SVG**: each diagonal is drawn inside its own `--bev` SQUARE, where `to top right` / `to top left` is exactly 45° at every width. ⛔ An SVG stretched with `preserveAspectRatio="none"` is only 45° at one width, and a stroke scaled on one axis stops being 1px. ⭐ **Measured after**: everything centres on 187.5 at 375px (title, both sub lines, CTAs, chips — the exact viewport centre); no horizontal scroll at 320 / 375 / 390 / 430; the hero still fits one screen at 430×932; and the facts row got **45px SHORTER** than the stacked version it replaces. ⛔ **Desktop and tablet proven unchanged, not assumed**: the pre-change file was served side by side and probed at **1440×900 and 768×1024** — every rect of the hero, its title, sub, facts, CTAs, chips, cue and the CTA card, plus the border, radius, clip-path, flex-direction and text-align, came back **identical**. ⚠️ The `.hl` line-mask's padding went from `.08em` on the right only to symmetric, or the centred title sits half that off-centre. D91, §2 rule 15 |
| D91 | 11 Aug | ⛔ **DESKTOP IS FROZEN. WORK MOVES TO MOBILE, THEN TABLET, AND ONLY ONE AT A TIME** | Client, closing the desktop design round: "after this, desktop will stay exactly as it is in its current design, until I decide to change it again… then you and I are going to work on the mobile and tablet sections. **When I mention changes, I'm only referring to mobile**, and then afterwards I will say we're going to work on tablet. So when I say let's do mobile, then we only work on mobile. **Everything else stays exactly as it is on the other devices.**" ⭐ **This is a SCOPE rule, and it is the one most likely to be broken by accident**, because the site is a single self-contained `index.html` with **inline CSS**: nearly every rule is unscoped and therefore applies to every width. ⛔ **So a mobile change must be made INSIDE a width-scoped media query, never by editing a base rule** — editing the base rule to "fix mobile" silently changes desktop, which is now out of bounds. ⚠️ The existing breakpoints on the landing page are `max-width:900px` and `max-width:560px` with a few others; **check which one governs before writing a new one**, and prefer `max-width` blocks so desktop keeps the untouched base. ⚠️ **Tablet is NOT in scope while mobile is in scope** — a `max-width:900px` block catches tablet widths too, so a mobile-only change usually belongs under a narrower ceiling. ⭐ **Desktop is unfrozen only by the client saying so.** ⚠️ Design only: the enquiry backend, the photography and the licensing work (§7) are unaffected and still open. |
| D90 | 11 Aug | ⭐ **THE FINAL CTA CARD IS A PLAIN GREY. The last desktop design change** | Client: "I don't like that there's such a detailed background in the one visit, one price, one team section. **Just make it a plain background, that grey that we have.**" ⚠️ The card was carrying **`--marbleBG` at `cover`** — the page floor's own photographed Portoro slab, but at full strength rather than whispered in the way the nav glass and the modal take it (§4l), so the veining and the crystalline grain ran straight through the six form fields and the drop zone. ⭐ **It is now `--ink-2` (#15151A), the site's standing panel grey** — the same background the FAQ plate, the gallery tiles, the About collage tiles and the estimator panels already use, so the card reads as one of the site's panels instead of as a second piece of stone competing with the floor it sits on. ⛔ **Not simply the image removed**: the `background-color` underneath was `#101015`, which is near-black rather than grey, so dropping the image alone would have given him a black card. ⚠️ **The form fields are a LIFT off this background** (`rgba(255,255,255,0.045)` plus a `--hair-soft` hairline) — if the grey is ever changed, check the fields still separate from it. ⚠️ Nothing else moved: measured at 1366×610 the section is **499px**, byte-identical to the figure recorded in §0 for the same size, and there is no horizontal scroll. **The card's gold border, the box shadow and every measure are untouched.** ⭐ **Desktop design closes here** — see D91. |
| D89 | 11 Aug | ⭐ **THE MIRROR RANGE IS IN — ALL SIX, UNDER THEIR REAL NAMES. The range is 132.** ⛔ **This CORRECTS D88, which reached the wrong conclusion** | Client: "mirror grey, mirror black and mirror white… those are the most popular slabs", then, on finding the site returned nothing he recognised: "when I go to all stones and I type in mirror, nothing shows up, because you got the wrong name. Now go get exactly what I asked for." ⛔ **He was right and D88 was wrong.** Bloom DO publish these, as six ordinary product pages at `/tiles-1/<colour>-mirror`: **White, Cream, Grey, Brown, Blue and Black Mirror**, all live now. ⚠️ **HOW THE FIRST PASS MISSED THEM, WHICH IS THE LESSON**: it parsed the product JSON embedded in Bloom's `/quartz` listing, found a category called **"Mirror Chip"** holding their *Sparkle* products, and concluded the Mirror names did not exist anywhere. It never used **the supplier's own search box** — `bloomstoneslondon.com/search?q=mirror` returns all six in one request. ⭐ **Search the supplier's own search before concluding a product does not exist. A category that merely sounds right is not the product.** ⛔ The three Sparkle stones shipped in D88 have been withdrawn (pages moved to `stones/.removed-2026-08-11/`, tiles and records removed) — they were a substitution the client never asked for. ⚠️ **The name is the supplier's, word order and all**: Bloom write "Grey Mirror", not "Mirror Grey", and check 9 does not normalise word order. Customer phrasing is handled by search, not by renaming the stone — per-word AND means "mirror grey" and "grey mirror" both land on it. ⭐ **The mirror→sparkle alias added to `STONE_WORDS` in D88 has been REMOVED**: it papered over the wrong names instead of fixing them, and the real names need no alias. **Measured live: "mirror" returns 6 on the collection page and 6 in the wheel; "mirror black", "black mirror", "mirror grey", "mirror white" each return exactly one.** ⭐ Photography is Bloom's own, **five native 1600x1600 flat scans** needing no crop at all, White Mirror resampled 960 → 1600. Tones measured off the tiles, not guessed: White 234, Cream 159, Grey 132 light; Brown 35, Blue 19, Black 14 dark. All six clear check 1 (nearest existing tile 20-40, fails at ≤8). ⚠️ Quartz is now 67 with 16 dark, and the opening nine are still all light (D86 holds). ⚠️ Bloom licensing still **open**, as for Caesarstone and CRL. |
| D88 | 11 Aug | ⛔ **WRONG, CORRECTED BY D89 THE SAME DAY. Kept because the error is the lesson.** It concluded that no supplier published "Mirror Black/White/Grey" and shipped Bloom's *Sparkle* range under a search alias instead. The conclusion was reached from one listing page's embedded JSON and never checked against the supplier's own search box, which returns all six immediately. ⚠️ Everything below about the open-web trap still stands; only the conclusion about Bloom was wrong. ~~THE THREE "MIRROR" BEST SELLERS ARE IN THE WHEEL, UNDER THE SUPPLIER'S NAMES, WITH "MIRROR" MADE SEARCHABLE~~ | Client, out of a meeting: "three stones we have to add to quartz as they are the best sellers… mirror grey, mirror black, mirror white… any means necessary and make sure the image is high quality." ⛔ **NO SUPPLIER ON HIS OWN LIST PUBLISHES A STONE UNDER THOSE NAMES — the search is finished, do not repeat it.** All 1,785 harvested images and every harvest JSON (0), Nile, Next, Caesarstone, CRL, Noble Stone (0), AKG (the word appears only in WordPress CSS gradient names), Cosentino (307s), Fugen (403, blocks us), **and topcatworktops.co.uk itself including sitemap, portfolio and blog (0)**. ⭐ **"Mirror" is a TRADE DESCRIPTION, not a product name**: a dozen unrelated UK merchants sell "Black Mirror Quartz" and they are not the same slab as each other, which is D82's exact trap. ⭐ **Bloom Stones London — on the client's list, `ok=True`, robots.txt allows — sell the family under a category they literally call "Mirror Chip"**, as Bianco Sparkle, Grigio Sparkle Light and Nero Sparkle. Those three now ship **under Bloom's own names**, so checks 8 and 9 pass, and **`STONE_WORDS` in index.html maps mirror → sparkle** so "mirror black" narrows to Nero Sparkle, "mirror white" to Bianco Sparkle, "mirror grey" to Grigio Sparkle Light. Measured live: "mirror" returns 3 of 64. ⚠️ **If TopCat confirm these ARE their Mirror range, the rename is a `name=` change in `catalogue_mirror.py` plus a `supplier_names.py` entry** — and the STONE_WORDS entry must move with it. ⛔ **Two of Bloom's whites were REJECTED**: Bianco Galaxy and Bianco Glitter are featureless flats with no stone visible, which is D75's own rejection reason and a check-1 hash risk. Red Sparkle has the room reflected in it. ⚠️ Licensing on Bloom is **open**, exactly as for Caesarstone and CRL — confirm the account before go-live. §2a, D8 (never named publicly, verified 0 mentions on the built pages). |
| D88a | 11 Aug | ⛔ **SUPER-RESOLUTION DESTROYS SPECKLE. It was measured, rejected, and D77's drift metric did not catch it** | The three mirror-chip tiles were put through the documented route — 2×2 montage, `bytedance_image_upscale` at 4k, **2 credits spent** — and the result was **rejected for the grey**. ⭐ Measured as fine-detail energy retained against the supplier's own crop: **AI keeps 32.5% (Grigio Sparkle Light), 63.0% (Nero Sparkle), 74.9% (Bianco Sparkle); a plain Lanczos resample keeps 81.1%, 94.1%, 80.3%.** ⛔ **The model reads mirror-fleck sparkle as noise and smooths it away** — the opposite of the veined stones in D77, where it sharpened. The grey came back visibly blotched, with colour banding where dense speckle had been. ⚠️ **D77's pattern-drift check PASSED it at 3.36, inside its own 1.0–5.1 band**, because a mean absolute difference at 128px cannot see fine texture being destroyed. ⭐ **The rule this sets: drift is blind to smoothing. For a speckled or sparkled stone, measure high-frequency energy and look at the tile before installing any upscale — and prefer a plain resample.** All three ship as the supplier's own crop resampled to 1600 (959 → 1600, 923 → 1600, 1306 → 1600). ⚠️ **A second self-inflicted fault worth recording**: the small wheel variants were first written at **300px when every other `-s.webp` on the site is 800px**, a third of the resolution the `srcset` declares — caught by comparing against an existing tile rather than by any check. Now 800/1600 like the rest, and audited: every small tile is 800, every full tile ≥1200. |
| D87 | 10 Aug | ⛔ **V2 IS GONE. The whole second version, the switcher and the chooser page are out of the site** | Client: "remove the V2 from the side completely… where there's version one of the website and version two of the website, completely remove version two and everything about it." ⭐ Removed: the fixed **V1/V2 pill** bottom-right of every page (it was inline in index.html and a `PILL` constant in **three** builders — stones, services and trade), **`/versions.html`** (the chooser splash), **`/v2/`** (69 pages, 1.3MB) and **`/index-v2.html`** (683KB, the original A/B landing page that was still sitting in the site root). ⚠️ **Parked, not shredded**: everything moved to `.removed-2026-08-10-v2/` **outside `Website Demo/`**, so it cannot be served or deployed, following the same precedent as D60's removed stone pages. There is no git here, so say the word to delete it for good. ⭐ **This closes a live risk**: §2 rule 1 recorded that "V2 was not rewritten and still carries the old claims" about in-house fabrication — dormant but one revival away from being published. It is now impossible to publish by accident. ⚠️ The go-live compliance scans still carry their `grep -Ev "(^\|/)v2/"` filters; harmless, and they can be simplified next time they are touched. ⚠️ **The `./v2/` grep trap bit again while checking this** — BSD grep prints `v2/about.html`, so `grep -v "^./v2/"` matched nothing and the sweep looked like 72 pages still carried the pill. The live count is **0**. Measured after: 161 live pages, 1,071 internal links, 0 broken, verify.py ✅. |
| D86 | 10 Aug | ⭐ **NO DARK STONE ON THE FIRST SCREEN, AND THE LEFTOVERS BALANCED. All NINE visible cards are light in every range** | Client: "the first five showing cards should be light, so the centre one and the two on the side, and then the centre one and the two on the other side", then on seeing it wide: **"let's rather not have dark ones on the first screen if we can help it, I want balance."** ⛔ **NO CHOICE OF LANDING CARD CAN DELIVER THIS**: granite has 9 light stones in 20 and its longest run of consecutive lights A-to-Z is **THREE**, so there is no position in granite where even five in a row are light. ⛔ **FIVE WAS ALSO THE WRONG NUMBER AND THAT IS THE LESSON — measure the window before protecting it.** The wheel shows **9 cards** fully (`--dim` 1.00 / 0.78 / 0.56 / 0.34 / 0.18, four each side) plus partials, at every width from 912 to 1920. A five-card window therefore left the two deferred darks at **+4 and +5 — on screen, and both on the same side**, which is exactly what the client saw and objected to. ⭐ **`clearOpening` now walks outward taking the next LIGHT stone each way until it has OPEN_SPAN=7 a side, and puts the darks it stepped over back SPLIT EVENLY, half beyond each edge** — that split is the "balance", and it is why they are not simply dumped after the window. ⚠️ The span is capped at (lights−1)/2, so a range cannot protect more than it owns. **Measured: quartz and marble are clean to ±7 (15 cards); granite is clean to ±4 — its 9 lights exactly fill the 9 visible cards — and its remaining darks sit 3 left, 3 right.** ⭐ Cost in alphabetical order: quartz and marble keep A-to-Z apart from two deferred stones each. ⛔ **Granite does not and cannot**: 11 of its 20 are dark, so holding 9 lights together means the rest of that belt is the dark stones. That is the client's own trade and he was shown the numbers. ⛔ **The collection grid is NOT touched** — no centre card, so it stays a plain, perfectly sorted A-to-Z list. If the two ever have to match, MOVE `clearOpening` into `catalogue_active.py` rather than copying it, or they will drift (D51's failure mode). ⭐ Landing cards are the most popular stone in each range **that is not dark**: **Azul Shimmer** (quartz, the client's own pick), **Carrara Honed** (marble — ⭐ restored, the most popular UK marble, which D85 had pushed off) and **Bianco Crystal** (granite). ⚠️ Verified in the browser at 1920 on all three tabs, and against the pixels rather than the `tone` labels: every visible card measures a median luminance of 129–234. |
| D85 | 10 Aug | ⭐ **THE WHOLE RANGE IS ALPHABETICAL, ON BOTH SURFACES. ⛔ This REVERSES D74** | Client: "we should probably be smarter and order all the slabs in the slab wheel and in the collection thing in alphabetical order, because that would make it easier for people to navigate. And if they already want one." ⛔ **D74 was also his instruction and the two cannot both be had** — a stone's NAME says nothing about its colour, so sorting by name drops the tone wherever the alphabet leaves it. Measured after: quartz longest dark run 3, marble 4, **granite 7**, and granite's seven are at the FRONT now (Absolute Black ×4, Angola Black, Antiq Brown ×2). ⚠️ **That is the exact wall D74 was built to remove, moved from the end of granite to the start of it, and the client has been shown it.** The one mitigation that keeps both is his own hedge — "possibly only worry about the first letter" — i.e. sort on the first letter and spread tone within each letter; it is a change to `_alphabetical` in `catalogue_active.py` and nothing else. ⭐ **One sort, both surfaces**: `catalogue_active.S` feeds the collection grid, the estimator's picker and the wheel, so alphabetical is done once. ⚠️ Sorted on the **displayed name**, case-folded, **not the slug** — `calacatta-oro-quartz` displays as "Calacatta Oro" and `arctic-cream` ships `artic-cream`, so a slug sort would produce an order the customer can see no logic in. ⭐ **Per material, not across the whole catalogue**, or the "All" view would interleave the three ranges. ⛔ **THE WHEEL NEEDED A REAL CHANGE, NOT JUST NEW DATA, AND THIS IS THE PART TO UNDERSTAND**: `fanOrder` seated rank 0 at the centre, rank 1 to its LEFT, rank 2 to its RIGHT and so on outwards, which was correct for a popularity list (it kept the least popular out of the left wing). Feed an alphabetical list into that and the eleven on screen read 9,7,5,3,1,0,2,4,6,8,10 — two interleaved sequences. **It would have been perfectly sorted and no customer could ever have seen it.** The belt is now sequential, so dragging right walks A to Z. ⚠️ If the wheel ever goes back to popularity order, the centre-out seating has to go back with it. ⭐ **Where each range OPENS is now the only job `POPULAR` has left**, and D48 survives there: `landingIndex()` opens on the most popular stone whose card and the two after it are light. **Quartz keeps Azul Shimmer (D84); marble moves to Calacatta Gold Oro Honed** because Carrara Honed has Cosmic Black behind it; **granite moves off Absolute Black Extra to Bianco Antico** — ⛔ D48 giving way to the tone rule below, deliberately. ⚠️ The estimator's default derives through the same function, so the two still cannot drift. §4m |
| D85a | 10 Aug | ⭐ **NO DARK STONE MAY LEAD A RANGE. The wheel enters the alphabet wherever that rule is satisfied** | Client, with D85: "whatever looks best, as long as there's not a dark stone that's in the first slot or the first three slots." ⚠️ **The catalogue cannot honour this and the wheel has to** — rotating the entry point does not break alphabetical order, because dragging still walks A to Z, it just starts partway in, which is what a wrap-around carousel does anyway. ⛔ **The first version took the earliest qualifying stone and that was wrong, in a way only looking would catch**: granite opened on Arctic Cream, whose entire LEFT wing is Absolute Black Honed, Absolute Black Leather, Angola Black Leather and both Antiq Browns — five near-black cards in a row at a `--dim` of 0.34 falling to 0.18, which reads as **a hole where the wheel should be**, not as dark slabs. ⭐ So the rule now scores the whole **eleven-card window** and takes the fewest darks: Bianco Antico (4, spread) over Arctic Cream (6, banked). ⚠️ It reads the list it is GIVEN, so a filtered wheel also opens on a light stone rather than on whatever the filter left first. |
| D84 | 10 Aug | ⭐ **THE QUARTZ WHEEL OPENS ON AZUL SHIMMER, PLACED BY THE CLIENT** ⚠️ **The LANDING CARD stands; the hand-placed neighbours and the two darks were superseded within the hour by D85's alphabetical order** | Client: "in the quartz part of the stone wheel you are going to put Azul Shimmer as the first slab, that one will be the first one that they see before interacting. Then add a couple of brighter slabs next to it, and then a dark slab there here and there… put Arabescato Elegance next to it, and Calacatta Oro on the other side of it." ⛔ **This PARTLY SUPERSEDES D48 for quartz only** — "the one that displays first must be the most popular" no longer holds there, because Azul Shimmer is his choice rather than a trade ranking. ⚠️ Do not "correct" the quartz list back; the comment above `POPULAR` now says so. ⭐ **The eleven, left to right as the fan seats them** (ranks 9,7,5,3,1,**0**,2,4,6,8,10): Sahara Dunes, Carrara Jumbo, **Laurent Black**, Borghini Royal, Arabescato Elegance, **⭐AZUL SHIMMER**, Calacatta Oro, Calacatta Fantastico, **Marquina**, Carrara Shimmer, Crema Tempest. ⭐ **A new `PINNED` constant was needed and it is the interesting part.** D74's `spread()` interleaves ranks 1–10 by tone, and it only left the old quartz head alone because that list contained no dark stone at all. The moment two darks were placed in it, spread() had both tones to merge and **would have pulled Arabescato Elegance and Calacatta Oro off the landing card's shoulders** — the one thing the client named explicitly. `PINNED={Quartz:FAN_HEAD}` fixes the eleven he placed; the default is 1, which is D74 unchanged, so **Marble and Granite are byte-identical** (measured: longest dark run still Marble 1, Granite 2). The tail is still spread automatically. ⭐ **The darks sit at ranks 5 and 6, and that is a measurement, not a taste**: depth is `filter:brightness(--dim)`, which is 0.34 at ranks 5–6 and **0.18 from rank 7 out**. A white slab survives 0.18; Marquina measures a median of **4**, so further out it renders at about 1/255 and reads as a *hole in the fan* rather than a slab. Ranks 5 and 6 are the outermost slots where a dark stone is still visibly stone, one per wing. ⭐ **Laurent Black and Marquina were chosen by LOOKING, not by measuring darkness** — Royal Grey, Black Tempal and Nero Starlight are darker still and are flat fills with no pattern at card size, so beside the veined whites they read as blank cards. ⚠️ **The estimator's opening stone followed automatically** to Azul Shimmer (`BEST[m]=POPULAR[m][0]`, D48's deliberate derivation) and the opening price is unchanged at £2,000 – £2,500, because the bracket table is keyed on material and slab count, not on the stone. ⚠️ **The `/stones/` collection grid is a SEPARATE order** (`_interleave` in `catalogue_active.py`) and was not touched; ask the client whether he wants it to match. Measured: 126 stones all ranked, 0 unranked, inline JS parses, all 61 quartz tiles 200, no drawn-SVG fallback in the eleven, verify.py ✅. §4m |
| D83 | 10 Aug | ⛔ **FIFTEEN STONES CARRIED A PHOTOGRAPH OF A DIFFERENT FINISH FROM THEIR NAME. Renamed, and verify check 9 now fails the build on it** | Client: "if you're getting this one wrong, how should I believe that every other stone is correct?" — a fair question, and the audit answered it badly. ⛔ **Provenance was not enough.** Check 8 proved every tile came from the right supplier under a matching filename, and fifteen stones were still wrong, because our NAME dropped the supplier's finish word: we called Nile's "BELVEDERE LEATHER" simply **Belvedere**, their "Carrara Honed" simply **Carrara**, their "Blue Dunes Leather" **Blue Dunes**, their "PATAGONIA EXTRA" **Patagonia**. ⚠️ **That is the wrong-slab-at-the-house scenario exactly**: choose Belvedere here, ask TopCat for Belvedere, receive a different finish. All fifteen renamed to the supplier's own title. ⭐ **Check 9 compares the stone's NAME to the supplier's TITLE for the exact file it ships** and fails the build when they disagree; proven by injecting a wrong name and watching it fire. ⚠️ Seven legitimate differences are recorded in **`stones/supplier_names.py`** — five are the supplier's own misspellings of well-known stones (Artic Cream, Verde Gautemala, Macaubus Fantasy, White Eclpyse, Grigio Shimmerr, where OUR spelling is what a customer googles), and JUMBO is a slab FORMAT rather than a different stone. ⭐ An entry there AUTHORISES the difference, and the check still fails if the record itself goes stale against the supplier. ⭐ **Calacatta Oro was also re-cropped**: right supplier, right name, but the window was a tight zoom on a quiet patch, so it read as a plain white stone instead of the dramatic copper-veined slab a customer finds on Google. Now the full slab height, 2956px native. **The rule that sets: a tile must show the stone at a scale where its PATTERN is recognisable, not merely at the right resolution.** |
| D82 | 10 Aug | ⛔ **A STONE'S NAME AND ITS PHOTOGRAPH MUST MATCH THE SUPPLIER'S OWN. Now enforced by verify check 8** | Client: "these names cannot be wrong. If someone googles it and sees it looks different here, then we have a big problem… if someone chooses this one by this name and TopCat somehow shows up at the house with a wrong looking slab, then we are fucked." ⛔ **The thing that triggered it was mine**: chasing his Calacatta Gold reference I RENAMED the marble "Calacatta Gold Oro" to "Calacatta Gold" and attached a crop I had chosen. That is the exact failure he describes and it was reverted. ⭐ **A stone name is only meaningful RELATIVE TO A SUPPLIER** — "Calacatta Gold" is a marketing name different manufacturers put on completely different-looking products, so the site can never be validated against a generic Google image. The only defensible test is that the photograph shipped under a name is the one THAT supplier publishes under it. **Check 8 enforces exactly that** and was proven by pointing Carrara at a Caesarstone file and watching it fail. ⭐ **Audited: 121 of 126 clean.** The other five: one had lost its provenance in the revert (restored), and **four are spelled correctly here and INCORRECTLY by the supplier** — Nile's own titles are "Artic Cream", "Verde Gautemala", "Macaubus Fantasy" and "White Eclpyse". ⭐ The public name stays correct, because that is what a customer googles; the supplier's exact string is recorded in **`stones/supplier_names.py`** so an order can actually be placed. ⚠️ **Calacatta Gold is unresolved and needs Nick**: Nile's own quartz of that name is GREY veined (0.03% warm pixels) and their marble of that name is a busy brown, while the client's reference is a clean white with fine gold — a third manufacturer's product. ⚠️ Its tile is also a 4.2x upscale from 384px, the weakest image in the range. |
| D81 | 10 Aug | ⭐ **CALACATTA GOLD ADDED AS QUARTZ AND LEADS THE RANGE. The range is 127** | Client: "when we were first doing research it said the most popular stone was Calacatta Gold, why is this not one of our options? our whole intro video is based on that", then "Google says Calacatta Gold can be quartz or marble, add it to whatever category sells most in the UK", and "it must be the first thing that displays for whatever category it's in". ⭐ **Added as QUARTZ** — the industry brief calls quartz "the most popular and most all-round choice in UK kitchens" — and set as `POPULAR.Quartz[0]`, so the wheel now lands on it. ⭐ **NO UPSCALING AT ALL**: Nile's flat scan is 6496x3898, the slab face measures x 720-5760 / y 760-3280, and a 2400px square comes out of it natively, so the shipped 1600 tile is a DOWNSAMPLE. ⚠️ **The range already contained this stone under another name** — "Calacatta Gold Oro" (marble), since **oro is Italian for gold**. They are different products and both stay. ⛔ **verify check 3 correctly flagged "Calacatta Oro == Calacatta Gold"**, because its normalised key translates oro to gold — the same rule that caught Black Marinace == Nero Marinace. Here Nile genuinely publish two different quartz (warm cream with copper veining vs bright white with grey and fine gold, **perceptual distance 121** against a fail threshold of 8), so a `CONFIRMED_DISTINCT` exception was added **with the evidence written down**. ⚠️ That list is how the guard gets quietly disabled; measure, look at both tiles, and describe the difference or it is not an exception. ⚠️ **Open for the client**: the quartz range now holds Calacatta Gold, Calacatta Oro, Calacatta Gold Soft and Calacatta Gold Shimmer. Those are the suppliers' own product names, but four near-identical names in one range is the confusion D66 exists to prevent. |
| D80 | 10 Aug | ⛔ **"VEINING" WAS RETURNING STONES WITH NO VEINS. `vein` MEANS BUSY, NOT VEINED** | Client: "when I search veining, some without veining come up." He is right and the cause is a conflation in the search vocabulary: `vein` is a BUSYNESS classifier measured off the pixels (statement / soft / calm), and `SEARCH_WORDS` expanded `statement` to "veiny veined veining bold dramatic busy…". ⚠️ **Dense speckle reads as busy without being linear**, so Bianco Sardo, Baltic Brown, Angola Black Leather and Nero Marinace all carry `statement` and all answered a search for veining with no vein in them anywhere. Measured on the tiles they sit at the BOTTOM of the vein-structure scale (p99 over median ≈ 3.1–4.4 against 8–19 for genuinely veined stones). ⭐ **The vein words now come from the DESCRIPTION**, which was written with each photograph open and is the only record of what is actually in the picture — Nero Marinace's own line calls it "a conglomerate rather than a veined stone". ⚠️ Three traps inside that: the description had to be scanned for NEGATION (indexing that line verbatim made the one stone that denies having veins answer "veined"); the vocabulary had to widen beyond the word "vein" (Fusion Black says "striations" and is one of the most veined stones in the range); and "marbled" had to go, it matched 107 of 126. ⭐ **The whole description is now indexed**, so "sparkle", "copper", "pebbles", "crystals" and "diagonal" all work where they previously returned nothing — the client's actual complaint was "I can type whatever I want and it comes back with no stones". **Measured after: "veining" returns 79, zero unveined leaking, zero veined missing, and no term in a 20-word sweep returns nothing.** |
| D79 | 10 Aug | ⭐ **A GOLD BACK ARROW SITS BESIDE THE BREADCRUMB ON ALL 160 GENERATED PAGES** | Client: "a golden back arrow button, to go back to the previous page, in the top corner of the page below the nav bar… and if there's a directory of the pages that you're on, the back button must be next to that", then "make it a simple golden gradient back arrow, not a circle around it, not anything, just simple." ⭐ It lives INSIDE `.crumb`, which every generated page already carries in exactly that spot, so the two can never drift apart. ⚠️ It is an `<a>` with a REAL href taken from the parent crumb, not a bare button: right without JS, right on a cold open from search where there is no history, and right in a new tab. The inline handler only prefers `history.back()` when there is genuinely same-origin history. ⛔ First build gave it a ring, which the client cut; the gold is the brand gradient on the stroke itself now. ⚠️ Two build errors worth remembering: the inline handler's braces must be DOUBLED inside the builders' f-string templates, and anchoring the insert on the `<nav>` tag put both of build_stones.py's buttons on the FIRST crumb — anchor on each trail instead. |
| D78 | 10 Aug | ⛔ **THE WHEEL WAS SHOWING DRAWN SVG CARTOONS FOR THE 14 NEW STONES, AND THE BREADCRUMB WAS INVISIBLE ON 127 PAGES** | Two separate silent-render faults found from the client's screenshots. **(1)** `SLAB_TILES` in index.html is the slug→tile map the WHEEL reads and it was written only by `harvest/match.py`, which was never re-run — so the stone PAGES showed the photograph while the wheel fell back to `marble()`'s drawn SVG for the same stone. ⚠️ Nothing 404'd and nothing errored, because the fallback is by design; the customer just saw a cartoon under a real stone's name. ⭐ `apply_catalogue.py` now DERIVES SLAB_TILES from `manifest.json` and fails the build if any catalogue stone has no tile. **Fourth instance of the same root cause as D51, D59 and D68: a second file that has to be hand-synced.** **(2)** `nav.crumb` is a direct child of `<body>`, and D71's fixed page floor at z-index 0 painted straight over it, so the breadcrumb was invisible on every stone page from the moment the floor went in. Found only because the new back button did not appear either. ⛔ List every content-bearing direct child of `<body>` before adding a fixed background. |
| D77 | 10 Aug | ⛔ **THE TEN UNDERSIZED DARK TILES ARE UPSCALED. NOTHING ON THE SITE IS BELOW 1200px NOW** | Client: "these are terribly, terribly low quality images… there may be no fucking blurry images on the entire site." He is right and it should never have shipped: ten of the fourteen new tiles went live at **512–799px** against a range standard of 1600, on a site whose stated bar is "everything has to look 4k perfect quality" (D50). ⛔ **They were shipped knowing they were small, with the upscale left as a follow-up item. That was the wrong call** — the handover's own quality floor is not a nice-to-have and a soft tile is exactly what the client checks. ⭐ Fixed with the documented cheap route: **three 2×2 montages, 6 credits total** against 20 one at a time, `bytedance_image_upscale` at 4k, returning 4096px sheets that split to 2048 per stone and install at 1600. **Pattern drift measured at 1.0–5.1** (mean absolute difference at 128px), so the model sharpened rather than re-imagined, and all ten were compared against their originals at real display size before installing. ⛔ Restore set synced, provenance in `upscaled.json`. **Every tile on the site is now ≥1200px, 121 of 129 at ≥1600.** |
| D76 | 10 Aug | ⛔ **THE WHOLE SITE WENT BLANK FOR ONE INVENTED WORD, AND NOTHING CAUGHT IT** | Client: "none of the animations are loading in, the whole website is currently empty… most of the hero section is gone." Cause: the 14 dark quartz were given `preset="noir"`, **a preset name that does not exist**. `marble()` does `const p=STONES[preset]` then reads `p.grey`, so it threw a TypeError at the top of the script, before the reveal observer was wired — every `.rise` element stayed at opacity 0 and the hero, the copy and the reviews were simply not there. ⚠️ **The measurement had the right value and it was overwritten by hand**: `derive.measure()` returns a `preset` keyed on (tone, hue), and it was ignored in favour of a typed-in name. ⛔ **Everything passed**: `node --check`, the build, `verify.py` all seven, 200 on every route. Nothing tests that a data value means anything to the code that consumes it. ⭐ `check_presets()` in `apply_catalogue.py` now reads the valid names out of the JS `STONES` block and **fails the build** on any preset the engine does not know; it was tested by re-introducing "noir" and confirming it fires. ⚠️ The guard was itself written wrong first — it checked `s["stone"]` when the catalogue key is `preset`, so it silently passed everything. Check that a new guard actually fires. |
| D75 | 10 Aug | ⭐ **FOURTEEN DARK QUARTZ ADDED. The range is 129, and quartz goes from 2 dark in 50 to 16 dark in 64** | Client, escalating: "are you telling me you're not going to get any more dark stones… if there are, then add them", with his own list of nine suppliers. ⛔ **The first answer given to him was wrong in SCOPE and that is the lesson**: the search was limited to the two suppliers TopCat already buy from, because D45 put the rest on "ask first" — so the honest report of "about 2 available" was true of a question nobody had asked. **485 images from Caesarstone, CRL and Noble Stone were already sitting in `raw/` unharvested-for.** ⭐ Now live: **6 black quartz, 4 of them veined** — Laurent Black (gold veining on black), Marquina and Azalai Negro (white veining on black), Black Tempal, Nero Starlight, plus Vanilla Noir, Woodlands, Darcrest, Belvedere Black, Terra Marron, Umbra Marron, Forest, Cristallo Gris, Labradorite Royal. ⛔ **Six candidates were rejected and the reasons are recorded in `catalogue_dark.py`** — Soft Black is a flat fill with no stone in it, Marquina Shimmer carries a room reflection, Concrete Oyster and Urban Concrete measured LIGHT (adding those under "dark" is D58's exact defect), and Croma Black / Silk Negro were **caught by verify check 1 as pixel-identical to each other and to Fresh Cement** — ⚠️ not really the same photograph, but all three are featureless flats and a perceptual hash of a flat image is the same whatever its colour. Dropped rather than loosening the guard. ⚠️ **CRL's tiles ship at 625px** against the range's 1600 because that is the largest frame CRL publish; they want an upscale pass (~6 credits) before go-live. ⚠️ Licensing on Caesarstone and CRL is **open** — see §2a. §2a, §4r |
| D74 | 10 Aug | ⛔ **REVERSED the same day by D85 (alphabetical).** Kept because the reasoning still matters: it is why granite must never be sorted on tone, and its `_spread` merge is the code to restore if the client wants the spread back within each letter. ⭐ **DARK AND LIGHT ARE INTERLEAVED IN EVERY RANGE. An ordering change only, no stone added, removed or reclassified** | Client: "make sure to spread them in between, don't keep all the dark next to each other. Also spread the dark in between the light with marble and with granite. I don't mean adding more to marble and granite, I just mean take the ones that are already there and move them in between." ⚠️ **Granite closed on SEVEN dark stones in a row** and marble had three clumps of three, because the catalogue was grouped by material then alphabetical and the tone fell where the alphabet left it. Longest dark run now: **Quartz 1, Marble 1, Granite 2** (11 darks in 20 cannot do better than 2). ⭐ Two places, one method: `_interleave` in `catalogue_active.py` covers the collection grid, the estimator's picker and the wheel's tail; `RANK` in index.html covers the wheel's fan. ⛔ **D48 IS PRESERVED**: rank 0 is pinned to `POPULAR[0]` so the landing card is still the most popular, and ranks 1-10 are shuffled only among themselves so the visible eleven is the same set. ⛔ **Both were got wrong first time by interleaving POPULAR and the catalogue separately** — the light group sorts first, which moved granite's landing card off Absolute Black Extra, and lifting the ranked stones out of an interleaved catalogue re-clustered the tail into a run of six. It has to be applied to the FINAL order. ⛔ Do not "simplify" either to a sort on tone; sorting on tone is what makes the clumps. §4r |
| D73 | 10 Aug | ⭐ **THE RANGE ON SHOW MUST NOT READ AS THE WHOLE RANGE. A sourcing line now sits on every surface where a stone is chosen** | Client: "we must make it very clear and obvious somewhere to say if you don't find your stone, we can source it… because if they think this is the only range that we have, that wouldn't make sense, because there is more. So in all those spaces where they have to choose a stone." ⚠️ **It already existed in four places and none of them were where it was needed**: the collection's lede (last clause of a skimmed paragraph), the collection's empty state (only after a filter returns nothing), the enquiry form's upload, and the marble/granite POA text. It was absent from the **wheel**, from **every stone page** and from the **estimator's picker** — the three places a person actually runs out of options. Now on all three, plus lifted out of the collection lede onto its own line. ⛔ **Hedged deliberately**: "we can usually source it", "we will source it where we can". §2 rule 12 forbids an absolute and this is a promise about other people's quarries. ⛔ Suppliers still never named (D8). §4r |
| D72 | 10 Aug | ⛔ **THE "REFINE" BUTTON DID NOTHING, AND A SECOND BUTTON HAD THE SAME BUG. One missing CSS line each** | Client: "there's a button that says refine, I don't know what that does. If it doesn't do anything, remove it." It did nothing **visible**: the drawer is opened by toggling the HTML `hidden` attribute, which the browser honours via a UA rule of `[hidden]{display:none}` — and a UA rule loses to any author rule, so `.st-drawer{display:flex}` held it open in every state. All three filter groups sat permanently open and **pushed the stones off the first screen**, which is the exact failure D53 built the drawer to avoid. ⭐ **Not removed, fixed** — removing it would have left the filters permanently taking the space D53 reclaimed. Two full rows of stones now sit above the fold where one partial row did. ⚠️ **Found a second live instance in the estimator**: `.est-lm{display:flex}` meant the "Total linear metres" field showed the moment the edge panel opened, contradicting both `renderEdge()` and the markup comment beside it saying it "stays out of sight until a profile is chosen". ⭐ **Third instance overall** — `.st-badge` three lines above `.st-drawer` in the same file had already been caught, fixed and commented, and the lesson was not carried across. A scan for the pattern now exists and returns clean. §4r |
| D71 | 10 Aug | ⭐ **The stone pages and the collection now stand on the landing page's floor, not flat `--ink`** | Client: "inside the stone pages it should have the same background as the landing page, that same faded black background." They were `background:var(--ink)`, which is why the site changed character the moment you left the home page. Now the same Portoro photograph under the same veil at the same measured strength (§4l). ⚠️ **The url must be ROOT-RELATIVE** — index.html can write `url('assets/…')` from the site root, these pages sit a level down. ⚠️ `main` **and** `body > footer` both need lifting over the fixed floor, because on generated pages they are siblings rather than nested as on the landing page. §4r |
| D70 | 10 Aug | **Blue Roma re-cropped: warehouse racking and the slab's cut edge were across the top of the tile** | Client, from the wheel: "there's this strip on the top where it looks like it's above the slab, just crop it slightly more in." Measured rather than eyeballed — saturation runs 26–34 across rows 24–44 (the warm cut edge) and settles at 5.5 from row 56, so the cut is at row 60. Re-squared to 1540 and centred; no re-upscale and **no credits**, because pixels were removed from an already-upscaled tile rather than added. ⛔ **The restore set was synced FIRST**, per the trap that "fixed" Aqua Gucci and Calacatta Gold Oro twice each — `_upscale/installed/` now holds the corrected tile, so the documented post-run restore carries the fix instead of reverting it. Provenance in `upscaled.json`. ⭐ **A new scan then measured every tile's top and bottom band against its own body**: 11 flagged across 115, all reviewed at 330px, **all genuine stone**. Blue Roma was the only real fault. §4r |
| D69 | 10 Aug | ⭐ **THE REAL LOGO IS IN. The client supplied his own artwork and it replaces a hand-rebuilt approximation that was live on all 150 pages** | Client: "this is the real logo, not the one that we currently have on the site. The one we currently have on the site is very wrong." He is right, and it was worse than a wrong drawing: the mark was **redrawn by hand in inline SVG** and the wordmark was **live text** set in Cinzel and Montserrat, so the "logo" was a reconstruction that could drift again whenever a webfont fell back. Now: **nav takes the horizontal lockup, footer takes the vertical** (his instruction), favicon takes the icon squared off. ⭐ **One `<img>` per surface pointing at a file** — the old mark was pasted into four builders AND the head of every page, which is why correcting it meant editing five source files. ⚠️ **The generated pages also changed colour**: their wordmark was set in `--bone`, so they carried a white TOPCAT where the landing page carried gold; the supplied lockup is gold throughout and the two now match. ⛔ **viewBoxes are retightened to the ink** and **height is the only dimension ever set** — see `assets/brand/README.md`. Measured: bar height unchanged at 78.5px, 150/150 pages carrying all three assets, 0 broken links, verify.py ✅. §4q |
| D68 | 10 Aug | ⛔ **The HTML sitemap was built from the 52-STONE SNAPSHOT and 63 stone pages were missing from it** | Found while checking which surfaces name a material. `_stone_catalogue()` in `build_seo_pages.py` read `catalogue_source.py`, not `catalogue_active.py`, so the one page built to prove nothing is orphaned listed 52 of 115. ⚠️ **The handover's "0 orphaned pages" did not catch it** — that figure was measured before the range grew and was never re-measured. ⚠️ Its `except` swallowed the import error and shipped a sitemap with **no stones at all** on the first fix attempt, silently. Now 115 linked, 0 broken, and the stone columns are headed by the range label. ⭐ Same failure as D51 and D59 for the third time: **a second file that looks like the source of truth**. |
| D67 | 10 Aug | ⛔ **A hand-typed list of quartzite slugs had gone stale and made 16 stones unsearchable** | `QUARTZITE` in index.html named 10 slugs against a real 26, so Mont Blanc, Macaubas Fantasy, Cosmic Black, Dover White and twelve others could not be found by a customer typing the one word that describes them — on the range's highest-margin stones. Travertine was a hard-coded slug beside it. Both are gone: the wheel reads `kind` off the catalogue, which `apply_catalogue.py` now emits. **Measured after: "quartzite" returns 26 of 45, "marble" 18, "travertine" 1, and "quartzite" inside the Quartz range correctly returns nothing.** |
| D66 | 10 Aug | ⭐ **The browse range is renamed "MARBLE & QUARTZITE" on every customer-facing surface. The stone always states what it is; the range around it is named for what it contains** | Client: "on the collection page it shows marble, but on the actual page it says quartzite, natural stone. So we have to say available in marble and quartzite or something like that. We cannot have that confusion." ⚠️ **D65 fixed the page and broke the journey** — naming the true stone was right, but nothing connected it to the Marble tab the customer had just pressed. ⛔ **The two literal readings of his instruction were both rejected, and here is why**: "Taj Mahal is available in marble and quartzite" is not true and could not be made true (it is one rock, and every UK merchant sells it as quartzite); and "just say natural stone" throws away the word the customer searches, the reason the stone costs what it does, and re-opens D65. ⭐ **The honest version of what he asked for is at RANGE level, and it is what the trade does**: the range is 18 marbles, 26 quartzites and 1 travertine, so "Marble" was wrong about the majority of its own contents. Now: the tab, the collection filter, the estimator tab and the sitemap column say **Marble & Quartzite**; the wheel readout, the card tag, the estimator's stone line, the enquiry chip and the image alt say the **true rock**; and every stone page carries a **Range** row under Stone. ⛔ `mat` is untouched, so no price, filter, POA or deep link moved. ⚠️ **The travertine is deliberately not in the label** — one stone in 45, its own card and page say Travertine, and a three-noun range name reads as a list. ⭐ **To change the wording, change `RANGE_LABEL` in build_stones.py and `MAT_LABEL` in index.html.** §4p |
| D65 | 10 Aug | ⛔ **27 stone pages CONTRADICTED THEMSELVES: titled "Marble", spec block said "Quartzite". The visible copy now names the true stone; the CATEGORY is unchanged** | Client: "Take Fusion Black. It says stone is quartzite, natural stone. Why doesn't the stone say marble, or why is it in the marble when it's a quartzite? That doesn't make any sense to me." He is right — `<title>` read *Fusion Black Marble Worktops* while the spec block on the same screen read *Stone: Quartzite (natural stone)*, because `mat` is the browse-and-pricing category and `facts['kind']` is what the stone actually is. 26 quartzites and one travertine. ⭐ New `shown_mat()` in build_stones.py drives the title, meta description, hero tag, eyebrow and the "About …" heading from the TRUE type. ⛔ **`mat` is deliberately untouched**, so the estimator, filters, wheel, POA behaviour and every price are exactly as TopCat set them. ⚠️ Do NOT "finish the job" by adding Quartzite to `MATS` in index.html — it would put a fourth material tab and a fourth range on a site TopCat present as quartz, marble and granite, and that is TopCat's call. **Open question for TopCat, in §7.** |
| D64 | 10 Aug | **Thickness is now "20 mm or 30 mm" on all 115 pages, and it is what TopCat SUPPLY, not what one slab in a yard measured** | Client: "some places where you say quartzite, natural stone, you don't give slab sizings on those, like twenty or thirty millimetres." 59 of 115 showed no thickness at all, because the supplier's stock system only publishes a figure for slabs it happens to hold. ⚠️ The old row was the **wrong fact anyway** — it printed the thickness of one physical slab in a supplier's yard, which is not what the customer is sold. TopCat's own estimator offers exactly two (`THICK=[20,30]` in index.html), so that is the honest answer and it is identical on every page. ⛔ Slab SIZE is still shown only for the 56 where the supplier publishes it and omitted for the other 59 — a size is planned around, and inventing one is the defect this whole round exists to remove. |
| D63 | 10 Aug | ⛔ **Three UNKEEPABLE PROMISES were live in `MAT_FACTS`, one of them on all 45 marble pages. All rewritten, and a scan now fails the build** | Client, finding it himself on Arabescato Gold: "It says the pattern is consistent across the slab. No it fucking isn't. You cannot say that something is consistent across the slab when it's not. Don't say things that you cannot guarantee. Be more vague." Two more went out beside it: granite's "so there are no surprises on fitting day", and marble's **"we vein-match every joint by hand"**, which is a guarantee AND ⛔ a claim to fabrication work TopCat outsource (D21, §2 rule 1). ⭐ **Absolutes are the tell** — "all it asks for", "the one thing it minds", "keeps it perfect", "in its stride". A comparative is safe, an absolute is not: quartz now reads "varies less between slabs than natural stone does". ⭐ `verify.py` check 7 scans every built page for the whole family and **found 355 hits** on the pre-fix pages. ⚠️ It strips HTML comments first — a comment quoting a banned phrase shipped in all 115 pages once and tripped a naive scan. |
| D62 | 10 Aug | ⭐ **Every stone description was REWRITTEN from the photograph we ship. Colour and pattern only, no claims** | Client: "Just describe the colour of the stone. Don't talk about things like, oh, it can match to another stone or make promises we do not guarantee that we can fulfil. Only write about the way that it looks that someone might search." Nothing about sealing, heat, durability, joints, or what a stone suits — an earlier draft carried all of those and the client cut them. ⛔ **NEITHER SUPPLIER PUBLISHES ANY COPY**: checked directly, Nile's `description` field returns "Click on the image to enlarge" for all 138 products and Next Stone Slabs has no product pages at all, so there was nothing to reword. ⛔ **Never look an engineered quartz up on the open web** — the name is a manufacturer's marketing name and a different maker's slab answers to it, the same reasoning that killed marble.com (D45). All 115 tiles were reviewed at 330px before a word was written. ⭐ Copy now lives in its own file, `stones/descriptions.py`, because `grow.py` regenerates `catalogue_expanded.py` — which is exactly how D46 was silently reverted. `build_stones.py` RAISES rather than falling back to an old blurb. |
| D61 | 10 Aug | ⛔ **63 of the 115 descriptions were SCRIPT-ASSEMBLED from a phrase bank, and one of the canned lines was factually backwards** | The client read Calacatta Classic and objected to "a bright white ground" (trade jargon), "very little movement" (trade jargon) and **"It hides everyday marks better than a busier stone will"** — "busier stones will obviously hide marks better because it's busier." He is right, and it was worse than one bad line: `expand.py:153` filled four slots from fixed lists keyed off measured pixels, so 63 blurbs drew on 78 sentences, ten stones closed on the same one, and the reversed claim landed on five pages **including Absolute Black Honed** — a plain matt near-black granite, the single worst surface in the range for showing marks. The word "ground" was on 81 pages. |
| D60 | 10 Aug | ⛔ **The four duplicates removed by D55b still had LIVE, INDEXABLE pages, including `black-marinace.html`** | Checks 1-4 of verify.py all read the CATALOGUE, so a page that outlived its entry was invisible to every one of them — including the exact Black Marinace duplicate check 3 was built to catch. Moved to `stones/.removed-2026-08-10/` rather than deleted. ⭐ verify.py check 5 now lists the DISK and fails on any stone page the catalogue no longer sells. |
| D59 | 10 Aug | ⛔ **The D46 Misterio Gold correction had been SILENTLY REVERTED and was wrong on the live site** | D46 fixed it from "dark brown" to pale cream on 9 Aug. That fix is still in `catalogue_source.py` — but the site builds from `catalogue_expanded.py`, which `grow.py` rebuilt from an older snapshot, restoring `tone='dark', hue='brown'` and the old blurb. It had been answering the "dark" filter with a pale stone ever since. ⚠️ **This is the failure mode D51 was created to prevent and it happened anyway**: one source file is not enough if a generator can overwrite it. Restored, and copy moved out to descriptions.py where grow.py cannot reach it. |
| D58 | 10 Aug | **20 stones were filed under a colour the photograph contradicts. Corrected against the tiles** | The filter runs on `tone`/`hue`, so this is what a customer actually clicks. Fusion Wow Multicolour was *light cream* and is dark orange-brown; Calacatta Viola was *dark brown* and is a white marble; Blue Dunes was *dark blue* and is pale cream; Cristallo was *light white* and is warm honey brown. Found by running the catalogue's own classifier (`derive.measure`) over every shipping tile and reviewing all 20 by eye — the classifier alone is not trusted, it reads Fusion Black as brown because of the gold. |
| D57 | 10 Aug | ⛔ **EVERY MEASUREMENT ON THE SITE IS IN MILLIMETRES. 22 pages were printing a WRONG slab size, not merely a wrong unit** | Client: "It's also showing the thickness as three centimetres. Every slab has to show in millimetres. Every measurement has to be in millimetres." ⚠️ Worse than a unit inconsistency: `slab_facts()` appended the letters "mm" to whatever figure it held, so a slab recorded as 322 x 162 **centimetres** printed as **"322 x 162 mm"** — a worktop the size of a sheet of A4 — on 22 stone pages. Data normalised to mm; the renderer and verify.py check 6 both now **fail** on a figure under 1000 in either dimension, because a unit error survives every visual check: the page looks perfectly correct. ⚠️ The estimator's "Total linear metres" for edge profiling is a PRICING unit and is deliberately left alone. |
| D56b | 10 Aug | **The "more to consider" strip now shows stones that actually LOOK alike, measured off the photographs** | Client: "At the bottom of the actual slab page, it shows slabs that look similar to that. It doesn't just show random slabs." It was `same[(idx + k) % len(same)]` — the next three entries in the list, sharing nothing but material. Measured: 30% matched on colour and **on 76 of 115 pages all three suggestions were unlike the stone**; Nero Marquina, a black marble, offered a pale blue, a white and a blue. ⭐ New `harvest/similar.py` describes each tile by ground, veins, contrast and busyness and picks nearest neighbours. ⚠️ Three things it taught: the MEAN colour is useless (a white marble with black veins averages to grey), features must be **normalised before weighting** or colour silently stops counting, and busyness needs a **log** or the busiest stone in the range matches to nothing. ⛔ Same material only, and it must be re-run before build_stones.py whenever tiles change. |
| D56a | 10 Aug | **Calacatta Viola and Arabescato Classico re-cropped and re-upscaled; 2 credits** | Viola's green lifting strap could not be cropped out of its frame — the straps run diagonally, so a band clear at the bottom is not clear at the top, and the widest clean window was only 512px. Moved to a **different frame of the same slab** with no straps across the face: 368px → 1600px. ⚠️ Arabescato Classico's "blown highlights" were **not a defect in the stone** — every flat frame Next publish is an installed worktop with sunlight falling across it. Window chosen by measuring the smallest brightness sweep across three frames, 440px → 1600px. ⚠️ `upscale.py --install` does NOT populate `_upscale/installed/`, so all three would have reverted on the next full run; the restore set is now synced and covers all 94. |
| D56 | 10 Aug | ⭐ **`harvest/verify.py` is now a REQUIRED pre-deploy gate, after the client found EIGHT defects live that the pipeline had passed** | Client: "make hundred percent sure … that no two pictures are the same and that every stone is exactly the stone we're looking for … it would be detrimental to the company if someone chooses the stone and it's actually the wrong stone." It checks four failures that **do not catch each other**: (1) same IMAGE two names — Almond Beige and Calacatta Gold Soft were pixel-identical; (2) same TILE FILE two stones — Dolce Vita and its Leather variant both pointed at one .webp; (3) ⚠️ **same STONE two names — Black Marinace == Nero Marinace, TWO DIFFERENT photographs of one stone because "nero" is Italian for "black", which a pixel check passes happily**; (4) stale tiles from an older run. ⛔ Same stone in a different FINISH is legitimate (Absolute Black is sold in four), and MATERIAL is part of the identity key (quartz "Carrara Jumbo" ≠ marble "Carrara Polished") — remove either nuance and the guard becomes a nuisance that gets switched off. ⚠️ The other lesson: the range must be reviewed at **330px minimum**; the 215px contact sheet used earlier is exactly how eight faults reached the client. §0 |
| D55b | 10 Aug | ⛔ **Four entries removed as the same stone listed twice. The range is 115: Quartz 50, Marble 45, Granite 20** | Dolce Vita Leather, Mystic Grey Leather and Bianco Eclypsia Calacatta Leather each had **no photograph of their own** — the supplier published one image under both the plain and the leathered name, and cropping them differently only disguised it. Black Marinace was the same stone AND the same finish as Nero Marinace. ⭐ **The rule this sets: a finish variant earns a place only if the supplier photographed it SEPARATELY.** Absolute Black in four finishes is fine — four real photographs. ⚠️ Patagonia's pin is deliberately WIDE and it is the only tile under 1200px (880): the stone genuinely is angular white shards with dark seams, and a tighter crop lands inside one shard and reads as a single crack ("is it supposed to look like this?" — yes, but not cropped like that). Resolution traded for recognisability. §3, §4 |
| D55 | 10 Aug | ⛔ **Three added stones reached a contact sheet with the WAREHOUSE still in frame. Pinned to middle-band crops** | Fusion Wow Multicolour had **red shutter doors and a rendered building wall** across its top third; Arabescato Corchia Extra and Dover White each had a **rack pole** through the slab. All three passed `classify` as a scan because the stone is the dominant texture — the scene gate is a filter, not a guarantee. ⚠️ **Trimming ONE side only moved the window onto the pole at the other**, which was tried first; the fix is a middle band. ⭐ The lesson is the standing one: **a contact sheet of every shipping tile, looked at by a person, is the only check that catches this**, and it is not optional. |
| D54 | 10 Aug | **The range is 119 — Quartz 50, Marble 49, Granite 20 — after a recount that found D52's marble ceiling was wrong** | Client, pushing back: "I don't understand why you're saying you only have ninety six when I said fifty of each… I still want fifty of each if their suppliers allow that." He was right to push. D52 reported Marble capped at 29, taken from `expand.py`'s own count — but that count predated the pipeline fixes that lifted accepted tiles from ~150 to 176. A recount against the tiles actually on disk gave Marble 63 raw / **49 unique**, and Granite 27 raw / **20 unique**. ⛔ **Marble's 50th genuinely does not exist**: "Cote D Azur" and "Cote D'Azur" are the same stone listed by both suppliers, one apostrophe apart. Granite stops at 20 for the same reason it always did. ⚠️ Finish variants ARE counted separately (Absolute Black Honed / Leathered), because the live range already listed Antiq Brown Extra beside Antiq Brown Leather and they are different products a customer picks between — deduping them cost 9 marbles and 9 granites. New tool `harvest/grow.py`; ⛔ do NOT re-run `expand.py`, it rebuilds from the original 52 and would delete live stones. |
| D53 | 10 Aug | **The filtering is redesigned, and the /stones/ collection can now refine on colour, veining and finish** | Client: "they've redesigned the filtering system so that it looks and functions better. And on the all stones, we need to have the filter option there as well." The collection page could filter by material and tone only — so the one surface showing every stone was the least narrowable, which is untenable at 96. Colour/Veining/Finish now sit in a **Refine drawer** (a button, not three permanent chip rows, which would have pushed the stone below the fold). ⭐ On BOTH surfaces a chip that would return nothing is now **dimmed and unclickable**, counted against every other group's choices so the alternatives beside it stay reachable — clicking a filter and getting nothing back is what makes a panel feel broken. OR inside a group, AND across groups. §4n |
| D52 | 10 Aug | **The range is switched on at 96 stones — Quartz 50, Marble 29, Granite 17 — and every one carries a real photograph** | Client, for the third time: "I want fifty in each category, not fifty two in total through the 3 categories. 50 in each unless one of them doesn't have 50." **Quartz reaches 50.** ⛔ Marble and Granite cannot, and that is a supply fact: the two licensed suppliers list 27 marble and 27 granite names between them, and after the ones with no usable photograph the pool is 29 and 17. Padding either would mean listing stone TopCat cannot get, or taking photography from a supplier they have no account with (LICENSING.md forbids the second, D45). The route to a wider granite range is another supplier account or the Caesarstone/CRL/Cosentino packs. ⚠️ 44 of the 96 carry **generated copy** (`review=True`) that has not been through the client's voice. §4n |
| D51 | 10 Aug | ⭐ **The stone list now lives in ONE file. `catalogue_active.py` is the switch** | It was three hand-kept copies — `catalogue_source.S`, `STONE_LIST` in build_stones.py, `MATERIALS` in index.html — each with a comment telling the next person to remember the other two. That held at 52 because someone checked by hand every time; it does not hold at 96, and drift does not error, it just shows a different slab from the stone page under the same name. build_stones imports it, `apply_catalogue.py` injects it into index.html. **To change the range, change one import.** |
| D50 | 10 Aug | **Tiles that had no larger photograph are super-resolved. 73 stones, 46 credits, every one checked by eye against the original before it shipped** | Client: "if it's not four k HD or at least very clear, fucking fix it… I don't care how you do it", with Higgsfield allowed and a 100-credit ceiling. ⛔ **It does not generate or re-imagine a stone** — it enlarges the supplier's own photograph of the crop slabify already accepted. Batched **four stones per job** (2x2 montage, flat 2 credits/job whatever the size) which cut the bill from 146 credits to 46. ⚠️ **`harvest/_upscale/installed/` holds every upscaled tile and MUST be copied back over `assets/slabs/` after any full `slabify.py` run**, or the run silently reverts them. Provenance is in `harvest/upscaled.json`. ⚠️ Two supplier photographs carry defects that are in the ORIGINAL, not introduced: Arabescato Classico has two small blown reflections, Verde Alpi and Aqua Gucci have yard marks. §4n |
| D49 | 10 Aug | ⛔ **The wheel's selected card was rendering at `brightness(1.10)` and THAT was the "overexposure", not the photographs** | Client, diagnosing it himself: "some of the overexposed images is simply because of the light… on the design because the front image that's selected is highlighted, it's causing images to overexpose. So some of the images that you created aren't even overexposed." He was right. `layout()` and `fanOut()` added `+near*0.10` to `--dim`, which only ever reached the centre card. On a pale stone that clips — Calacatta Fantastico sits at median 235 and 1.10x drives it past 255. **Depth may only ever DIM; the centre now shows the true photograph** and still pops through scale, lift and its halo shadow. ⚠️ Carrara Jumbo and Carrara Shimmer still MEASURE 31% clipped and are correct: a white quartz photographed on white. §4n |
| D48 | 9 Aug | **The stone wheel opens on the UK's most popular stone and fans outward in popularity order** ⛔ **THE ORDERING HALF IS SUPERSEDED BY D85 — the wheel is alphabetical. The LANDING half survives** | Client: "the 11 stones that show are the UK's top 11 most popular choices, and the one that displays first must be the most popular of each stone." One table, `POPULAR` in index.html, and the estimator's default stone is derived through the same `landingIndex()` so the two can no longer drift. ⚠️ **"The eleven that show are the top eleven" is no longer true and cannot be** — the eleven on screen are now whatever sits either side of the landing card in the alphabet. The surviving rule is the landing card itself, subject to D85a's tone rule. Landing cards now: **Azul Shimmer** (quartz, the client's own pick, D84), **Calacatta Gold Oro Honed** (marble), **Bianco Antico** (granite). ⚠️ The ranking is evidenced from UK trade sources, **not TopCat's own sales data** — swap the lists the moment the client has real figures. §4m |
| D47 | 9 Aug | ⛔ **An orphaned marble.com harvest from a previous session had downloaded 542 files and was still running. Killed, and `raw/marbledotcom` deleted** | It started 21:01 from another session and survived into this one. It was missed by the first sweep because its command line runs Python from a heredoc and never contains the literal string `harvest.py`. **Nothing from it ever reached the site** (verified against the manifest), and marble.com is now `ok=False` in `harvest.py` per D45. ⚠️ When checking for stray jobs, grep for the working directory, not the script name. |
| D46 | 9 Aug | **Misterio Gold's colour data and blurb were WRONG and have been corrected against the supplier's photograph** ⚠️ client to read the new sentence | It was written as `dark / brown / statement` — "Deep warm veining on a darker ground" — before anyone had seen it. Next Stone's own photograph is a **pale cream** with fine gold veining. It was answering the "dark" filter and then showing a pale stone. Now `light / cream / calm`, preset `crema`, new blurb. Corrected in all three copies of the list. §4m |
| D45 | 9 Aug | ⛔ **marble.com is NOT used, and the ~2,535-stone harvest planned in the last handover was NOT run** | **Reverses the plan in the then-current START HERE (§3 of it), since superseded twice.** Two reasons. (1) `LICENSING.md`'s own test is that TopCat must **buy from** a source for its photography to be defensible; marble.com is a US countertop retailer TopCat has no account with, so it fails the same test that put Caesarstone and CRL on "ask first". (2) It was not needed — the licensed suppliers reached 47 of 52 once the pipeline's own bugs were fixed, and 4 of the 5 remaining stones are **engineered quartz**, where a same-named product from another maker is a *different product* and marble.com would be the wrong image under the right name. The cheap half is kept: `harvest/mdc_index.py` indexes all 2,363 names from ONE request if the client ever licenses it. §4m |
| D44 | 9 Aug | **Natural stone now carries a photograph too** ⚠️ **reverses the "natural stone keeps the drawn slab" rule** | The rule (written into `index.html` above `SLAB_TILES`) was that marble/granite/quartzite keep the drawing, because each block is unique and a stock photo becomes a promise about veining TopCat cannot keep. Client, 9 Aug, overriding: *"every single stone there in its actual pattern and image… so that there's no fucking mistakes"*, and earlier *"find an image of that exact slab for that exact name, then add that image"*. ⚠️ **The safeguard the old rule provided still has to come from the copy** — the site must keep saying a photograph is indicative and that the customer approves their ACTUAL slab before cutting. §4m |
| D43 | 9 Aug | **The page floor is a real PORTORO photograph — HAIRLINE veining only — under a tunable dark veil** | Client, over **six** passes: "it almost looks like just paintbrush strokes… low quality and blurry" → "I don't like the stone, the thick grey line looks bad, has to look like a stone everyone would love to buy, better veining" → "there should be some sort of dark transparent cover over it so the sections stand out more" → "the background shouldn't be too complicated so it doesn't distract" → **"background needs to be even darker"** → **"I also don't like the thick gold veining"**. ⛔ The last two are the binding ones: no thick vein of ANY colour, and darker than the old floor. §4l |
| D42 | 9 Aug | **The footer is cut to MAIN PAGES: two link columns, not five** | Client: "completely clean up the footer, it's way too big… you won't have to list everything out, you'll have those main pages. Services counts as one page." 931px → 411px, 31 links → 17. ⚠️ Materials, Guides and Areas each keep ONE hub link — read §4k before trimming further. §4k |
| D41 | 9 Aug | **The FAQ and the final CTA lose their dead vertical space** | Client: "move Answers set in stone closer up to the divider, there's too much space… and adjust the space on the final CTA as well." The FAQ was pinned to 100vh with its content centred, so 366px of a 900px box was empty. §4a |
| D40 | 9 Aug | ⛔ **NEVER a gold line across the top of a card or a section — anywhere, as a general rule.** The Why tiles take the review cards' black marble instead | Client, unprompted and general: "don't ever use that fingernail design on top of sections… give the cards the same black stone marble you have in the review cards, but faded so you can read the text." **This is now §2 rule 10.** §4i |
| D39 | 9 Aug | **The three owners are the TOP band of the About collage, the work photographs below** | Client: "the three owners must be on the top of the collage and then the team below." Reverses the order built earlier the same day. §4a |
| D38 | 9 Aug | **The About copy is bound to the collage's edges** | Client: "the title has to be in line with the top of the collage, and Chat with TopCat has to be aligned with the bottom." §4a |
| D37 | 9 Aug | **The nav bar's flash is 0.76s, not 0.58s** | Client: "slightly, slightly slower." §4h |
| D36 | 9 Aug | **The final CTA drops the "Call us" ghost button; the number is underlined in the left column instead** | **Partially reverses D30.** Client: "remove the call us button with the number and just underline the number on the reach out to us directly, and then everything just moves up with the what happens next, that's perfectly fine, we just need to balance out that section so there's not so much empty space." The "what happens next" strip STAYS. §4g |
| D35 | 9 Aug | **The Why reason tiles are designed, not grey blocks, and the feature slot takes a placeholder photograph** | Client: "we need to add some sort of design into the more reasons to choose us, we can't just have it grey blocks… where it says feature image, just add any image in there now so we can have it as a placeholder." §4i |
| D34 | 9 Aug | **The About team moves INSIDE the collage as three equal tiles; the copy is cut and gains a "Chat with TopCat" CTA** | **Supersedes the team-row placement in D11.** Client: "instead of having the people below with their names, we'll have the team members actually be inside the collage with their name and their role there, so three of those images can be the same size… and then on the other side it'll just be about us text and then a chat CTA, chat with TopCat." §4a |
| D33 | 9 Aug | **The nav bar's hairline FLASHES from the centre outwards as it forms** | Client: "the same thing as that divider where the shine goes across as you scroll, but start in the middle and go to the two sides quite quickly." §4h |
| D32 | 9 Aug | **"Let's bring your vision to life" under the reviews is a CTA** | Client: "a button below, or underline it and an arrow to make it clickable, or just something to make that a CTA." Built as an underlined gold link with an arrow, not a button. §4j |
| D31 | 7 Aug | **The helix cards' reverse is a REAL generated slab, not black marble** | Client, over three passes: "slightly too dark, make it a lighter card, some grey or faded white that fits the brand, I want those to stand out a bit more" → "let's not make it pure white" → "give it a stone design". It runs through the site's own `marble()` engine, preset calacatta, a different seed per card. §4e |
| D30 | 7 Aug | **The enquiry form gains a "what happens next" strip** | ⚠️ **HALF of this was reversed on 9 Aug by D36** — the "Call us" ghost button it also added is gone. The strip stays and is the thing filling the column. Original client wording: "add the call us directly button back below the send my enquiry, or maybe request a call, whatever you think is best, otherwise optimise that empty space". §4g |
| D29 | 7 Aug | **The FAQ is a grouped CONTENTS PAGE, four categories of three** | **Supersedes the FAQ half of D11.** Client: "now that we have so many questions we need to redesign that whole section", then "three in each category, either three or four, so it all looks even". §4a |
| D28 | 7 Aug | **The helix arrival is ~1.9s, not 1.16s** | Client: "a little bit too fast, it finishes before the user even reaches the end". Threshold also raised so it starts later. §4e |
| D27 | 7 Aug | **The services helix BUILDS as you arrive, one card at a time from the top** | Client: "form one by one from the top card, so you see the back, then the next one and the next one, almost animate in one by one semi quickly." Spatial order top → bottom. Pacing later slowed by D28. §4e |
| D26 | 7 Aug | **The Why mosaic is tied to the VIEW, not the scroll** | **Partially reverses D11.** Client: "make sure it always fully animates in and is not tied to the scroll, it's just tied to the view." ⚠️ About is still scroll-tied, deliberately. §4a and §4e |
| D25 | 7 Aug | **An HTML sitemap at `/sitemap.html`, linked from every footer** | Client's words: so the SEO build can be explored when needed. Generated, never hand-written. §4f |
| D24 | 7 Aug | **Track every decision and every reversal in this handover, and keep all SEO in the docs** | This section exists because of it |
| D23 | 7 Aug | **Kitchens lead, but the site is not kitchens-only** | Kitchens get the most focus and lead every title and H1. Bathrooms, vanity tops, splashbacks, outdoor, commercial, fireplaces and tables appear on every material and location page via the "Not only kitchens" block. Supersedes D22. |
| D21 | 7 Aug | **Fabrication is OUTSOURCED. Never claim in-house.** | **Reverses D6.** Client: "we don't do in house fabrication, do not talk about in house fabrication." See §2.1 and §4d. |
| D20 | 7 Aug | **Build the SEO content: 5 material, 9 guide, 9 location pages** | §4d and the SEO log |
| D19 | 7 Aug | **Porcelain is offered again, bespoke and enquiry-led only** | **Reverses D14.** Estimator tab is POA with `noCat:true`, plus a material page. Dekton and Neolith back in the brand strip. |
| D18 | 7 Aug | **Trade returns to the primary nav, as a dedicated page** | **Reverses D9.** B2B is the client's stated first priority. |
| D17 | 7 Aug | **The hero carries the place as a REGION, not a county list** | "across London and the Home Counties" |
| D16 | 7 Aug | **No hero eyebrow at all** | Client rejected even the shortened four-county version: naming four counties reads as a limit, not a promise |
| D15 | 6 Aug | **Estimator priced from the client's bracket table, marble and granite POA** | §6 |
| D13 | 6 Aug | **The 52-stone catalogue is the client's real range** | Not placeholder data. §5 |
| D12 | 6 Aug | **Estimator moved to sit directly after the stone showroom** | Pick the stone, then price it |
| D11 | 6 Aug | **About, Why, FAQ and contact redesigned** | FAQ approved as built. §4a |
| D10 | 1 Aug | **Nav reduced to six links, "Get a quote" promoted** | Trade later re-added by D18, so it is seven now |
| D8 | Jul | **Suppliers are never named publicly** | Nile Stone and Next Stone Slabs stay in the data, never rendered |
| D7 | Jul | **Never show the review count (~16)** | 5.0 rating only, no `aggregateRating` anywhere |
| D5 | Jul | **No physical showroom, ever** | Samples come to the customer, slabs approved from photographs |
| D4 | Jul | **Never signal a young or new company** | No founding year |
| D3 | Jul | **Value, not cheap** | No discount language. The supplier-discount claim stays off the site, see D2 |
| D2 | Jul | **"Best supplier discounts in the country" is INTERNAL ONLY** | Unverifiable and it argues on price. The client has re-sent it twice and it has stayed off both times |
| D1 | Jul | **Voice: quietly confident master** | British English, commas not em dashes, no exclamation marks |

### Reversed and superseded, kept for the record

| # | Date | Decision | What happened |
|---|---|---|---|
| D43c | 9 Aug | Page floor as Portoro with a bold gold vein network | ⛔ **QUIETENED same day.** Client: "the background shouldn't be too complicated so it doesn't distract", then "I also don't like the thick gold veining", then "needs to be even darker". The stone is right; the drama was not. Now hairlines only, on a darker veil. |
| D43b | 9 Aug | Page floor as a plain dark marble with one sweeping grey vein | ⛔ **REJECTED same day.** Client: "I don't like the stone, the thick grey line looks bad." ⚠️ The lesson generalises and was then confirmed a second time on the gold version: **a thick vein reads as a CRACK, not as a desirable slab, whatever colour it is.** Hairlines only. Applies to any stone imagery added anywhere on this site. |
| D43a | 9 Aug | Page floor as a hand-built SVG of blurred strokes | ⛔ **REPLACED by D43.** It was six `feGaussianBlur stdDeviation="52"` strokes stretched over the viewport, and it measured only 10 luminance levels wide with no detail above the blur — hence "paintbrush strokes… low quality and blurry". ⚠️ Several rounds were spent trying to rebuild it procedurally with `feTurbulence`; the tone could be matched exactly but not the *quality*, which is why it ended up a photograph. Do not re-attempt the procedural route without reading §4l first. |
| D35a | 9 Aug | The Why tiles dressed with a gold hairline seam across their top edge | ⛔ **REVERSED the same day by D40.** ⚠️ **This should never have been built.** "No bright band across the top of anything — that was tried and rejected outright" was ALREADY in the handover, in §6.5, about the estimator. It was read as an estimator note rather than a site rule, and the client had to reject the same idea twice. It is §2 rule 10 now, stated generally, so the next person does not have to infer it from a section they may never read. |
| D34a | 9 Aug | The three directors as the BOTTOM band of the About collage | ⛔ **REVERSED the same day by D39.** Built bottom-first that morning on the reasoning that the flip build should land on the people last; the client wanted them leading. ⚠️ Flipping the band means flipping the DOM too — DOM order IS build order (§4a). |
| D30a | 7 Aug | The enquiry form's second way in: a "Call us on 0800 098 2812" ghost button under the submit | ⛔ **REVERSED 9 Aug by D36.** It duplicated the number already in the left column, and the client's call was to carry it once, underlined, where it already lives. ⚠️ Note the shape of this: the button was added on 7 Aug *because the client asked for a call option there*, and removed on 9 Aug *by the same client seeing it built*. The requirement it answered — a second way in that is not the form — is now served by the underlined number. Do not re-add the button; if the need comes back, the conversation is about the left column. |
| D11c | 6 Aug | The team as a credit strip UNDER the story in the left column, "the people who look after your job" | ⛔ **SUPERSEDED 9 Aug by D34.** The people are inside the collage now. Client's read was that the collage was the half of the section that worked and the text side was the half that did not, so the people moved into the good half rather than sitting beside it. The rest of D11 stands. This also retired the `@media(max-height:840px)` block's "do not delete" note, which existed only because copy and people were in series in one column. |
| D11b | 6 Aug | The FAQ as a rail of questions with the answer panel beside it — "that's perfectly fine" | ⛔ **SUPERSEDED 7 Aug by D29.** It was approved at EIGHT questions and it worked there. Twelve broke it: the rail went to two columns, every row wrapped to two or three lines, and it read as a wall of text next to a mostly empty panel. **The approval was of a design at a size, not of the design forever.** The rest of D11 stands. |
| D11a | 6 Aug | The Why mosaic builds on SCROLL POSITION, same engine as About | ⛔ **REVERSED 7 Aug by D26.** Not a change of taste: the scroll-driven build could never finish on a screen tall enough to frame the whole section, so the last tiles sat permanently dim. The one-by-one motion the client asked for on 6 Aug is unchanged, only its clock. **The rest of D11 stands.** |
| D14 | 6 Aug | Porcelain NOT offered at all, hard rule, owner by phone | ⛔ **REVERSED 7 Aug by D19.** Note this had already reversed a July "we do it but don't advertise it" position. Porcelain has now flipped three times, so get it in writing before changing it again. |
| D22 | 7 Aug | Location URLs at `/kitchen-worktops/` | ⛔ **SUPERSEDED same day by D23.** Renamed to `/worktops/` because the business is not kitchens-only. No live URLs existed, so no redirects were needed. Renaming again after go-live would need 301s. |
| D9 | 1 Aug | Trade removed from the nav | ⛔ **REVERSED 7 Aug by D18** |
| D6 | Jul | Present fabrication as fully in-house | ⛔ **REVERSED 7 Aug by D21.** This one had itself overridden an earlier "never claim in-house" position, and the ASA risk was flagged and accepted at the time. Now settled the honest way. |

### Still waiting on the client

| Question | Raised | Status |
|---|---|---|
| ⭐ **Confirm the Bloom account, and that TopCat's Mirror range is Bloom's** | 11 Aug | ⭐ **Largely answered: the six Mirror stones are LIVE (D89)** — White, Cream, Grey, Brown, Blue and Black Mirror, from Bloom Stones London's own product pages under their own names. ⚠️ Two things still need Nick: **confirm the Bloom trade account** (same open licensing question as Caesarstone and CRL, §2a), and confirm these are the slabs TopCat actually sell as their best sellers. ⛔ Do not re-run the supplier search — it is finished and recorded in D89.
| ⭐ **Should Quartzite be a fourth range on the site?** 26 stones sold as Marble are quartzite, and one is travertine | 10 Aug | **Answered enough to ship, still open in principle.** The range is now labelled **Marble & Quartzite** and every stone names its own rock (D66), so nothing on the site misdescribes anything and the question is no longer urgent. A genuine fourth tab still costs nothing in price (both POA) but puts a fourth range on a site TopCat present as quartz/marble/granite. ⭐ **The groundwork is done** — `mat` is the key, the labels are two constants, and `kind` is already emitted per stone — so splitting it later is a small change. **TopCat's call. Worth asking Nick whether his customers ask for quartzite by name.** See D65, D66 |
| ⭐ **Upscale the ten CRL tiles, and harvest the four suppliers never touched** | 10 Aug | **Open, and both are small jobs.** The 14 dark quartz are live (D75), but CRL's ten ship at **625px** against the range's 1600, because 1280x625 is the largest frame CRL publish. They are sharp, just small, and want an upscale pass at about **6 credits** (four stones a job). ⛔ Separately, **four of the client's nine suppliers have never been harvested at all** — AKG, Bloom, Classic Quartz and **Cosentino/Silestone**. Silestone alone is likely to carry more dark quartz than everything added so far. See §2a for the list and the open licensing question on Caesarstone and CRL. |
| ⭐ **Should the range take on DARK QUARTZ? The suppliers list it and TopCat do not show it** | 10 Aug | **Asked by the client, answered with data, needs his decision.** He is right: **48 of the 50 quartz are light, 96%**, against 33% dark in marble and 55% in granite, and there is no black quartz at all. It is **not a supply fact** — the licensed suppliers publish at least six dark or mid quartz TopCat do not list, and two already **passed the pipeline's own gate**: ⭐ **Nero Starlight** (a near-black sparkle, 762px, would need upscaling) and **Grigio Shimmer** (a plain warm grey, 2130px, the best source of the lot, no upscale needed). **Marquina Shimmer** is a genuine black with white veining, rejected only for softness (2.3). Also available: Urban Concrete, Misterio Grey, and Calacatta Black Fusion (white with dramatic black veining, not a dark stone). ⚠️ **The cause is D54's cap**: quartz filled its 50 on Calacattas and whites, and the dark ones were never reached. ⛔ **Not added unilaterally** — the catalogue is what TopCat SELL (D13), not what the suppliers list, so adding needs Nick to confirm he can supply them. |
| **Read the 115 new stone descriptions.** They are colour and pattern only and were written from the tiles, but no human at TopCat has read them | 10 Aug | Unanswered. This replaces the old "63 carry generated copy" item, which is now resolved |
| Do the suppliers have better frames for Aqua Gucci, Verde Alpi and Arabescato Classico? | 10 Aug | Unanswered. All three faults are in the ORIGINAL photograph. Arabescato Classico's is sunlight on an installed worktop and no crop removes it entirely |
| Demote the hero's "Request a call" to a text link or the phone number | 4 times | Unanswered. Two co-equal buttons split intent, this is the biggest remaining hero gain. |
| Is the 1-slab-with-island bracket deliberate? | 6 Aug | Unanswered |
| Any brackets for vanity tops, fireplaces and tables? | 6 Aug | Unanswered, blocks §6.6 |
| Should 20mm vs 30mm change the price? | 6 Aug | Unanswered. It currently does not. |
| A 3-slab job: sales notes say £3k–£3.2k, the estimator says £3,850–£4,300 | 7 Aug | Probably VAT-inclusive vs ex-VAT. Two-minute check with Nick. |


---

## 0. START HERE

```bash
node "Website Demo/dev-server.js"      # → http://localhost:5501
```

**⚠️ There is no git.** This repo is a GitHub ZIP download, not a clone. Take a dated
`index.html.pre-*.bak` before any large edit — those backups are the only version control there is.

### Where the project is

**The site is now 35 indexable pages and ~33,400 words**, up from 9 pages. The desktop design is
essentially complete and the SEO content layer is built (§4d), with an HTML sitemap over the top
of it (§4f). What is missing is not design, it is **plumbing and photography**.

### The two things that actually block go-live

1. ⭐⭐ **The enquiry form has no backend, and it carries file uploads.** `buildEnquiry()` in the
   CTA IIFE assembles everything into a `FormData` and has nowhere to POST. This is bigger than it
   looks: the client was burned by a previous agency, their live site produced **one client in
   nine months**, and they will judge this engagement on **measurable leads**. There is currently
   nothing to measure. Netlify Forms with uploads is the obvious fit. §7.0.
2. **Real photography — ⭐ THE STONES ARE DONE, and as of the copy round their DESCRIPTIONS
   are too.** All **115 stones** carry a real supplier photograph and a description written from
   it. What is still invented is the **eight gallery projects** and the **four people/feature
   slots**, which are dashed placeholders. Nobody commits to a £3,000 surface off a generated
   pattern, and that argument now applies only to the project gallery. §7.3 and §7.5.

Behind those, **the estimator's product-type selector** (§6.6) is the last significant build job,
and it is blocked on Nick supplying brackets for anything other than kitchens.

### Verified at the end of the 9–10 Aug session (slab photography)

**52 of 52 stones carry a real supplier photograph**, up from 6 (⚠️ the range grew to **115** later that day, D54/D55b). Full reasoning in
`HANDOVER-2026-08-10-slab-photography-complete.md`; operational summary in
`HANDOVER-2026-08-10-photography-start-here.md`. Decisions **D44–D48**.

The headline is not that photographs were found. It is that **the images were already on disk and
the pipeline was throwing them away** — six collection bugs, five of them the same mistake: *a
measurement that condemns a stone for being what it is*. The scene gate rejected every black stone
because it counted deep-shadow pixels; the exposure gate rejected every pale plain stone for being
pale and plain. Of the 60 images the first gate refused, **49 were flat slab scans**.

⚠️ **Two wrong images were already live and were found by the guards built this session** — the
natural marble Calacatta Gold Oro was wearing Next Stone's *engineered quartz* "Calacatta Gold" via
an alias inherited as "confirmed by eye", and Carrara Shimmer was showing the wrong maker's product.
`match.py` now refuses both classes structurally.

**Not seen by the client yet.** Backup: `index.html.pre-slabs.bak`.

### Verified at the end of the 9 Aug session

**Thirteen changes over three rounds**, all client-asked (D32–D43, plus a re-confirmation of the
FAQ).
**None has been seen by the client in its finished state.** Backups, one per round:
`index.html.pre-about-rebuild.bak`, `index.html.pre-footer-cleanup.bak` and
`index.html.pre-background.bak`, plus a `.pre-footer.bak` beside each of the four builders.

⚠️ **The client DID steer the page floor live, over six messages, without seeing where it landed**
(§4l). Show that one first — every other change sits on top of it.

**Measured, not eyeballed.** Every section inside one screen at all five test sizes, and the page
got substantially shorter — four sections lost dead space rather than content:

| | 1366×610 | 1200×655 | 1512×824 | 1440×900 | 1920×1080 |
|---|---|---|---|---|---|
| About | 444 | 487 | 665 | 745 | 766 |
| Why | 489 | 520 | 655 | 709 | 827 |
| FAQ | 473 | 479 | 582 | 601 | 670 |
| Final CTA | 499 | 514 | 583 | 622 | 655 |
| Footer | 379 | 403 | 401 | 411 | 435 |

Where that came from, at 1440×900: the **FAQ 900 → 601** (it was pinned to 100vh with its content
centred, §4a), the **footer 931 → 411** (§4k), and the final CTA 646 → 622. The FAQ and the footer
were both *taller than the viewport* before.

**The About alignment is exact**: the title's top and the CTA's bottom each land within 0px of the
collage's edges at all five sizes (§4a). The three director names share a baseline at every size.
The CTA's two columns finish level at every size.

**The page floor** (§4l) measures median 9 / p90 12 / max 24 against the old floor's 13 / 16 / 19,
so it is darker than the site has ever had; measured thick-vein content is **0.0000%**; and all
five things that draw on `--marbleBG` were checked individually, including the scrolled nav glass
and the CTA card, which take the stone **without** the veil.

Inline JS parses (`node --check`), every `ld+json` block parses on the landing page **and** on a
sample of generated pages, **zero JS errors** from an instrumented copy walked top to bottom and
back with a FAQ question clicked, the upload disclosure toggled and the nav bar's `.scrolled` state
cycled. No horizontal scroll at 1920 / 1512 / 1440 / 1366 / 1200 / 375. Estimator still opens at
£2,000 – £2,500, FAQ still 12 rows. The About collage's `scrollSequence` still reaches its settled
state on all six tiles after the band swap, and all six tiles' images load.

**Sitewide after the footer cut and four builder re-runs: 3,393 internal links, 0 broken, 0
orphaned pages** across 89 non-V2 pages. All four go-live compliance scans clean — the only hits
are the two long-documented ones (the code comment recording the FAQPage removal, and the `sup:`
field in the stone data, which is never rendered).

### Verified at the end of the 7 Aug session

**4,185 internal links checked, 0 broken, 0 orphaned pages** (was 3,975 before the sitemap).
JSON-LD valid across all non-V2 pages. Inline JS parses (`node --check`). Zero JS errors captured
by an instrumented copy walked from top to bottom of the landing page with every rAF path pumped.
No horizontal scroll at 375px on every page type including the sitemap and the wide comparison
tables. All routes 200 including `/sitemap.html`. Estimator still opens at £2,000 – £2,500 with
four material tabs, FAQ still 12 rows, footer 6 columns, legal bar now four links.
Compliance scans clean under the **corrected** V2 filter (§2 rule 1): the only hits are the two
documented ones, a code comment in `index.html` recording the FAQPage removal, and the `sup:`
field in the stone data, which is never rendered.

**The builds, measured rather than eyeballed** (§4a, §4e): the Why mosaic reaches opacity 1.00 on
all six tiles at 1440×900, 1920×1080 and 1366×610, from the scroll position where the old engine
stalled the last tile at 0.82. The services helix arrives top-first over ~1.9s and its seated
state is byte-identical to before the entrance was added. All six card backs carry a generated
slab.

**The FAQ redesign** (§4a): four even columns of three; the plate holds ONE height across all
twelve answers at every size tested (1440×900 → 196px, 1366×610 → 164px, 1200×655 → 164px,
1512×824 → 199px, 1920×1080 → 216px, 900×900 → 176px) with no answer overflowing; Tab walks the
twelve, arrows step column-wise and Left/Right jump groups; the plate moves inline under the
picked question at one column.

**The enquiry form** (§4g): both columns finish level, and the section is 582px at 1366×610 after
the clamp-floor fix, against 647px before it.

Hero, About, Why, the FAQ and the contact card each still inside one screen at 1366×610,
1200×655, 1512×824, 1440×900 and 1920×1080.


## 1. What this is

A marketing site demo for **TopCat Worktops Ltd**, a real UK stone-worktop company (quartz, marble,
granite) serving **London, Hertfordshire, Essex and Berkshire**. They run paid ads from month one,
so everything has to convert and give a buyer no reason to hesitate.

| | Path | What it is |
|---|---|---|
| **V1** | `Website Demo/index.html` | The live single page, ~7,700 lines, one self-contained file with inline CSS and JS. **All active work happens here.** |
| ~~**V2**~~ | ⛔ **REMOVED 10 Aug 2026 (D87)** | The ~70-page rebuild, the `versions.html` chooser and `index-v2.html` are out of the site, moved to `.removed-2026-08-10-v2/` outside `Website Demo/`. The client asked for it gone completely. ⚠️ There is now only one version, so "non-V2 pages" in older measurements just means "pages". |

Generated sub-sites under V1: `services/` (6 pages), `stones/` (52 stone pages + collection),
`trade/`. Each has its own `build_*.py` — **edit the builder, never the generated HTML**.

---

## 2. ⭐ STANDING RULES — client decisions, do not break these

Also in `memory/topcat-copy-constraints.md`, loaded every session.

1. ⛔⛔ **REVERSED AGAIN 14 Aug 2026 (D202). FABRICATION IS IN-HOUSE, AND THE WORDING IS HIS:
   "by our experienced fabricators".** He claimed it in FIVE separate places in his own notes
   document, including an HSE dust-control answer, so it is a position and not a slip.
   ⛔ **THE SCAN BELOW IS OBSOLETE AND NOW RETURNS FOUR FILES BY DESIGN.** Do not "fix" them.
   ⚠️ **This has now flipped three times — July in-house, 7 Aug outsourced, 14 Aug in-house —
   so read D202 before writing a single sentence about who cuts the stone.** Templating and
   fitting were always theirs and were never in question. The paragraphs below are the 7 Aug
   position, KEPT because they explain what the site used to say and why every page was
   rewritten once already.

   *The superseded 7 Aug rule read:*
   ⛔ **Fabrication is OUTSOURCED and the site must NEVER claim in-house.**
   The client's decision, resolving a conflict open for several sessions: "we don't do in house
   fabrication, do not talk about in house fabrication." TopCat advise, source the slab,
   laser-template, place the cut with long-standing fabrication workshops, fit, and carry the ten
   year guarantee. **Templating, fitting and aftercare ARE theirs and may be claimed freely** —
   "fitted by our own team" is true and was deliberately kept. Cutting and polishing are not. The
   positioning that replaced it is *one contract, one contact, and we answer for all of it*, which
   is what buyers actually value and is entirely true.
   Scan before go-live, must return nothing:
   `grep -ril "our own workshop\|we cut\|in-house fabrication" --include="*.html" . | grep -Ev "(^|/)v2/"`
   ⚠️ **That filter was `grep -v "^./v2/"` until 7 Aug 2026 and it silently did nothing.** BSD
   grep on macOS prints `v2/faq.html`, not `./v2/faq.html`, so the anchored pattern never
   matched and every scan came back full of dormant V2 hits. Same fix applied to all the scans
   in the SEO log §8. If you copy a scan from an older doc, check its filter first.
   ⭐ **This risk is closed as of 10 Aug 2026 (D87): V2 has been removed from the site entirely.**
   It was never rewritten and still carries the old in-house-fabrication claims, which is why it
   was one revival away from publishing them. It now sits outside `Website Demo/` and cannot be
   deployed. ⛔ If it is ever brought back, rewrite those claims BEFORE it goes anywhere near a
   server. The scans' `v2/` filters are now redundant but harmless.
   Full detail in `HANDOVER-2026-08-07-seo-build.md` §1.
2. **No physical showroom, ever.** "Showroom" means the online experience. In person means a free
   home visit with samples; slab approval is from photographs of the customer's actual slab.
3. **Never signal young or new.** No founding year, no "est. 2024". Lead on craft and experience.
4. **Never show the review count** (~16). Show the 5.0 rating and the testimonials. No
   `reviewCount` or `aggregateRating` in schema anywhere.
   ⚠️ **This was being broken.** The landing page JSON-LD carried `aggregateRating` with
   `"reviewCount": "16"`, publishing the count to Google. Removed 7 Aug 2026. It produced
   nothing anyway, Google makes self-reviewed star markup ineligible. Re-scan before go-live.
5. **Value, not cheap.** No discount language. ⚠️ This is the rule that keeps biting — see §4b for
   the supplier-discount claim the client keeps sending and why it stays off the page.
6. **Voice:** quietly confident master. Plain, exact, warm, British English. **No exclamation
   marks, commas not em dashes.** No AI-tell words.
7. **Service area — EIGHT COUNTIES since 14 Aug 2026 (D203):** London, Hertfordshire, Essex,
   Berkshire, Buckinghamshire, Surrey, Oxfordshire and Bedfordshire, plus nationwide
   templating. His own list. It lives in the footer of all 150 pages, the FAQ, the schema
   `areaServed`, `/worktops/`, and the `AREA`/`AREAS_SERVED` constants in all four builders.
   ⚠️ **The hero says "across England and the British Isles" and the gallery says "across
   England" — also his wording, and deliberately broader than the visit area.** ⛔ The enquiry
   card says "across our service area" and NOT "England", because that card promises a free
   home VISIT and the broad phrase would promise a visit we cannot always make.
   ⚠️ **The four county landing pages are still London / Hertfordshire / Essex / Berkshire
   only** — there are no pages for the four new counties yet.
   *The superseded rule read:* London, Hertfordshire, Essex and Berkshire, plus nationwide templating.
   ⚠️ **But do not print the county list as a hero strip** — see §4b, the client rejected exactly
   that because four named counties reads as a limit rather than a promise.
8. **PORCELAIN — status reversed twice, current position (client, 7 Aug 2026): OFFERED again,
   bespoke and enquiry-led only.** July: "we do it but don't advertise it". 6 Aug: banned outright
   by the owner and stripped from V1 and V2, Dekton and Neolith pulled from the brand strip.
   **7 Aug: the client reinstated it as a made-to-order, enquiry-led service (no calculator, PDF
   or photo upload).** So porcelain, Dekton and Neolith are back, and `/materials/porcelain-worktops.html`
   now exists. ⚠️ Do NOT invent porcelain slab names or add porcelain to the stone wheel, there is
   no supplied range, which is why the estimator tab carries `noCat:true`. Owner sign-off came via
   the client rather than a second call, so treat as confirmed but noted.
9. ⚠️⚠️ **HE CROSSED THIS RULE HIMSELF ON 14 Aug 2026 (D203) AND IT WAS BUILT AS ASKED.** His
   notes document says *"you need to add in Next Stone Slabs and remove CQ stone"*, so one of
   the two trade sources is now named in the public brand marquee and in the quartz page's
   brand sentence. **The rule below is otherwise unchanged and Nile Stone is still not named
   anywhere.** ⛔ If the rule is ever reinstated, the marquee entry and the quartz sentence are
   the two places to undo. ⚠️ "CQ Stone" came out of both, but the same maker is still recorded
   against twelve stone pages, where removing it would drop stones out of the 132 that
   `harvest/verify.py` gates on.

   ⛔ **The suppliers' names never appear publicly.** Nile Stone and Next Stone Slabs are the
   client's own trade sources; printing them hands a competitor his buying list. `sup` stays in the
   data, but every public surface shows **finish** instead.
10. ⛔ **NEVER a bright or gold line across the TOP of a card or a section.** Client's own words,
    9 Aug 2026, given as a general rule: *"Don't ever use that fingernail design on top of
    sections."* No top hairline, no lit seam, no bright band, on any card, tile, panel or section
    edge, anywhere on the site.
    ⚠️ **This was already the rule and it still got broken.** It lived in §6.5 as an estimator
    note — "⛔ No bright band across the top of anything, that was tried and rejected outright" —
    and on 9 Aug a gold seam was added to the Why tiles anyway, because a rule filed under one
    section reads as being about that section. The client rejected the same idea a second time.
    **It is stated here, generally, so nobody has to infer it from a section they may never read.**
    What to use instead when a card needs to stop looking flat: the site's own stone
    (`marbleSVG()`) veiled back, which is what the review cards and now the Why tiles do (§4i).
11. ⛔ **EVERY MEASUREMENT ON THE SITE IS IN MILLIMETRES.** Client, 10 Aug 2026: "every slab has
    to show in millimetres, every measurement has to be in millimetres." ⚠️ The one deliberate
    exception is the estimator's **linear metres** of edge profile, which is a PRICING unit.
    Scan before go-live, must return nothing outside `v2/`:
    `grep -rlE "[0-9]+ ?cm\b" --include="*.html" . | grep -Ev "(^|/)v2/"`
12. ⛔ **NEVER STATE SOMETHING WE CANNOT GUARANTEE, AND NEVER USE AN ABSOLUTE.** Client, 10 Aug
    2026, on finding "the pattern is consistent across the slab" live: "You cannot say that
    something is consistent when it's not. Don't say things that you cannot guarantee. Be more
    vague." ⚠️ **Comparatives are safe, absolutes are not** — "varies less than quarried stone"
    is defensible, "consistent" is not. Enforced by `harvest/verify.py` check 7, which fails the
    build. It had 355 hits when it was written. ⚠️ This is the same family as rule 1: "we
    vein-match every joint by hand" was BOTH an unkeepable promise AND a claim to fabrication.
13. ⛔ **A STONE IS ALWAYS CALLED WHAT IT IS. THE RANGE AROUND IT IS NAMED FOR WHAT IT CONTAINS.**
    Client, 10 Aug 2026: "on the collection page it shows marble, but on the actual page it says
    quartzite… we cannot have that confusion." ⚠️ **The fix is never to soften the stone's name
    to match a category.** 26 of the marble range are quartzite and one is travertine; calling
    any of them marble is a misdescription, and it attaches marble's care copy — etches with
    acid, softer — to a stone that does neither. The range is labelled **Marble & Quartzite**
    instead: `RANGE_LABEL` in `stones/build_stones.py` and `MAT_LABEL` in `index.html`, and
    those two constants are the only place the wording lives.
    ⛔ **`mat` is the pricing key and is never changed to fix a wording problem.** §4p, D66.
14. ⛔ **THE LOGO IS THE CLIENT'S ARTWORK AND IS NEVER RE-DRAWN.** Client, 10 Aug 2026, on the
    version that had been live: *"the one we currently have on the site is very wrong."* The
    files live in `Website Demo/assets/brand/` and every surface references one of them.
    ⚠️ **The failure this replaces was not a bad drawing, it was a drawing at all** — the mark
    was hand-built inline SVG and the wordmark was live text, so it could never be more than an
    approximation and it could drift on a font fallback. If the logo needs to change, replace
    the file. ⛔ Never paste path data into a template, a builder or a `<head>`.
    ⛔ **Set HEIGHT only.** Width follows the intrinsic ratio; setting both squashes the lockup,
    and at nav size that reads as a subtly wrong logo rather than an obviously broken one.
    §4q, D69, and `assets/brand/README.md`.
15. ⛔ **ONE DEVICE AT A TIME. DESKTOP IS FROZEN AND ONLY THE CLIENT UNFREEZES IT.** Client,
    11 Aug 2026: "when I say let's do mobile, then we only work on mobile. Everything else stays
    exactly as it is on the other devices." Desktop design is closed as of D90; mobile is the
    current scope, tablet comes after, and the client says when each begins.
    ⛔ **A mobile change goes inside a width-scoped media query, never into a base rule.**
    `index.html` is one file with inline CSS, so almost every rule is unscoped and applies at
    every width — "fixing it on mobile" by editing the base rule silently changes the desktop
    the client has just frozen. ⚠️ Check which existing breakpoint governs before adding a new
    one, and prefer `max-width` blocks so desktop keeps the untouched base. ⚠️ A
    `max-width:900px` block catches TABLET too, which is not in scope while mobile is. D91.
16. ⛔ **NEVER "LASER" ANYTHING. THEY TEMPLATE BY HAND.** Client, 14 Aug 2026: *"please remove
    the word laser template from everything as we template everything by hand."* 21 occurrences
    were removed across 18 files, including the Step 3 heading on thirteen pages. Say
    **templated**, **measured** or **templated by hand**. ⚠️ His own document says
    "laser-measure" in a FAQ answer he pasted; that sentence predates the instruction and the
    instruction wins.

---

## 2a. ⭐ THE SUPPLIER LIST — the client's own, sent 10 Aug 2026

⚠️ **Keep this list here. The client sent it explicitly and asked that it live in the handover.**
It is the answer to "where does more range come from", and the reason the dark quartz exists.

| Supplier | URL | Status |
|---|---|---|
| **Nile Stone** | https://www.nilestone.co.uk/quartz-surfaces | ⭐ **Licensed.** TopCat buy here. 413 + 746 images harvested |
| **Next Stone Slabs** | https://nextstoneslabs.co.uk/quartz/ | ⭐ **Licensed.** TopCat buy here. 141 images harvested |
| **Caesarstone** | https://www.caesarstone.co.uk/ | ⚠️ 176 images harvested, **4 stones now live**. Account not confirmed |
| **CRL Stone** | https://crlstone.co.uk/ | ⚠️ 261 images harvested, **10 stones now live**. Account not confirmed |
| **Noble Stone UK** | https://noblestone.uk/ | 48 images harvested, all white marble-effect, **nothing dark** |
| **Cosentino / Silestone** | https://www.cosentino.com/en-gb/silestone/ | ⛔ **NEVER HARVESTED.** ⭐ The best remaining source of dark |
| **Classic Quartz** | https://www.classicquartzstone.com/ | ⛔ **NEVER HARVESTED** |
| **AKG Surfaces** | https://akgsurfaces.co.uk/ | ⛔ **NEVER HARVESTED** (empty folder exists) |
| **Bloom Stones London** | https://www.bloomstoneslondon.com/ | ⛔ **NEVER HARVESTED** (empty folder exists) |
| **Fugen Stone** | https://fugenstone.co.uk/ | ⛔ **NEVER HARVESTED** (empty folder exists) |

⛔ **FOUR OF THESE HAVE NEVER BEEN HARVESTED AT ALL** and that is where the next batch of range
comes from. It is a harvest run, not a research problem. **Silestone especially** is likely to
carry more dark quartz than everything currently added put together.

⚠️ **The licensing question is open on Caesarstone and CRL and must be closed before go-live.**
`LICENSING.md`'s test is that TopCat must **buy from** a source for its photography to be
defensible (D45). The client instructed these directly and supplied the links himself, so the
work proceeded — but confirm the accounts or get written permission. ⛔ This is a business risk,
not a code risk, and no scan will catch it. Suppliers are still never named publicly (D8).

---

## 3. Real business facts

- **Phone** 0800 098 2812 (`tel:+448000982812`), Mon–Fri 8am–6pm. **Email**
  info@topcatworktops.co.uk (NOT hello@). ✅ The last `hello@`, in the homepage JSON-LD, was fixed
  7 Aug 2026, along with `areaServed`, which had listed only Hertfordshire towns and now names the
  four counties.
- **No public address.** Home visits only.
- **Team:** Nick (Managing Director, front of house, naming him is approved), Rimsha (Operations
  Director), Ali (Sales Consultant). "Founded by three lifelong friends." All three are named on
  the page, in the About team row.
- **Guarantee** ten years. **Lead time** ~3–5 working days. Reviews are real, 5.0 on Google.
- **Tagline** "Surfaces worth building around" (the live site's price-led one was dropped).
- ⚠️ **Checkatrade rating is UNVERIFIED** and was removed. Confirm the real platform and figure
  before it ever returns.

### The owner's four differentiators, as he sent them

Worth having in full, because three of them are now the hero's three reasons (§4b):

1. **Customer service** — second to none.
2. **Supplier relationships** — "no one gets better discounts than us anywhere in the country".
   ⛔ **INTERNAL ONLY. Never on the site.** Unverifiable, and it argues on price, which breaks rule
   5. It surfaces as the number at quote stage instead.
3. **Above and beyond** — all cut-outs free (plug, sink, tap), pencil edging as standard so
   children are safe around sharp corners, and sink/drainer grooves as standard.
4. **Aftercare** — every kitchen is handcrafted so slight adjustments happen; others make you wait
   a month or more once they have been paid, and TopCat have never taken more than **72 hours**,
   completely free of charge.

---

## 4. Section order and the layout system (V1)

hero → reviews → services → gallery → **stones → brand strip → estimator** → process → about →
why → faq → cta → trade prompt → footer

⚠️ **The estimator moved on 6 Aug** to sit directly after the stone showroom: you pick your stone,
then you price it. Two consequences, both deliberate:
- The **brand strip stayed with the stones** and is now the stones → estimator boundary, not
  stones → process. It is stone content, so it belongs there. A plain `.section-divider` carries
  estimator → process.
- The old "estimator and Why read as one block, so no divider between them" rule is **dead** — the
  client said they are not one section. About → Why now takes that divider. Seven dividers.

**Layout rules that are load-bearing** (full detail in the archive §5a):

- **Everything is normal flow.** No pinning, no sticky, no runways, no cross-section overlays. The
  old hold/arrival choreography was stripped out and must not come back. The About and Why builds
  (§4a) are scroll-tied but stay entirely within this rule.
- **The clamp-floor rule.** Every vertical measure must keep shrinking on a short window. A
  `clamp()` whose floor is in px stops responding the moment the screen is short enough to hit it,
  which is exactly the screen with no height to spare. Pick floors small enough that the **vh term
  still governs at ~610px of viewport height**.
  - ⚠️ It applies to more than padding: a `max-width` that sets height (`.at-shot`) needs the same
    treatment, and **display type must be capped against vh as well as vw** —
    `clamp(24px,min(3.1vw,4.6vh),42px)`.
  - Violations were found by accident rather than by a sweep (`#cta` padding, `.cta-title`,
    `.cta-line`). **Assume there are more.** Check the floor whenever you touch a clamp. §7.8.
- **One full-bleed background** (`body::before`). No section gets its own marble. Marble on framed
  **cards** is fine, and that exception is what the estimator's stone wash and the FAQ answer panel
  rely on.
- **Every section fits one screen** (the estimator is the standing exception, §7.7). Test at
  **1366×610** and **1200×655**, not just 1440×900. ⚠️ **And at 1512×824** — the Why section's
  compression breakpoint was `max-height:820px`, so a MacBook at 824 fell straight through the gap
  and got tall-screen spacing on a screen with 7px to spare. It is 845 now.

---

## 4a. ⭐ About, Why, FAQ and the contact block — 6 Aug, with About rebuilt again 9 Aug

The client's brief, in their words: the About images should build **"flipping off each other"** as
you scroll; the Why reasons should come **"up one by one out of the abyss from the back and setting
up in the front"**; the team belongs in About, not in Why; the FAQ was **"very basic… in the middle
and text heavy, there's nothing interesting about it"**; and the contact block had **"too much
empty space on the left side, and the right side is too busy"**.

All four were reviewed once by the client. **About and Why were sent back** — the animation was too
quick to notice and the Why layout did not read — and the below are the corrected versions.
**The FAQ was approved as built** ("that's perfectly fine").

### `scrollSequence()` and `viewSequence()` — TWO engines, one contract

⚠️ **Updated 7 Aug 2026 (D26). They used to be one engine. Read this before moving a block
between them.**

Both take `(host, tiles, apply, opts)` and both call `apply(el, e, i)` with that tile's own eased
0→1, so a block can be swapped from one to the other without touching its `apply()`. The only
difference is **what drives `e`**:

| | `scrollSequence` | `viewSequence` |
|---|---|---|
| clock | scroll **position** | elapsed **time** |
| used by | **About** | **Why**, and the services helix has its own copy of the idea (§4e) |
| scrolling back up | unwinds | stays built |
| damping | yes, a playhead that TRAILS the wheel | not applicable |
| ⚠️ can it fail to finish? | **yes** — see below | no |

**`scrollSequence`** reads a progress figure off the block's own travel through the viewport,
gives each tile a staggered slice of it, and puts a **damped playhead** between the two so the
build TRAILS the scroll by a beat. The client asked for a slide lag, not a scrubber welded to the
wheel.

⚠️ **Its failure mode, which is what cost the Why mosaic.** Progress only reaches 1 once the
block's top has climbed to `END·vh`, near the top of the screen. If the section is built to fit
ONE screen, a visitor who frames the whole section, head and all, never scrolls the block that
high — and the taller their window, the sooner the section is framed and the worse it gets.
Measured at 1920×1080 with the Why section framed: the mosaic's top sat at 0.259vh against an
END of 0.10, progress reached 0.81, and the last tile finished at **0.82 opacity**. That is
exactly what the client screenshotted.

**About is unaffected and was checked, not assumed.** Its collage sits high in its own section, so
with the About section framed the collage top is at 0.12vh, progress reaches 0.89, and the last
tile's opacity ramp (`e*2.1`) saturates at 1. Verified at both 1080 and 610 viewport heights.
**So About keeps the scroll lag the client explicitly asked for. Do not "fix" it.**

**Before moving any block onto `scrollSequence`, do this sum:** measure the block's top as a
fraction of vh with its section framed, and check it is below `END`. If it is not, the build will
never finish and you want `viewSequence`.

**`viewSequence`** fires once from an IntersectionObserver at 0.2 of the block's own area, runs a
plain timeline, and disconnects. `dur` is one tile's own travel in ms, `gap` the ms between one
tile starting and the next. It does **not** replay on the way back up: "tied to the view" means it
arrives when you get there and then it is simply built.

- ⚠️ **It is normal flow.** No pin, no sticky, no runway, no overlay. This is not the choreography
  that was stripped out, and it is **not** the scroll-tied treatment the client killed in the
  PROCESS section — that was that section's whole centrepiece, and the standing rule that nothing
  in `#process` may be tied to scroll position still holds.
- ⚠️ **Tiles it drives must not also be `.rise`.** That class pins `opacity:0` until its own
  observer fires and fights the opacity written every frame. Both mosaics had `.rise` removed.
- `tick()` runs **once synchronously** at setup, or the tiles paint fully formed for one frame.
- The first read **snaps** rather than eases, so a page loaded below the section is already built.
- The rAF only runs while an IntersectionObserver says the block is near the screen.

**Pacing.** Slowed on the client's review ("delay that animation a little bit more, so it's
visually obvious as the user scrolls" — About; "just slight" — Why). Both pass explicit opts.
The two engines take different knobs:

| About — `scrollSequence` | | Why — `viewSequence` | |
|---|---|---|---|
| `start` → `end` | 0.94 → 0.02 (0.92vh of scroll) | `dur` — one tile's own travel | 620ms |
| `span` — one tile's own travel | 0.42 | `gap` — between one tile and the next | 175ms |
| `step` — gap between tiles | 0.112 | `threshold` — how much must be on screen | 0.2 |
| `scrub` — damping, lower is laggier | 0.062 | total for six tiles | ~1.5s |

- ⚠️ **About only: `5*step + span` must stay under 1**, or the sixth tile never finishes inside
  the block's travel and the mosaic is permanently one tile short. `viewSequence` has no
  equivalent bound, because time does not run out.
- ⚠️ **About only: don't push `start` far past 1.0** to slow it further. Progress starts when the
  block's top crosses `start·vh`, so a high `start` means the first tiles are most of the way
  built before the mosaic is on screen at all, which is the opposite of what was asked for.
  Lengthen the window at the `end` side instead.
- Opacity saturates before the motion does (About ramps `e*2.1`, Why uses `e²`), so a tile reading
  as solid may still be moving. **Judge the pace by eye, not by an opacity readout.**
- ⚠️ **You cannot judge either build from the Browser pane.** It runs with
  `document.visibilityState === 'hidden'`, so rAF is fully paused and a time-driven build simply
  never advances between tool calls. The method that works, and the one that verified this
  session: copy the page, insert a shim above `</head>` that replaces `requestAnimationFrame`
  with a queue plus a `window.__pump(t)` that drains it at a timestamp you choose, then step the
  timeline by hand and read the tiles after each step. Delete the copy afterwards. §8.

### About — ⭐ REBUILT 9 Aug (D34): the people are in the collage now

**Client:** *"We need to design that better. I just don't think the text side of the about us
section looks good, I like the images side, that looks good. Instead of having the people below
with their names, we'll have the team members actually be inside the collage with their name and
their role there. So three of those images can be the same size, and then the rest will be a
collage of the team, but they all need to fit within the collage. And then on the other side it'll
just be about us text and then a chat CTA, chat with TopCat."*

**The diagnosis matters more than the fix:** the client liked the collage and disliked the column
beside it. So the answer was not to redesign the collage, it was to move the good thing's territory
outward — the people went *into* the composition that was working, and the column that was not
working lost two thirds of its content.

- **The copy is two paragraphs, down from three** (~198 words to ~122). Nothing load-bearing came
  out: the D21-safe wording is intact ("place the cut with **workshops**", never in-house), and
  "Ask for Nick" now closes the second paragraph instead of owning a third.
- **The column ends on one gold CTA, "Chat with TopCat"**, left-aligned. Left, not centred: it is
  a column of prose and a centred button in it reads as a stray.
- ⭐ **The title and the CTA are bound to the collage's edges (D38).** "About us" sits exactly on
  the collage's top edge and "Chat with TopCat" exactly on its bottom edge, measured to 0px at
  every size. Three things do it together and all three are needed: `align-items:stretch` on the
  wrap, `.about-copy` as a flex **column**, and `margin-top:auto` on `.about-cta`. The collage is
  `align-self:start` so the row's top edge IS its top edge.
  - ⚠️ `.about-cta` also needs `display:flex`. `.rev-cta-primary` sets no display of its own, so
    as a bare inline `<a>` its 15px vertical padding spilled out of the line box and the button
    hung **12.5px below** the collage. Everywhere else these buttons already sit in a flex row,
    which is why it had never shown up before.
  - ⚠️ **If the copy ever grows taller than the collage, the bottom alignment breaks** (the top
    one survives). There is simply no slack left for `auto` to eat. Watch it if anyone lengthens
    this copy.
- **The collage is SIX ROWS, three columns**, `"p1 p2 p3" ×3 / "w1 w1 w2" / "w1 w1 w3" ×2`.
  ⚠️ **The three directors are the TOP band (D39), the work photographs below.** They were built
  the other way round earlier the same day and the client flipped them.
  **Flipping the band means flipping the DOM as well** — DOM order is build order — and the
  `HINGE` array is indexed by DOM position, so that had to be re-dealt too. Areas are named
  `p1..p3` / `w1..w3` rather than `a..f` precisely because letters stopped matching reading order
  the moment the bands swapped.
  The three directors are three **identical** 1:2 tiles. ⚠️ Equal is the point: three people at
  three sizes is a hierarchy nobody asked for. Below them the work — one big square, a short
  landscape, a portrait.
- **The portraits take a triptych hinge**: the left one swings off its left edge, the middle falls
  off its top, the right one swings off its right edge, so the row opens outward from the centre.
- ⚠️ **Still SIX tiles.** `scrollSequence`'s bound is `5*step + span < 1` and it is currently at
  0.98. A seventh tile breaks the build permanently, one tile short. See below.
- The tiles still hinge off their own **edges**, top to bottom, and the three portraits inherit the
  d/e/f hinges — left edge, top edge, right edge — which opens them outward like a triptych.
  `perspective:1250px` on `.about-collage` is what makes a rotation read as a panel swinging.
- **Three work photographs, not six**: handshake (big square), samples (short landscape), fitting
  (portrait). Read in order they are the job — we meet you, we help you choose, we fit it.
  ⚠️ The two that came out were **"One of our stone masons"** and **"Polishing an edge by hand"**,
  which were the last images on the landing page implying in-house fabrication after the D21
  rewrite. **Do not put them back.** Fitting is genuinely theirs and is safe to show.
- ⚠️ **The three portraits are PLACEHOLDERS and are deliberately not filled with stock faces.** A
  real name against someone else's photograph is a lie the page cannot afford. The plate is built
  at its finished size, so the shoot lands as an `<img>` dropped in above the `figcaption` and
  nothing else moves. Do not restyle the tile to suit the placeholder.
- ⚠️ **`.acp-role` reserves TWO LINES with `min-height`, used or not.** "Operations Director" wraps
  at every size; without the reservation Rimsha's plate grows a line taller than the other two and
  her name sits visibly above Nick's and Ali's. That is the same ragged-baseline fault the old
  credit strip had, just moved into the collage. Tracking is `0.12em`, not the 0.16em the other
  eyebrows use, for the same reason it always was.
- ⚠️ **Everything inside a portrait tile is sized off `--sideW`, never `vw`.** The collage is capped
  by the viewport's HEIGHT as well as its width, so on a wide short laptop the tiles are narrow
  while vw is large. Keyed to vw, the medallion came out **84px inside an 87px tile** at 1366×610.
  `--sideW` is the only measure that knows how big the tiles actually are. Same family of trap as
  the display-type-vs-vh rule in §4.
- **`.about-wrap` is ONE row again**, `"copy collage"`. The two-row grid, the `.about-team` block
  and every `.at-*` class are gone. `--sideW`'s height budget relaxed from `100vh - 250px` to
  `100vh - 210px`, which is what pays for the portraits being legible.
- ⚠️ **The `@media(max-height:840px)` block used to carry a "do not delete" and no longer does.**
  It was load-bearing for one specific reason — copy and people in series in one column — and that
  reason left with the credit strip. What remains of it is the copy-measure rule, which was always
  about the copy: 52ch is right on a tall screen and three wasted lines on a wide short one.

### Why — a tile mosaic that reads in rows

- ⚠️ **Driven by `viewSequence`, NOT `scrollSequence` (client, 7 Aug, D26).** The motion is
  unchanged; only the clock is. Putting it back on the scroll reintroduces a build that cannot
  finish on a screen tall enough to frame the section, which is the bug the client reported.
- The tall feature image and the bulleted list are **gone**. Tiles arrive from **depth**
  (`translateZ(-640px)` → 0) rather than flipping — same `apply()` shape as About, different body.
  ⚠️ Opacity is **e², not e**: a tile 500px back at half brightness reads as a flat card fading in,
  and holding it dark until it is nearly home is what makes the distance read.
- ⚠️ **THE LAYOUT WAS REJECTED ONCE ALREADY — read this before changing it again.** The first
  version was a 3×3 (`"p p a" / "p p b" / "c d e"`), with 01 and 02 stacked down the right and 03
  to 05 along the bottom. The client's words: *"having one and two stacked on top of each other and
  then three, four, and five from the side doesn't really look nice or work nicely."* It is now
  **twelve columns, two rows**, with the photo taking a three-column block down the **left** (About
  puts its images on the right, so this mirrors it) and the reasons running in plain rows beside it:
  ```
  "p p p a a a a a b b b b"     01 wide, 02 narrower
  "p p p c c c d d d e e e"     03, 04, 05 in equal thirds
  ```
  **The non-negotiable is the path: left to right, top to bottom.** Sizes may vary — that is what
  stops it reading as a plain card grid — but a reader must never have to work out where to look
  next. The client has now rejected two separate layouts on exactly that ground; the other was the
  process section's ashlar interlock, in the archive.
- The breakpoints keep the rows: 6 columns at ≤1000px (photo over the top, then 2-up, then 3-up),
  one column at ≤620px.
- **The photo is optional here.** The client said so directly, and the section would survive as
  five tiles in a 3+2 if the feature shot never arrives.
- The reasons carry **numerals 01–05**. Not a ranking, just the order. With the row layout they are
  belt and braces, so swapping them back for the old gold diamond is a one-line change.
- The section has a normal centred `.section-head`, so the **nav FIT map entry** is
  `['#why .section-head','#why .why-mosaic']`.
- Dead classes removed site-wide: `why-wrap`, `why-feature`, `why-body`, `why-lead`, `why-list`,
  `why-people`, `why-person`.

### ⭐ FAQ and final CTA — the dead vertical space, removed 9 Aug (D41)

**Client:** *"Move Answers set in stone closer up to the divider between More reasons to choose us
and it, because there's too much space. And adjust the space on the final CTA as well."*

**What it actually was.** `#faq#faq` carried `min-height:100vh` with `justify-content:center`, the
last trace of the sticky sheet the FAQ used to arrive on. At 1440×900 that put a **534px
composition inside a 900px box — 207px of empty space above it and 159px below**. The comment
above it described "a full-height marble panel with the slab stack centred in it", but the panel
had already gone: this section has no background of its own, because the one full-bleed marble on
`body::before` is the rule (§4). The height was buying literally nothing.

| | before | after |
|---|---|---|
| divider → FAQ title | 261px | 85px |
| FAQ section height | 900px (= exactly 100vh at every size) | 601px |
| FAQ foot → CTA card | 213px | 90px |

- ⚠️ Its `padding-top` was `clamp(88px,9vh,116px)`. At 610px of viewport 9vh is 55px, so the
  **88px floor won on exactly the screen with no room** — a clamp-floor violation of the kind
  §7.8 keeps predicting. Now `clamp(20px,3.4vh,52px)`, vh-governed all the way down.
- `#cta`'s padding came down with it, from `clamp(26px,6vh,86px)` to `clamp(18px,4.2vh,64px)`.
  Nothing was wrong with it in isolation; it looked loose next to a FAQ that had just lost 300px.
- ⚠️ **A section being exactly 100vh at every screen size is a smell, not a coincidence.** Three
  other sections still carry `min-height:100vh` — `#reviews`, `#services` and `#process`. Those
  three are genuinely full-screen compositions, so they are fine, but if any of them ever look
  top-heavy this is the first thing to measure.

### FAQ — a grouped contents page ⭐ REDESIGNED 7 Aug (D29, supersedes D11b)

**Client:** *"Now that we have so many questions, we need to redesign that whole section. It has
to be incredible, immaculate, fit with the rest of the design."* Then, on the first version:
*"Three in each category, either three or four, so it all looks even and nicely spaced."*

**What was wrong.** The previous design was approved at EIGHT questions and worked there. Twelve
broke it: the rail split into two columns, every row wrapped to two or three lines, and the
section read as a wall of text beside a panel that was mostly empty. **Nothing was wrong with
the old design except the number of questions**, which is worth remembering before treating any
approval as permanent.

**The three moves that fix it, in order of how much they matter:**

1. **The twelve are GROUPED into four named columns of three.** Four chunks of three is a set a
   reader can hold; twelve equals is not. This is the whole idea, the rest is detail.
   ⚠️ **The groups must stay EVEN** — the client asked for that explicitly after seeing a
   2/3/3/4 version, where the short first column read as a mistake. A thirteenth question means
   going to four rows in *every* group, not five in one.
2. **Each row shows a SHORT LABEL, not the question.** `What it costs`, not *How much does a
   stone worktop cost?* One line per row is what makes the eye run down a column instead of
   reading paragraphs. ⚠️ `.faq-qt` is `white-space:nowrap` with an ellipsis, deliberately —
   keep labels under about 22 characters. The full question is on the plate and is the button's
   `aria-label`, so nothing is lost.
3. **The plate moved from BESIDE the index to BENEATH it, full width.** A two-sentence answer in
   a tall narrow column is mostly air. Wide and shallow fits what is actually in it.

**The groups**, and why each question sits where it does:

| Group | Rows |
|---|---|
| Price and guarantee | What it costs · Hidden costs · Our guarantee |
| How it works | How long it takes · Who comes to your home · Where we work |
| Your stone | Choosing your stone · Matching your slab · Porcelain and sintered |
| Living with it | Seams and joints · Hot pans and heat · Silica and safety |

The aftercare question ("what happens if something is not right after fitting") sits under
**Price and guarantee** rather than Living with it. That is what balanced the groups to 3/3/3/3,
and it is honest: the answer is about coming back free of charge and the ten year guarantee,
which is a commercial promise, not a maintenance tip.

- ⚠️ **`FAQS` array order IS the page order.** The index fills the columns in array order, so
  entries sharing a `g` must stay contiguous and in the order they should read down their
  column. Reordering the array reorders the page.
- ⚠️ **The vertical rules between columns need `align-items:stretch`.** A rule on a
  start-aligned group stops at its last row. Belt and braces now the groups are even, but it is
  what stops the index looking broken if one ever ends up a row short.

**⭐ THE RULE THAT SURVIVES ALL FOUR VERSIONS OF THIS SECTION: nothing moves when you pick a
question.** It is now enforced differently, and better:

- The plate's height is **MEASURED, not guessed**. `lockPlate()` collapses it, runs all twelve
  answers through it, keeps the tallest and pins it. It re-runs on `fonts.ready` (a height
  measured in the fallback font is the wrong height) and on resize, debounced.
- ⚠️ **This replaced a hand-picked `min-height` clamp, which was out by 11px at 1366×610** and
  made the section jump. How tall an answer renders depends on the viewport width, the font size
  at that width and where the lines happen to break — nobody can hold that in their head, and
  the next person to edit an answer would not think to re-check it. The CSS clamp is still there
  as the pre-JS fallback only.
- Verified stable across all twelve at 1440×900 (196px), 1366×610 (164px), 1920×1080 (216px) and
  900×900 (176px), with no answer overflowing.

**Accessibility — ⚠️ the pattern CHANGED and the reason matters.** It was a `role="tablist"`
with a roving tabindex. That was right for a flat rail of eight. It is wrong now: the questions
sit under four real headings, which makes this a table of contents, and **a tablist whose
children include headings is invalid ARIA**. So each question is now a plain `<button>` with
`aria-controls` on the plate, the live one carries `aria-current="true"`, and the plate is
`role="region" aria-live="polite"` so the swap is announced. Tab walks the twelve, which is what
anyone expects of a contents list, and headings give screen reader users something to navigate
by. Arrows are kept as an enhancement: Up/Down walk the flat order (down a column, on to the top
of the next), Left/Right jump to the neighbouring group at the same depth, clamped.
**Do not put `role="tablist"` back without also flattening the groups.**

- The swap still replays a staggered animation by dropping a class and **forcing a reflow**
  (`void panel.offsetWidth`). Without the reflow the browser coalesces the remove and the add
  and nothing restarts.
- ⚠️ The index's arrival stagger is an **animation, not a transition**: the rows already
  transition `padding-left` and colour on hover, and a `transition-delay` for the stagger would
  delay those too, so every hover would lag by up to half a second.
- Four columns down to 1040px, then two, then one at 760px. ⚠️ **The plate only moves inline
  (under the question you picked) at the ONE-column breakpoint.** At two columns an inline plate
  would land inside a half-width group and be unreadable.
- History, so nobody re-treads it: fourteen flip slabs → eight accordion rows (1 Aug) → the
  eight-question rail with the panel beside it (6 Aug, approved) → this (7 Aug).
  ⛔ The flip grid is not a direction to go back to; it ran 2.5 screens and read as square tiles.

### Contact block — rebalanced, and the uploader is a disclosure

- The **direct-contact block moved to the left column**: phone, email and hours are contact
  details, not form controls, and they belong with the copy. It is pushed to the foot of the column
  with `margin-top:auto`, so the two sides finish level whatever the form does. `.cta-direct` /
  `.cta-call` are gone and the trust marks run side by side instead of stacked.
- The uploader is **"+ Add plans or measurements"**, a 0fr→1fr disclosure.
  ⚠️ **It opens itself whenever the shared store is non-empty, and says how many files are on the
  enquiry.** Every `.tc-up` on the page is one store (§6.5), so a plan attached in the estimator or
  on the POA panel is already on this form — behind a closed toggle with no count it reads as lost,
  and the visitor attaches it twice or not at all. It never auto-*closes*, which would fight a
  deliberate open.
- ⚠️ The collapsed body needs **`visibility:hidden`**, not just zero height: an `overflow:hidden`
  box of zero height still holds its contents in the focus order, so without it a keyboard user
  tabs into an invisible drop zone. It is delayed 0.48s on the way closed so the collapse still
  animates.


## 4b. ⭐ The hero — rebuilt 6 Aug over TWO passes (second pass NOT yet seen)

The client's brief: the subhead was too long, and they wanted the pattern off an HVAC site they
sent — **title, then a one-liner, then three quick reasons with custom icons, then the badges**.

**Stack now:** H1 → trust line (gold rule) → three reasons, icon + stacked label → two CTAs → two
proof chips. **No eyebrow.**

### ⛔ Rejected in pass one — do not re-offer these

1. **A shortened eyebrow.** Pass one cut "Worktops across London, Hertfordshire, Essex &
   Berkshire" (9px, 62 characters) down to the four counties at a legible 12px. The client killed
   it, and the reason is worth keeping: **naming four counties implies those are the only four**,
   when the real coverage runs everywhere between them and templating is nationwide. A coverage
   list at the top of the page reads as a limit, not a promise. Coverage now lives where it can be
   qualified — footer, FAQ, service pages. **Do not reinstate a geography strip in the hero
   without solving that.**
2. **Thin outline icons.** Pass one used 1.35px line icons on a gradient stroke. Client: *"I hate
   the icons… weak cutout looking ones."* The brief is **thick, gold, solid, premium.**
3. **A one-liner that lists rooms.** "Quartz, marble and granite for kitchens, bathrooms and
   commercial spaces" was rejected because **a list always leaves someone out** — vanity tops,
   fireplaces and tables are all real enquiries.

### The trust line

> Chosen from the slab you approve, fitted by the team that measured it.

Names no room and no material on purpose, so it holds for any project type. It carries a **gold
rule down its left edge** because the client asked for it to "stand out in a different way" and a
brighter grey would not have done it.

- ⚠️ The rule is a **pseudo-element, not `border-left`**: a border draws the full box height
  including line-box leading, so it overshoots the text by ~4px top and bottom and reads as a
  misaligned bar. `top/bottom:.18em` trims it to the visual text.
- ⚠️ **No `ch` measure on `.hero-sub`,** deliberately. A ch cap narrower than the column guarantees
  a wrap and strands the last word on line two. The column is the only limit and the copy is kept
  short enough to fit it. **Lengthen that sentence and it wraps — re-measure if you edit it.**

### The three reasons — and the fourth that is NOT here

| | label | sub-line |
|---|---|---|
| 1 | Service second to none | guided at every step |
| 2 | Every cut-out included | pencil edges and grooves |
| 3 | Aftercare in 72 hours | free, not a month's wait |

⛔ **The fourth differentiator — "no one gets better supplier discounts in the country" — is
deliberately off the page** (§3, and `memory/topcat-copy-constraints.md`). Unverifiable, and it
argues on price, which breaks rule 5 and undercuts everything else the page is building.
**The client re-sent it on 6 Aug and it was still left out, with that reasoning given back to
them.** If they push, that is the conversation to have, not a silent addition.

- ⚠️ **The label STACKS over its sub-line.** That is the whole point of pass two: one long line per
  reason pushed the row edge to edge with nothing between the three.
- ⚠️ **Keep all three at one label line + one sub-line.** Ragged counts (1+1, 1+2, 2+1) was the
  state mid-pass and it looks broken. Budget is ~22 characters for a label and ~24 for a sub-line
  at the 1440 column width. **Re-measure if you reword.**
- `.hf` is `flex:1 1 0`, so a long label wraps inside its own column instead of stealing width
  from the two beside it.
- `.hero-copy` is `clamp(560px,48vw,720px)`, widened from a flat 600px. At 600 the H1 broke to
  THREE lines at 1440, the trust line wrapped, and every reason label wrapped. It tracks vw rather
  than sitting at 720 flat because the copy must stay inside the dark half of the photo —
  `.hero-shade` is still fairly opaque at 52% and thins fast after that.

### The icons

Solid shapes filled with **`#tcGoldSolid`** — a *second* gradient, added because the ramp tuned for
a 1.3px stroke goes flat once it fills a solid shape. It runs diagonally with a brighter highlight
and a deeper shadow, which is what makes a solid glyph read as metal rather than a gold sticker.
`#tcGold` (the stroke ramp, matching the favicon and `.hero-title em`) is still defined.

- Both live in the `.tc-defs` sprite at the top of `.hero`. ⚠️ Zero-sized and absolutely
  positioned: `display:none` kills the paint server in some engines, and an unpositioned 0×0 box
  still takes part in layout.
- ⚠️ **No animated sweep.** The shimmer was deliberately dropped from every button on the site for
  one consistent look, and a glinting icon row would put it straight back.
- **Medal** for service: ribbon tails behind, disc in front with a ring knocked out. ⚠️ **No star**
  — the five gold stars of the Google chip sit ~60px below and a second star reads as a rating.
- ⚠️ **The cut-out icon took FIVE attempts, four of them the same mistake.** A **rectangle holding
  smaller shapes reads as a device, every time**: as an outline it was a camera, then a car stereo;
  as a solid rounded rectangle with a slot and a ring punched out it went straight back to being a
  camera. It is now an **L-shaped run in plan** with the openings punched through by `evenodd` —
  the site's own drawing language, the view the estimator renders cut plans in. Nothing else in a
  UI is that shape, so the eye reads a plan instead of an object. **Do not tidy it back into a
  rectangle and do not convert it to a stroke.**
- The L carries a **tighter `viewBox` (`2.2 2.2 19.6 19.6`)** than the other two: it occupies only
  17.2 of its 24 units where the medal fills 22 and the clock 21.3, so at a shared CSS width it
  rendered visibly smaller and the row read as two sizes.

### Still open, for the client

- ⚠️ **Recommended and NOT done: demote "Request a call" to a text link or the phone number.** Two
  co-equal buttons split intent, and this is the highest-value change left in the hero. It is the
  **third** time it has been raised (archive §6.3, still unanswered) — it wants a decision, not
  another unilateral edit.
- **Mobile stacks the three reasons and pushes the CTA below the fold at 390px.** Left alone on
  purpose: mobile is the next phase in the agreed order of work (§7.6). The fix when it comes is
  to move the reasons *below* the CTAs on mobile.

---

## 4c. ⭐ 7 Aug changes — porcelain, Trade, the region line, four FAQs

Prompted by three client research docs (`Docs/topcat-worktops-*.md`: industry, customer psychology,
sales-call notes). All of the below is built and verified. **None of it has been seen by the client
yet.** Backup: `index.html.pre-porcelain.bak`.

### Porcelain is back, bespoke and enquiry-led (reverses the 6 Aug ban for the estimator)

⚠️ **Standing Rule 8 still says porcelain was banned on 6 Aug. That was reversed on 7 Aug by the
client:** porcelain IS offered again, but **bespoke and enquiry-led only**, no calculator. Update
Rule 8's wording next time it is touched. Owner sign-off on the reversal is on the record via the
client, not a second phone call, so treat it as confirmed but note it.

- **Estimator gained a fourth material tab, Porcelain**, POA like marble and granite. It has
  **`noCat:true`** in `MATS` because the client has not supplied a porcelain stone range, so there
  is nothing for the picker to show. Rather than invent porcelain slab names (the exact
  placeholder problem the real catalogue fixed), it carries one honest stand-in, `NO_CAT.Porcelain`
  = "Porcelain and sintered stone", and the stone picker (`#estStoneBtn`) is **hidden** on that
  tab. Drop `noCat` the day a real range lands and the picker returns on its own.
  - ⚠️ `bestFor()`, `findStone()` and `setStone()` all had to learn `noCat` so they never index an
    undefined `MATERIALS[m]`. `renderPoaSlab()` and the ticket meta both drop the second line on a
    no-catalogue material, or the plate read "Porcelain and sintered stone / Porcelain".
  - ⚠️ `.est-stonebtn` sets its own `display:flex`, so hiding it needed an explicit
    `[hidden]{display:none}` (the §6.7 hidden-attribute trap, again).
- **POA copy is now per-material** (`POA_LEAD` map). Marble and granite keep the owner's "the
  block and the distributor swing the cost" reason. **Porcelain gets a different reason** because
  it is manufactured to a spec: the JOB varies (waterjet not saw, fewer suppliers, mitred edges),
  not the stone. Do not give porcelain the marble paragraph, it would be factually wrong.
- ⚠️ **Porcelain is on the ESTIMATOR tabs only, never the stone wheel's** (`#matTabs` still has
  three). The wheel picks a specific slab and there is no porcelain range, so a fourth wheel tab
  would fan an empty belt.
- **Dekton and Neolith are back in the brand strip** (`SUPPLIERS`), restored because they are
  sintered-porcelain brands and porcelain is offered again. The porcelain scan for Rule 8 will no
  longer come back clean, that is expected now.
- **Porcelain still needs a proper material page** (see §10), the estimator route is only half of
  it.

### Trade is back in the nav (reverses the 1 Aug removal)

B2B is the client's stated first priority and the customer-psychology brief is explicit that trade
buyers must be able to self-identify in seconds. Trade went back into **both** the desktop nav
(`nav.top`) and the mobile nav, as `/trade/` — the one nav item that is a real page, not a hash.
⚠️ The in-page nav JS only intercepts `a[href^="#"]`, so it passes straight through. The three
sub-site builders (`services/`, `stones/`, `trade/build_*.py`) all had `NAV_LINKS` updated and were
**re-run**, so every generated page carries the Trade link too. The client wanted a dedicated Trade
**page**, not a homepage section, which is exactly what already exists at `/trade/`.

### Hero region line (client: "add the place back as a region")

The trust line is now "Chosen from the slab you approve, fitted by us across London and the Home
Counties." ⚠️ **Region, never the four counties** — the county list was tried as an eyebrow and
rejected (§4b), naming four counties reads as the only four. "London and the Home Counties" is an
inclusion, not a boundary. ⚠️ The line now runs to **two lines** at desktop, and the region phrase
is wrapped in `<span class="nowrap">` so it breaks BEFORE "across London…" rather than splitting
the place name. The exact counties still live in the footer, FAQ and schema, where they can be
qualified.

### Four new FAQs, and the FAQ rail is now TWO columns

Answering more of the documented homeowner fears (customer-psychology brief §2.6). Added:
**aftercare in 72 hours, who comes to your home, hot pans / heat by material, and do you work with
porcelain.** The FAQ went from 8 questions to **12**.

- ⚠️ **The `FAQS` array and the `FAQPage` JSON-LD are still two separate copies** — both were
  updated, verified in sync, both parse. A syntax-gate is now part of the health check (see §8).
- ⚠️ **The rail is a two-column grid on desktop (≥1000px).** Twelve single-column rows ran the
  section ~320px past one screen. `grid-auto-flow:column` with six fixed rows fills column 1 with
  01–06 then column 2 with 07–12, so DOM order still runs top-to-bottom down each column. **That
  column-wise fill is what keeps the keyboard handler correct with zero JS change** — it steps by
  index, which now walks down a column and wraps to the top of the next. Do NOT switch to `row`
  flow, it would desync the arrows from the visual order. Below 1000px it is one column (the
  861–1000 tablet band is taller than a screen, fine, that is the deferred tablet pass); below
  860px the panel moves inline and it is an accordion, unchanged. The seventh row grows its own top
  hairline in grid mode.
- Verified: section back to exactly 100vh at 1440×900 and at 1000×700; 6+6 columns; arrows,
  Home/End, roving tabindex and the panel swap all still work; the accordion still moves the panel
  inline below 860; console clean over the whole page; no h-scroll.

⚠️ **FAQPage schema is DEAD as of 2026** (Google deprecated it, stopped showing FAQ rich results 7
May 2026, deleted the docs 15 June 2026). The block we keep in sync is now **inert, not harmful**.
The visible FAQ content is still worth having for users and AI. **Remove the FAQPage JSON-LD before
go-live, or leave it as dead weight, but never add FAQPage markup to a new page.** Full detail in
`Docs/topcat-worktops-seo-build-plan.md`.

---

## 4d. ⭐ The SEO content layer — built 7 Aug (NOT yet seen by the client)

**25 pages, 22,346 words.** All generated by `Website Demo/build_seo_pages.py`.
⚠️ **Never hand-edit the HTML. Edit the builder and re-run it.** The builder's docstring carries
the house rules and the schema policy, read it before changing anything.

| Family | URL pattern | Count |
|---|---|---|
| Materials | `/materials/<slug>.html` | 5 + index |
| Guides | `/guides/<slug>.html` | 9 + index |
| Areas | `/worktops/<county>/<town>/` | 4 counties + 4 towns + index |

New stylesheet `Website Demo/seo.css`, loaded **after** `/services/service.css`. The shell is
reused, so the new pages are visibly the same site. Only genuinely new components live in seo.css.

### The fabrication rewrite that had to come first

D21 reversed the in-house claim, so roughly **25 customer-facing claims** were rewritten across
`index.html` and all three sub-site builders before a single new page was written. The replacement
positioning is **one contract, one contact, and we answer for all of it**, which the customer
psychology brief identifies as what buyers actually value, and which is entirely true.

⚠️ **What is still true and was deliberately kept:** "fitted by our own team". Installation is
genuinely theirs. Do not strip it in an over-correction. Templating and aftercare are theirs too.

⚠️ **V2 was NOT rewritten** and still carries the old in-house claims and dead schema. It is
dormant. Fix it before any revival.

### Two live rule violations found and fixed in passing

Neither was introduced by this session.

1. **`aggregateRating` with `"reviewCount": "16"` was in the landing page JSON-LD**, publishing
   the low review count to Google and breaking D7. It produced nothing anyway, because Google
   makes self-reviewed star markup ineligible. Removed.
2. **The landing page schema still carried `hello@`**, and `areaServed` listed twelve
   Hertfordshire towns only, reflecting an old narrow positioning. Both corrected.
3. The landing page **title** said "St Albans & London". Now names London and the Home Counties.

### Schema policy, which is not what most people expect

**Built:** `HomeAndConstructionBusiness`, `BreadcrumbList`, `Article` + `author` as `Person`.

⛔ **Never build:** `FAQPage` (Google deprecated it, rich results stopped **7 May 2026**, docs
deleted **15 June 2026** — it was removed from the landing page, all six service pages and the
trade page), `HowTo`, `Service`, `WebSite`+`SearchAction`, `aggregateRating`/`review` on our own
pages, `Product`/`Offer` without a real visible price, and **`LocalBusiness` on location pages**
(TopCat is not located in those towns, and marking up an address it does not have risks a manual
action). Also **do not create an `llms.txt`**, Google ignores it.

**The visible FAQ content stays.** Only the markup is dead.

### Location pages: why they are safe

Google renamed "doorway pages" to "doorway abuse" and the definition turns on **funnelling**.
Enforcement is **site-level**, so a bad rollout drags the whole domain. Every location page
therefore converts standalone, swaps **nine** real local variables, and sits under a real county
hub so the URL is a browseable hierarchy. Location pages are **26% of URLs** against a ~40%
ceiling.

⚠️ **DO NOT bulk-add Phase 2 towns.** Gate at 90 days: all 8 indexed, ≥6 with impressions, ≥3 with
clicks. Phase 2 and 3 lists live in the SEO log §5. Towns were chosen on **autocomplete demand,
not affluence** — Harpenden, Windsor, Ascot and prime-central London returned zero demand signal
despite the money.

### The silica page is the biggest opportunity, and the most dangerous to edit

`/guides/is-quartz-safe-silica.html`. Across four competitor sitemaps totalling 684 URLs there is
exactly **one** safety page and it mentions silica zero times. It is also **YMYL**, so errors are
expensive. Accuracy rules, all of which the page currently respects:

- ⚠️ **Never write "HSE banned dry cutting" as law.** HSE's own wording is that the guidance
  "effectively rules out dry-cutting" but "is not a new law, or a formal prohibition".
- ⚠️ **There is no UK ban and none coming.** Australia banned it July 2024. The UK government
  explicitly **declined** a ban on 2 June 2026.
- ⚠️ **Never claim any product is "silica free" without a manufacturer SDS on file.** The most
  credible products qualify to "less than 1%" in their own small print.
- ⚠️ **Porcelain is NOT the safe alternative.** Roughly 15 to 25% crystalline silica, higher than
  several low-silica quartzes.
- The page's genuine edge is that, **because fabrication is outsourced**, TopCat can honestly
  describe what they require of and verify in their partners. No competitor is doing this. That
  edge only exists because of D21.

### Internal linking

Hub and spoke, verified 0 orphans. **The footer is the only sitewide route into Materials, Guides
and Areas.** ⚠️ It carried a full link column per family from 7 Aug until 9 Aug, when it was cut
to one hub link each — see the ⭐ note below before touching it.
`.est-readmore` under the estimator's help band is the highest-intent internal link on the site.
`/guides/best-kitchen-worktop-material.html` is the intended linking hub.

⚠️ **If you restructure the footer, keep a real path to all three new families.** Unlinked page
sets are the textbook doorway signature.

⭐ **UPDATED 9 Aug (D42).** The footer was cut from five link columns to two, so the sitewide route
into those families is now **one hub link each** (`/materials/`, `/guides/`, `/worktops/`) in the
Browse column, rather than 25 individual links. Every family is still reachable in two clicks, and
verified at 0 orphans across all 89 non-V2 pages. **The rule above is unchanged and is now the
single reason the Browse column exists — do not remove it.** Full detail and the before/after in
§4k.

---

## 4e. ⭐ The services helix now BUILDS on arrival — 7 Aug (NOT yet seen by the client)

**Client:** *"As the user scrolls I want it to basically form one by one from the top card. So the
top card is obviously angled, so you see the back, then the next one and the next one and the next
one. I want those to almost animate in one by one semi quickly."*

Before this the six helix cards were simply there when you reached the section. Now they arrive
one at a time, **in spatial order, top of the spiral first**, over about 1.16 seconds.

- **The order is computed, not hardcoded.** `EN_SLOT` sorts the cards by their resting `d` (the
  signed step from the front) descending, so it survives a change to `ORDER` above it. The
  sequence you see is: the top card showing its gold **back**, then the angled one below it, then
  the **front** card third, then the two beneath. The front card is *not* first — the client
  described a path down the spiral, not an emphasis on the front card.
- ⚠️ **The card at `d=3` is skipped in the ordering.** `render()` zeroes opacity two steps out, so
  with six cards that one is the invisible back pole. Giving it its own slot opened the build with
  150ms of nothing, so it shares slot 0 with the first real card.
- **Each card comes forward out of the depth** into its own slot: 340px back, 40px high, 12%
  small, faded out. Sliding them *along* the spiral was the other option and it fights itself —
  the cards above the front and the cards below it would have to travel in opposite directions to
  both read as "further out", and the front card has no "further out" to come from.
- ⚠️ **The entrance is a MULTIPLIER inside the existing `render()`, not a second transform
  system.** At `en=1` every added term is identity and the line is the original one. **Verified:
  the seated state is byte-identical to before the change**, every card's transform, `--hxO`,
  `--hxB` and z-index. A resize, a drag or a click mid-arrival all stay correct because the
  resting geometry is still the only source of truth.
- ⚠️ **The observer is installed from inside `boot()`, not at parse time.** On smaller screens the
  helix is `display:none` and the stage measures 0×0, and a zero-area element can never satisfy an
  IntersectionObserver threshold — arming it early would leave the cards at `en=0`, which is
  invisible. `boot()` already retries until the stage has size, and the resize handler re-runs it
  if the window is later widened into the desktop layout.
- ⚠️ **`restD()` writes its own modulo instead of calling `mod()`.** `mod` is a `const` declared
  further down the same IIFE, so calling it from the `EN_SLOT` setup hits its temporal dead zone.
  That threw `Cannot access 'mod' before initialization`, which killed the whole IIFE and left
  every card invisible. Caught in one pass; worth knowing the shape of it.
- Timing knobs, at the top of the IIFE: `EN_DUR` **820ms** per card, `EN_GAP` **265ms** between
  cards, `EN_BACK` 340px, `EN_RISE` 40px, `EN_SHRINK` 0.12. Six cards land in **~1.9s**.
- ⚠️ **SLOWED on the client's review, 7 Aug (D28): "a little bit too fast, it finishes the
  animation before the user even reaches the end."** It was 560/150, so all six were down in
  1.16s — over before someone scrolling at a normal pace had settled into the section.
  **Two changes, and both were needed:** the pacing above, AND the observer threshold went from
  0.25 to **0.42**, so the build does not start until the spiral is properly on screen rather
  than peeking in at the bottom. Slowing it alone would just have moved the same problem later.
- ⚠️ The observer uses `threshold:[0,0.42]` plus a `boundingClientRect.top < 0` escape hatch. A
  bare 0.42 would be unreachable if the stage were ever taller than the viewport, and the build
  would simply never start.
- `prefers-reduced-motion` seats all six immediately.

### The cards' reverse — a real generated slab, 7 Aug (D31)

**Client, in three passes:** *"The top card and the bottom card showing the back, that's
currently just slightly too dark. Make it a lighter card, some grey or faded white that fits
within the brand, I want those to stand out a little bit more."* → *"Let's not make it pure
white."* → *"Give it a stone design."*

The back was black marble, and two things stacked against it: the page's ground is near-black,
and `render()` dims the far cards to tell depth. The two cards turned far enough to show their
reverse sank into the section.

- ⭐ **The reverse is now a real slab from the site's own `marble()` engine**, preset
  `calacatta` — warm off-white ground, grey structure, gold veining. Not a hand-drawn pale
  background: literally the same stone the wheel and the estimator draw, which is the point.
  **Not pure white**, which the client ruled out explicitly.
- **Each card takes its own seed** (`611 + j*37`), so the six backs are six different slabs
  rather than one repeat. That is what stops the spiral looking printed.
- ⚠️ **It is applied in the deferred `setTimeout(…,0)` block**, alongside `attachGlow`, because
  `marble()`'s `STONES` presets are a `const` declared further down the script and calling it
  while the helix IIFE runs would hit the TDZ.
- ⚠️ **`preserveAspectRatio="none"` plus `background-size:100% 100%`**, the same swap the
  estimator's `face()` makes. `marble()` draws a PORTRAIT slab and these cards are landscape, so
  slicing it shows a narrow band and most of the vein network never appears. Stretched, the
  whole slab reads, and stone is organic enough that the distortion is invisible. **Do not put
  `cover` back** — it crops it straight to the band again.
- ⚠️ **Everything inside had to flip with the ground.** Gold type on bone is about 1.6:1 and
  unreadable, so the wordmark is warm charcoal `#241F18` and only the diamond and the rules stay
  metal. **Do not put `--gold` text back on this face.**
- ⚠️ **The back face has its OWN depth ramp**, remapped in CSS off the same `--hxB` the script
  writes: `brightness(calc(0.42 + 0.58 * var(--hxB)))`. render()'s ramp (0.5 → 1) is tuned for
  the photo faces, and a pale stone taken to 0.5 goes muddy grey and loses the veining that
  makes it read as stone. This lands the top and bottom of the spiral near 0.78. **Remapping
  here rather than changing `render()` is deliberate** — the photo faces keep exactly the depth
  falloff they were designed with.

---

## 4f. ⭐ `/sitemap.html` — the HTML sitemap, 7 Aug (NOT yet seen by the client)

Asked for so the SEO build could be walked from one screen. **Generated by
`build_seo_pages.py`, never hand-written** — it is built from the same `MATERIALS`, `GUIDES`,
`COUNTIES` and `TOWNS` lists the pages come from, plus `stones/catalogue_source.py` for the 52
stones, so it cannot drift out of sync.

- Groups: the main pages, 6 services, 5 materials, 9 guides, 4 counties + 4 towns, and all 52
  stones in three columns by material. 591 words, `index, follow`.
- **Linked from the legal bar of every footer**, which meant a one-line change in `index.html` and
  in all four builders. Present on **87 of 89** non-V2 pages; the two without it are
  `versions.html` (the dev version chooser) and `index-v2.html` (stale and unlinked, §9).
- On the landing page it sits **first** in the legal bar, ahead of Privacy, Terms and Cookies.
  ⚠️ Those three are still `href="#"` placeholders waiting on the client's policies. Sitemap is
  the only one of the four that goes anywhere.
- ⚠️ **`.foot-legal` is `display:flex` with a 20px gap**, so in the generated footers the small
  print needed wrapping in its own `<span>` or the link would have been gapped off an anonymous
  flex box. And `.foot-legal a{color:var(--faint)}` in `service.css` beats a bare `.foot-sitemap`
  on specificity, so the seo.css rule is `.foot-legal a.foot-sitemap`.
- ⚠️ **There is no `/services/` index page.** Materials, guides and areas all have a hub; services
  does not. The sitemap therefore lists the six service pages directly and points at the landing
  page's services section as the hub. Flagged, not built — it needs copy the client has not seen.

---

## 4g. ⭐ The foot of the enquiry form — 7 Aug (NOT yet seen by the client)

**Client:** *"Add the call us directly button back below the send my enquiry. Or maybe request a
call, whatever you think is best. Otherwise we just need to optimise that empty space below the
send my enquiry button."*

There was **~119px of dead column** under the submit button: the copy side finished level with
the card, the form side stopped short. Three things fill it, in the order someone hesitating
over a submit button actually wants them.

1. ⛔ **A second way in — REMOVED 9 Aug (D36).** It was a ghost "Call us on 0800 098 2812" button
   under the gold submit. The client's call on seeing it built: *"remove the call us button with
   the number and just underline the number on the reach out to us directly."* The reasoning on
   7 Aug was that the duplication was correct — the left column a reference, the right an action
   at the point of decision. The client did not read it that way; they read it as the same number
   twice. **The requirement is still served**, by the number in the left column, which now carries
   a standing gold underline and a size bump so it looks like the action it is.
   ⚠️ A **standing** underline, not one that waits for a hover: a hover-only affordance is
   invisible on a touch screen, which is where most calls come from.
   ⚠️ The selector is `.cta-lines a.cta-tel`. A bare `.cta-tel` is (0,1,0) and loses to the
   `.cta-lines a` rules above it (0,2,0), which silently ate both the colour and the size.
2. **"What happens next", three numbered lines.** The unspoken question at a submit button is
   "what am I actually signing up for". Every line is true and none of it is new copy the client
   has to approve on trust: it restates the free home visit, the fixed itemised quote and the
   no-pressure callback that the page already promises elsewhere.
3. **The reply promise**, kept, now as the last word.

- ⚠️ **This blew the one-screen budget and the fix was a CLAMP-FLOOR SWEEP of the form** (§7.8).
  The additions took the section from 463px to 647px at 1366×610, past one screen. The cause was
  not the additions on their own: `.cta-form`'s row gap (11px), the input padding (12px), the
  textarea floor (76px) and the submit padding (13px) were all **flat px** and could not shrink.
  That was survivable while the form was the shorter of the two columns and stopped being
  survivable the moment it became the taller one. All four are vh-aware now, and the section is
  **582px at 610** with both columns finishing level. **This is exactly the class of violation
  §7.8 says to expect more of.**
- ⚠️ `--gold-lo` (`#8C6B34`) is the deep end of the gold ramp and all but disappears at 10px on
  ink. The step numerals use mid gold at 0.72 opacity instead. Worth knowing before using
  `--gold-lo` for anything small again.
- **Where the ~54px went when the button came out (9 Aug).** Not into air: the client's brief was
  *"balance out that section so there's not so much empty space"*, so it went into the spacing and
  size of "what happens next" — bigger line type, more gap between the steps, more room above the
  rule. All still vh-aware clamps with small floors, because this column is the one that decides
  whether the card fits a 610px screen. **The two columns finish level at all five test sizes**,
  which they now do naturally rather than by `margin-top:auto` stretching the left one.

---

## 4h. ⭐ The nav bar's line flashes as it forms — 9 Aug (D33, NOT yet seen by the client)

**Client:** *"When the user first scrolls and the nav bar forms, I want almost the same thing to
happen as that divider where the shine goes across as you scroll. But start in the middle and go
to the two sides quite quickly, so as it forms it just does that flash or shine thing."*

The bar's bottom hairline (`header.bar::after`) used to simply fade up with the glass. Now it
**draws out from its own centre**, and a bright head rides each leading edge out to the screen edge
and leaves with it.

- The heads use the **same gradient, blur and border-radius as `.sd-line::after`**, which is the
  site's one shine language (§4). This is that shine, split in two and fired once rather than
  tracked against scroll position.
- ⚠️ **The flare is a real element (`<i class="bar-flare">`), not a third pseudo.** `::before` is
  the glass and `::after` is the line, both taken, and the flare needs two heads travelling
  independently. It is `position:absolute`, so it is **not** a flex item of the bar and does not
  touch the nav's layout.
- ⚠️ **`overflow:hidden` on `.bar-flare` is LOAD-BEARING.** The heads travel `50vw`, which puts
  them half off the viewport at the end. Without the clip that is horizontal scroll at every width,
  which is a standing rule. Verified: `scrollWidth === innerWidth` at 1920 through 375.
- Timing is **0.76s** for the flare and 0.76s for the line's draw. It was 0.58s and the client
  asked for "slightly, slightly slower" on review (D37). ⚠️ **Keep the two on the same duration.**
  The heads are meant to read as the leading edges of the line drawing itself, and they visibly
  come apart if the flare and the draw differ.
- It replays whenever `.scrolled` is re-added, so scrolling back to the top and down again fires it
  again. That is deliberate and reads as the bar re-forming.
- `prefers-reduced-motion` drops the flare entirely and seats the line at `scaleX(1)`, so it still
  arrives, just without the flash.
- ⚠️ **Landing page only.** The generated sub-sites' `header.bar` (in `services/service.css`) is
  `position:sticky` with permanent glass, a plain `border-bottom` and no `.scrolled` state at all,
  so there is no forming moment there to decorate. If those bars ever get one, this is the effect
  to bring across.

---

## 4i. ⭐ The Why tiles are designed now — 9 Aug (D35, NOT yet seen by the client)

**Client:** *"We need to add some sort of design into the more reasons to choose us. We can't just
have it grey blocks… the other blocks need to look better designed. And where it says feature
image, just add any image in there now so we can have it as a placeholder."*

**The feature slot** now carries `assets/team/team.jpg`, which the About collage stopped using the
same day, so nothing on the page is doubled. ⚠️ It is a **placeholder**, and the real brief for the
slot is unchanged and still owed (§7.5): the whole team together, or a finished install.

**The five reason tiles** get three things, and **none of them costs vertical height**, which is the
constraint this section has always been up against:

| | what it is | why |
|---|---|---|
| `.wy-stone` | the review cards' black marble, one slab per tile, veiled back | client, 9 Aug (D40) — see below |
| `.wy-ico` | a solid gold icon per reason | the hero's icon language (client, 6 Aug: "thick, gold, solid, premium") |
| `.wy-top` | puts the icon on the **same line** as the numeral | which is what makes the icon free |

**⛔ The gold seam that used to be here, and why it is never coming back.** The first version of
these tiles carried a lit gold hairline across the top edge. The client's response was a general
rule, not a note about this section: *"Don't ever use that fingernail design on top of sections."*
⚠️ **The rule already existed** — §6.5 has carried "no bright band across the top of anything,
that was tried and rejected outright" since the estimator round — and it got broken because it was
filed under the estimator. It is **§2 rule 10** now, stated site-wide. Do not add a top seam to
anything.

**What replaced it.** `marbleSVG()`, literally the same generator the review cards use, filled per
tile from the WHY MOSAIC IIFE with its own seed so no two tiles carry the same slab.

- ⚠️ **The veil is DEEPER than the review cards' (0.74→0.86 against their 0.55→0.72).** These
  tiles are about a third of a review card's area and carry nearly as many words, so the veining
  crowds the copy at the review setting. If you lift it, check the body text against the
  **brightest vein**, not the average — the failure is local, not overall.
- ⚠️ **`.wy-stone` is a real child element, not a pseudo.** `.glow-card` already owns `::before`
  and `::after` on every tile for its cursor-reactive rim and halo, and a pseudo here would have
  fought them for `background`. Same trap applies to anything else added to a `.glow-card`.
- ⚠️ **No `overflow:hidden` on `.wy-r`.** `.glow-card`'s halo is `inset:-1px` and blurred, so it
  spills outside the box on purpose; clipping there would cut the cursor light off at the border.
  The stone that *does* need clipping is clipped by `.wy-stone` instead.
- ⚠️ **`marbleSVG`, `rng` and `veinPath` are `function` declarations, so they are hoisted** and
  safe to call from the IIFE. This is **not** true of `marble(preset,seed)`, whose `STONES` presets
  are a `const` further down — that is the temporal dead zone that killed the helix IIFE once
  already (§4e). Swap to `marble()` and it has to move into a deferred `setTimeout(…,0)`.
- **The icons, and what they must not be.** ⚠️ **No icon is a rectangle holding smaller shapes** —
  that silhouette read as a camera and then a car stereo through five attempts in the hero, and the
  lesson holds here. Nothing is a star, either, for the same reason the hero's medal has none.
  - 01 two linked rings · 02 a punched price tag · 03 an arrow clearing a baseline ·
    04 a vein · 05 a clock disc with the hands knocked out.
  - ⚠️ **04 is the one STROKED icon in a set of five filled ones, deliberately.** Its subject is a
    line. Every filled version read as something else: two mirrored halves came out as a **coffee
    bean**, and two slabs side by side is the rectangle-holding-shapes silhouette. At 2.9 units on
    a 24 box it is a thick ribbon, not the 1.35px hairline that was rejected on 6 Aug.
  - ⚠️ **03's gap between the arrow and the bar is what stops it reading as an upload glyph.**
    Do not close it up.
  - All five were checked at **18px as well as at 96px**. An icon that only works large is not
    finished — they render at 15–21px on the page.
- On a short window the **numeral** is the first thing dropped (`max-height:620px`), not the icon:
  the icon is the thing carrying the design and it costs no height of its own.

---

## 4j. ⭐ The reviews line is a CTA — 9 Aug (D32, NOT yet seen by the client)

**Client:** *"On the section that says 'like what you're reading, let's bring your vision to life',
we need to make that a CTA. Whether we make it a button below, or underline it and an arrow to make
it clickable, or just something to make that a CTA."*

`.rev-cta` under the review wall held a sentence and nothing else, sitting in the one place every
other section carries an action. Now **"Let's bring your vision to life"** is a gold link to `#cta`
with a standing underline and an arrow that steps right on hover.

- ⚠️ **A link, not a button, and that was a choice.** The page already pairs a gold button with a
  ghost in the hero, the services intro and the process foot. A fourth pair here is a fourth place
  the eye has to choose, and the hero already taught us what two co-equal buttons cost (§4b).
- The underline is **permanent**: it IS the affordance, so it cannot wait for a hover a touch
  screen never sends. Hover only brightens the gold and nudges the arrow.
- "Like what you're reading?" stays in bone as the lead-in, so the gold half is unambiguously the
  clickable half.

---

## 4k. ⭐ The footer, cut to main pages — 9 Aug (D42, NOT yet seen by the client)

**Client:** *"You need to completely clean up the footer, it's way too big. You won't have to list
everything out, you'll have those main pages. So Services counts as one page, when they click on
that it goes to the services page and all that section of the site. And then you'll have the
Explore section, then whatever usually goes into a footer. Not everything."*

It was **five link columns, 31 links, 931px tall** — taller than the viewport on a 900px screen.
Now **two link columns, 17 links, 411px**.

| | before | after |
|---|---|---|
| Brand | logo, tagline, stars | unchanged |
| **Explore** | 9 links | Services · Projects · Stones · Estimate · About us · For the trade |
| Services | all 6 service pages | ⛔ gone |
| Materials | all 5 material pages | ⛔ gone |
| Guides | 6 guide links | ⛔ gone |
| Areas we cover | 5 area links | ⛔ gone |
| **Browse** | — | Materials · Worktop guides · Areas we cover · FAQ (**the three hubs**) |
| Contact | phone, email, area, hours | unchanged |
| Legal bar | Sitemap · Privacy · Terms · Cookies | unchanged |

### ⚠️ READ THIS BEFORE TRIMMING THE FOOTER FURTHER

The footer is the **only sitewide route** into `/materials/`, `/guides/` and `/worktops/`, and an
unlinked page set is the textbook **doorway signature**. Enforcement for that is **site-level**, so
getting it wrong drags the whole domain, not just those pages (§4d).

**The cut did not drop those families — it links each one ONCE, at its hub, and lets the hub link
its own children.** That is cleaner hub-and-spoke than 25 sitewide links were, and every family
stays reachable in two clicks. **Do not remove the Browse column.** If the footer has to shrink
again, take rows out of Explore instead.

Verified after the cut: **3,393 internal links, 0 broken, 0 orphaned pages** across all 89 non-V2
pages.

### The same cut, in all five places

⚠️ **The footer is written FIVE times** — `index.html` plus a `footer_html()` in each of
`services/build_services.py`, `stones/build_stones.py`, `trade/build_trade.py` and
`build_seo_pages.py`. All five were changed and **all four builders were re-run** (6 + 53 + 1 + 26
pages). Change one and you have to change all five, or the site's footers drift apart.

- The sub-site grid in `services/service.css` was **already** `1.4fr 1fr 1fr 1.3fr` — four columns.
  The builders had been emitting **six** `.foot-col`s into it, so the last two wrapped onto a
  second grid row. That is why the generated footers were tall too, and it fixed itself the moment
  the count came down to four. No CSS change was needed there.
- Three builders were left with dead locals (`services_links`, `mat_links`, `mats`/`guides`/
  `places`) once their columns went. Removed.

### ⚠️ "Services" has nowhere real to point

The client described clicking Services and landing on "the services page… and all that section of
the site". **There is no `/services/` index page** — it is the one family without a hub (§7.18).
The link points at `/index.html#services`, the landing section, which does list all six and links
to each. That satisfies the behaviour today.

**This is now the second time the gap has surfaced**, and this time the client described the page
as if it exists. Building it needs copy they have not seen, so it was not built — but it should be
the next thing offered. Repoint the footer link the day it lands.

---

## 4l. ⭐ The page floor — 9 Aug (D43, NOT yet seen by the client)

**Client, over six passes.** Worth reading in order, because the destination is not where it
started: *"That dark background almost looks like just paintbrush strokes. I like how it looks
almost far away, it gives the site depth, and I like that everything floats over it. But it looks
low quality and blurry… it's currently just a whole bunch of stripes."* → *"I don't like the stone,
the thick grey line looks bad, it has to look like a stone everyone would love to buy, better
veining."* → *"There should be some sort of dark transparent cover over it so the site sections
stand out more."* → *"The background shouldn't be too complicated so it doesn't distract."* →
*"Background needs to be even darker."* → *"I also don't like the thick gold veining."*

**Read as a whole, the brief is: real stone, hairline veining only, and darker than before.**
The first three passes pulled toward richer; the last three pulled hard back toward quieter. Do
not re-introduce drama here.

**What it is now:** `assets/stone-floor.webp` — a **Nero Portoro** slab, black stone with a fine
hairline vein web and crystalline grain, graded down and sat under a dark veil. 90KB, 2048×1152.

### Why Portoro specifically

Gold-veined black marble is a stone people genuinely covet, and **the veining is the brand's own
champagne gold**, so the floor and the accent colour are the same material. Every other dark stone
tried came back to a grey vein, which is the first thing the client rejected.

### ⭐ THE NUMBERS THAT MATTER

Tuned against **measured pixel luminance**, not by eye — the darks here are so close together that
a screenshot genuinely cannot be trusted, and several rounds were wasted before this was measured
instead of eyeballed. On an 800×450 raster:

| | median | p90 | max |
|---|---|---|---|
| The OLD floor | 13 | 16 | 19 |
| The graded slab, before the veil | 13 | 21 | 51 |
| **What the page sees, at `--floorVeil:0.6` over `rgba(6,6,9)`** | **9** | **12** | **24** |

So the floor is now **darker than the site has ever had** (median 9 against 13), and the only
thing left in the top end is hairline vein and grain.

⚠️ **If you replace this image, match those numbers.** A floor a few levels lighter changes the
weight of every section on the site at once.

### ⛔ NO THICK VEINING, OF ANY COLOUR

The client rejected a thick grey vein and then a thick gold one. **A thick vein reads as a crack,
not as a slab.** The crop behind this was not chosen by eye: every candidate window was scored by
thresholding the vein level and **eroding** it — a thick vein survives an erosion, a hairline does
not — and the window kept was one where **0.0000%** survived. If you swap the image, run that same
check rather than judging it visually; at these luminances the eye is unreliable and the client
has now caught the same fault twice.

### The knobs, in the order you should reach for them

1. **`--floorVeil`** on `:root` (currently `0.6`). One number. Up = quieter, down = more stone.
   ⚠️ It sits **third** in `body::before`'s background stack, *below* the two gold corner
   radials — CSS paints the first layer on top, so this dims the stone without dimming the brand
   glows with it. Do not move it to the front.
   ⚠️ **The veil colour is `rgba(6,6,9)`, DARKER than `--ink` (11,11,14), and that is the whole
   reason the floor can get darker at all.** A veil the same colour as the ink can only pull the
   stone *towards* ink, never past it — raising its alpha just kills the veining and leaves the
   page looking exactly as bright as before. This was the fix for "needs to be even darker".
2. **The grade.** The image is remapped percentile-by-percentile onto a target curve; the top
   anchor controls how loud the veining is. It went 105 → 58 → 51 across the client's
   "not too complicated" and "even darker" notes.
3. **The crop.** Chosen by scoring, not by eye — see the erosion test above.

### ⚠️ Things that will bite

- **One asset, five consumers.** `--marbleBG` feeds the page floor, the scrolled nav glass, the
  CTA card, the estimator modal and the trade prompt. That is deliberate (§4, "no second marble
  scale, so it can't re-open the patchwork complaint") — but **only the page floor gets the
  veil**. The other four already carry their own darkening and were checked individually.
- ⛔ **A single dominant vein reads as a CRACK.** That is what got the first stone rejected. Stone
  wants a *network*: several fine veins branching and crossing. This applies to any stone imagery
  added anywhere, not just here.
- **It must stay far from the card stone.** The review cards, the Why tiles and the helix backs
  all carry `marbleSVG()` at card scale. The floor is one enormous slab at very low contrast, and
  that difference in scale is what stops the two reading as a patchwork.
- ⚠️ **Do not re-attempt the procedural SVG route casually.** Several rounds went into rebuilding
  the floor with `feTurbulence`. The tone could be matched exactly, but not the perceived quality.
  Two findings worth keeping if anyone tries again: `feFlood` fills the **entire** filter region,
  so a mask that never returns zero alpha covers every pixel and no amount of opacity reduction
  will let the darks go dark; and `feFuncA type="gamma"` with a high exponent is the fix, because
  it keeps only the bright tail of the noise.
- The old SVG floor is in `index.html.pre-background.bak` if it is ever wanted back.

---

## 4m. ⭐ Real slab photography and the popularity order — 9–10 Aug (D44–D48)

**Full reasoning: `HANDOVER-2026-08-10-slab-photography-complete.md`. Operational summary:
`HANDOVER-2026-08-10-photography-start-here.md`.** This section is the map.

**All stones carry a real photograph** from Nile Stone and Next Stone Slabs (⚠️ written at 52; the range is **115** now). One map,
`assets/slabs/manifest.json`, feeds five surfaces: the wheel (`SLAB_TILES` → `stoneMarkup()`), the
estimator's picker, swatch and cut plan (`face(preset, seed, slug)`), the `/stones/` grid and all
52 stone pages (`build_stones.py` → `stone_face()`). SEO/material pages carry no stone tiles.

Rebuild with:

```bash
cd "Website Demo/stones/harvest" && python3 slabify.py && python3 match.py --prune
cd .. && python3 build_stones.py
```

### The things that will bite the next person

- ⛔ **Only `nile`, `nile-inv` and `next` may become a published tile** (`PUBLISHABLE` in
  `slabify.py`). Everything else in `raw/` is surveyed-only or excluded — see `LICENSING.md`.
- ⛔ **Two structural guards in `match.py`, and both caught wrong images that were already live.**
  An engineered quartz may only wear a *quartz* photograph (judged on the supplier's own section in
  `catalogue.json`, **never** the folder — Nile's stock system carries quartz too), and only **its
  own maker's** photograph, because a quartz name belongs to the brand. Natural stone is exempt
  from the second: there the name is the quarry's. Porcelain (`top-marazzi`) is refused outright.
- ⚠️ **The gates are calibrated by eye, not by taste.** Almost every threshold in `slabify.py`
  carries the measurement that set it and the failure that forced it. The recurring bug is *a
  measurement that condemns a stone for being what it is* — black stones read as "rooms", pale
  plain stones as "blown". **Read §2 and §3 of the write-up before changing any of them.**
- ⚠️ **`match.py --prune` or the extras deploy** — slabify writes 176 tiles, the site uses 52.
- ⚠️ **Natural-stone photography is indicative, and only the COPY says so** (D44). Every marble and
  granite page carries "You approve photographs of your actual slab before a single cut". That line
  is now the sole safeguard against a stock photo reading as a promise about veining.

### The selector's order (D48)

`POPULAR` in `index.html` — three lists, most popular first — and `fanOrder()` lays them out
**centre-out** around the landing slot, so the eleven cards on screen are the top eleven and rank 1
is dead centre. Sorted-not-fanned would put the least popular in the left wing. Opens on
**Calacatta Oro** / **Carrara** / **Absolute Black Extra**. `BEST` (the estimator's default) is
derived from the same table so the two cannot drift.
⚠️ **Evidenced editorial ranking, not TopCat's sales data** — replace the three lists when the
client has real figures; nothing else needs touching.

⛔ **QUARTZ NO LONGER FOLLOWS ANY OF THAT (D84, 10 Aug).** The client placed the quartz eleven
himself: **Azul Shimmer** lands, **Arabescato Elegance** and **Calacatta Oro** sit on its
shoulders, two brighter slabs beyond them, then one dark slab in each wing. So the quartz list is
an editorial and visual arrangement and **must not be "corrected" back to the trade ranking**.
Marble and Granite are untouched and still read exactly as described above.

⚠️ **Two things about that arrangement are load-bearing and easy to undo by accident:**

- **`PINNED` exists because of D74.** `spread()` interleaves ranks 1–10 by tone, and it used to
  leave quartz alone only because the quartz list held **no dark stone at all**. Put darks in it
  and spread() has both tones to merge, which pulls the two stones the client named off the
  landing card's shoulders. `PINNED={Quartz:FAN_HEAD}` fixes all eleven; the default of 1 is D74
  exactly, so Marble and Granite are unchanged. The tail is still spread automatically.
- ⛔ **Do not push the darks further out to "spread them more."** Depth is
  `filter:brightness(var(--dim))` on the faces: **0.34 at ranks 5–6, 0.18 from rank 7 outwards**.
  A white slab survives 0.18 and a black one does not — Marquina's median is **4**, so out there
  it renders at roughly 1/255 and reads as a hole in the fan. Ranks 5 and 6 are the outermost
  slots where a dark stone still looks like stone.
- ⭐ **The two darks were chosen by eye, not by darkness.** Royal Grey, Black Tempal and Nero
  Starlight measure darker and are flat fills with no pattern at card size; next to the veined
  whites they read as blank cards. Laurent Black (copper on black) and Marquina (white on black)
  read as stone. A dark slab only does the client's job here if you can see the stone in it.

---

## 4o. ⭐ THE COPY ROUND — 10 Aug, second session (D56a–D65)

**Nothing in this section has been seen by the client in its finished state**, other than the
three faults he found himself. Backups: `HANDOVER.pre-10aug-copy-round.bak.md`,
`stones/catalogue_expanded.py.pre-mm.bak`, `.pre-colour.bak`, `stones/build_stones.py.pre-mm.bak`.

### What was actually wrong, and why none of it was visible

⚠️ **Every defect this round rendered perfectly.** A page showing `322 x 162 mm` looks no
different from one showing `3220 x 1620 mm`. "The pattern is consistent across the slab" reads
like ordinary marketing. That is why the checks below fail the BUILD rather than warn.

| What | Scale | How it is now caught |
|---|---|---|
| Script-assembled blurbs, one factually reversed | 63 of 115 | `descriptions.py` + build raises if a stone has no entry |
| Slab size printed in cm under an "mm" label | 22 pages | `slab_facts()` raises; verify check 6 |
| Unkeepable promises in the shared facts block | 355 hits | verify check 7 |
| Pages titled "Marble" for a quartzite | 27 pages | `shown_mat()` |
| Dead pages for stones we no longer sell | 4 pages | verify check 5 |
| Colour data contradicting the photograph | 20 stones | reviewed by eye against every tile |

### ⛔ The rules for writing stone copy, which is the part most likely to be undone

1. **Colour and pattern. Nothing else.** No sealing, heat, durability, joints, what it suits,
   what it matches. The client cut every one of those from a draft that had them.
2. **Write from the tile we ship**, with the photograph open. ⚠️ If a tile is recropped, its
   description is stale — `descriptions.py` says so at the top and it applies to your own work.
3. ⛔ **Never research an engineered quartz on the open web.** The name is a manufacturer's
   marketing name; another maker's slab answers to it. Same reasoning as D45.
4. ⛔ **Neither supplier publishes any copy.** Checked directly on 10 Aug. Nile's `description`
   field is the string "Click on the image to enlarge" for all 138 products; Next Stone Slabs has
   no product pages. `harvest/supplier_copy.py` re-checks this if you doubt it. Do not go looking
   again expecting a different answer.
5. **Absolutes are the tell.** "consistent", "exactly", "all it asks for", "never stains". A
   comparative is defensible, an absolute is not.

### The pipeline, in order

```bash
cd "Website Demo/stones"
python3 apply_catalogue.py            # inject MATERIALS into ../index.html
python3 harvest/match.py --prune      # name -> tile, manifest.json + SLAB_TILES
python3 harvest/similar.py            # ⭐ NEW: measures tiles -> similar.json. BEFORE build.
python3 build_stones.py               # 115 stone pages + the collection grid
python3 harvest/verify.py             # ⛔ the gate, seven checks
```

### ⚠️ Traps this round added to the pile

- **`upscale.py --install` does not populate `_upscale/installed/`.** Three re-upscaled tiles
  would have reverted on the next full run. The restore set is synced now and covers all 94.
- **A pin must be keyed on the stem the MANIFEST names.** Arabescato Classico's catalogue row
  says `tile='arabescato-classico-ft'` but the manifest ships `arabescato-classico`; pinning the
  wrong one does nothing, silently.
- **Explanations belong in Python, never in an HTML comment in the template.** A comment quoting
  a banned phrase ships in the source of all 115 pages.

---

## 4p. ⭐ THE RANGE IS "MARBLE & QUARTZITE" — 10 Aug, third session (D66–D68)

**Not seen by the client.** Backups: `index.html.pre-quartzite-label.bak`,
`stones/build_stones.py.pre-quartzite-label.bak`, `stones/apply_catalogue.py.pre-quartzite-label.bak`,
`sitemap.html.pre-quartzite-label.bak`, `HANDOVER.pre-quartzite-label.bak.md`.

### The problem D65 left behind

D65 made each stone page name the true rock. It was right, and on its own it made the **journey**
worse: the customer presses **Marble**, opens Fusion Black, and is told **Quartzite**, with nothing
on either screen connecting the two. The client found it immediately.

⚠️ **The range was never mostly marble.** 18 of the 45 are marble. **26 are quartzite** and one is
travertine. A tab labelled "Marble" was wrong about the majority of what sat under it.

### ⛔ Why the stone's name was not the thing to soften

Both literal readings of the instruction were tried on paper and rejected:

| Reading | Why not |
|---|---|
| "Available in marble **and** quartzite" | Not true of any single stone, and unfixable. Taj Mahal is one rock. Every UK merchant sells it as quartzite, and one of them sells an engineered **quartz** imitation under the same name — the confusion is real and it runs the other way |
| "Just say **natural stone**" | Throws away the word the customer searches, the reason the stone costs what it does, and re-opens D65. It would also attach marble's care copy — etches with acid, softer — to a stone that does neither |

⭐ **The honest version of what the client asked for is at RANGE level, and it is what the trade
does.** The stone always states what it is. The range around it is named for what it contains.

### Where each name is used now

| Says the **range** — "Marble & Quartzite" | Says the **true rock** — Quartzite / Travertine / Marble |
|---|---|
| wheel rail tab, collection filter chip | wheel readout, collection card tag |
| estimator material tab, its picker's tabs and count | estimator's picked-stone line, enquiry chip |
| the stone page's new **Range** row | page title, meta, hero tag, eyebrow, "About …", image alt |
| sitemap stone column | |

⛔ **`mat` is untouched.** It is still the browse-and-pricing key, so no price, filter, POA
behaviour or deep link moved. Only printed words changed.

### ⚠️ Things that will bite

- ⭐ **To change the wording, change it in TWO places**: `RANGE_LABEL` in `stones/build_stones.py`
  and `MAT_LABEL` in `index.html`. Everything else reads from them.
- **`kind` is emitted per wheel entry by `apply_catalogue.py`**, which imports `shown_mat` from
  `build_stones.py` so the wheel and the pages cannot disagree about what a rock is. Re-run
  `apply_catalogue.py` after any catalogue change or the wheel keeps the old kinds.
- ⚠️ **"Marble & Quartzite is priced by hand" does not parse.** A compound range takes a plural
  verb, so the POA heading's subject carries its own verb in `POA_HEAD` rather than being
  assembled from the label. Any new heading built by concatenation will hit this.
- ⚠️ **The estimator tab wraps to two lines** and that is intentional — the label needs 180px and
  a quarter of that rail is 125px. Measured cost to the section: **6px**. All four tabs stay the
  same size and the wheel rail's 190×46 blocks are unchanged.
- **`titled_mat()` drops the rock from a title when the name already carries it**, so Travertine
  Romano Classico is not "Travertine Romano Classico Travertine Worktops". Scanned: 0 of 115
  titles now repeat a word.
- ⚠️ **`10cm` in Judy Z.'s review trips the rule 11 scan and must stay.** It is a real customer's
  own words. Do not edit a review to satisfy a scan — check the hit before acting on it.

### Measured

115 stones, 115 photographs, 115 pages — `verify.py` all seven checks ✅. **2,716 internal links,
0 broken.** Inline JS parses, JSON-LD valid, **zero console errors**. No horizontal scroll at 1440
or 375. Rule 1 scan clean; rule 11 scan clean apart from the review above and a base64 blob.
Card tags across the marble range: **18 Marble, 26 Quartzite, 1 Travertine**.

---

## 4q. ⭐ THE REAL LOGO — 10 Aug, fourth session (D69)

**Not seen by the client.** Backups: `index.html.pre-real-logo.bak`, and a `.pre-real-logo.bak`
beside each of the four builders.

### What was actually there before

Not a logo file. The mark was **hand-rebuilt as inline SVG** (a 200×250 viewBox of two bars,
two crossing strokes and a diamond) and the wordmark was **live text** — TOPCAT in Cinzel,
WORKTOPS in Montserrat, tracked out to fake the lockup. It had the right silhouette and was
wrong throughout. ⚠️ **Being type, it could also drift**: any webfont fallback changed the
"logo".

Three further things fell out of it, all of which the file-based version removes:

- The favicon was the same wrong drawing **URL-encoded into the `<head>` of all 150 pages**.
- The landing page footer's mark filled itself from a gradient defined inside the **nav's**
  inline SVG. It rendered only because the two happened to share a document.
- The generated pages set the wordmark in `--bone`, so they carried a **white** TOPCAT while
  the landing page carried gold. Nobody had noticed, because the two are never on screen
  together.

### Where each lockup goes

| Surface | Lockup | Height |
|---|---|---|
| Nav bar, landing page | horizontal | 34px |
| Nav bar, all 149 generated pages | horizontal | 32px |
| Footer, landing page | **vertical** | 104px |
| Footer, all generated pages | **vertical** | 96px |
| Favicon, everywhere | icon, squared | 16–32px |

⭐ **The client's instruction was explicit: horizontal in the nav, vertical in the footer.**

### ⚠️ Things that will bite

- ⛔ **Height is the only dimension ever set.** Width follows the intrinsic ratio (horizontal
  4.5053:1, vertical 1.0664:1). Setting both squashes it, and a squashed logo at 34px reads as
  subtly wrong rather than obviously broken, which is the harder fault to catch.
- ⚠️ **The viewBoxes are retightened to the ink** and the supplied originals are kept beside
  them, untouched, in `assets/brand/`. The supplied horizontal carries 36 units of air on the
  left against 21 on the right; left as-is it throws the nav's alignment out by ~1.6px and
  makes `height:34px` draw a 27px logo. Bounds were measured with `getBBox()` in a real
  renderer, not estimated from path data, because the wordmark is all curves.
- ⭐ **`services/service.css` dresses every generated page.** Its `.brand` block is four lines
  and it covers all 149. That is one edit, not four.
- ⚠️ **`assets/brand/` also holds `make_brand.py`**, which rebuilds the derived files from the
  originals. If new artwork arrives, re-measure rather than reusing the numbers.
- ⚠️ The link is the accessible name (`aria-label`); the image is decorative and takes `alt=""`
  so the brand is not announced twice.

### Measured

Bar height **unchanged at 78.5px**; nav lockup 153.2×34 against the old 155×35. Footer lockup
110.9×104 against the old 150.6×102. **150 of 150 live pages** carry all three assets, every
request 200. **0 broken internal links** over 6,948 checked. `verify.py` all seven ✅. No
horizontal scroll at 1440 or 375; at 375 the lockup ends at 173px with the burger at 313px.
Inline JS parses, JSON-LD parses, rule 1 and rule 11 scans at their documented baseline.

### ⛔ Still missing, and worth asking for

The client holds the logo in a **darker gradient, black, white and solid champagne gold**. Only
the gold gradient is on disk. ⭐ **White is the one that will be felt first** — it is what a
logo over a photograph needs, and what a social share card needs. Related gaps, both of which
predate this work: there is **no `apple-touch-icon`** (iOS wants a 180px PNG on a real
background, not a transparent SVG) and **no `og:image` anywhere on the site**, so a link shared
to WhatsApp previews with no image at all.

---

## 4r. ⭐ THE STONE-CHOOSING ROUND — 10 Aug, fourth session (D70–D73)

**Not seen by the client.** Backups: `index.html.pre-real-logo.bak`, `stones/stone.css.pre-floor.bak`,
`stones/build_stones.py.pre-source-note.bak`, `assets/slabs/blue-roma*.webp.pre-topcrop.bak`,
`stones/harvest/upscaled.json.pre-topcrop.bak`.

### ⛔ The `hidden` bug, three times in one codebase

The single most reusable thing in this session. `[hidden]{display:none}` is a **user-agent** rule,
so **any author rule that sets `display` beats it** and the element never hides:

| Element | What it cost |
|---|---|
| `.st-badge` | A bare gold dot on screen with no refinements active. **Found, fixed, commented** |
| `.st-drawer` | Three lines below it in the same file. The Refine button did nothing visible for its whole life, and the filters pushed the stones off the first screen |
| `.est-lm` | The estimator's metres field appeared before a profile was chosen, contradicting its own code and the comment beside it |

⭐ **The lesson is not the CSS, it is that the fix was already in the file.** The `.st-badge`
comment even states the general rule. It was written as a note about one element instead of being
applied to its neighbours or turned into a check. There is a scan for the pattern now; it returns
clean.

⛔ **Any element you hide with `hidden` and give a `display` value needs its own `[hidden]` rule.**

### The floor on the stone pages

Same Portoro photograph, same veil, same measured strength as §4l. ⚠️ **Root-relative url** (these
pages sit a level down) and ⚠️ **`main` and `body > footer` both need `position:relative;z-index:1`**,
because on generated pages they are siblings rather than nested as they are on the landing page.
⛔ Do not lift them with `body > *`, that would break the header's `position:sticky`.

### Blue Roma, and the scan that came out of it

Cut at row 60 of 1600, measured: saturation runs 26–34 across rows 24–44 (the slab's warm cut edge,
with racking above it) and settles at 5.5 from row 56. Re-squared to 1540, centred. **No credits** —
pixels were removed from an already-upscaled tile, not added.

⛔ **The restore set was synced first.** `_upscale/installed/` holds the *pre-fix* tile and the
documented post-run restore copies it back; Aqua Gucci and Calacatta Gold Oro were each "fixed"
twice before this was understood.

⭐ A new scan measures every tile's top and bottom 5% against its own body. **11 bands flagged
across 115, all reviewed at 330px, all genuine stone.** Blue Roma was the only real fault, which is
the useful result: the range is clean and now there is a cheap way to re-ask the question.

### The sourcing line

⭐ **It existed in four places and none of them were where somebody runs out of options.** Now on
the wheel, on every stone page (closing "more to consider", where a person is already hunting for
an alternative), in the estimator's picker (appended unconditionally, so it is there when a search
returns nothing), and lifted out of the collection's lede onto its own line.

⚠️ In the estimator's picker it is a `<button>`, not an `<a>`: the dialog's Tab trap collects
`modal.querySelectorAll("button")`, and it closes the dialog before jumping, or the page scrolls
away behind an open modal.

⛔ **Hedged on purpose** — "usually", "where we can". §2 rule 12, and it is a promise about other
people's quarries and stock.

### Measured

`verify.py` all seven ✅. **7,064 internal links, 0 broken.** Inline JS and JSON-LD parse. Rule 1
scan clean, rule 11 at its documented baseline, no absolutes in any of the new copy. `#stones`
still inside one screen: **744px at 1440×900, 539px at 1366×610.** Refine drawer 0 → 179 → 0px.
No horizontal scroll at 1440 or 375.

---

## 5. The stone catalogue — the client's real range

⚠️ **Written at 52 stones: Quartz 24, Marble 20, Granite 8.** The range is now **115 — Quartz 50, Marble 45, Granite 20** (D54, D55b). The reasoning below still holds; the counts do not.

- **`stones/catalogue_source.py` is the source of truth.** Edit that, then re-run the injector,
  because ⚠️ **the catalogue is duplicated** into `MATERIALS` (index.html, the wheel) and
  `STONE_LIST` (`stones/build_stones.py`, the collection and the 52 stone pages). If the two
  disagree, the same stone renders differently in different places.
- **Nile Stone** runs an Angular front end over a JSON API:
  `POST /secure/inventory/{category}/{page}/{id}`, the id being the number in the product slug.
  Thickness, block number and real slab sizes came off their live inventory. Nine stones returned
  `total_items: 0` (no stock that day) and carry names only, no invented specs. **Next Stone Slabs**
  publish no specs; their ten names were verified against `/quartz/`.
- ⚠️ **Nine stones filed under Marble are QUARTZITE** (Taj Mahal, Cristallo, Patagonia, Lemurian
  Blue, Fusion Blue, Fusion Black, Bianco Eclypsia, Blue Roma, Belvedere) and one is **travertine**.
  The client groups them under Marble, which is fine, but marble care copy says the stone *etches*
  and is *softer*, which is **plainly wrong for quartzite**. Each carries a `facts=` override that
  replaces kind/care/wear on its own page. **Never let a quartzite inherit marble care copy.**
- The colour filter gained **Blues and Greens**, because the real range has them.
- Stone pages show **finish, typical slab size and thickness**, omitted rather than guessed.
- ⚠️ If the catalogue ever shrinks, **prune `stones/*.html`** or dead pages linger. 34 were deleted
  on 6 Aug.
- ~~**Still to do: real photography.** Every stone renders procedurally from a preset + seed.~~
  ✅ **DONE 10 Aug — all 115 stones carry a real supplier photograph, and a description written from it (D62).** The procedural `marble()`
  render is now the FALLBACK only, used where a stone has no tile in `assets/slabs/manifest.json`
  (currently none). Keep it: it is vector, sharp at any size, and it is what makes it safe to
  refuse a doubtful photograph. `preset` and `seed` are still carried on every stone for that
  reason, and `derive.py` keeps them resembling the real slab.
  See `HANDOVER-2026-08-10-slab-photography-complete.md`.

### The slab wheel

- ⚠️ **Two lists, on purpose.** `SLABS_UNIQUE` is what the filter matched and is what the
  "Showing 8 of 8" count reads. **`SLABS` is the belt the wheel turns, which is that list REPEATED
  until it is wider than the screen.** On desktop the pitch comes from the rail geometry and
  ignores how many stones there are, so granite's eight stones made an 880px belt inside an 1182px
  wheel and the fan visibly stopped short of both edges (client, 6 Aug). `makeBelt()` repeats the
  range instead. **Do not fix this by stretching the pitch** — at 1920 an eight-stone belt would
  need a step wider than the card and the shingle overlap would break into separate floating cards.
  Verified: all three materials span the screen at 1182 and 1920, granite holds through a full
  rotation, and a filter down to one stone still fills the wheel.
- Below 1121px `metrics()` already carries its own `(W+cw*2)/n` coverage term, so `makeBelt`
  deliberately does nothing there (repeating would feed back into that term).
- The wheel is full-bleed, the two rails are docked to the viewport edges and must stay a matched
  set (same width, height, type, one even gap). Depth is **brightness, never opacity**.
- `--railW` / `--railBlockH` live on `.stone-stage` and `metrics()` **reads** `--railW`. Never
  hardcode it.

---

## 6. The estimator

The most worked-on part of the site. Read this whole section before touching it.

### 6.1 The price is a LOOKUP, not a build-up

```
low  = BRACKETS[worktopSlabs][island ? 'island' : 'solo'][0] + (removal ? 200 : 0) + metres*150
high = BRACKETS[worktopSlabs][island ? 'island' : 'solo'][1] + (removal ? 200 : 0) + metres*300
```

| worktop slabs | no island | with island |
|---|---|---|
| 1 | £2,000 – £2,500 | £2,300 – £2,800 |
| 2 | £3,000 – £3,600 | £3,850 – £4,300 |
| 3 | £3,850 – £4,300 | £4,350 – £5,000 |
| 4 | £4,350 – £5,000 | £5,100 – £5,550 |

- ⚠️ **The index is the slabs the WORKTOPS need, NOT counting the island.** The client's own figures
  force this: 2-with-island is exactly the 3-slab solo bracket, and 3-with-island is exactly the
  4-slab solo bracket. That is "an island adds a slab" already priced in. `compute()` packs
  **twice** — once with everything for the drawing, once with non-island rows for the price. The
  ticket may honestly say "3 slabs" while the bracket read was "2 + island". Intended.
- ⚠️ **The 1-slab island row breaks that pattern.** Built exactly as given. Flagged, unanswered.
- **Past four worktop slabs there is no bracket** → "Priced by hand". Do not extrapolate a fifth.
- ⚠️ **The slab count IS the price**, so a wasted slab is an £850 error. `pack()` runs the same
  guillotine packer over four sort orders and keeps the fewest-slab result. Do not collapse it.
- `round50` is gone. The brackets are exact client numbers.

### 6.2 Marble, granite and now porcelain have no calculator

`MATS[mat].poa` is true for all three (**porcelain added 7 Aug, §4c**). The panel is replaced by a
POA block, the ticket draws **one whole slab** with a name plate, the stamp reads "By appointment"
and the ticket's CTA is hidden (the POA panel already carries a CTA and the phone number). ⚠️
Porcelain also carries `noCat:true`, so its stone picker is hidden and its POA reason differs (the
job varies, not the block) — see §4c.

⭐ **The owner's actual reason, and it is in the copy:** not mystique about blocks and craft, but
that **the material cost swings enormously between one stone and the next and between
distributors**. Two customers asking for "marble" can be quoted wildly different numbers.

### 6.3 The rows

`label · length · width · thickness · used for · remove`, all in mm.
- **One row is one piece.** Quantity was removed; "depth" is called **width**.
- **No native `<select>` anywhere.** The OS dropdown is the one thing that cannot be styled.
  Thickness is a **segmented pair**; "used for" is **our own listbox** (`usePop`, appended to
  `<body>`, `position:fixed`, placed off the trigger's rect, flips above when there is no room,
  clamps into the viewport, closes when the trigger scrolls away, full keyboard support).
  ⚠️ Outside clicks close it on **`pointerdown` in the capture phase** — a plain click fires after
  the row rebuild and lands on whatever moved under the cursor.
- **"Used for"** is the client's five plus two the model needs: `Worktop run` (their list has no
  word for a plain run) and **`Island`, which is load-bearing** because it picks the bracket column.
- **Upstands clamp width to 100–150mm** and rebuild the row so the field's own min/max follow.
- ⚠️ **First-run state: ONE row, no shape chip preselected.** It used to open on a preset L-shape,
  which is somebody else's kitchen the visitor has to undo first.

### 6.4 Extras and edging

⚠️ **No prices beside the toggles.** Removal (£200) and detailed edging (£150–£300 per linear metre)
still move the number, but they move it **in the estimate**, and `#estAdds` names them in words. A
running bill beside the switches turns the panel into a menu and invites haggling.

Edging flow: tick → **choose a profile** → *then* the metres field appears. Nothing is added until a
real number is entered. The **18 profiles are drawn as SVG cross-sections** from the client's chart.
⚠️ The Ogee needs its convex-then-concave S or it is indistinguishable from the Cove.
⚠️ "Waterfall" is both a profile and an extra. Same word, two things, both on screen. Flagged.

### 6.5 The stone, and uploads

- The chosen stone is **never blank**: whatever the wheel or a deep link handed over, otherwise
  `BEST[mat]` (currently `calacatta-fantastico` / `calacatta-gold-oro` / `absolute-black-extra`).
  ⚠️ Those are slugs — if the catalogue changes, check them.
- The picker shows the full range per material with **search by name**, and choosing one dispatches
  `topcat:stone` so the enquiry chip follows. The thread runs both ways.
- ⚠️ `.sp-face` needs `display:block` — it is a `<span>` and inline boxes ignore `aspect-ratio`.
  ⚠️ Swatches use `background-size:100% 100%`, not `cover`; a cover-crop of a pale stone at 44px
  lands on a plain patch and reads as a blank card.
- **Uploads** (`TC_UP`, top level, just above the estimator IIFE): one shared store behind **three**
  controls — the POA panel, the quartz calculator (`.compact`) and the enquiry form, where it lives
  behind a disclosure (§4a). Attach a plan anywhere and it is already on the form. 8 files, 10MB
  each, `pdf jpg jpeg png heic heif webp gif doc docx dwg dxf`. Rejections are named out loud.
  Blob URLs are revoked on remove. Plus the owner's **supplier-link field**, for a slab they have
  already found.
- The faded stone wash is on the **cutting-plan side only**, inside the card. ⛔ **No bright band
  across the top of anything** — that was tried and rejected outright.

### 6.6 ⭐ NEXT: the product-type selector

The client wants a "what are you looking for?" step so the estimator speaks to every buyer.
**The finding that shapes it: they have only ever priced kitchens.** So:

**Priced (calculator + brackets, quartz only):** kitchen worktops, island, splashbacks, outdoor,
commercial. Marble and granite stay POA.

**Enquiry-led (no invented numbers):**

| | measurements | material | notes |
|---|---|---|---|
| **Vanity tops** | PDF **or** manual | quartz colour picker, or photo/link for marble and granite | edging extra at £150–300/lm |
| **Fireplaces** | **PDF only**, no manual entry | photo or link, **marble and granite only, no quartz** | the client's call |
| **Tables** | PDF **or** manual | quartz picker, or photo/link | shape selector including custom |

Plus a way out for anything else, which the `.est-help` band already does ("Not quite your space?"
→ Talk to our team / Call).

**Ask Nick for:** any brackets at all for vanity tops, fireplaces and tables; whether 20 vs 30mm
should change the price (it currently does not, because his table does not vary by it); and whether
the odd 1-slab-with-island bracket is deliberate.

### 6.7 The eleven guarded defects

All still hold, and every control built since was built to the same rules: jointed segments never
rotate; `hasOwnProperty` not truthiness on any preset from a URL; functional copy is `--muted` never
`--faint`; the rolling price writes `#estPrice` per frame while `#estPriceSR` is written **once**
per recompute; `aria-pressed` on every tab and chip; focus is handed on after a row is removed;
`+ Island` honours the row cap; an emptied piece field restores the model's value (the linear-metres
field is deliberately the exception); `#estBoard` has `role="group"`.

⚠️ **The `hidden`-attribute trap:** `.est-modal`, `.est-stats`, `.est-pop`, `.est-adds` and
`.est-out .btn-gold` all set their own `display`, which **beats** the `hidden` attribute's UA
`display:none`. Each needed an explicit `[hidden]{display:none}`. Check any new element you toggle.

⚠️ **Dialog open state is a FLAG, never the `.open` class** — the class lands on the next frame, so
an Escape in the same tick would read the dialog as shut.

---

## 7. Open items

0. ⭐⭐ **THE ENQUIRY FORM HAS NO BACKEND, AND IT CARRIES FILE UPLOADS.** The biggest gap between
   demo and working site. `buildEnquiry()` in the CTA IIFE assembles everything into a `FormData`
   (details, stone, supplier link, up to 8 attachments) and has nowhere to POST. Needs a handler
   that accepts `multipart/form-data` — Netlify Forms with uploads is the obvious fit since the
   site has been deployed there before. Decide where enquiries land (info@topcatworktops.co.uk) and
   whether attachments are emailed or linked. Until then the form says plainly that nothing was
   sent, which is the only honest thing it can say.
1. ⚠️ **Cannot push to GitHub from this machine.** No `.git`, no `gh`, no credential helper. A fresh
   `git init` shares no ancestry with `github.com/lukecopley6/Topcat-Worktops`, so a push would need
   `--force` and would destroy their history. Confirm with the repo owner first.
2. **The estimator product-type selector** — §6.6. The live job.
3. ✅ **Real stone photography — DONE for all 115 (D44–D48).** Written when the client was sending images for 52. Biggest conversion
   cap on the site. ⚠️ It is now also an SEO dependency: the location pages were built to assemble
   themselves from real dated projects, which is the one asset no competitor in this vertical has.
   Photograph every install with its town and date.
4. **Real project photos and names.** The eight `PROJECTS` entries reuse service images with
   invented names and places.
5. **Photography still owed** — ⚠️ **the shot list CHANGED on 9 Aug (D34, D35), re-read it.**
   - ⭐ **Three head-and-shoulders portraits of Nick, Rimsha and Ali**, and they are now
     **1:2 standing crops, not squares** — they are the bottom half of the About collage
     (`.ac-p`), not thumbnails in a strip. This is the most visible unfinished thing on the page
     after the stone photography, because the three tiles are a third of the composition. They
     are the only remaining marked placeholders on the landing page. Drop an `<img>` in above
     each `figcaption` and the plate, scrim and name are already right.
   - **One feature shot for the Why mosaic** (`.wy-p`): the whole team together, or a finished
     install. ⚠️ That slot currently holds `assets/team/team.jpg` as a **stand-in** so the
     section can be judged (client's instruction). It no longer *looks* unfinished, so it is
     easier to forget — it is still a placeholder.
   - **The three About work photographs** (handshake, samples, fitting) are AI-generated
     placeholders in `assets/team/`. Keep the shapes, swap the images.
   - ⚠️ Do **not** commission replacements for `mason.jpg` or `polishing.jpg`. They were dropped
     on 9 Aug because they imply in-house fabrication, which D21 forbids.
6. **Agreed order of work (client, 30 July):** finish desktop sections, **then** the mobile and
   tablet pass across the whole site, **then** real photography and copy. Mobile is genuinely
   untouched: at 375px the sections stack and run well past one screen, which is expected, but
   nothing there has been designed yet. ⚠️ The hero's known mobile issue is in §4b.
7. **The estimator section overruns the fold**, and always has. Head→`.est-grid` is ~878px against
   an 824px budget at 1440×900; it was 918 before the pricing session. ⚠️ **The driver is the
   right-hand ticket, not the input panel** — closing the rest means shrinking the drawn slab,
   which is a design decision rather than a tidy-up.
8. **A clamp-floor sweep has STILL not been done sitewide.** Violations turned up by accident on
   6 Aug (§4) and again on 7 Aug, when four flat measures in `.cta-form` pushed the contact card
   37px past one screen the moment anything was added below the submit button (§4g). Both times
   they were found by a section breaking, not by a sweep. Grep every `clamp(` **and every flat px
   vertical measure** in the style block, work out its value at a 610px viewport, and fix any
   where the floor wins. ⚠️ Flat px measures are the worse half of this and the easier half to
   miss, because they do not look like clamps at all.
9. **Copy the client has not seen:** the `/trade/` page, `.trade-prompt`, `PROC_DETAIL`, and the 52
   stone blurbs are all ours. Worth a read-through with them.
10. **Confirm live paths before go-live.** Canonical and OG on the service, stone and trade pages
    assume `/services/<slug>.html`, `/stones/<slug>.html` and `/trade/`.
11. **V2 is well behind V1** and has had no work for many sessions beyond the porcelain strip-out.
    If the client picks it up, expect a sizeable catch-up.
12. **Closed, do not reopen unprompted:** ornate dividers (three designs rejected); the scroll-tied
    process animation (⚠️ `#process` only — About and Why are scroll-tied on purpose, §4a); the
    fourteen-slab flip FAQ; the Why 3×3 mosaic (§4a); stone veining inside the nav CTA; the
    standalone estimator page; **the hero eyebrow and the thin outline icons (§4b)**.
13. **Where the client is up to.**
    - **FAQ — approved twice.** "That's perfectly fine" on 6 Aug at eight questions, and again on
      9 Aug on the four-column contents page. ⛔ It is settled. Do not redesign it unprompted.
    - **About** — rebuilt 9 Aug to their brief (D34), **not seen since**.
    - **Why** — the mosaic had one round of feedback applied on 7 Aug; the tiles were designed
      on 9 Aug (D35), **not seen**.
    - **Contact block** — seen once, no comment; the foot changed again on 9 Aug (D36), not seen.
    - **Hero** — pass one seen and sent back; **pass two has NOT been seen.** It carries one open
      recommendation ("Request a call", §4b) and one live disagreement (the supplier-discount
      reason, D2).
    - **The whole SEO layer (§4d)** — not seen at all.
    - ⚠️ The pacing of the two scroll builds, and the nav bar's flash, can only be judged by eye,
      so watch them scroll it rather than describing it.
14. ✅ **DONE 7 Aug.** The last `hello@` in the homepage JSON-LD, and the `areaServed` list that
    named only Hertfordshire towns, were both corrected. So was the landing page title, which
    still said "St Albans & London".
15. ⚠️ **V2 (`/v2/`) was not touched by the fabrication rewrite** and still carries in-house
    claims and dead schema. It is dormant and unlinked from the main journey. If it is ever
    revived it needs the same pass V1 got, see §4d.
16. **The client has not seen most of the 7 Aug work.** That is the hero second pass (§4b),
    porcelain, the Trade nav and the region line (§4c), the entire 25-page SEO layer (§4d), the
    animation changes and the helix's new card backs (§4e), the sitemap (§4f), the FAQ redesign
    (§4a) and the enquiry form's new foot (§4g). Show the SEO pages by walking a material page, a
    guide and a town page, in that order, because the town page only makes sense once they have
    seen the pattern. ⚠️ **The animations can only be judged by eye and only in a real browser**
    — watch them scroll it, and see §4a on why the Browser pane cannot show them.
    ⚠️ The FAQ and the helix back faces each went through client feedback rounds inside the same
    session, so what is on the page is the third iteration of both. Show them the current state
    rather than describing the journey.
17. **The client has not seen ANY of the 9 Aug work** — the reviews CTA (§4j), the nav bar's
    flash (§4h), the About rebuild and alignment (§4a), the Why tiles in stone (§4i), the
    enquiry form's foot (§4g), the FAQ and CTA spacing (§4a), the footer cut (§4k) and the new
    page floor (§4l). ⚠️ They gave four rounds of direction on the floor without seeing the
    finished state, so **show that one first** — everything else sits on top of it.
    ⚠️ **Show About and Why by scrolling, not by screenshot**: both sections' builds are motion,
    and the nav flash only exists in the moment you leave the top of the page.
    ⚠️ Say out loud that the three director portraits and the Why feature shot are placeholders —
    the Why one now holds a real photograph and no longer announces itself.
    ⚠️ **Walk the footer on a generated page as well as the landing page.** The cut went into all
    five copies and the sub-sites are where a drift would show first.
18. ⭐⭐ **BUILD `/services/` — the client now assumes it exists.** ⚠️ Upgraded from a note to a
    recommendation on 9 Aug. Describing the footer, the client said Services should be one link
    and "when they click on that it goes to the services page, and all that section of the site".
    That page does not exist; the link points at the landing page's services section instead
    (§4k). It is also the one page family without a hub, and a hub is the natural landing page
    for the broader-than-kitchens positioning D23 asked for. **It needs copy the client has not
    seen, which is the only reason it was not built.** Offer it next; repoint the footer link in
    all five places the day it lands.
19. ⚠️ **There is no `/services/` index page.** Materials, guides and areas each have a hub;
    services does not, and building the sitemap is what surfaced it (§4f). Not a break — the six
    pages are linked from the landing section, the footer and every "Not only kitchens" block —
    but it is the one family without a hub, and a hub is the natural landing page for the
    broader-than-kitchens positioning D23 asked for. Needs client copy, so it was not built.

---

## 8. Verification gotchas

- ⛔⛔ **THE BROWSER PANE CANNOT TAP ANYTHING IN MOBILE-EMULATION MODE, AND IT FAILS SILENTLY —
  12 Aug 2026.** At any viewport under 768px the pane translates `computer left_click` into touch
  events, and **the finger never lifts**. An event probe on the window recorded `pointerdown` and
  `touchstart` and then **nothing** — no `touchend`, no `mouseup`, no `click` — and the call itself
  dies after a 30s timeout with the misleading message *"The pane may be stuck (modal dialog,
  navigation hang, or unresponsive renderer)"*. ⚠️ **Nothing on the page is clickable under
  automation at phone width**, so this reads exactly like "the feature I just built does not work".
  ⭐ **THE CONTROL EXPERIMENT IS THE WHOLE TRICK, AND IT TAKES ONE MINUTE**: tap something that
  long predates your change and needs no JavaScript — the sticky bar's `Get a quote`, a plain
  `<a href="#cta">`. It does not navigate either, and the hash does not change. That proves the
  environment, not the page. ⭐ **HOW TO ACTUALLY VERIFY A PHONE-ONLY TAP HANDLER**: resize to a
  width ≥768 so real mouse events are delivered, then force the phone branch on with an injected
  `<style>` setting the mode custom property (`--svcMode`, `--hxMode`, `--galMode`) and fire a real
  click. That exercises the genuine handler with genuine user input. ⚠️ A `dispatchEvent(new
  MouseEvent('click'))` does run the handler and IS worth doing, but it only proves the handler
  exists — never that anyone can reach it (§10). ⚠️ **And `location.href` read in the same tick as
  the assignment still returns the OLD url** — navigation is asynchronous, so a same-tick check
  reports failure on a navigation that is about to succeed. Read it in a later call.
- ⚠️ **The Browser pane's console is unreliable** — it replays stale entries from earlier loads,
  including from files you have since deleted, with line numbers that do not match. **Do not trust
  it.** The reliable method: write an instrumented copy that installs its own error handler before
  the page scripts, load that, scroll the whole page, and read `window.__errs`:
  ```
  python3 -c "…insert <script>window.__errs=[]…</script> right after <head>…"
  ```
  Delete the copy afterwards.
- ⚠️ **Syntax-gate the inline JS BEFORE loading the page — a missing comma cost a debug loop on 7
  Aug.** Editing the `FAQS` array left the previous last entry without a trailing comma, and the
  whole script block threw `Unexpected token '{'`, blanking the FAQ. The pane's console lied about
  where. The gate: extract every inline `<script>` that is not `application/ld+json`, concatenate,
  and `node --check`. Also `json.loads` every `ld+json` block. Both are one Python snippet and they
  catch this class of error before the browser ever runs:
  ```
  # non-json inline scripts -> node --check ; ld+json blocks -> json.loads
  ```
- ⚠️ **Anchored find-and-replace in this file needs an ORDER ASSERTION.** `index.html` is ~886KB
  and short CSS anchors repeat. A two-marker slice whose start index lands *after* its end index
  silently duplicates everything between them — that happened on 6 Aug and added 121KB of
  duplicated CSS and markup before it was caught. **Always `assert start < end`, print the byte
  delta, and diff the selector list against the backup afterwards:**
  ```
  diff <(grep -o '^\s*\.[a-zA-Z][a-zA-Z0-9_-]*[{, ]' OLD.bak | tr -d '{, ' | sort -u) \
       <(grep -o '^\s*\.[a-zA-Z][a-zA-Z0-9_-]*[{, ]' index.html | tr -d '{, ' | sort -u)
  ```
  Anything unexpectedly deleted shows up immediately.
- ⚠️ **The pane runs HIDDEN, so `requestAnimationFrame` is paused.** `document.visibilityState`
  is `'hidden'`, and a rAF counter installed from `javascript_tool` reads **zero frames** between
  two calls. Anything on a time-driven clock — the Why mosaic, the services helix entrance — will
  appear frozen part-built and you will "find" a bug that is not there. The method that works:
  copy the page, insert a shim directly above `</head>` that swaps rAF for a queue you drain
  yourself, then step the timeline by hand and read the DOM after each step.
  ```
  window.__q=[];
  window.requestAnimationFrame=function(cb){window.__q.push(cb);return window.__q.length;};
  window.__pump=function(t){var q=window.__q;window.__q=[];q.forEach(function(cb){cb(t);});};
  ```
  Then `__pump(0)`, `__pump(150)`, `__pump(300)`… and read `--hxO` or `style.opacity` after each.
  ⚠️ **The IntersectionObserver that starts these builds also needs a rendering opportunity**, so
  scroll in one `javascript_tool` call and pump in the *next* one. Pumping in the same call as
  the scroll reads all zeroes, because the observer callback has not been delivered yet.
  Delete the copy afterwards.
- ⚠️ **Transformed elements measure wrong.** `.rise` elements are translated until revealed, and the
  §4a mosaics are scaled by their own perspective mid-build, so `getBoundingClientRect()` returns
  the *visual* box, not the layout box. Force `.in` on every `.rise`, scroll the mosaics to their
  settled state, and only then measure. Section heights are safe (the section itself is not
  transformed); tile sizes are not.
- **Trust geometry over screenshots.** The pane renders emulated widths scaled, and it sometimes
  paints a stale frame after a resize — it did so repeatedly on 6 Aug, rendering the page in a
  ~508px box inside an 800px shot. `resize_window` to the `desktop` preset clears it. Drive
  `javascript_tool` and read rects and computed styles.
- **To inspect something small (an icon, a chip) close up:** clone it into a fixed full-screen
  overlay at a large CSS size and screenshot that. ⚠️ Strip `hero-el` / `rise` off the clone and
  force `opacity:1`, or it inherits `opacity:0` and you photograph an empty box. `zoom` with a
  region is **not** supported by the pane — it returns the full screenshot.
- **A hidden pane cannot paint mid-page screenshots.** The workaround: pin the section over the
  viewport (`#x{position:fixed;inset:0;z-index:9998;background:var(--ink)}` plus
  `*{transition:none}`), force its scripts to their settled state, screenshot at scroll 0, reload.
  Sections are transparent, so the background is required or whatever is underneath bleeds through.
- **`scroll-behavior` is smooth**, so set `document.documentElement.style.scrollBehavior='auto'`
  before assigning `scrollTop`, then dispatch a `scroll` event.
- ⚠️ **Do not drive the stone wheel's material tabs from a scripted loop.** Clicking through them
  programmatically navigated the page to `/stones/` twice during verification and killed the run.
  Check the wheel by hand, or measure it one material at a time.
- ⚠️ **The Browser pane's click coordinates are not CSS pixels.** Clicks placed from a screenshot
  landed hundreds of pixels off. Use `read_page` and click by `ref`, or call `.click()` in JS.

**Health check:** reload → zero errors after scrolling the whole page → no horizontal scroll at
1920/1440/375 → the wheel spans the screen on **all three** materials → the estimator prices an
L-shape at £2,000–£2,500 and a marble tab goes POA → the listbox opens, arrows move, Escape returns
focus → hero, About, Why and the contact card each inside one screen at 1366×610 / 1200×655 /
1512×824 → the hero's trust line on one line and all three reasons at 1+1 lines → the About tiles
arrive one at a time over roughly a screen of scrolling → **the six services helix cards build
top-first over about two seconds, and the six Why tiles reach FULL brightness with the section
simply framed, without scrolling further** (§4a, §4e — the Why one has to be checked with the
whole section in view, not with the mosaic pushed to the top of the screen) → **the helix's top
and bottom cards read as pale stone, clearly visible against the ground** (§4e) → **the FAQ shows
four even columns of three, the plate does not change height on ANY of the twelve, and Tab walks
the twelve while the arrows step column-wise** (§4a) → **the enquiry form's two columns finish
level, with the three next-steps lines below the submit and NO Call us button** (§4g, D36) → the
upload disclosure opens itself and shows a count when a file is attached in the estimator →
**the page floor is visibly stone — a gold vein network in near-black, not stripes — and it stays
BEHIND everything, with no section competing with it** (§4l; if it looks loud, `--floorVeil` is
the one knob) →
**the nav bar's gold line draws out from its centre with a bright head running to each edge the
first time you scroll past 40px, and there is still no horizontal scroll while it does** (§4h) →
**"Let's bring your vision to life" under the reviews is gold, underlined and clickable** (§4j) →
**the About collage is three equal portrait tiles OVER three work photographs, Nick, Rimsha and
Ali's names all sit on ONE baseline, and the section title's top and the CTA's bottom line up with
the collage's top and bottom edges** (§4a — the role reserves two lines, so a name sitting proud of
the other two means that reservation has been lost) → **the five Why tiles are veined black stone
with a solid gold icon and ⛔ NO gold line on their top edge** (§4i, §2 rule 10) → **the feature
tile holds a photograph** → **the FAQ sits close under its divider and is nowhere near 100vh**
(§4a) → **the footer is four columns and fits the viewport, with Materials, Guides and Areas each
linked once at its hub** (§4k — if the Browse column has gone, three page families have been
orphaned) →
`/sitemap.html`, `/stones/`, `/trade/`, `/services/*`, `/materials/*`, `/guides/*`, `/worktops/*`,
`/v2/` all 200.

---

## 9. Reference

- **`stones/catalogue_source.py`** — the 52-stone dataset, source of truth for the catalogue.
- **`HANDOVER-2026-08-07-design-round.md`** — the narrative of the 7 Aug design round, and the
  best single explanation of why the Why mosaic and the FAQ are built the way they are.
- **`HANDOVER-2026-08-07-seo-build.md`** — the narrative of the 7 Aug SEO session.
- **`HANDOVER-archive-to-2026-08-06.md`** — every earlier round, including the rejected designs.
  Read before redesigning the process section, the gallery, or any divider.
- **`HANDOVER.pre-hero-session.bak.md`** — the previous copy of this file, before the hero session.
- **`TopCat-Website-Copy-and-SEO-Strategy.pdf`** — research, copy and SEO strategy.
- **`Docs/`** — client briefs and sales-call notes, the ground truth about the business.
- **`Section Specs/`** — per-section specs from the client.
- **`memory/topcat-copy-constraints.md`** — the standing rules, auto-loaded each session.
- **Backups** (`Website Demo/index.html.pre-*.bak`), newest first. ⚠️ **These are the only version
  control. Keep the recent few.**

  | file | the state it preserves |
  |---|---|
  | `.pre-background.bak` | ⭐ before the page floor was replaced — the old blurred-stroke SVG in `--marbleBG`, and `body::before` with no veil layer (§4l) |
  | `.pre-footer-cleanup.bak` | ⭐ before the 9 Aug **second** round — the six-column 931px footer, the FAQ still pinned to 100vh, the Why tiles with their gold top seam, the owners at the BOTTOM of the collage, the un-aligned About column (§4a, §4i, §4k). ⚠️ Each of the four builders has its own `.pre-footer.bak` beside it from the same moment. |
  | `.pre-about-rebuild.bak` | ⭐ before the 9 Aug **first** round — the About credit strip and four-row collage, the undressed Why tiles and dashed feature slot, the CTA's Call us button and `.cta-or2` seam, the non-clickable reviews line, and the nav bar with no flare (§4a, §4g–§4j) |
  | `.pre-faq-redesign.bak` | before the 7 Aug FAQ redesign, the CTA form foot and the helix back faces (§4a, §4e, §4g) |
  | `.pre-sequence.bak` | before the 7 Aug animation changes — Why still on `scrollSequence`, helix with no entrance (§4a, §4e) |
  | `.pre-partners.bak` | before the 7 Aug fabrication rewrite (§4d). Also exists for all three builders. |
  | `.pre-porcelain.bak` | before the 7 Aug porcelain / Trade-nav / region / FAQ changes (§4c) |
  | `.pre-hero2.bak` | before the hero's **second** pass — shortened eyebrow, thin outline icons, room-list one-liner |
  | `.pre-hero.bak` | before the hero touched at all — 9px eyebrow, three-line subhead, no reason row |
  | `.pre-sections.bak` | before the About / Why / FAQ / contact redesign — two-column Why, accordion FAQ, always-open uploader |
  | `.pre-catalogue.bak` | before the real 52-stone catalogue went in |
  | `.pre-reorder.bak` | before the estimator moved to sit after the stones |
  | `.pre-pricing.bak` | before the bracket-table re-pricing |

- `Website Demo/index-v2.html` — an old unrelated design, unlinked and stale, safe to delete.
- ⚠️ **Dead CSS worth knowing about:**
  - `.reasons` (a four-cell proof bar ported from V2) has rules in the style block but **no markup
    anywhere**. Either use it or delete it; do not assume it is live.
  - `.ph-title` — ⚠️ **there are TWO different rules with this name.** The one near the top of the
    sheet is a 34–64px display title and has been dead for some time; it is left alone. The other
    was part of the placeholder skin and **was removed on 9 Aug** along with `.ph-slot` and
    `.ph-note`, because both of their users went that day (§4a, §4i). If you need a dashed
    "shot to come" frame again, it is in `index.html.pre-about-rebuild.bak`.
- **`Docs/topcat-worktops-SEO-LOG.md`** — ⭐ the SEO record for the client's specialist. Update it
  whenever anything SEO changes.
- **`Docs/topcat-worktops-seo-build-plan.md`** — the research behind the build (§10).
- **`Website Demo/build_seo_pages.py`** — generates all 25 SEO pages **and `/sitemap.html`**.
  House rules in its docstring.
- **`Website Demo/seo.css`** — components for the material, guide, location and sitemap pages.
- **`Website Demo/assets/stone-floor.webp`** — ⭐ the page floor (§4l). 90KB, 2048×1152, a graded
  Nero Portoro slab. It is the value of `--marbleBG`, so it is ALSO the nav glass, the CTA card,
  the estimator modal and the trade prompt. ⚠️ Do not swap it without reading §4l: it is tuned to
  measured luminance targets and to zero thick-vein content, and getting either wrong changes the
  weight of every section on the site at once.

---

## 10. ⭐ SEO — where everything lives, and the rules

**The full record is `Docs/topcat-worktops-SEO-LOG.md`.** It is written to be handed to the
client's SEO specialist cold, and it is the file to update whenever anything SEO changes. It
contains every URL with its title, word count, schema and target query; the internal linking
structure; every schema decision with the reason it was built or removed; the location page rules;
the phased town rollout with its gate; the silica accuracy rules; copy-paste compliance scans; and
a dated change log.

### The six rules that are easiest to break by accident

1. **Never add `FAQPage`, `HowTo`, `Service`, `WebSite`+`SearchAction`, or `aggregateRating` on
   our own pages.** All dead or ineligible in 2026. §4d.
2. **Never claim in-house fabrication.** D21.
3. **Location pages stay under ~40% of URLs**, currently 26%, and never bulk-publish.
4. **Never expose the review count**, in copy or in schema. D7.
5. **Never name the suppliers publicly.** D8. The `sup` field stays in the data and is never
   rendered, which was verified.
6. **Never hand-edit `/sitemap.html`.** It is generated by `build_seo_pages.py` from the same
   lists as the pages (§4f). A hand-edit is lost on the next rebuild, and a sitemap that has
   drifted out of sync is worse than none.

### Run these before go-live

They are written out in full in the SEO log §8. In short: scan for fabrication claims, dead
schema, supplier names and founding-year language; syntax-gate the inline JS with `node --check`;
validate every `ld+json` block; and check for broken links and orphans.

### The next SEO actions, in order

1. **Conversion tracking**, which is blocked on the enquiry backend (§7.0). Nothing else matters
   as much, because leads are the metric this engagement is judged on.
2. **Buy one month of Ahrefs or Semrush** and validate the town list before Phase 2. All current
   prioritisation is ordinal, from SERP composition and UK autocomplete, with no cardinal volumes.
3. **Real project photography with the town and date**, which is what makes the location pages
   genuinely local and is the one asset no competitor in this vertical has.
4. **Review generation naming the town and the fitter.**
5. **Phase 2 towns**, only after the 90-day gate: Watford, Hemel Hempstead, Chelmsford, Romford,
   Slough, Wimbledon.
6. **Consider the WFF Quality Mark**, the cheapest E-E-A-T signal available in this niche.
